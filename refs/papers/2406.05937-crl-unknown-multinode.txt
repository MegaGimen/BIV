source: https://arxiv.org/html/2406.05937
arxiv: https://arxiv.org/abs/2406.05937
note: HTML dump via w3m; not a PDF.
========================================================================

Report GitHub Issue

×
Title: [                    ]

Content selection saved. Describe the issue below:

Description: [                    ]
Submit without GitHub Submit in GitHub
[smileybone] arXiv is now an independent nonprofit! Learn more ×
arXiv logo Back to arXiv
Why HTML? Report Issue Back to Abstract Download PDF   

 1. Abstract
 2. 1 Introduction
     1. 1.1 Related literature
 3. 2 CRL setting and preliminaries
     1. 2.1 Latent causal model
     2. 2.2 Unknown multi-node intervention models
     3. 2.3 Identifiability criteria
 4. 3 Identifiability under UMN interventions
 5. 4 UMN interventional CRL algorithm
 6. 5 Simulations
 7. 6 Discussion
 8. References
 9. ‣ Linear Causal Representation Learning from Unknown Multi-node Interventions
     1. A Proofs
         1. Additional notations.
         2. Definition 2 (Intervention regularity)
         3. A.1 Proof of Lemma 
             1. Base case.
             2. Induction hypothesis.
         4. A.2 Proof of Theorem 
             1. Base case.
             2. Induction hypothesis.
             3. Base case for the inner induction.
             4. Induction hypothesis for the inner induction.
         5. A.3 Proof of Theorem 
             1. A.3.1 Recovery of the latent variables
                 1. Step 1: Cov⁡(𝐮⋅Z^an^​(k)m+Z^km,Z^an^​(k)m)=𝟎\Cov(\mathbf{u}\cdot\hat{Z}^{m}_{\
                    hat{\an}(k)}+\hat{Z}^{m}_{k},\hat{Z}^{m}_{\hat{\an}(k)})=\boldsymbol{0} has a
                    unique solution for 𝐮\mathbf{u}.
                 2. Step 2: Cov⁡(𝐮⋅Z^an^​(k)m+Z^km,Z^an^​(k)m)=0\Cov(\mathbf{u}\cdot\hat{Z}^{m}_{\
                    hat{\an}(k)}+\hat{Z}^{m}_{k},\hat{Z}^{m}_{\hat{\an}(k)})=0 and 𝐮⋅Z^an^​(k)m⟂⟂Z^
                    an^​(k)m\mathbf{u}\cdot\hat{Z}^{m}_{\hat{\an}(k)}\perp\!\!\!\perp\hat{Z}^{m}_{\
                    hat{\an}(k)} implies that fπkmf^{m}_{\pi_{k}} cannot be nonlinear.
                 3. Step 3: πk∈Im\pi_{k}\in I^{m} and f=0f=0.
                 4. Step 4: πk∉Im\pi_{k}\notin I^{m} and ff is linear.
             2. A.3.2 Recovery of the latent graph
         6. A.4 Proofs of Theorem  and Theorem 
         7. A.5 Proof of Lemma 
         8. A.6 Insufficiency of strongly separating sets
             1. Background.
             2. First model.
             3. Second model.
             4. Non-identifiability.
         9. A.7 Analysis of interventional regularity
             1. Case 1: ∂fi​(zpa⁡(i))∂zj\frac{\partial f_{i}(z_{\pa(i)})}{\partial z_{j}} is not
                constant.
             2. Case 2: ∂fi​(zpa⁡(i))∂zj=β\frac{\partial f_{i}(z_{\pa(i)})}{\partial z_{j}}=\beta
                for some nonzero constant β\beta.
        10. A.8 Computational complexity of UMNI-CRL algorithm
             1. Bounds on κ\kappa.
             2. Empirical tricks.
     2. B Additional simulations
         1. Details of evaluation metrics.
         2. B.1 Simulations with nonlinear latent causal models and sensitivity to noisy scores

License: arXiv.org perpetual non-exclusive license
arXiv:2406.05937v2 [cs.LG] 05 Dec 2024

Linear Causal Representation Learning from Unknown Multi-node Interventions

Burak Varıcı ^†^†thanks: Work was done while BV was a Ph.D. student at Rensselaer Polytechnic
Institute. Affiliation: Carnegie Mellon University    Emre Acartürk Affiliation: Rensselaer
Polytechnic Institute    Karthikeyan Shanmugam Affiliation: Google DeepMind    Ali Tajer 
Affiliation: Rensselaer Polytechnic Institute

Abstract

Despite the multifaceted recent advances in interventional causal representation learning (CRL),
they primarily focus on the stylized assumption of single-node interventions. This assumption is
not valid in a wide range of applications, and generally, the subset of nodes intervened in an
interventional environment is fully unknown. This paper focuses on interventional CRL under unknown
multi-node (UMN) interventional environments and establishes the first identifiability results for
general latent causal models (parametric or nonparametric) under stochastic interventions (soft or
hard) and linear transformation from the latent to observed space. Specifically, it is established
that given sufficiently diverse interventional environments, (i) identifiability up to ancestors is
possible using only soft interventions, and (ii) perfect identifiability is possible using hard
interventions. Remarkably, these guarantees match the best-known results for more restrictive
single-node interventions. Furthermore, CRL algorithms are also provided that achieve the
identifiability guarantees. A central step in designing these algorithms is establishing the
relationships between UMN interventional CRL and score functions associated with the statistical
models of different interventional environments. Establishing these relationships also serves as
constructive proof of the identifiability guarantees.

1 Introduction

Causal representation learning (CRL) is a major leap in causal inference, moving away from the
conventional objective of discovering causal relationships among a set of variables and learning
the variables themselves. By combining the strengths of causal inference and machine learning, CRL
specifies data representations that facilitate reasoning and planning [1]. CRL is motivated by the
premise that in a wide range of applications, a lower-dimensional latent set of variables with
causal interactions generates the usually high-dimensional observed data. Therefore, CRL’s
objective is to use the observed data and learn the latent causal generative factors, which include
the causal latent variables and their causal relationships.

CRL objectives. Formally, consider a set of latent causal random variables Z∈ℝnZ\in{\mathbb{R}}^{n}
and a directed acyclic graph (DAG) 𝒢\mathcal{G} that encodes the causal relationships among ZZ. The
latent variables are transformed by an unknown function gg to generate the observed random
variables X∈ℝdX\in{\mathbb{R}}^{d}, where X≜g⁡(Z)X\triangleq g(Z). CRL aims to use XX to recover
the latent causal variables ZZ and the causal structure 𝒢\mathcal{G}.

Two central questions of CRL pertain to identifiability, which refers to determining the conditions
under which ZZ and 𝒢\mathcal{G} can be recovered, and achievability, which refers to designing CRL
algorithms that can achieve the foreseen identifiability guarantees. Identifiability has been
demonstrated to be inherently under-constrained [2], prompting the development of diverse
methodologies that incorporate inductive biases to enable identifiability. Interventional CRL is
one direction with significant recent advances in which interventions on latent causal variables
are used to create statistical diversity in the observed data [1, 3, 4, 5, 6].

Unknown multi-node interventions. Despite covering many aspects of interventional CRL, such as
parametric versus nonparametric causal models, parametric versus nonparametric transformations, and
intervention types, the majority of the existing studies assume that the interventions are
single-node, i.e., exactly one latent variable is intervened in each environment [3, 4, 5, 6, 7, 8,
9, 10]. This assumption, however, is restrictive in some of the application domains of CRL such as
biology and robotics in which generally the subset of nodes intervened in an intervention
environment can be fully unknown. For instance, biological perturbations in genomics are imperfect
interventions with off-target effects on other genes [11, 12], or interventions on robotics
applications are likely to affect multiple causal variables [13]. Hence, realizing the promises of
CRL critically hinges on dispensing with the assumption of single-node interventions.

In this paper, we address the open problem of using unknown multi-node (UMN) stochastic
interventions to recover the latent causal variables ZZ and their causal graph 𝒢\mathcal{G},
wherein each environment an unknown subset of nodes are intervened. We consider a general latent
causal model (parametric or nonparametric) and focus on the linear transformations as an important
class of parametric transformation models. We establish identifiability results and design
algorithms to achieve them under both soft and hard interventions. For this purpose, we delineate
connections between UMN interventions and the properties of score functions, i.e., the gradients of
the logarithm of density functions. This score-based framework is the UMN counterpart of the
single-node framework proposed in [5, 7], albeit with significant technical differences. Our
contributions are summarized below.

  • •
   
    We show that under sufficiently diverse interventional environments, UMN stochastic hard
    interventions suffice to guarantee perfect identifiability of the latent causal graph and the
    latent variables (up to permutations and element-wise scaling).

  • •
   
    We show that under sufficiently diverse interventional environments, UMN soft interventions
    guarantee identifiability up to ancestors – transitive closure of the latent DAG is recovered,
    and latent variables are recovered up to a linear function of their ancestors. Remarkably,
    these guarantees match the best possible results in the literature of single-node
    interventions.

  • •
   
    We design score-based CRL algorithms for implementing CRL with UMN interventions with provable
    guarantees. These guarantees also serve as constructive proof steps of the identifiability
    results.

Challenges of UMN interventions. There are two broad challenges specific to addressing the UMN
intervention setting that render it substantially distinct from the single-node (SN) intervention
setting. First, in SN interventions, since the learner knows exactly one node is intervened in each
environment, it can readily identify the intervention targets up to a permutation. In contrast, in
UMN interventions, the learner does not know how many nodes are intervened in each environment.
Therefore, the nature of resolving the uncertainty about the intervention targets becomes
fundamentally different. An immediate impact of this is that it becomes more challenging to
properly capitalize on the statistical diversity embedded in the interventional data. Secondly, in
SN interventions, only one causal mechanism changes across the environments. Such sparse variations
of the causal mechanisms are a core property leveraged by various existing CRL approaches, e.g.,
contrastive learning [8], and score-based framework [6, 7]. On the contrary, UMN interventions
allow for many concurrent causal mechanism changes, which renders leveraging sparsity patterns in
mechanism variations futile. Finally, since intervention targets are unknown, our central
algorithmic idea is to properly aggregate the UMN interventional environments to create new
distinct environments under which the inherent statistical diversity is more accessible.

1.1 Related literature

Single-node interventional CRL. The majority of the studies on interventional CRL focus on SN
interventions [3, 4, 5, 6, 7, 8, 9, 10, 14], which can be categorized based on their assumptions on
the latent causal model, transformation, and intervention model. Based on this taxonomy, it has
been shown that SN hard interventions suffice for identifiability with general latent causal models
and linear transformations (one intervention per node) [7], with linear Gaussian latent models and
general transformations (one intervention per node) [8], and with general latent models and general
transformations (two interventions per node) [6, 9]. For the less restrictive SN soft
interventions, identifiability up to ancestors is shown for general latent models and linear
transformations [7] and linear Gaussian latent models and general transformations [8]. Furthermore,
under additional assumptions such as sufficiently nonlinear latent models, the latent DAG is shown
to be perfectly identifiable [7, 10, 14]. In a related study, [15] focuses on learning the latent
DAG (but without learning latent causal variables) using SN hard interventions without parametric
assumptions on the model.

Multi-node interventional CRL. The studies on MN intervention settings are sparser than the SN
intervention settings. Table 1 summarizes the results closely related to the scope of this paper
along with the identifiability results established in this paper. In summary, the existing studies
either provide partial identifiability or focus on non-stochastic do interventions. The study in [
14] focuses on linear non-Gaussian latent models and linear transformations and uses soft
interventions to establish identifiability up to surrounding variables by using multiple
interventional mechanisms for each node. In a different study, [16] uses strongly separated
multi-node do interventions and provides perfect identifiability results for general latent models
and linear transformations. We also note the partially related study in [17] that applies soft
interventions on a subset of nodes and aims to disentangle the non-intervened variables from the
intervened ones. Distinct from all these studies, we address the open problem of perfect
identifiability under UMN stochastic interventions.

Other approaches to CRL. We note that there exist other interesting settings that address CRL
without interventions. Some examples include using multi-view data [18, 19, 20, 21], leveraging
temporal sequences [22, 23], building on nonlinear independent component analysis (ICA) principles
to identify polynomial latent causal models [24], and imposing sparsity constraints to obtain
partial disentanglement [25, 26], and grouping of observational variables [27]. We refer to [28]
for a detailed literature review on various CRL problems.


Table 1: Comparison of the results to existing work in multi-node interventional CRL. We note that
all studies assume linear transformation.

  Work    Latent model Int. type       Main assumption on interventions       Identifiability (ID)
[14]      Linear       Soft      |pa⁡(i)||\pa(i)| independent int. mechanisms ID up to surrounding
[16]      General      do        strongly separated interventions             perfect ID
Theorem 1 General      Hard      lin. indep. interv. (Assumption 1)           perfect ID
Theorem 2 General      Soft      lin. indep. interv. (Assumption 1)           ID up to ancestors

2 CRL setting and preliminaries

Notations. Vectors are represented by lowercase bold letters, and element ii of vector 𝐚\mathbf{a}
is denoted by 𝐚i\mathbf{a}_{i}. Matrices are represented by uppercase bold letters, and we denote
row ii and column jj of matrix 𝐀\mathbf{A} by 𝐀i\mathbf{A}_{i} and by 𝐀:,j\mathbf{A}_{:,j},
respectively, and 𝐀i,j\mathbf{A}_{i,j} denotes the entry at row ii and column jj. We use 𝗇𝗎𝗅𝗅⁡
({𝐀1,…,𝐀r}){\sf null}(\{\mathbf{A}_{1},\dots,\mathbf{A}_{r}\}) to denote the nullspace of the
matrix consisting of the row vectors {𝐀1,…,𝐀r}\{\mathbf{A}_{1},\dots,\mathbf{A}_{r}\}. For n∈ℕn\in
{\mathbb{N}}, we define [n]≜{1,…,n}[n]\triangleq\{1,\dots,n\}. The row permutation matrix
associated with any permutation π\pi of [n][n] is denoted by 𝐏π\mathbf{P}_{\pi}. We denote the
indicator function by 𝟙\mathds{1}. We use im⁡(f)\im(f) to denote the image of a function ff and dim
(𝒱)\dim(\mathcal{V}) to denote the dimension of a subspace 𝒱\mathcal{V}. Random variables and their
realizations are presented by upper and lower case letters, respectively.

2.1 Latent causal model

Consider a latent causal space consisting of nn causal random variables Z≜[Z1,…,Zn]⊤Z\triangleq[Z_
{1},\dots,Z_{n}]^{\top}. An unknown linear transformation 𝐆∈ℝd×n\mathbf{G}\in{\mathbb{R}}^{d\times
n} maps ZZ to the observed random variables denoted by X≜[X1,…,Xd]⊤X\triangleq[X_{1},\dots,X_{d}]^
{\top} according to:

 X=𝐆⋅Z,X=\mathbf{G}\cdot Z\ ,   (1)

where d≥nd\geq n and 𝐆\mathbf{G} is full rank. The probability density functions (pdfs) of XX and
ZZ are denoted by pXp_{X} and pZp_{Z}, respectively. We assume that pZp_{Z} has full support on ℝn
{\mathbb{R}}^{n}. Subsequently, pXp_{X} is supported on 𝒳≜im⁡(𝐆)\mathcal{X}\triangleq\im(\mathbf
{G}). The causal relationships among latent variables ZZ are represented by a DAG 𝒢\mathcal{G} in
which the ii-th node corresponds to ZiZ_{i}. Hence, pZp_{Z} factorizes according to:

 pZ​(z)=∏i=1npi​(zi∣zpa⁡(i)),p_{Z}(z)=\prod_{i=1}^{n}p_{i}(z_{i}\mid z_{\pa(i)})\ ,  (2)

where pa⁡(i)\pa(i) denotes the set of parents of node ii in 𝒢\mathcal{G}. The conditional pdfs {pi​
(zi∣zpa⁡(i)):i∈[n]}\{p_{i}(z_{i}\mid z_{\pa(i)}):i\in[n]\} are assumed to be continuously
differentiable with respect to all zz variables. We use ch⁡(i)\ch(i), an⁡(i)\an(i), and de⁡(i)\de
(i) to denote the children, ancestors, and descendants of node ii, respectively. We say that a
permutation (π1,…,πn)(\pi_{1},\dots,\pi_{n}) of [n][n] is a valid causal order if the membership
πi∈an⁡(πj)\pi_{i}\in\an(\pi_{j}) indicates that i<ji<j. Without loss of generality, we assume that
(1,…,n)(1,\dots,n) is a valid causal order. We will specialize some of our results for the latent
causal models with additive noise ^1^1 1 Perfect identifiability results in the closely related
literature are given for additive noise models [7, 4, 8, 16]. specified by

 Zi=fi​(Zpa⁡(i))+Ni,Z_{i}=f_{i}(Z_{\pa(i)})+N_{i}\ ,  (3)

where functions {fi:i∈[n]}\{f_{i}:i\in[n]\} capture the causal dependence of node ii on its parents
and the terms {Ni:i∈[n]}\{N_{i}:i\in[n]\} represent the exogenous noise variables.

2.2 Unknown multi-node intervention models

In addition to the observational environment, we have MM UMN interventional environments denoted by
{ℰm:m∈[M]}\{\mathcal{E}^{m}:m\in[M]\}. We assume that the set of nodes intervened in each
environment is unknown, and denote the set intervened in environment ℰm\mathcal{E}^{m} by Im⊆[n]I^
{m}\subseteq[n]. Accordingly, we define the intervention signature matrix 𝐃int∈{0,1}n×M\mathbf{D}_
{\rm int}\in\{0,1\}^{n\times M} to compactly represent the intervention targets under various
environments as

 [𝐃int]i,m=𝟙{i∈Im},∀i∈[n],∀m∈[M].[\mathbf{D}_{\rm int}]_{i,m}=\mathds{1}\{i\in I^{m}\}\ ,\quad  (4)
 \forall i\in[n]\ ,\;\;\forall m\in[M]\ .

The mm-th column of 𝐃int\mathbf{D}_{\rm int} lists the indices of the nodes intervened in
environment ℰm\mathcal{E}^{m}, which we refer to as the intervention vector of environment ℰm\
mathcal{E}^{m}. Ensuring identifiability inevitably imposes restrictions on the structure of 𝐃int\
mathbf{D}_{\rm int}. For instance, if the ii-th row of 𝐃int\mathbf{D}_{\rm int} is a zero vector,
it means that node ii is not intervened in any environment, then the perfect identifiability is not
possible [4] ^2^2 2 [4, Proposition 5] shows that if the non-intervened node ii has at least one
parent, then perfect identifiability is not possible.. Therefore, to avoid such impossibility
cases, we impose the mild condition that 𝐃int\mathbf{D}_{\rm int} has sufficiently diverse columns,
formalized next.

Assumption 1.

Intervention signature matrix 𝐃int\mathbf{D}_{\rm int} defined in (4) is full row rank, i.e., it
contains nn linearly independent intervention vectors.

In this paper, we consider UMN stochastic interventions and address identifiability results under
both hard interventions as well as soft interventions as the most general form of intervention.

Soft interventions. A soft intervention on node ii alters the observational causal mechanism pi​
(zi∣zpa⁡(i))p_{i}(z_{i}\mid z_{\pa(i)}) to an interventional causal mechanism qi​(zi∣zpa⁡(i))q_{i}
(z_{i}\mid z_{\pa(i)}). Such a change occurs in node ii in all the environments ℰm\mathcal{E}^{m}
that contain node ii, i.e., i∈Imi\in I^{m}. Subsequently, the pdf of the latent variables in
environment ℰm\mathcal{E}^{m}, denoted by pZmp_{Z}^{m}, factorizes according to:

 pZm​(z)≜∏i∈Imqi​(zi∣zpa⁡(i))​∏i∉Impi​(zi∣zpa⁡(i)),∀m∈[M].p_{Z}^{m}(z)\triangleq\prod_{i\in I^
 {m}}q_{i}(z_{i}\mid z_{\pa(i)})\prod_{i\not\in I^{m}}p_{i}(z_{i}\mid z_{\pa(i)})\ ,\quad\      (5)
 forall m\in[M]\ .

Hard interventions. Under a hard intervention on node ii, the functional dependence of node ii on
its parents is removed, and the observational causal mechanism pi​(zi∣zpa⁡(i))p_{i}(z_{i}\mid z_{\
pa(i)}) is changed to an interventional causal mechanism qi​(zi)q_{i}(z_{i}), independent of
parents of node ii.

To distinguish the observational and interventional data, we denote the latent and observed random
variables in environment ℰm\mathcal{E}^{m} by ZmZ^{m} and XmX^{m}, respectively. We note that
interventions do not affect the transformation 𝐆\mathbf{G}. Hence, in ℰm\mathcal{E}^{m} we have Xm=
𝐆⋅ZmX^{m}=\mathbf{G}\cdot Z^{m} for all m∈[M]m\in[M].

Score functions. The score function of a pdf is defined as the gradient of its logarithm. We denote
the score functions associated with the distributions of ZmZ^{m} and XmX^{m} by

 𝒔Zm​(z)≜∇log⁡pZm​(z),and𝒔Xm​(x)≜∇log⁡pXm​(x),∀m∈[M].\boldsymbol{s}_{Z}^{m}(z)\triangleq\nabla
 \log p_{Z}^{m}(z)\ ,\quad\mbox{and}\quad\boldsymbol{s}_{X}^{m}(x)\triangleq\nabla\log p_{X}^   (6)
 {m}(x)\ ,\quad\forall m\in[M]\ .

Note that, using the factorization in (5), 𝒔Zm\boldsymbol{s}_{Z}^{m} decomposes as

 𝒔Zm​(z)=∑i∈Im∇log⁡qi​(zi∣zpa⁡(i))+∑i∉Im∇log⁡pi​(zi∣zpa⁡(i)).\boldsymbol{s}_{Z}^{m}(z)=\sum_{i
 \in I^{m}}\nabla\log q_{i}(z_{i}\mid z_{\pa(i)})+\sum_{i\not\in I^{m}}\nabla\log p_{i}(z_{i}\  (7)
 mid z_{\pa(i)})\ .

We denote the difference in score functions between interventional and observational environments
by

 Δ​𝒔Zm​(z)≜𝒔Zm​(z)−𝒔Z​(z)andΔ​𝒔Xm​(x)≜𝒔Xm​(x)−𝒔X​(x),∀m∈[M].\Delta\boldsymbol{s}_{Z}^{m}(z)\
 triangleq\boldsymbol{s}_{Z}^{m}(z)-\boldsymbol{s}_{Z}(z)\quad\mbox{and}\quad\Delta\boldsymbol  (8)
 {s}_{X}^{m}(x)\triangleq\boldsymbol{s}_{X}^{m}(x)-\boldsymbol{s}_{X}(x)\ ,\quad\forall m\in
 [M]\ .

2.3 Identifiability criteria

In CRL, we use observed variables XX to recover the true latent variables ZZ and the latent causal
graph 𝒢\mathcal{G}. We denote a generic estimator of ZZ given XX by Z^​(X):ℝd→ℝn\hat{Z}(X):{\mathbb
{R}}^{d}\to{\mathbb{R}}^{n}. We also consider a generic estimate of 𝒢\mathcal{G} denoted by 𝒢^\hat
{\mathcal{G}}. To assess the fidelity of the estimates Z^​(X)\hat{Z}(X) and 𝒢^\hat{\mathcal{G}}
with respect to the ground truth ZZ and 𝒢\mathcal{G}, we provide the following well-known
identifiability measures.

Definition 1 (Identifiability).

For CRL under linear transformations, we define:

 1. 1.
   
    Perfect identifiability: 𝒢^\hat{\mathcal{G}} and 𝒢\mathcal{G} are isomorphic, and the estimator
    Z^​(X)\hat{Z}(X) satisfies that

     Z^​(X)=𝐏π⋅𝐂s⋅Z,∀Z∈ℝn,\hat{Z}(X)=\mathbf{P}_{\pi}\cdot\mathbf{C}_{\rm s}\cdot Z\ ,\qquad\   (9)
     forall Z\in{\mathbb{R}}^{n}\ ,

    where 𝐂s∈ℝn×n\mathbf{C}_{\rm s}\in{\mathbb{R}}^{n\times n} is a constant diagonal matrix with
    nonzero diagonal entries and 𝐏π\mathbf{P}_{\pi} is a row permutation matrix.

 2. 2.
   
    Identifiability up to ancestors: 𝒢^\hat{\mathcal{G}} and transitive closure of 𝒢\mathcal{G},
    denoted by 𝒢tc\mathcal{G}_{\rm tc}, are isomorphic, and the estimator Z^​(X)\hat{Z}(X)
    satisfies that

     Z^​(X)=𝐏π⋅𝐂an⋅Z,∀Z∈ℝn,\hat{Z}(X)=\mathbf{P}_{\pi}\cdot\mathbf{C}_{\rm an}\cdot Z\ ,\      (10)
     qquad\forall Z\in{\mathbb{R}}^{n}\ ,

    where 𝐂an∈ℝn×n\mathbf{C}_{\rm an}\in{\mathbb{R}}^{n\times n} is a constant matrix with nonzero
    diagonal entries that satisfies [𝐂an]i,j=0[\mathbf{C}_{\rm an}]_{i,j}=0 for all j∉{an⁡(i)∪{i}}j
    \notin\{\an(i)\cup\{i\}\}, and 𝐏π\mathbf{P}_{\pi} is a row permutation matrix.

In the algorithm we will design, estimating Z^​(X)\hat{Z}(X) and 𝒢^\hat{\mathcal{G}} are
facilitated by estimating the inverse of the transformation 𝐆\mathbf{G}, that is Moore-Penrose
inverse 𝐆†≜[𝐆⊤⋅𝐆]−1⋅𝐆⊤\mathbf{G}^{{\dagger}}\triangleq[\mathbf{G}^{\top}\cdot\mathbf{G}]^{-1}\cdot\
mathbf{G}^{\top}, which we refer to as the true encoder. To formalize the process of estimating the
true encoder, we define ℋ\mathcal{H} as the set of candidate encoders specified by ℋ≜{𝐇∈ℝn×d:𝗋𝖺𝗇𝗄
(𝐇)=nand𝐇⊤⋅𝐇⋅X=X,∀X∈𝒳}\mathcal{H}\triangleq\{\mathbf{H}\in{\mathbb{R}}^{n\times d}:{\sf rank}(\
mathbf{H})=n\;\mbox{and}\;\mathbf{H}^{\top}\cdot\mathbf{H}\cdot X=X\ ,\;\forall X\in\mathcal{X}\}.
Corresponding to any pair of observation XX and valid encoder 𝐇∈ℋ\mathbf{H}\in\mathcal{H}, we
define Z^​(X,𝐇)\hat{Z}(X;\mathbf{H}) as an auxiliary estimate of ZZ generated as Z^​(X,𝐇)≜𝐇⋅X=(𝐇⋅𝐆)
⋅Z\hat{Z}(X;\mathbf{H})\triangleq\mathbf{H}\cdot X=(\mathbf{H}\cdot\mathbf{G})\cdot Z.

3 Identifiability under UMN interventions

In this section, we present the main identifiability and achievability results for CRL with UMN
interventions and interpret them in the context of the recent results in the literature. We start
by specifying the regularity conditions on the statistical models, which are needed to ensure
sufficient statistical diversity and establish identifiability results for hard and soft UMN
interventions. The constructive proofs of the results are based on CRL algorithms, the details of
which are presented in Section 4. Complete proofs are deferred to Appendix A.

We note that the UMN setting subsumes SN interventions. Similarly to all the existing
identifiability results from SN interventions, it is necessary to have sufficient statistical
diversity created by the intervention models.^3^3 3 Some examples include Assumption 1 in [6],
generic SN interventions in [4], no pure shift interventions condition in [8], and the genericity
condition in [9]. These conditions can be generally presented in the form of regularity conditions
on the probability distributions. Specifically, a commonly adopted regularity condition (or its
variations) in the SN intervention setting is that for every possible pair (i,j)(i,j) where i∈
[n],j∈pa⁡(i)i\in[n],j\in\pa(i), the following term cannot be a constant function in zz,

 ∂∂zj​(log⁡pi​(zi∣zpa⁡(i))qi​(zi∣zpa⁡(i)))​[∂∂zi​log⁡pi​(zi∣zpa⁡(i))qi​(zi∣zpa⁡(i))]−1.\small
 \frac{\partial}{\partial z_{j}}\left(\log\frac{p_{i}(z_{i}\mid z_{\pa(i)})}{q_{i}(z_{i}\mid   (11)
 z_{\pa(i)})}\right)\left[\frac{\partial}{\partial z_{i}}\log\frac{p_{i}(z_{i}\mid z_{\pa
 (i)})}{q_{i}(z_{i}\mid z_{\pa(i)})}\right]^{-1}\ .

We present a counterpart of these conditions for UMN interventions, which involves one additional
term to account for the effect of intervening on multiple nodes simultaneously.

Definition 2 (Intervention regularity).

We say that an interventions are regular if for every possible triplet (i,j,c)(i,j,c) where i∈
[n],j∈pa⁡(i)i\in[n],j\in\pa(i) and c∈ℚc\in\mathbb{Q}, the following ratio cannot be a constant
function in zz

 ∂∂zj​(log⁡pi​(zi∣zpa⁡(i))qi​(zi∣zpa⁡(i))+c⋅log⁡pj​(zj∣zpa⁡(j))qj​(zj∣zpa⁡(j)))​[∂∂zi​log⁡pi​
 (zi∣zpa⁡(i))qi​(zi∣zpa⁡(i))]−1.\small\frac{\partial}{\partial z_{j}}\left(\log\frac{p_{i}(z_
 {i}\mid z_{\pa(i)})}{q_{i}(z_{i}\mid z_{\pa(i)})}+c\cdot\log\frac{p_{j}(z_{j}\mid z_{\pa      (12)
 (j)})}{q_{j}(z_{j}\mid z_{\pa(j)})}\right)\left[\frac{\partial}{\partial z_{i}}\log\frac{p_
 {i}(z_{i}\mid z_{\pa(i)})}{q_{i}(z_{i}\mid z_{\pa(i)})}\right]^{-1}\ .

Essentially, ∂∂zj​log⁡pi​(zi∣zpa⁡(i))qi​(zi∣zpa⁡(i))\frac{\partial}{\partial z_{j}}\log\frac{p_{i}
(z_{i}\mid z_{\pa(i)})}{q_{i}(z_{i}\mid z_{\pa(i)})} captures the effect of intervening on node ii
on the score associated with node jj. In our method, we will use combinations of score differences
of multi-node environments. This regularity condition ensures that the effect of a multi-node
intervention is not the same on the scores associated with different nodes. Given these properties,
we establish perfect identifiability for CRL with linear transformations using UMN stochastic hard
interventions.

Theorem 1 (Identifiability under UMN hard interventions).

Under Assumption 1 and a latent model with additive noise,

 1. 1.
   
    perfect latent recovery is possible using regular UMN hard interventions; and

 2. 2.
   
    if the latent causal model satisfies adjacency-faithfulness, then perfect latent DAG recovery
    is possible using regular UMN hard interventions.

Theorem 1 is the first perfect identifiability result using UMN stochastic hard interventions. In
contrast, [16] establishes perfect latent recovery using highly more stringent do-interventions.
Furthermore, Theorem 1 establishes the first perfect latent DAG recovery result (under any type of
multi-node interventions) for nonparametric latent models. We note that the capability of handling
nonparametric latent models stems from leveraging the score functions. Similar properties are
demonstrated by the prior work on score-based CRL for SN interventions [7]. It is noteworthy that
we use a total of n+1n+1 environments whereas the study in [16] requires 2​⌈log2⁡n⌉2\lceil\log_{2}n
\rceil do interventions of strongly separating sets. However, we show that identifiability is
impossible using strongly separating sets of UMN stochastic hard interventions (see Appendix A.6).

Next, we consider UMN soft interventions. Since soft interventions retain the ancestral dependence
of the intervened node, in general, the identifiability guarantees for soft interventions are
weaker than those of hard interventions. Next, we establish that UMN soft interventions guarantee
identifiability up to ancestors for the general causal latent models and linear transformations.

Theorem 2 (Identifiability under UMN soft interventions).

Under Assumption 1, identifiability up to ancestors is possible using regular UMN soft
interventions.

Identifiability up to ancestors has recently shown to be possible using SN soft interventions on
general latent models [7]. Theorem 2 establishes the same identifiability guarantees without the
restrictive assumption of SN interventions. Furthermore, Theorem 2 is significantly different from
existing results for UMN soft interventions. Specifically, the study in [14] focuses on linear
non-Gaussian latent models and requires |pa⁡(i)|+1|\pa(i)|+1 distinct mechanisms for each node ii.
In contrast, Theorem 2 does not make parametric assumptions on latent variables and works with
sufficiently diverse interventions described by Assumption 1 instead of requiring multiple
interventional mechanisms for the same node.

4 UMN interventional CRL algorithm

In this section, we design the Unknown Multi-node Interventional (UMNI)-CRL algorithm that achieves
identifiability guarantees presented in Section 3. This algorithm falls in the category of
score-based frameworks for CRL [5, 7] and incorporates novel components to this framework that
facilitate UMN interventions with provable guarantees. Our score-based approach uses the structural
properties of score functions and their variations across different interventional environments to
find reliable estimates for the true encoder 𝐆†\mathbf{G}^{{\dagger}}. The critical step involved
is a process that can aggregate the score differences under the available interventional
environments, which have entirely unknown intervention targets, and reconstruct the score
differences for any desired hypothetical set of intervention targets. In particular, we establish
that such desired score differences can be computed by aggregating the score differences available
under the given UMN interventions. The proposed UMNI-CRL algorithm consists of four stages for
implementing CRL. The properties of these stages also serve as the steps of constructive proof for
identifiability results. We present the key algorithmic stages and their properties in the
remainder of this section and defer their proofs to Appendix A.


Algorithm 1 Unknown Multi-node Interventional (UMNI)-CRL
1: Input: Samples of XX from environment ℰ0\mathcal{E}^{0} and interventional environments {ℰm:m∈
[M]}\{\mathcal{E}^{m}:m\in[M]\}
2: Stage 1: Choose basis score differences and construct Δ​𝐒X\Delta\mathbf{S}_{X} using (17)
3:  
4: Stage 2: Identifiability up to a causal order
5: for t∈(1,…,n)t\in(1,\dots,n) do
6:   for 𝐰∈𝒲\mathbf{w}\in\mathcal{W} do ⊳\triangleright 𝒲\mathcal{W} is specified in (18)
7:    𝒱←proj𝗇𝗎𝗅𝗅⁡({𝐇i∗:i∈[t−1]})im(Δ𝐒X⋅𝐰)\mathcal{V}\leftarrow\proj_{{\sf null}(\{\mathbf{H}^{*}_
{i}\,:\,i\,\in\,[t-1]\})}\im(\Delta\mathbf{S}_{X}\cdot\mathbf{w})
8:    if dim(𝒱)=1\dim(\mathcal{V})=1 then
9:      pick 𝐯∈im⁡(Δ​𝐒X⋅𝐰)∖span⁡({𝐇i∗:i∈[t−1]})\mathbf{v}\in\im(\Delta\mathbf{S}_{X}\cdot\mathbf
{w})\setminus\sspan(\{\mathbf{H}^{*}_{i}:i\in[t-1]\})
10:      𝐇t∗←𝐯/‖𝐯‖2\mathbf{H}^{*}_{t}\leftarrow\mathbf{v}/\|\mathbf{v}\|_{2}   and   [𝐖]:,t←𝐰[\
mathbf{W}]_{:,t}\leftarrow\mathbf{w}
11:      break      
12: Stage 2 outputs: 𝐇∗\mathbf{H}^{*} and 𝐖\mathbf{W}
13:  
14: Stage 3: Identifiability up ancestors
15: Initialize 𝒢^\hat{\mathcal{G}} with empty graph over nodes [n][n]
16: for t∈(n−1,…,1)t\in(n-1,\dots,1) do
17:   for j∈(t+1,…,n)j\in(t+1,\dots,n) do
18:    if j∈ch^​(t)j\in\hat{\ch}(t) then
19:      continue    
20:    if_parent ←\leftarrow True
21:    ℳt,j←[j−1]∖{ch^​(t)∪{t}}\mathcal{M}_{t,j}\leftarrow[j-1]\setminus\{\hat{\ch}(t)\cup\{t\}\}
22:    for (α,β)∈{−(∥𝐖:,j∥1,…,∥𝐖:,j∥1)}×[∥𝐖:,t∥1](\alpha,\beta)\in\{-(\|\mathbf{W}_{:,j}\|_{1},\
dots,\|\mathbf{W}_{:,j}\|_{1})\}\times[\|\mathbf{W}_{:,t}\|_{1}] do
23:      𝐰∗←α[𝐖]:,t+β[𝐖]:,j\mathbf{w}^{*}\leftarrow\alpha[\mathbf{W}]_{:,t}+\beta[\mathbf{W}]_{:,j}
24:      𝒱←proj𝗇𝗎𝗅𝗅⁡({𝐇i∗:i∈ℳt,j})im(Δ𝐒X⋅𝐰∗)\mathcal{V}\leftarrow\proj_{{\sf null}(\{\mathbf{H}^{*}
_{i}\;:\;i\;\in\;\mathcal{M}_{t,j}\})}\im(\Delta\mathbf{S}_{X}\cdot\mathbf{w}^{*})
25:      if dim(𝒱)=1\dim(\mathcal{V})=1 then
26:       pick 𝐯∈im⁡(Δ​𝐒X⋅𝐰∗)∖span⁡({𝐇i∗:i∈ℳt,j})\mathbf{v}\in\im(\Delta\mathbf{S}_{X}\cdot\mathbf
{w}^{*})\setminus\sspan(\{\mathbf{H}^{*}_{i}\;:\;i\;\in\;\mathcal{M}_{t,j}\})
27:       𝐇j∗←𝐯/‖𝐯‖2\mathbf{H}^{*}_{j}\leftarrow\mathbf{v}/\|\mathbf{v}\|_{2}   and   [𝐖]:,j←𝐰∗[\
mathbf{W}]_{:,j}\leftarrow\mathbf{w}^{*}
28:       Set if_parent ←\leftarrow False and break         
29:    if if_parent is True then
30:      Add t→jt\to j and t→ut\to u to 𝒢^\hat{\mathcal{G}} for all u∈de^​(j)u\in\hat{\de}(j) ⊳\
triangleright edges to identified descendants      
31: Stage 3 outputs: 𝒢^,𝐇∗\hat{\mathcal{G}},\mathbf{H}^{*} and 𝐖\mathbf{W}
32:  
33: if the interventions are hard then
34:   Stage 4: Unmixing for hard interventions
35:   for t∈(2,…,n)t\in(2,\dots,n) do ⊳\triangleright refine rows of 𝐇∗\mathbf{H}^{*} sequentially
36:    Z^←Z^​(X,𝐇∗)\hat{Z}\leftarrow\hat{Z}(X;\mathbf{H}^{*})
37:    𝐮obs←−Cov(Z^t,Z^an^​(t))⋅[Cov(Z^an^​(t))]−1\mathbf{u}^{\rm obs}\leftarrow-\Cov(\hat{Z}_{t},\
hat{Z}_{\hat{\an}(t)})\cdot[\Cov(\hat{Z}_{\hat{\an}(t)})]^{-1}
38:    for m∈{i:𝐖i,t≠0}m\in\{i:\mathbf{W}_{i,t}\neq 0\} do ⊳\triangleright searching for a suitable
environment
39:      Z^m←Z^m​(X,𝐇∗)\hat{Z}^{m}\leftarrow\hat{Z}^{m}(X;\mathbf{H}^{*})
40:      𝐮m←−Cov(Z^tm,Z^an^​(t)m)⋅[Cov(Z^an^​(t)m)]−1\mathbf{u}^{m}\leftarrow-\Cov(\hat{Z}^{m}_{t},
\hat{Z}^{m}_{\hat{\an}(t)})\cdot[\Cov(\hat{Z}^{m}_{\hat{\an}(t)})]^{-1}
41:      if (𝐮m⋅Z^an^​(t)m+Z^tm)⟂⟂Z^an^​(t)m(\mathbf{u}^{m}\cdot\hat{Z}^{m}_{\hat{\an}(t)}+\hat{Z}^
{m}_{t})\perp\!\!\!\perp\hat{Z}^{m}_{\hat{\an}(t)} and 𝐮m≠𝐮obs\mathbf{u}^{m}\neq\mathbf{u}^{\rm
obs} then
42:       𝐇t∗←𝐇t∗+𝐮m⋅𝐇an^​(t)∗\mathbf{H}^{*}_{t}\leftarrow\mathbf{H}^{*}_{t}+\mathbf{u}^{m}\cdot\
mathbf{H}^{*}_{\hat{\an}(t)} ⊳\triangleright removing the effect of the ancestors on ZtZ_{t}
43:       break           
44:   Z^←Z^​(X,𝐇∗)\hat{Z}\leftarrow\hat{Z}(X;\mathbf{H}^{*}) ⊳\triangleright use recovered ZZ in
obs. env. for graph recovery
45:   for t∈(1,…,n)t\in(1,\dots,n) do
46:    for j∈ch^​(t)j\in\hat{\ch}(t) do
47:      if Z^t⟂⟂Z^j|{Z^i:i∈pa^​(j)∖{t}}\hat{Z}_{t}\perp\!\!\!\perp\hat{Z}_{j}\mid\{\hat{Z}_{i}:i\
in\hat{\pa}(j)\setminus\{t\}\} then
48:       Remove t→jt\to j from 𝒢^\hat{\mathcal{G}} ⊳\triangleright removing the edges from the
nonparent ancestors           
49:  
50: Return 𝒢^\hat{\mathcal{G}} and Z^\hat{Z}

Stage 1: Basis score differences. In the first stage, we compute score differences for each
interventional environment and construct the basis score difference functions that are linearly
independent. The purpose of these functions is to subsequently use them and reconstruct the score
differences under any arbitrary hypothetical interventional environment. To this end, we use the
following relationship between score functions of XX and ZZ.

Lemma 1 ([7, Corollary 2]).

Latent and observational score functions are related via 𝐬X​(x)=[𝐆†]⊤⋅𝐬Z​(z)\boldsymbol{s}_{X}(x)=
[\mathbf{G}^{{\dagger}}]^{\top}\cdot\boldsymbol{s}_{Z}(z), where x=𝐆⋅zx=\mathbf{G}\cdot z.

Using Lemma 1 for the scores and score differences defined in (7) and (8), respectively, we have

 Δ​𝒔Xm​(x)=[𝐆†]⊤⋅Δ​𝒔Zm​(z)​=(7)​[𝐆†]⊤⋅∑i∈Im∇log⁡pi​(zi∣zpa⁡(i))qi​(zi∣zpa⁡(i)).\Delta\
 boldsymbol{s}_{X}^{m}(x)=[\mathbf{G}^{{\dagger}}]^{\top}\cdot\Delta\boldsymbol{s}_{Z}^{m}(z)  (13)
 \overset{\eqref{eq:sz-decomposition}}{=}[\mathbf{G}^{{\dagger}}]^{\top}\cdot\sum_{i\in I^
 {m}}\nabla\log\frac{p_{i}(z_{i}\mid z_{\pa(i)})}{q_{i}(z_{i}\mid z_{\pa(i)})}\ .

We compactly represent the summands in the right-hand side of (13) by defining the matrix-valued
function 𝚲:ℝn→ℝn×n\mathbf{\Lambda}:{\mathbb{R}}^{n}\to{\mathbb{R}}^{n\times n} with the entries

 [𝚲⁡(z)]i,j≜∂∂zj​log⁡pi​(zi∣zpa⁡(i))qi​(zi∣zpa⁡(i)),∀i,j∈[n],[\mathbf{\Lambda}(z)]_{i,j}\
 triangleq\frac{\partial}{\partial z_{j}}\log\frac{p_{i}(z_{i}\mid z_{\pa(i)})}{q_{i}(z_{i}\   (14)
 mid z_{\pa(i)})}\ ,\quad\forall i,j\in[n]\ ,

based on which (13) can be restated as

 Δ𝒔Xm(x)=[𝐆†]⊤⋅𝚲(z)⋅[𝐃int]:,m.\Delta\boldsymbol{s}_{X}^{m}(x)=[{\mathbf{G}^{{\dagger}}}]^{\    (15)
 top}\cdot\mathbf{\Lambda}(z)\cdot[\mathbf{D}_{\rm int}]_{:,m}\ .

We note that [𝚲⁡(z)]i,j[\mathbf{\Lambda}(z)]_{i,j} is constantly zero for i∉{pa⁡(j)∪{j}}i\notin\{\
pa(j)\cup\{j\}\}, and jj-th column of 𝚲\mathbf{\Lambda} is a function of the variables in {zk:k∈pa⁡
(j)∪{j}}\{z_{k}:k\in\pa(j)\cup\{j\}\} which implies that the columns of 𝚲\mathbf{\Lambda} are
linearly independent. Throughout the rest of the paper, we omit the arguments of the functions
Δ​𝒔Xm\Delta\boldsymbol{s}_{X}^{m} and 𝚲\mathbf{\Lambda} when the dependence is clear from the
context. Note that 𝐃int\mathbf{D}_{\rm int} has nn linearly independent columns (Assumption 1).
Denote the indices of the independent columns by {b1,…,bn}\{b_{1},\dots,b_{n}\} and define the
basis intervention matrix 𝐃∈ℝn×n\mathbf{D}\in{\mathbb{R}}^{n\times n} using these columns as

 [𝐃]i,m≜[𝐃int]i,bm=𝟙{i∈Ibm},∀i,m∈[n].[\mathbf{D}]_{i,m}\triangleq[\mathbf{D}_{\rm int}]_{i,b_  (16)
 {m}}=\mathds{1}\{i\in I^{b_{m}}\}\ ,\quad\forall i,m\in[n]\ .

Subsequently, it can be readily verified that the score difference functions {Δ​𝒔Xm:m∈{b1,…,bn}}\{\
Delta\boldsymbol{s}_{X}^{m}:m\in\{b_{1},\dots,b_{n}\}\} are also linearly independent by leveraging
(15) and linearly independent columns of 𝚲\mathbf{\Lambda}. Hence, these score difference functions
are sufficient to reconstruct the remaining unavailable score difference functions. As such,
{Δ​𝒔Xm:m∈{b1,…,bn}}\{\Delta\boldsymbol{s}_{X}^{m}:m\in\{b_{1},\dots,b_{n}\}\} serve as basis score
difference functions. We stack these basis score differences, where each is a dd-dimensional
vector, to construct the matrix-valued function Δ​𝐒X:𝒳→ℝd×n\Delta\mathbf{S}_{X}:\mathcal{X}\to{\
mathbb{R}}^{d\times n}, which is used as the basis score difference matrix in the subsequent
stages.

 Δ​𝐒X≜[Δ​𝒔Xb1,…,Δ​𝒔Xbn]=[𝐆†]⊤⋅𝚲⋅𝐃.\Delta\mathbf{S}_{X}\triangleq\big[\Delta\boldsymbol{s}_{X}
 ^{b_{1}},\dots,\Delta\boldsymbol{s}_{X}^{b_{n}}\big]=[\mathbf{G}^{{\dagger}}]^{\top}\cdot\    (17)
 mathbf{\Lambda}\cdot\mathbf{D}\ .

Finally, we note that Δ​𝐒X\Delta\mathbf{S}_{X} is directly estimated from samples of XX via
learning {Δ​𝒔Xbm:m∈[n]}\{\Delta\boldsymbol{s}_{X}^{b_{m}}:m\in[n]\}. Since Λ\Lambda encodes the
score differences in latent space, it cannot be estimated directly. Furthermore, 𝐃\mathbf{D} is
unknown, and (17) is given to emphasize the relationship between observed and latent score
differences.

Stage 2: Identifiability up to an unknown causal order. We design a process that aggregates score
differences of the UMN interventions and obtains a partial identifiability guarantee
(identifiability up to an unknown causal order) as an intermediate step toward more accurate
identifiability. Specifically, we linearly aggregate the columns of Δ​𝐒X\Delta\mathbf{S}_{X} such
that those aggregate scores facilitate identifiability up to an unknown causal order. Such mixing
of the columns is facilitated by computing Δ​𝐒X⋅𝐖\Delta\mathbf{S}_{X}\cdot\mathbf{W}, where the
mixing matrix 𝐖∈ℝn×n\mathbf{W}\in{\mathbb{R}}^{n\times n} should be learned. Given the
decomposition of Δ​𝐒X\Delta\mathbf{S}_{X} in (17), if we learn 𝐖\mathbf{W} such that 𝐃⋅𝐖\mathbf{D}\
cdot\mathbf{W} is upper triangular up to a row permutation, then we can subsequently learn an
intermediate estimate 𝐇∗\mathbf{H}^{*} using the image of Δ​𝐒X\Delta\mathbf{S}_{X}. This ensures
identifiability up to an unknown causal order since rows of 𝐇∗\mathbf{H}^{*} will be equal to
combinations of rows of 𝐆†\mathbf{G}^{{\dagger}} up to a causal order.

We design an iterative process to sequentially learn the columns of 𝐖\mathbf{W}. Specifically, at
each iteration of Stage 2, we learn an integer-valued vector 𝐰\mathbf{w} such that the projection
of im⁡(Δ​𝐒X⋅𝐰)\im(\Delta\mathbf{S}_{X}\cdot\mathbf{w}) onto the nullspace of the partially
recovered encoder estimate becomes a one-dimensional subspace. To see why this procedure works,
note that the function Δ​𝐒X⋅𝐰\Delta\mathbf{S}_{X}\cdot\mathbf{w} is essentially a combination of SN
latent score differences via (17), and the SN score difference ∇log⁡pi​(zi∣zpa⁡(i))−∇log⁡qi​
(zi∣zpa⁡(i))\nabla\log p_{i}(z_{i}\mid z_{\pa(i)})-\nabla\log q_{i}(z_{i}\mid z_{\pa(i)}) is a
one-dimensional subspace if and only if the intervened node ii has no parents. By taking the
projection of the im⁡(Δ​𝐒X⋅𝐰)\im(\Delta\mathbf{S}_{X}\cdot\mathbf{w}) onto the nullspace of the
partially learned encoder while searching for a desired 𝐰\mathbf{w}, we ensure that the final
encoder estimate 𝐇∗\mathbf{H}^{*} of this stage will be full-rank. Finally, we use κ\kappa to
denote maximum determinant of a matrix in {0,1}(n−1)×(n−1)\{0,1\}^{(n-1)\times(n-1)}, and show that
the set

 𝒲≜{−κ,…,+κ}n\mathcal{W}\triangleq\{-\kappa,\dots,+\kappa\}^{n}  (18)

is guaranteed to contain such 𝐰\mathbf{w} vectors. The following result summarizes the guarantees
of this procedure.

Lemma 2.

Under Assumption 1 and intervention regularity, outputs of Stage 2 of Algorithm 1 satisfy:

 1. 1.
   
    𝐃⋅𝐖\mathbf{D}\cdot\mathbf{W} has nonzero diagonal entries and is upper triangular up to a row
    permutation.

 2. 2.
   
    [𝐇∗]t∈span⁡({[𝐆†]πj:j∈[t]})[\mathbf{H}^{*}]_{t}\in\sspan(\{[\mathbf{G}^{{\dagger}}]_{\pi_{j}}:j
    \in[t]\}) for all t∈[n]t\in[n] and {[𝐇∗]t:t∈[n]}\{[\mathbf{H}^{*}]_{t}:t\in[n]\} are linearly
    independent.

Proof: See Appendix A.1.  □\square

Stage 3: Identifiability up to ancestors. Next, we refine the outcome of Stage 2 to ensure
identifiability up to ancestors by updating the columns of 𝐖\mathbf{W} such that the entries of 𝐃⋅𝐖
\mathbf{D}\cdot\mathbf{W} can be nonzero only for the coordinates that correspond to
ancestor-descendant node pairs. For this purpose, we design Stage 3 of UMNI-CRL that iteratively
updates the columns of 𝐖\mathbf{W}. The key idea is that the edges in the transitive closure graph
𝒢tc\mathcal{G}_{\rm tc} can be determined by investigating the subspaces’ dimensions similarly to
Stage 2. Leveraging this property, for all node pairs that do not constitute an edge in 𝒢tc\mathcal
{G}_{\rm tc}, we aggregate the corresponding columns of 𝐖\mathbf{W} such that the corresponding
entry of 𝐃⋅𝐖\mathbf{D}\cdot\mathbf{W} will be zero. In Theorem 3, we show that the outputs of this
stage achieve identifiability up to ancestors, that is 𝒢^\hat{\mathcal{G}} is isomorphic to 𝒢tc\
mathcal{G}_{\rm tc} and Z^i\hat{Z}_{i} is a linear function of {Zπj:j∈an⁡(i)∪{i}}\{Z_{\pi_{j}}:j\in
\an(i)\cup\{i\}\} for all i∈[n]i\in[n].

Theorem 3.

Under Assumption 1 and regular UMN soft interventions, outputs of Stage 3 of Algorithm 1 have the
following properties.

 1. 1.
   
    The estimate Z^​(X,𝐇∗)\hat{Z}(X;\mathbf{H}^{*}) satisfies identifiability up to ancestors.

 2. 2.
   
    𝒢^\hat{\mathcal{G}} and 𝒢tc\mathcal{G}_{\rm tc} are related through a graph isomorphism.

Proof: See Appendix A.2.  □\square

Stage 4: Perfect identifiability via hard interventions. In the case of hard interventions, we
apply an unmixing procedure to further refine our estimates and achieve perfect identifiability.
This stage consists of two steps. The first step relies on the property that the intervened node
becomes independent of its non-descendants and updates rows of the encoder estimate sequentially.
In the second step, we leverage the knowledge of ancestral relationships and use a small number of
conditional independence tests to refine the graph estimate from transitive closure 𝒢tc\mathcal{G}_
{\rm tc} to the true latent DAG 𝒢\mathcal{G}. The following theorem summarizes the guarantees
achieved by Algorithm 1.

Theorem 4.

Under Assumption 1 and regular UMN hard interventions for an additive noise model, outputs of
Stage 4 of Algorithm 1 have the following properties.

 1. 1.
   
    Estimate Z^​(X,𝐇∗)\hat{Z}(X;\mathbf{H}^{*}) satisfies perfect latent variable recovery.

 2. 2.
   
    If pZp_{Z} is adjacency-faithful to 𝒢\mathcal{G}, then 𝒢^\hat{\mathcal{G}} and 𝒢\mathcal{G} are
    related through a graph isomorphism.

Proof: See Appendix A.3.  □\square

Finally, we note that the computational cost of UMNI-CRL is dominated by the cardinality of the
search space for aggregating the score differences, e.g., in the worst-case, Stage 2 has 𝒪⁡((2​κ)n)
\mathcal{O}((2\kappa)^{n}) complexity. Therefore, the structure of the UMN interventions determines
the complexity via its determinant. We elaborate on the computational complexity of the algorithm
and the range of κ\kappa in Appendix A.8.

5 Simulations

We empirically assess the performance of the UMNI-CRL algorithm for recovering the latent DAG 𝒢\
mathcal{G} and latent variables ZZ. Implementation details and additional results are provided in
Appendix B^4^4 4 The codebase for the experiments can be found at https://github.com/acarturk-e/
umni-crl..

Data generation. To generate 𝒢\mathcal{G}, we use Erdős-Rényi model with density 0.50.5 and n∈
{4,5,6,7,8}n\in\{4,5,6,7,8\} nodes. For the causal models, we adopt linear structural equation
models (SEMs) with Gaussian noise. The nonzero edge weights of the linear SEMs are sampled from
Unif⁡(±[0.5,1.5]){\rm Unif}(\pm[0.5,1.5]), and the noise terms are zero-mean Gaussian variables
with variances σi2\sigma_{i}^{2} sampled from Unif⁡([0.5,1.5]){\rm Unif}([0.5,1.5]). For a soft
intervention on node ii, the edge weight vector of node ii is reduced by a factor of 1/21/2, and
for a hard intervention, the edge weights are set to zero. The variance of the noise term is
reduced to σi2/4\nicefrac{{\sigma_{i}^{2}}}{{4}} in both intervention types. We consider target
dimensions d∈{10,50}d\in\{10,50\}, generate 100 latent graphs for each (n,d)(n,d) pair, and
generate ns=105n_{\rm s}=10^{5} samples of ZZ from each environment. Transformation 𝐆∈ℝd×n\mathbf
{G}\in{\mathbb{R}}^{d\times n} is randomly sampled under full-rank constraint, and observed
variables are generated as X=𝐆⋅ZX=\mathbf{G}\cdot Z. Finally, for each graph realization,
intervention matrix 𝐃\mathbf{D} is chosen randomly among column permutations of full-rank {0,1}n×n\
{0,1\}^{n\times n} matrices, which satisfies Assumption 1.

Score functions. The algorithm uses score differences between environment pairs. Since we use a
linear Gaussian model, XX is also multivariate Gaussian, and its score function can be estimated by
sX(x)=−Θ^⋅xs_{X}(x)=-\hat{\Theta}\cdot x in which Θ^\hat{\Theta} is the sample estimate of the
precision matrix of XX. We note that the design of UMNI-CRL is agnostic to the choice of the
estimator and can adopt any reliable score estimator for nonparametric distributions [29, 30].

Graph recovery. To assess the graph recovery, we report the structural Hamming distance (SHD)
between the true and estimated DAGs. Recall that UMN hard and soft interventions ensure different
levels of identifiability guarantees. Hence, we report the SHD between (i) transitive closure 𝒢tc\
mathcal{G}_{\rm tc} and 𝒢^\hat{\mathcal{G}} for soft interventions, and (ii) true DAG 𝒢\mathcal{G}
and 𝒢^\hat{\mathcal{G}} for hard interventions. Table 2 shows that latent graph recovery
performance remains consistent for both soft and hard interventions when observed variables
dimension dd increases from 1010 to 5050, which conforms to our expectations due to theoretical
results. Note that the expected number of edges is n⁡(n−1)/4n(n-1)/4 since we set the density of
random graphs to 0.50.5. Hence, the increasing SHD is also unsurprising when the latent dimension
nn increases from 44 to 88, and the performance remains reasonable at n=8n=8. Finally, we note that
n=8n=8 is the largest latent graph size considered among the closely related SN intervention
studies [4, 8, 7, 9].

Latent variable recovery. The estimates are given by Z^​(X,𝐇∗)=(𝐇∗⋅𝐆)⋅Z\hat{Z}(X;\mathbf{H}^{*})=(\
mathbf{H}^{*}\cdot\mathbf{G})\cdot Z. Hence, we scrutinize the effective mixing matrix (𝐇∗⋅𝐆)(\
mathbf{H}^{*}\cdot\mathbf{G}) and report the ratio of its incorrect mixing entries to the number of
zeros in constant matrices 𝐂s\mathbf{C}_{\rm s} and 𝐂an\mathbf{C}_{\rm an} according to Definition 
1, denoted by

 ℓ𝗁𝖺𝗋𝖽≜∑j≠i𝟙​([𝐇∗⋅𝐆]i,j≠0)n2−n,andℓ𝗌𝗈𝖿𝗍≜∑j∉an¯⁡(i)𝟙​([𝐇∗⋅𝐆]i,j≠0)n2−∑i|an¯⁡(i)|.\ell_{\sf
 hard}\triangleq\frac{\sum_{j\neq i}\mathds{1}([\mathbf{H}^{*}\cdot\mathbf{G}]_{i,j}\neq 0)}   (19)
 {n^{2}-n}\ ,\qquad\mbox{and}\qquad\ell_{\sf soft}\triangleq\frac{\sum_{j\notin\anplus(i)}\
 mathds{1}([\mathbf{H}^{*}\cdot\mathbf{G}]_{i,j}\neq 0)}{n^{2}-\sum_{i}|\anplus(i)|}\ .

We also report the mean correlation coefficient (MCC) [31], which measures linear correlations
between the estimated and ground truth latent variables and is commonly used in related work.
Table 2 shows that the UMNI-CRL algorithm achieves strong MCC performance (over 0.900.90) in all
cases. Furthermore, the ratio of incorrect mixing entries remains less than 0.200.20 for both soft
and hard interventions This demonstrates a strong performance of the UMNI-CRL algorithm at
recovering latent variables for as many as n=8n=8 latent variables even for observed variables
dimension d=50d=50.


Table 2: UMNI-CRL for a linear causal model with UMN interventions (mean ±\pm standard error)

                          UMN Soft                                       UMN Hard
        SHD⁡(𝒢tc,𝒢^){\rm                                SHD⁡(𝒢,𝒢^){\rm                
nn dd SHD}(\mathcal{G}_{\       MCC      ℓ𝗌𝗈𝖿𝗍\ell_{\   SHD}(\mathcal        MCC      ℓ𝗁𝖺𝗋𝖽\ell_{\
      rm tc},\hat{\mathcal                 sf soft}       {G},\hat{\                    sf hard}
             {G}})                                       mathcal{G}})
4  10 0.91±0.120.91\pm     0.95±0.010.95 0.08±0.010.08 0.75±0.110.75\pm 0.98±0.020.98 0.13±0.020.13
      0.12                 \pm 0.01      \pm 0.01      0.11             \pm 0.02      \pm 0.02
5  10 1.67±0.201.67\pm     0.93±0.010.93 0.09±0.010.09 1.65±0.111.65\pm 0.97±0.020.97 0.13±0.020.13
      0.20                 \pm 0.01      \pm 0.01      0.11             \pm 0.02      \pm 0.02
6  10 3.19±0.263.19\pm     0.92±0.010.92 0.12±0.010.12 3.12±0.253.12\pm 0.96±0.020.96 0.12±0.020.12
      0.26                 \pm 0.01      \pm 0.01      0.25             \pm 0.02      \pm 0.02
7  10 5.44±0.345.44\pm     0.90±0.010.90 0.15±0.010.15 5.36±0.355.36\pm 0.93±0.030.93 0.15±0.030.15
      0.34                 \pm 0.01      \pm 0.01      0.35             \pm 0.03      \pm 0.03
8  10 7.63±0.417.63\pm     0.89±0.010.89 0.16±0.010.16 9.70±0.529.70\pm 0.87±0.030.87 0.20±0.030.20
      0.41                 \pm 0.01      \pm 0.01      0.52             \pm 0.03      \pm 0.03
4  50 0.77±0.120.77\pm     0.96±0.010.96 0.06±0.010.06 0.66±0.100.66\pm 0.98±0.010.98 0.13±0.020.13
      0.12                 \pm 0.01      \pm 0.01      0.10             \pm 0.01      \pm 0.02
5  50 1.93±0.201.93\pm     0.93±0.010.93 0.10±0.010.10 1.80±0.191.80\pm 0.98±0.020.98 0.13±0.010.13
      0.20                 \pm 0.01      \pm 0.01      0.19             \pm 0.02      \pm 0.01
6  50 3.39±0.273.39\pm     0.92±0.010.92 0.13±0.010.13 3.05±0.253.05\pm 0.95±0.030.95 0.13±0.010.13
      0.27                 \pm 0.01      \pm 0.01      0.25             \pm 0.03      \pm 0.01
7  50 4.62±0.304.62\pm     0.91±0.010.91 0.13±0.010.13 6.12±0.346.12\pm 0.91±0.020.91 0.16±0.010.16
      0.30                 \pm 0.01      \pm 0.01      0.34             \pm 0.02      \pm 0.01
8  50 8.26±0.498.26\pm     0.90±0.010.90 0.14±0.010.14 9.01±0.539.01\pm 0.88±0.030.88 0.28±0.020.28
      0.49                 \pm 0.01      \pm 0.01      0.53             \pm 0.03      \pm 0.02

6 Discussion

In this paper, we established novel identifiability results using unknown multi-node (UMN)
interventions for CRL under linear transformations. Specifically, we designed the provably correct
UMNI-CRL algorithm, leveraging the structural properties of score functions across different
environments. To facilitate identifiability, we introduced a sufficient condition for the set of
UMN interventions, abstracted as having nn sufficiently diverse interventional environments.
Investigating the necessary conditions for UMN interventions to enable identifiability remains an
open problem. The main limitation is the assumption of linear transformations. Given existing
results for general transformations using two SN interventions per node [6, 9], a promising
direction for future work is extending our results to general transformations using UMN
interventions with multiple interventional mechanisms per node.

Acknowledgements and disclosure of funding

This work was supported by IBM through the IBM-Rensselaer Future of Computing Research
Collaboration.

References

  • [1] Bernhard Schölkopf, Francesco Locatello, Stefan Bauer, Nan Rosemary Ke, Nal Kalchbrenner,
    Anirudh Goyal, and Yoshua Bengio. Toward causal representation learning. Proceedings of the
    IEEE, 109(5):612–634, May 2021.
  • [2] Francesco Locatello, Stefan Bauer, Mario Lucic, Gunnar Raetsch, Sylvain Gelly, Bernhard
    Schölkopf, and Olivier Bachem. Challenging common assumptions in the unsupervised learning of
    disentangled representations. In Proc. International Conference on Machine Learning, Long
    Beach, CA, June 2019.
  • [3] Kartik Ahuja, Divyat Mahajan, Yixin Wang, and Yoshua Bengio. Interventional causal
    representation learning. In Proc. International Conference on Machine Learning, Honolulu,
    Hawaii, July 2023.
  • [4] Chandler Squires, Anna Seigal, Salil S. Bhate, and Caroline Uhler. Linear causal
    disentanglement via interventions. In Proc. International Conference on Machine Learning,
    Honolulu, Hawaii, July 2023.
  • [5] Burak Varıcı, Emre Acartürk, Karthikeyan Shanmugam, Abhishek Kumar, and Ali Tajer. 
    Score-based causal representation learning with interventions. arXiv:2301.08230, 2023.
  • [6] Burak Varıcı, Emre Acartürk, Karthikeyan Shanmugam, and Ali Tajer. General identifiability
    and achievability for causal representation learning. In Proc. International Conference on
    Artificial Intelligence and Statistics, Valencia, Spain, May 2024a.
  • [7] Burak Varıcı, Emre Acartürk, Karthikeyan Shanmugam, Abhishek Kumar, and Ali Tajer. 
    Score-based causal representation learning: Linear and general transformations. 
    arXiv:2402.00849, 2024b.
  • [8] Simon Buchholz, Goutham Rajendran, Elan Rosenfeld, Bryon Aragam, Bernhard Schölkopf, and
    Pradeep Ravikumar. Learning linear causal representations from interventions under general
    nonlinear mixing. In Proc. Advances in Neural Information Processing Systems, New Orleans, LA,
    December 2023.
  • [9] Julius von Kügelgen, Michel Besserve, Wendong Liang, Luigi Gresele, Armin Kekić, Elias
    Bareinboim, David M Blei, and Bernhard Schölkopf. Nonparametric identifiability of causal
    representations from unknown interventions. In Proc. Advances in Neural Information Processing
    Systems, New Orleans, LA, December 2023.
  • [10] Jiaqi Zhang, Chandler Squires, Kristjan Greenewald, Akash Srivastava, Karthikeyan
    Shanmugam, and Caroline Uhler. Identifiability guarantees for causal disentanglement from soft
    interventions. In Proc. Advances in Neural Information Processing Systems, New Orleans, LA,
    December 2023.
  • [11] Chandler Squires, Yuhao Wang, and Caroline Uhler. Permutation-based causal structure
    learning with unknown intervention targets. In Proc. Conference in Uncertainty in Artificial
    Intelligence, August 2020.
  • [12] Alejandro Tejada-Lapuerta, Paul Bertin, Stefan Bauer, Hananeh Aliee, Yoshua Bengio, and
    Fabian J Theis. Causal machine learning for single-cell genomics. arXiv:2310.14935, 2023.
  • [13] Tabitha Edith Lee, Shivam Vats, Siddharth Girdhar, and Oliver Kroemer. Scale: Causal
    learning and discovery of robot manipulation skills using simulation. In CoRL 2023 Workshop on
    Learning Effective Abstractions for Planning, Atlanta, GA, November 2023.
  • [14] Jikai Jin and Vasilis Syrgkanis. Learning causal representations from general
    environments: Identifiability and intrinsic ambiguity. arXiv:2311.12267, 2023.
  • [15] Yibo Jiang and Bryon Aragam. Learning nonparametric latent causal graphs with unknown
    interventions. In Proc. Advances in Neural Information Processing Systems, New Orleans, LA,
    December 2023.
  • [16] Simon Bing, Urmi Ninad, Jonas Wahl, and Jakob Runge. Identifying linearly-mixed causal
    representations from multi-node interventions. In Proc. Causal Learning and Reasoning, Los
    Angeles, CA, April 2024.
  • [17] Kartik Ahuja, Amin Mansouri, and Yixin Wang. Multi-domain causal representation learning
    via weak distributional invariances. In Proc. International Conference on Artificial
    Intelligence and Statistics, Valencia, Spain, May 2024.
  • [18] Julius von Kügelgen, Yash Sharma, Luigi Gresele, Wieland Brendel, Bernhard Schölkopf,
    Michel Besserve, and Francesco Locatello. Self-supervised learning with data augmentations
    provably isolates content from style. In Proc. Advances in Neural Information Processing
    Systems, virtual, December 2021.
  • [19] Johann Brehmer, Pim De Haan, Phillip Lippe, and Taco S Cohen. Weakly supervised causal
    representation learning. In Proc. Advances in Neural Information Processing Systems, New
    Orleans, LA, December 2022.
  • [20] Dingling Yao, Danru Xu, Sebastien Lachapelle, Sara Magliacane, Perouz Taslakian, Georg
    Martius, Julius von Kügelgen, and Francesco Locatello. Multi-view causal representation
    learning with partial observability. In Proc. International Conference on Learning
    Representations, Vienna, Austria, May 2024.
  • [21] Nils Sturma, Chandler Squires, Mathias Drton, and Caroline Uhler. Unpaired multi-domain
    causal representation learning. In Proc. Advances in Neural Information Processing Systems, New
    Orleans, LA, December 2023.
  • [22] Phillip Lippe, Sara Magliacane, Sindy Löwe, Yuki M Asano, Taco Cohen, and Efstratios
    Gavves. Causal representation learning for instantaneous and temporal effects in interactive
    systems. In Proc. International Conference on Learning Representations, Kigali, Rwanda, May
    2023a.
  • [23] Phillip Lippe, Sara Magliacane, Sindy Löwe, Yuki M Asano, Taco Cohen, and Efstratios
    Gavves. BISCUIT: Causal representation learning from binary interactions. In Proc. Uncertainty
    in Artificial Intelligence, Pittsburgh, PA, August 2023b.
  • [24] Yuhang Liu, Zhen Zhang, Dong Gong, Mingming Gong, Biwei Huang, Anton van den Hengel, Kun
    Zhang, and Javen Qinfeng Shi. Identifiable latent polynomial causal models through the lens of
    change. In Proc. International Conference on Learning Representations, Vienna, Austria, May
    2024.
  • [25] Sébastien Lachapelle, Pau Rodríguez López, Yash Sharma, Katie Everett, Rémi Le Priol,
    Alexandre Lacoste, and Simon Lacoste-Julien. Nonparametric partial disentanglement via
    mechanism sparsity: Sparse actions, interventions and sparse temporal dependencies. 
    arXiv:2401.04890, 2024.
  • [26] Kun Zhang, Shaoan Xie, Ignavier Ng, and Yujia Zheng. Causal representation learning from
    multiple distributions: A general setting. In Proc. International Conference on Machine
    Learning, Vienna, Austria, July 2024.
  • [27] Hiroshi Morioka and Aapo Hyvärinen. Causal representation learning made identifiable by
    grouping of observational variables. In Proc. International Conference on Machine Learning,
    Vienna, Austria, July 2024.
  • [28] Aneesh Komanduri, Xintao Wu, Yongkai Wu, and Feng Chen. From identifiable causal
    representations to controllable counterfactual generation: A survey on causal generative
    modeling. Transactions on Machine Learning Research, May 2024.
  • [29] Yang Song, Sahaj Garg, Jiaxin Shi, and Stefano Ermon. Sliced score matching: A scalable
    approach to density and score estimation. In Proc. Uncertainty in Artificial Intelligence,
    virtual, August 2020.
  • [30] Yuhao Zhou, Jiaxin Shi, and Jun Zhu. Nonparametric score estimators. In Proc.
    International Conference on Machine Learning, virtual, July 2020.
  • [31] Ilyes Khemakhem, Ricardo Monti, Diederik Kingma, and Aapo Hyvärinen. Ice-beem:
    Identifiable conditional energy-based deep models based on nonlinear ICA. In Proc. Advances in
    Neural Information Processing Systems, virtual, December 2020.
  • [32] Joel Brenner and Larry Cummings. The hadamard maximum determinant problem. The American
    Mathematical Monthly, 79(6):626–630, 1972.
  • [33] Igor Araujo, József Balogh, and Yuzhou Wang. Maximum determinant and permanent of sparse
    0-1 matrices. Linear Algebra and its Applications, 645:194–228, 2022.

 

Linear Causal Representation Learning from Unknown Multi-node Interventions
Appendices

 

Appendix A Proofs

We start this section by introducing the additional notations used in the subsequent proofs.

Additional notations.

We use {𝐞i:i∈[n]}\{\mathbf{e}_{i}:i\in[n]\} to denote nn-dimensional standard unit basis vectors.
For a set 𝒮∈[n]\mathcal{S}\in[n], we use 𝐀𝒮\mathbf{A}_{\mathcal{S}} to denote the submatrix of 𝐀\
mathbf{A} constructed by the rows {𝐀i:i∈𝒮}\{\mathbf{A}_{i}:i\in\mathcal{S}\}. For a valid encoder
𝐇∈ℋ\mathbf{H}\in\mathcal{H}, we denote the score functions associated with the pdfs of Z^​(X,𝐇)\hat
{Z}(X;\mathbf{H}) under environments ℰ0\mathcal{E}^{0} and ℰm\mathcal{E}^{m} by 𝒔Z^​(⋅,𝐇)\
boldsymbol{s}_{\hat{Z}}(\cdot;\mathbf{H}) and 𝒔Z^m​(⋅,𝐇)\boldsymbol{s}_{\hat{Z}}^{m}(\cdot;\mathbf
{H}) for all m∈[M]m\in[M]. In addition to the graph notations introduced in Section 2.1, we define

 pa¯(i)≜pa(i)∪{i},ch¯(i)≜ch(i)∪{i},andan¯(i)≜an(i)∪{i}.\paplus(i)\triangleq\pa(i)\cup\{i\},\
 quad\chplus(i)\triangleq\ch(i)\cup\{i\},\quad\mbox{and}\quad\anplus(i)\triangleq\an(i)\cup\   (20)
 {i\}\ .

Next, we provide an auxiliary result that will be used throughout the proofs of the main results.

Lemma 3.

For every pair (i,j)(i,j) where i∈[n]i\in[n] and j∈pa⁡(i)j\in\pa(i), the following ratio function
cannot be a constant in zz

 [𝚲⁡(z)]j,j[𝚲⁡(z)]i,i=[∂∂zj​log⁡pj​(zj∣zpa⁡(j))qj​(zj∣zpa⁡(j))]⋅[∂∂zi​log⁡pi​(zi∣zpa⁡(i))qi​
 (zi∣zpa⁡(i))]−1.\displaystyle\dfrac{\big[\mathbf{\Lambda}(z)]_{j,j}}{\big[\mathbf{\Lambda}
 (z)\big]_{i,i}}=\left[\frac{\partial}{\partial z_{j}}\log\frac{p_{j}(z_{j}\mid z_{\pa(j)})}   (21)
 {q_{j}(z_{j}\mid z_{\pa(j)})}\right]\cdot\left[\frac{\partial}{\partial z_{i}}\log\frac{p_
 {i}(z_{i}\mid z_{\pa(i)})}{q_{i}(z_{i}\mid z_{\pa(i)})}\right]^{-1}\ .

Furthermore, the columns of 𝚲\mathbf{\Lambda} are linearly independent vector-valued functions.

Proof: See Appendix A.5.  □\square

We also restate the interventional regularity in terms of the 𝚲\mathbf{\Lambda} function defined
in (14), which will help clarity in the subsequent proofs.

Definition 2 (Intervention regularity)

We say that an intervention on node ii is regular if for every possible triplet (i,j,c)(i,j,c)
where i∈[n],j∈pa⁡(i)i\in[n],j\in\pa(i) and c∈ℚc\in\mathbb{Q}, the following ratio cannot be a
constant function in zz

 ∂∂zj​(log⁡pi​(zi∣zpa⁡(i))qi​(zi∣zpa⁡(i))+c⋅log⁡pj​(zj∣zpa⁡(j))qj​(zj∣zpa⁡(j)))​[∂∂zi​log⁡pi​
 (zi∣zpa⁡(i))qi​(zi∣zpa⁡(i))]−1.\frac{\partial}{\partial z_{j}}\left(\log\frac{p_{i}(z_{i}\
 mid z_{\pa(i)})}{q_{i}(z_{i}\mid z_{\pa(i)})}+c\cdot\log\frac{p_{j}(z_{j}\mid z_{\pa(j)})}    (22)
 {q_{j}(z_{j}\mid z_{\pa(j)})}\right)\left[\frac{\partial}{\partial z_{i}}\log\frac{p_{i}(z_
 {i}\mid z_{\pa(i)})}{q_{i}(z_{i}\mid z_{\pa(i)})}\right]^{-1}\ .

By definition of 𝚲\mathbf{\Lambda}, this means that for (i,j,c)(i,j,c) where i∈[n],j∈pa⁡(i)i\in
[n],j\in\pa(i) and c∈ℚc\in\mathbb{Q}, the following ratio cannot be a constant function in zz

 [𝚲⁡(z)]j,i+c⋅[𝚲⁡(z)]j,j[𝚲⁡(z)]i,i.\dfrac{\big[\mathbf{\Lambda}(z)\big]_{j,i}+c\cdot\big[\     (23)
 mathbf{\Lambda}(z)\big]_{j,j}}{\big[\mathbf{\Lambda}(z)\big]_{i,i}}\ .

A.1 Proof of Lemma 2

First, we show that the search space in Stage 1 of Algorithm 1, i.e., the set 𝒲\mathcal{W}, has a
certain property that will be needed for the rest of the proof. Recall the definition in (18)

 𝒲≜{−κ,…,+κ}n,\mathcal{W}\triangleq\{-\kappa,\dots,+\kappa\}^{n}\ ,  (24)

in which κ\kappa denotes the maximum possible determinant of a matrix in {0,1}(n−1)×(n−1)\{0,1\}^
{(n-1)\times(n-1)}. We will show that for all i∈[n]i\in[n], there exists 𝐰∈𝒲\mathbf{w}\in\mathcal
{W} such that 𝟙​([𝐃⋅𝐰]≠0)=𝐞i\mathds{1}([\mathbf{D}\cdot\mathbf{w}]\neq 0)=\mathbf{e}_{i}. Let adj⁡
(𝐃){\rm adj}(\mathbf{D}) be the cofactor matrix of 𝐃\mathbf{D}, which is the matrix formed by the
entries

 [adj⁡(𝐃)]i,j≜det([𝐃]−i,−j),[{\rm adj}(\mathbf{D})]_{i,j}\triangleq\det([\mathbf{D}]_{-i,-j})  (25)
 \ ,

where [𝐃]−i,−j[\mathbf{D}]_{-i,-j} is the first minor of 𝐃\mathbf{D} with ii-th row and jj-th
columns are removed. Then, the inverse of 𝐃\mathbf{D} is given by

 𝐃−1=1det(𝐃)⋅[adj⁡(𝐃)]⊤.\mathbf{D}^{-1}=\frac{1}{\det(\mathbf{D})}\cdot[{\rm adj}(\mathbf      (26)
 {D})]^{\top}\ .

By multiplying both sides from left by 𝐃\mathbf{D} and rearranging we obtain

 𝐃⋅[adj⁡(𝐃)]⊤=det(𝐃)⋅𝐈n×n,\mathbf{D}\cdot[{\rm adj}(\mathbf{D})]^{\top}=\det(\mathbf{D})\cdot  (27)
 \mathbf{I}_{n\times n}\ ,

where all matrices are integer-valued and 𝐈n×n\mathbf{I}_{n\times n} denotes the nn-dimensional
identity matrix. The identity in (27) implies that

 𝐃⋅𝐰∗=det(𝐃)⋅𝐞i,where𝐰∗=[adj(𝐃)]:,i⊤.\mathbf{D}\cdot\mathbf{w}^{*}=\det(\mathbf{D})\cdot\      (28)
 mathbf{e}_{i}\ ,\quad\mbox{where}\quad\mathbf{w}^{*}=[{\rm adj}(\mathbf{D})]^{\top}_{:,i}\ .

Since entries of adj⁡(𝐃){\rm adj}(\mathbf{D}) are upper bounded by κ\kappa, we have 𝐰∗∈𝒲\mathbf{w}^
{*}\in\mathcal{W} and we conclude that

 ∀i​∃𝐰∈𝒲such that𝟙​(𝐃⋅𝐰≠0)=𝐞i.\forall i\ \exists\mathbf{w}\in\mathcal{W}\quad\mbox{such that}  (29)
 \quad\mathds{1}\big(\mathbf{D}\cdot\mathbf{w}\neq 0\big)=\mathbf{e}_{i}\ .

Next, we prove the lemma statements by induction as follows.

Base case.

At the base case t=1t=1, we will show that 𝟙([𝐃⋅𝐖]:,1≠0)=𝐞i\mathds{1}([\mathbf{D}\cdot\mathbf{W}]_
{:,1}\neq 0)=\mathbf{e}_{i} and 𝐇1∗∈span⁡([𝐆†]i)\mathbf{H}^{*}_{1}\in\sspan([\mathbf{G}^{{\
dagger}}]_{i}) for some root node ii. Consider a vector 𝐰∈𝒲\mathbf{w}\in\mathcal{W} and denote 𝐜=
𝐃⋅𝐰\mathbf{c}=\mathbf{D}\cdot\mathbf{w}. Then, using (17),

 Δ​𝐒X⋅𝐰=[𝐆†]⊤⋅𝚲⋅𝐃⋅𝐰=[𝐆†]⊤⋅𝚲⋅𝐜.\displaystyle\Delta\mathbf{S}_{X}\cdot\mathbf{w}=[\mathbf{G}^
 {{\dagger}}]^{\top}\cdot\mathbf{\Lambda}\cdot\mathbf{D}\cdot\mathbf{w}=[\mathbf{G}^{{\        (30)
 dagger}}]^{\top}\cdot\mathbf{\Lambda}\cdot\mathbf{c}\ .

In the base case, we investigate the dimension of im⁡(Δ​𝐒X⋅𝐰)\im(\Delta\mathbf{S}_{X}\cdot\mathbf
{w}). Then, using (30), we have

 dim(im⁡(Δ​𝐒X⋅𝐰))=dim(im⁡([𝐆†]⊤⋅𝚲⋅𝐜))=dim(im⁡(𝚲⋅𝐜)).\displaystyle\dim\big(\im(\Delta\mathbf
 {S}_{X}\cdot\mathbf{w})\big)=\dim\big(\im([\mathbf{G}^{{\dagger}}]^{\top}\cdot\mathbf{\       (31)
 Lambda}\cdot\mathbf{c})\big)=\dim\big(\im(\mathbf{\Lambda}\cdot\mathbf{c})\big)\ .

(29) implies that there exists 𝐰∗∈𝒲\mathbf{w}^{*}\in\mathcal{W} that makes 𝟙​(𝐜≠0)=𝐞i\mathds{1}(\
mathbf{c}\neq 0)=\mathbf{e}_{i}. Subsequently, if ii is a root node, only nonzero entry of the
column 𝚲:,i\mathbf{\Lambda}_{:,i} is 𝚲i,i\mathbf{\Lambda}_{i,i} and we have

 dim(im(Δ𝐒X⋅𝐰))=dim(im([𝐆†]⊤⋅𝚲:,i))=dim(im([𝐆†]i⋅𝚲i,i))=1.\displaystyle\dim\big(\im(\Delta\
 mathbf{S}_{X}\cdot\mathbf{w})\big)=\dim\big(\im([\mathbf{G}^{{\dagger}}]^{\top}\cdot\mathbf   (32)
 {\Lambda}_{:,i})\big)=\dim\big(\im([\mathbf{G}^{{\dagger}}]_{i}\cdot\mathbf{\Lambda}_{i,i})\
 big)=1\ .

Hence, by searching 𝐰\mathbf{w} within 𝒲\mathcal{W}, Algorithm 1 is guaranteed to find a vector 𝐰\
mathbf{w} such that dim(im⁡(𝚲⋅𝐜))=1\dim(\im(\mathbf{\Lambda}\cdot\mathbf{c}))=1. Next, we show that
if dim(im⁡(𝚲⋅𝐜))=1\dim(\im(\mathbf{\Lambda}\cdot\mathbf{c}))=1, then 𝟙​(𝐜≠0)=𝐞i\mathds{1}(\mathbf
{c}\neq 0)=\mathbf{e}_{i} for a root node ii. We prove this as follows. Consider the set 𝒜={i:𝐜i≠0}
\mathcal{A}=\{i:\mathbf{c}_{i}\neq 0\} and let ii be the youngest node in 𝒜\mathcal{A}, i.e., ii
has no descendants within 𝒜\mathcal{A}. Using the fact that 𝚲ℓ,k=0\mathbf{\Lambda}_{\ell,k}=0 for
all ℓ∉pa¯⁡(k)\ell\notin\paplus(k) and 𝐜k=0\mathbf{c}_{k}=0 for all k∈de⁡(i)k\in\de(i), we have

 𝚲ℓ⋅𝐜𝚲i⋅𝐜=∑k∈ch¯​(ℓ)𝐜k⋅𝚲ℓ,k∑k∈ch¯​(i)𝐜k⋅𝚲i,k=∑k∈ch¯​(ℓ)𝐜k⋅𝚲ℓ,k𝐜i⋅𝚲i,i,∀ℓ∈[n].\displaystyle\
 frac{\mathbf{\Lambda}_{\ell}\cdot\mathbf{c}}{\mathbf{\Lambda}_{i}\cdot\mathbf{c}}=\frac{\
 sum_{k\in\overline{\ch}(\ell)}\mathbf{c}_{k}\cdot\mathbf{\Lambda}_{\ell,k}}{\sum_{k\in\       (33)
 overline{\ch}(i)}\mathbf{c}_{k}\cdot\mathbf{\Lambda}_{i,k}}=\frac{\sum_{k\in\overline{\ch}(\
 ell)}\mathbf{c}_{k}\cdot\mathbf{\Lambda}_{\ell,k}}{\mathbf{c}_{i}\cdot\mathbf{\Lambda}_
 {i,i}}\ ,\quad\forall\ell\in[n]\ .

If 𝐜\mathbf{c} has multiple nonzero entries, let 𝐜ℓ\mathbf{c}_{\ell} denote the second youngest
node in 𝒜\mathcal{A}. If j∉pa⁡(i)j\notin\pa(i), then for ℓ=j\ell=j, (33) reduces to

 𝚲j⋅𝐜𝚲i⋅𝐜=𝐜j⋅𝚲j,j𝐜i⋅𝚲i,i,\frac{\mathbf{\Lambda}_{j}\cdot\mathbf{c}}{\mathbf{\Lambda}_{i}\cdot
 \mathbf{c}}=\frac{\mathbf{c}_{j}\cdot\mathbf{\Lambda}_{j,j}}{\mathbf{c}_{i}\cdot\mathbf{\     (34)
 Lambda}_{i,i}}\ ,

which is not constant due to Lemma 3. If j∈pa⁡(i)j\in\pa(i), then (33) reduces to

 𝚲j⋅𝐜𝚲i⋅𝐜=𝐜j⋅𝚲j,j+𝐜i⋅𝚲j,i𝐜i⋅𝚲i,i,\frac{\mathbf{\Lambda}_{j}\cdot\mathbf{c}}{\mathbf{\Lambda}_
 {i}\cdot\mathbf{c}}=\frac{\mathbf{c}_{j}\cdot\mathbf{\Lambda}_{j,j}+\mathbf{c}_{i}\cdot\      (35)
 mathbf{\Lambda}_{j,i}}{\mathbf{c}_{i}\cdot\mathbf{\Lambda}_{i,i}}\ ,

which is not constant due to interventional regularity. Hence, in either case, there exist multiple
vectors in im⁡(𝚲⋅𝐜)\im(\mathbf{\Lambda}\cdot\mathbf{c}) with distinct directions. Therefore, dim
(im⁡(𝚲⋅𝐜))=1\dim(\im(\mathbf{\Lambda}\cdot\mathbf{c}))=1 implies that 𝐜\mathbf{c} has only one
nonzero entry 𝐜i≠0\mathbf{c}_{i}\neq 0. Finally, if ii is not a root node, for j∈pa⁡(i)j\in\pa(i),
(33) reduces to

 𝚲j⋅𝐜𝚲i⋅𝐜=𝚲j,i𝚲i,i,\frac{\mathbf{\Lambda}_{j}\cdot\mathbf{c}}{\mathbf{\Lambda}_{i}\cdot\       (36)
 mathbf{c}}=\frac{\mathbf{\Lambda}_{j,i}}{\mathbf{\Lambda}_{i,i}}\ ,

which is not constant due to interventional regularity. Therefore, dim(im⁡(𝚲⋅𝐜))=1\dim(\im(\mathbf
{\Lambda}\cdot\mathbf{c}))=1 implies that 𝟙(c≠0)=𝟙([𝐃⋅𝐖]:,1≠0)=𝐞i\mathds{1}(c\neq 0)=\mathds{1}([\
mathbf{D}\cdot\mathbf{W}]_{:,1}\neq 0)=\mathbf{e}_{i} for a root node ii. Next, using (30), we have

 Δ​𝐒X⋅𝐰=𝐜i⋅𝚲i,i⋅[𝐆†]i.\Delta\mathbf{S}_{X}\cdot\mathbf{w}=\mathbf{c}_{i}\cdot\mathbf{\Lambda}  (37)
 _{i,i}\cdot[\mathbf{G}^{{\dagger}}]_{i}\ .

Hence, denoting the root node ii by π1\pi_{1}, setting 𝐇1∗=𝐯\mathbf{H}^{*}_{1}=\mathbf{v} for any
𝐯∈im⁡(Δ​𝐒X⋅𝐰)\mathbf{v}\in\im(\Delta\mathbf{S}_{X}\cdot\mathbf{w}) ensures that [𝐃⋅𝐖]:,1=𝐞π1[\
mathbf{D}\cdot\mathbf{W}]_{:,1}=\mathbf{e}_{\pi_{1}} and 𝐇1∗∈span⁡([𝐆†]π1)\mathbf{H}^{*}_{1}\in\
sspan([\mathbf{G}^{{\dagger}}]_{\pi_{1}}) which completes the base step of the induction.

Induction hypothesis.

For the induction step, assume that for all t∈[k]t\in[k], we have linearly independent vectors
𝐇t∗∈span⁡({[𝐆†]πj:j∈pa¯⁡(t)})\mathbf{H}^{*}_{t}\in\sspan(\{[\mathbf{G}^{{\dagger}}]_{\pi_{j}}:j\in\
paplus(t)\}) and

 [𝐃⋅𝐖]πj,t={0,j>t≠0,j=t,∀t∈[k],∀j≥t,\displaystyle[\mathbf{D}\cdot\mathbf{W}]_{\pi_{j},t}=\     (38)
 begin{cases}0\ ,&j>t\\ \neq 0\ ,&j=t\end{cases}\ ,\quad\forall t\in[k],\;\forall j\geq t\ ,

where {π1,…,πk}\{\pi_{1},\dots,\pi_{k}\} constitutes the first kk nodes of a causal order π\pi. We
will show that if

 dim(𝒱)=1where𝒱=proj𝗇𝗎𝗅𝗅⁡({𝐇i∗:i∈[k]})im(Δ𝐒X⋅𝐰),\dim(\mathcal{V})=1\quad\mbox{where}\quad\
 mathcal{V}=\proj_{{\sf null}(\{\mathbf{H}^{*}_{i}\,:\,i\,\in\,[k]\})}\im(\Delta\mathbf{S}_    (39)
 {X}\cdot\mathbf{w})\ ,

then 𝐰∈𝒲\mathbf{w}\in\mathcal{W} satisfies

 [𝐃⋅𝐰]πj={0,j>k+1≠0,j=k+1,[\mathbf{D}\cdot\mathbf{w}]_{\pi_{j}}=\begin{cases}0\ ,&j>k+1\\ \    (40)
 neq 0\ ,&j=k+1\end{cases}\ ,

for the causal order π\pi. First, define ℳk+1≜[n]∖{π1,…,πk}\mathcal{M}_{k+1}\triangleq[n]\setminus\
{\pi_{1},\dots,\pi_{k}\}. Note that, by the induction premise,

 𝗇𝗎𝗅𝗅⁡({𝐇i∗:i∈[k]})=𝗇𝗎𝗅𝗅⁡({[𝐆†]πi:i∈[k]}).{\sf null}(\{\mathbf{H}^{*}_{i}\,:\,i\in[k]\})={\sf  (41)
 null}(\{[\mathbf{G}^{{\dagger}}]_{\pi_{i}}\,:\,i\in[k]\})\ .

Then, for any 𝐰∈𝒲\mathbf{w}\in\mathcal{W} and 𝐜=𝐃⋅𝐰\mathbf{c}=\mathbf{D}\cdot\mathbf{w}, we have

 𝒱=proj𝗇𝗎𝗅𝗅⁡({[𝐆†]πi:i∈[k]})([𝐆†]⊤⋅im(𝚲⋅𝐜)).\displaystyle\mathcal{V}=\proj_{{\sf null}(\{[\
 mathbf{G}^{{\dagger}}]_{\pi_{i}}\,:\,i\,\in\,[k]\})}([\mathbf{G}^{{\dagger}}]^{\top}\cdot\im  (42)
 (\mathbf{\Lambda}\cdot\mathbf{c}))\ .

Subsequently, we have

 dim(𝒱)=dim(im⁡([𝐆†]ℳk+1⊤⋅𝚲ℳk+1⋅𝐜))=dim(im⁡(𝚲ℳk+1⋅𝐜)).\dim(\mathcal{V})=\dim\left(\im\big([\
 mathbf{G}^{{\dagger}}]^{\top}_{\mathcal{M}_{k+1}}\cdot\mathbf{\Lambda}_{\mathcal{M}_{k+1}}\   (43)
 cdot\mathbf{c}\big)\right)=\dim\left(\im(\mathbf{\Lambda}_{\mathcal{M}_{k+1}}\cdot\mathbf
 {c})\right)\ .

Note that, if node i∈ℳk+1i\in\mathcal{M}_{k+1} has no ancestors in ℳk+1\mathcal{M}_{k+1}, using the
vector 𝐰∈𝒲\mathbf{w}\in\mathcal{W} which makes 𝟙​(c≠0)=𝐞i\mathds{1}(c\neq 0)=\mathbf{e}_{i} ensures
that dim(im(𝚲ℳk+1⋅)𝐜)=dim(im(𝚲i,i⋅𝐞i))=1\dim(\im(\mathbf{\Lambda}_{\mathcal{M}_{k+1}}\cdot)\mathbf
{c})=\dim(\im(\mathbf{\Lambda}_{i,i}\cdot\mathbf{e}_{i}))=1. Hence, by searching 𝐰\mathbf{w} within
𝒲\mathcal{W}, Algorithm 1 is guaranteed to find a vector 𝐰\mathbf{w} such that dim(im⁡(𝚲ℳk+1⋅𝐜))=1\
dim(\im(\mathbf{\Lambda}_{\mathcal{M}_{k+1}}\cdot\mathbf{c}))=1. Next, consider the set 𝒜=
{i∈ℳk+1:𝐜i≠0}\mathcal{A}=\{i\in\mathcal{M}_{k+1}:\mathbf{c}_{i}\neq 0\}. Since π1,…,πk{\pi_{1},\
dots,\pi_{k}} are first kk-nodes of a causal order π\pi, we have

 𝚲i,πj=0,∀i∈ℳk+1,∀j∈[k].\mathbf{\Lambda}_{i,\pi_{j}}=0\ ,\quad\forall i\in\mathcal{M}_{k+1},   (44)
 \;\;\forall j\in[k]\ .

Then, if dim(𝒱)=1\dim(\mathcal{V})=1, 𝒜\mathcal{A} is not empty since otherwise (𝚲ℳk+1⋅𝐜)(\mathbf{\
Lambda}_{\mathcal{M}_{k+1}}\cdot\mathbf{c}) is equal to zero vector and dim(𝒱)=0\dim(\mathcal{V})=
0. Let ii be youngest node in 𝒜\mathcal{A}, i.e, ii has no descendants within 𝒜\mathcal{A}.
Similarly to (33), we have

 𝚲ℓ⋅𝐜𝚲i⋅𝐜=∑k∈ch¯​(ℓ)𝐜k⋅𝚲ℓ,k𝐜i⋅𝚲i,i,∀ℓ∈ℳk+1.\frac{\mathbf{\Lambda}_{\ell}\cdot\mathbf{c}}{\
 mathbf{\Lambda}_{i}\cdot\mathbf{c}}=\frac{\sum_{k\in\overline{\ch}(\ell)}\mathbf{c}_{k}\cdot  (45)
 \mathbf{\Lambda}_{\ell,k}}{\mathbf{c}_{i}\cdot\mathbf{\Lambda}_{i,i}}\ ,\quad\forall\ell\in\
 mathcal{M}_{k+1}\ .

If 𝒜\mathcal{A} has multiple elements, let jj be the second youngest node in 𝒜\mathcal{A}. If j∉pa⁡
(i)j\notin\pa(i), then for ℓ=j\ell=j, (45) reduces to

 𝚲j⋅𝐜𝚲i⋅𝐜=𝐜j⋅𝚲j,j𝐜i⋅𝚲i,i,\frac{\mathbf{\Lambda}_{j}\cdot\mathbf{c}}{\mathbf{\Lambda}_{i}\cdot
 \mathbf{c}}=\frac{\mathbf{c}_{j}\cdot\mathbf{\Lambda}_{j,j}}{\mathbf{c}_{i}\cdot\mathbf{\     (46)
 Lambda}_{i,i}}\ ,

which is not constant due to Lemma 3. If j∈pa⁡(i)j\in\pa(i), then for ℓ=j\ell=j, (45) reduces to

 𝚲j⋅𝐜𝚲i⋅𝐜=𝐜j⋅𝚲j,j+𝐜i⋅𝚲j,i𝐜i⋅𝚲i,i,\displaystyle\frac{\mathbf{\Lambda}_{j}\cdot\mathbf{c}}{\
 mathbf{\Lambda}_{i}\cdot\mathbf{c}}=\frac{\mathbf{c}_{j}\cdot\mathbf{\Lambda}_{j,j}+\mathbf   (47)
 {c}_{i}\cdot\mathbf{\Lambda}_{j,i}}{\mathbf{c}_{i}\cdot\mathbf{\Lambda}_{i,i}}\ ,

which is not constant due to interventional regularity. Hence, in either case, there exist vectors
with different directions in im⁡(𝚲ℳk+1⋅𝐜)\im(\mathbf{\Lambda}_{\mathcal{M}_{k+1}}\cdot\mathbf{c}).
Therefore, dim(im⁡(𝚲ℳk+1⋅𝐜))=1\dim(\im(\mathbf{\Lambda}_{\mathcal{M}_{k+1}}\cdot\mathbf{c}))=1
implies that 𝒜\mathcal{A} has only one element, i.e., there is only one node i∈[n]∖{π1,…,πk}i\in[n]
\setminus\{\pi_{1},\dots,\pi_{k}\} such that 𝐜i≠0\mathbf{c}_{i}\neq 0, and also pa⁡(i)⊆{π1,…,πk}\pa
(i)\subseteq\{\pi_{1},\dots,\pi_{k}\}. Hence, denoting πk+1=i\pi_{k+1}=i, [𝐃⋅𝐖]πj[\mathbf{D}\cdot\
mathbf{W}]_{\pi_{j}} is nonzero for j=k+1j=k+1 and zero for all j>k+1j>k+1. Subsequently, by
setting 𝐇k+1∗=v\mathbf{H}^{*}_{k+1}=v for any 𝐯∈im⁡(Δ​𝐒X⋅𝐰)∖span⁡({𝐇i∗:i∈[k]})\mathbf{v}\in\im(\
Delta\mathbf{S}_{X}\cdot\mathbf{w})\setminus\sspan(\{\mathbf{H}^{*}_{i}:i\in[k]\}) we have

 𝐇k+1∗∈span⁡({[𝐆†]πi:i∈[k+1]}).\mathbf{H}^{*}_{k+1}\in\sspan(\{[\mathbf{G}^{{\dagger}}]_{\pi_  (48)
 {i}}:i\in[k+1]\})\ .

Finally, note that the contribution of [𝐆†]i[\mathbf{G}^{{\dagger}}]_{i} in 𝐇k+1∗\mathbf{H}^{*}_
{k+1} is nonzero since 𝐜i≠0\mathbf{c}_{i}\neq 0. Hence, 𝐇k+1∗\mathbf{H}^{*}_{k+1} is linearly
independent of {𝐇1∗,…,𝐇k∗}\{\mathbf{H}^{*}_{1},\dots,\mathbf{H}^{*}_{k}\}, which completes the
proof of the induction hypothesis. Therefore, (38) holds for all t∈[n]t\in[n], which implies that
𝐏π⋅𝐃⋅𝐖\mathbf{P}_{\pi}\cdot\mathbf{D}\cdot\mathbf{W} is upper triangular and has nonzero diagonal
entries, and concludes the proof of the lemma.

A.2 Proof of Theorem 3

We will prove the theorem by proving the following equivalent statements: The output 𝐖\mathbf{W} of
Stage 3 of Algorithm 1 satisfies

 [𝐃⋅𝐖]πt,j\displaystyle[\mathbf  ={0,πt∉an¯⁡(πj)1,πt=πj,\displaystyle=\begin{cases}0\ ,&\pi_
 {D}\cdot\mathbf{W}]_{\pi_{t},j} {t}\notin\anplus(\pi_{j})\\ 1\ ,&\pi_{t}=\pi_{j}\end{cases}\  (49)
                                 ,

and

 t∈pa^​(j)\displaystyle t\in\hat{\pa}   ⇔πt∈an⁡(πj).\displaystyle\iff\pi_{t}\in\an(\pi_{j})\   (50)
 (j)                                    .

for all t,j∈[n]t,j\in[n]. The second statement of the theorem, graph recovery up to a transitive
closure, is equivalent to (50) by definition. Next, we show that (49) implies the first statement
of the theorem, that is identifiability of latent variables up to mixing with ancestors. For this
purpose, using (17), for any 𝐰∈ℝn\mathbf{w}\in{\mathbb{R}}^{n}, we have

 Δ​𝐒X⋅𝐰=[𝐆†]⊤⋅𝚲⋅𝐃⋅𝐰.\displaystyle\Delta\mathbf{S}_{X}\cdot\mathbf{w}=[\mathbf{G}^{{\dagger}}]  (51)
 ^{\top}\cdot\mathbf{\Lambda}\cdot\mathbf{D}\cdot\mathbf{w}\ .

Then, for 𝐰=[𝐖]:,j\mathbf{w}=[\mathbf{W}]_{:,j}, using the fact that 𝚲πt,πk=0\mathbf{\Lambda}_{\pi_
{t},\pi_{k}}=0 for all πt∉pa¯⁡(πk)\pi_{t}\notin\paplus(\pi_{k}), the coefficient of [𝐆†]πt[\mathbf
{G}^{{\dagger}}]_{\pi_{t}} on the right-hand side becomes

 ∑πk∈[n]𝚲πt,πk⋅[𝐃⋅𝐖]πk,j=∑πk∈ch¯⁡(πt)𝚲πt,πk⋅[𝐃⋅𝐖]πk,j.\sum_{\pi_{k}\in[n]}\mathbf{\Lambda}_{\
 pi_{t},\pi_{k}}\cdot[\mathbf{D}\cdot\mathbf{W}]_{\pi_{k},j}=\sum_{\pi_{k}\in\chplus(\pi_      (52)
 {t})}\mathbf{\Lambda}_{\pi_{t},\pi_{k}}\cdot[\mathbf{D}\cdot\mathbf{W}]_{\pi_{k},j}\ .

If πt∉an¯⁡(πj)\pi_{t}\notin\anplus(\pi_{j}), then πk∈ch¯⁡(πt)\pi_{k}\in\chplus(\pi_{t}) implies
that πk∉an¯⁡(πj)\pi_{k}\notin\anplus(\pi_{j}). Then, using (49), the sum in (52), i.e., the
coefficient of [𝐆†]πt[\mathbf{G}^{{\dagger}}]_{\pi_{t}} in function (Δ​𝐒X⋅𝐰)(\Delta\mathbf{S}_{X}\
cdot\mathbf{w}), becomes zero. Furthermore, since [𝐃⋅𝐖]πt,t≠0[\mathbf{D}\cdot\mathbf{W}]_{\pi_
{t},t}\neq 0, the coefficient of [𝐆†]πt[\mathbf{G}^{{\dagger}}]_{\pi_{t}} in (Δ​𝐒X⋅𝐰)(\Delta\mathbf
{S}_{X}\cdot\mathbf{w}) is nonzero. Therefore, (49) ensures, by choosing 𝐇t∗∈im(Δ𝐒X⋅[𝐖]:,t)\mathbf
{H}^{*}_{t}\in\im(\Delta\mathbf{S}_{X}\cdot[\mathbf{W}]_{:,t}) for all t∈[n]t\in[n], we have

 𝐇t∗∈span⁡({[𝐆†]πj:j∈an¯​(t)})\mathbf{H}^{*}_{t}\in\sspan\big(\{[\mathbf{G}^{{\dagger}}]_{\    (53)
 pi_{j}}:j\in\overline{\an}(t)\}\big)

and {𝐇1∗,…,𝐇n∗}\{\mathbf{H}^{*}_{1},\dots,\mathbf{H}^{*}_{n}\} are linearly independent. This
implies that we have

 𝐇∗=𝐏π⋅𝐂an⋅𝐆†,\mathbf{H}^{*}=\mathbf{P}_{\pi}\cdot\mathbf{C}_{\rm an}\cdot\mathbf{G}^{{\       (54)
 dagger}}\ ,

where 𝐏π\mathbf{P}_{\pi} is the row permutation matrix of π\pi, and 𝐂an∈ℝn×n\mathbf{C}_{\rm an}\in
{\mathbb{R}}^{n\times n} is a constant matrix with nonzero diagonal entries that satisfies [𝐂an]i,j
=0[\mathbf{C}_{\rm an}]_{i,j}=0 for all j∉an¯⁡(i)j\notin\anplus(i). Subsequently,

 Z^​(X,𝐇∗)=𝐇∗⋅X=𝐏π⋅𝐂an⋅𝐆†⋅𝐆⋅Z=𝐏π⋅𝐂an⋅Z,\hat{Z}(X;\mathbf{H}^{*})=\mathbf{H}^{*}\cdot X=\
 mathbf{P}_{\pi}\cdot\mathbf{C}_{\rm an}\cdot\mathbf{G}^{{\dagger}}\cdot\mathbf{G}\cdot Z=\    (55)
 mathbf{P}_{\pi}\cdot\mathbf{C}_{\rm an}\cdot Z\ ,

which is the definition of identifiability up to ancestors for latent variables, specified in
Definition 1. In the rest of the proof, we will prove (49) by induction.

Base case.

At the base case, we have t=n−1t=n-1 and j=nj=n. We will show that, at the end of step (t,j)=
(n−1,n)(t,j)=(n-1,n), [𝐃⋅𝐖]πn−1,n≠0[\mathbf{D}\cdot\mathbf{W}]_{\pi_{n-1},n}\neq 0 implies that
πn−1∈pa⁡(πn)\pi_{n-1}\in\pa(\pi_{n}). By Lemma 2, we know that

 [𝐃⋅𝐖]πn−1,n−1≠0,[𝐃⋅𝐖]πn,n≠0,and[𝐃⋅𝐖]πn,n−1=0.\displaystyle[\mathbf{D}\cdot\mathbf{W}]_{\pi_
 {n-1},n-1}\neq 0\ ,\quad[\mathbf{D}\cdot\mathbf{W}]_{\pi_{n},n}\neq 0\ ,\quad\mbox{and}\quad  (56)
 [\mathbf{D}\cdot\mathbf{W}]_{\pi_{n},n-1}=0\ .

Consider some integers α∈{−n​κ,…,n​κ}\alpha\in\{-n\kappa,\dots,n\kappa\} and β∈{1,…,n​κ}\beta\in\
{1,\dots,n\kappa\}. Let 𝐰∗=α[𝐖]:,n−1+β[𝐖]:,n\mathbf{w}^{*}=\alpha[\mathbf{W}]_{:,n-1}+\beta[\mathbf
{W}]_{:,n} and 𝐜∗=𝐃⋅𝐰∗\mathbf{c}^{*}=\mathbf{D}\cdot\mathbf{w}^{*}. Since β≠0\beta\neq 0, (56)
implies that 𝐜n∗≠0\mathbf{c}^{*}_{n}\neq 0. Using Lemma 2, we have

 𝗇𝗎𝗅𝗅⁡({𝐇i∗:i∈[n−2]})=𝗇𝗎𝗅𝗅⁡({[𝐆†]πi:i∈[n−2]}).\displaystyle{\sf null}(\{\mathbf{H}^{*}_{i}\,:  (57)
 \,i\in[n-2]\})={\sf null}(\{[\mathbf{G}^{{\dagger}}]_{\pi_{i}}\,:\,i\in[n-2]\})\ .

Then, we have

 𝒱\           =proj𝗇𝗎𝗅𝗅⁡({𝐇i∗:i∈[n−2]})im(Δ𝐒X⋅𝐰∗)\displaystyle=\proj_{{\sf null}(\{\mathbf{H}
 displaystyle ^{*}_{i}\,:\,i\,\in\,[n-2]\})}\im(\Delta\mathbf{S}_{X}\cdot\mathbf{w}^{*})       (58)
 \mathcal{V}
              =proj𝗇𝗎𝗅𝗅⁡({[𝐆†]πi:i∈[n−2]})([𝐆†]⊤⋅im(𝚲⋅𝐜∗)).\displaystyle=\proj_{{\sf null}(\
              {[\mathbf{G}^{{\dagger}}]_{\pi_{i}}\,:\,i\,\in\,[n-2]\})}([\mathbf{G}^{{\        (59)
              dagger}}]^{\top}\cdot\im(\mathbf{\Lambda}\cdot\mathbf{c}^{*}))\ .

Subsequently,

 dim(𝒱)\       =dim([[𝐆†]πn−1[𝐆†]πn]⊤⋅im⁡([𝚲πn−1𝚲πn]⋅𝐜∗))\displaystyle=\dim\left(\begin
 displaystyle\ {bmatrix}[\mathbf{G}^{{\dagger}}]_{\pi_{n-1}}\\ [\mathbf{G}^{{\dagger}}]_{\pi_
 dim(\mathcal  {n}}\end{bmatrix}^{\top}\cdot\im\left(\begin{bmatrix}\mathbf{\Lambda}_{\pi_     (60)
 {V})          {n-1}}\\ \mathbf{\Lambda}_{\pi_{n}}\end{bmatrix}\cdot\mathbf{c}^{*}\right)\
               right)
               =dim(im⁡([𝚲πn−1𝚲πn]⋅𝐜∗))\displaystyle=\dim\left(\im\left(\begin{bmatrix}\
               mathbf{\Lambda}_{\pi_{n-1}}\\ \mathbf{\Lambda}_{\pi_{n}}\end{bmatrix}\cdot\     (61)
               mathbf{c}^{*}\right)\right)
               =dim(im⁡([𝐜πn−1∗⋅𝚲πn−1,πn−1+𝐜πn∗⋅𝚲πn−1,πn𝐜πn∗⋅𝚲πn,πn])).\displaystyle=\dim\
               left(\im\left(\begin{bmatrix}\mathbf{c}^{*}_{\pi_{n-1}}\cdot\mathbf{\Lambda}_
               {\pi_{n-1},\pi_{n-1}}+\mathbf{c}^{*}_{\pi_{n}}\cdot\mathbf{\Lambda}_{\pi_       (62)
               {n-1},\pi_{n}}\\ \mathbf{c}^{*}_{\pi_{n}}\cdot\mathbf{\Lambda}_{\pi_{n},\pi_
               {n}}\end{bmatrix}\right)\right)\ .

Note that in the last step, we have used the fact that π\pi is a causal order and j∉pa¯⁡(i)j\notin\
paplus(i) implies that 𝚲i,j=0\mathbf{\Lambda}_{i,j}=0. Then, if πn−1∈pa⁡(πn)\pi_{n-1}\in\pa(\pi_
{n}), interventional regularity ensures that the ratio of the two entries in (62) is not a constant
function which implies that dim(𝒱)=2\dim(\mathcal{V})=2. Furthermore, if πn−1∉pa⁡(πn)\pi_{n-1}\
notin\pa(\pi_{n}) but 𝐜πn−1∗≠0\mathbf{c}^{*}_{\pi_{n-1}}\neq 0, then (62) reduces to

 dim(𝒱)=dim(im⁡([𝐜πn−1∗⋅𝚲πn−1,πn−1𝐜πn∗⋅𝚲πn,πn]))=2,\displaystyle\dim(\mathcal{V})=\dim\left(\
 im\left(\begin{bmatrix}\mathbf{c}^{*}_{\pi_{n-1}}\cdot\mathbf{\Lambda}_{\pi_{n-1},\pi_{n-1}}  (63)
 \\ \mathbf{c}^{*}_{\pi_{n}}\cdot\mathbf{\Lambda}_{\pi_{n},\pi_{n}}\end{bmatrix}\right)\
 right)=2\ ,

due to Lemma 3. Therefore, if 𝐰∗\mathbf{w}^{*} satisfies

 dim(proj𝗇𝗎𝗅𝗅⁡({𝐇i∗:i∈[n−2]})im(Δ𝐒X⋅𝐰∗))=1,\displaystyle\dim(\proj_{{\sf null}(\{\mathbf{H}^   (64)
 {*}_{i}\,:\,i\in[n-2]\})}\im(\Delta\mathbf{S}_{X}\cdot\mathbf{w}^{*}))=1\ ,

by setting [𝐖]:,n=𝐰∗[\mathbf{W}]_{:,n}=\mathbf{w}^{*}, we guarantee that [𝐃⋅𝐖]πn−1,n=0[\mathbf{D}\
cdot\mathbf{W}]_{\pi_{n-1},n}=0 if πn−1∉pa⁡(πn)\pi_{n-1}\notin\pa(\pi_{n}). Note that, for this
case, setting either (α,β)=(−[𝐃⋅𝐖]n−1,n,[𝐃⋅𝐖]n−1,n−1)(\alpha,\beta)=(-[\mathbf{D}\cdot\mathbf{W}]_
{n-1,n},[\mathbf{D}\cdot\mathbf{W}]_{n-1,n-1}) or (α,β)=([𝐃⋅𝐖]n−1,n,−[𝐃⋅𝐖]n−1,n−1)(\alpha,\beta)=
([\mathbf{D}\cdot\mathbf{W}]_{n-1,n},-[\mathbf{D}\cdot\mathbf{W}]_{n-1,n-1}) achieves

 𝐜πn−1∗=[𝐃⋅𝐰∗]πn−1=α⋅[𝐃⋅𝐖]πn−1,n−1+β⋅[𝐃⋅𝐖]πn−1,n=0.\displaystyle\mathbf{c}^{*}_{\pi_{n-1}}=[\
 mathbf{D}\cdot\mathbf{w}^{*}]_{\pi_{n-1}}=\alpha\cdot[\mathbf{D}\cdot\mathbf{W}]_{\pi_        (65)
 {n-1},n-1}+\beta\cdot[\mathbf{D}\cdot\mathbf{W}]_{\pi_{n-1},n}=0\ .

Note that the entries of [𝐃⋅𝐖][\mathbf{D}\cdot\mathbf{W}] are bounded as

 [𝐃⋅𝐖]i,j=∑k∈[n]𝐃i,k⋅𝐖k,j≤∑k∈[n]|𝐖k,j|=∥𝐖:,j∥1,∀i,j∈[n].\displaystyle[\mathbf{D}\cdot\mathbf
 {W}]_{i,j}=\sum_{k\in[n]}\mathbf{D}_{i,k}\cdot\mathbf{W}_{k,j}\leq\sum_{k\in[n]}|\mathbf{W}_  (66)
 {k,j}|=\|\mathbf{W}_{:,j}\|_{1}\ ,\quad\forall i,j\in[n]\ .

Hence, Algorithm 1 is guaranteed to find 𝐜πn−1∗=0\mathbf{c}^{*}_{\pi_{n-1}}=0 by searching over

 (α,β)∈{−∥𝐖:,n−1∥1,…,∥𝐖:,n−1∥1}×{1,…,∥𝐖:,n∥1}.(\alpha,\beta)\in\{-\|\mathbf{W}_{:,n-1}\|_{1},  (67)
 \dots,\|\mathbf{W}_{:,n-1}\|_{1}\}\times\{1,\dots,\|\mathbf{W}_{:,n}\|_{1}\}\ .

Therefore, if dim(𝒱)\dim(\mathcal{V}) is never found to be 11 for any (α,β)(\alpha,\beta), it means
that πn−1∈pa⁡(πn)\pi_{n-1}\in\pa(\pi_{n}), and the algorithm adds the edge (n−1)→n(n-1)\to n, which
concludes the proof of the base case.

Induction hypothesis.

Next, assume that for all t∈{k+1,…,n}t\in\{k+1,\dots,n\} and j∈{t+1,…,n}j\in\{t+1,\dots,n\},

 [𝐃⋅𝐖]πt,j\displaystyle[\ ={0πt∉an¯⁡(πj)1πt=πjandπt∈an(πj)⇔t∈pa^(j).\displaystyle=\begin
 mathbf{D}\cdot\mathbf    {cases}0&\pi_{t}\notin\anplus(\pi_{j})\\ 1&\pi_{t}=\pi_{j}\end       (68)
 {W}]_{\pi_{t},j}         {cases}\quad\mbox{and}\quad\pi_{t}\in\an(\pi_{j})\iff t\in\hat{\pa}
                          (j)\ .

We will prove that (68) holds for t=kt=k and j∈{t+1,…,n}j\in\{t+1,\dots,n\}. We prove this by
induction as well.

Base case for the inner induction.

At the base case, we have t=kt=k and j=k+1j=k+1. Since π\pi is a causal order, we have

 πk∈an⁡(πk+1)⇔πk∈pa⁡(πk+1).\displaystyle\pi_{k}\in\an(\pi_{k+1})\iff\pi_{k}\in\pa(\pi_{k+1})\  (69)
 .

Also, ch^​(k)\hat{\ch}(k) is empty at this iteration of the algorithm. Then, ℳk,k+1=[k−1]\mathcal
{M}_{k,k+1}=[k-1]. Consider some integers α∈{−n​κ,…,n​κ}\alpha\in\{-n\kappa,\dots,n\kappa\} and β∈
{1,…,n​κ}\beta\in\{1,\dots,n\kappa\}. Let 𝐰∗=α[𝐖]:,k+β[𝐖]:,k+1\mathbf{w}^{*}=\alpha[\mathbf{W}]_
{:,k}+\beta[\mathbf{W}]_{:,k+1} and 𝐜∗=𝐃⋅𝐰∗\mathbf{c}^{*}=\mathbf{D}\cdot\mathbf{w}^{*}. Using
Lemma 2, we have

 𝗇𝗎𝗅𝗅⁡({𝐇i∗:i∈[k−1]})=𝗇𝗎𝗅𝗅⁡({[𝐆†]πi:i∈[k−1]}).\displaystyle{\sf null}\big(\{\mathbf{H}^{*}_
 {i}\,:\,i\in[k-1]\}\big)={\sf null}\big(\{[\mathbf{G}^{{\dagger}}]_{\pi_{i}}\,:\,i\in[k-1]\}  (70)
 \big)\ .

Then, we have

 𝒱\           =proj𝗇𝗎𝗅𝗅⁡({𝐇i∗:i∈[k−1]})im(Δ𝐒X⋅𝐰∗)\displaystyle=\proj_{{\sf null}(\{\mathbf{H}
 displaystyle ^{*}_{i}\,:\,i\in[k-1]\})}\im(\Delta\mathbf{S}_{X}\cdot\mathbf{w}^{*})           (71)
 \mathcal{V}
              =proj𝗇𝗎𝗅𝗅⁡({[𝐆†]πi:i∈[k−1]})([𝐆†]⊤⋅im(𝚲⋅𝐜∗)).\displaystyle=\proj_{{\sf null}(\
              {[\mathbf{G}^{{\dagger}}]_{\pi_{i}}\,:\,i\in[k-1]\})}\big([\mathbf{G}^{{\        (72)
              dagger}}]^{\top}\cdot\im(\mathbf{\Lambda}\cdot\mathbf{c}^{*})\big)\ .

Using (68), 𝐜πi∗=0\mathbf{c}^{*}_{\pi_{i}}=0 for i>k+1i>k+1. Subsequently,

              =dim(im⁡([[𝐆†]πk[𝐆†]πk+1]⊤⋅[𝚲πk𝚲πk+1]⋅𝐜∗))=dim(im⁡([𝚲πk𝚲πk+1]⋅𝐜∗)).\
 dim(𝒱)\      displaystyle=\dim\left(\im\left(\begin{bmatrix}[\mathbf{G}^{{\dagger}}]_{\pi_
 displaystyle {k}}\\ [\mathbf{G}^{{\dagger}}]_{\pi_{k+1}}\end{bmatrix}^{\top}\cdot\begin
 \dim(\       {bmatrix}\mathbf{\Lambda}_{\pi_{k}}\\ \mathbf{\Lambda}_{\pi_{k+1}}\end{bmatrix}  (73)
 mathcal{V})  \cdot\mathbf{c}^{*}\right)\right)=\dim\left(\im\left(\begin{bmatrix}\mathbf{\
              Lambda}_{\pi_{k}}\\ \mathbf{\Lambda}_{\pi_{k+1}}\end{bmatrix}\cdot\mathbf{c}^
              {*}\right)\right)\ .

Also, recall that 𝚲πk,πj=0\mathbf{\Lambda}_{\pi_{k},\pi_{j}}=0 for all j<kj<k. Then,

 dim(𝒱)=dim(im⁡([𝚲πk𝚲πk+1]⋅𝐜∗))=dim(im⁡([𝐜πk∗⋅𝚲πk,πk+𝐜πk+1∗⋅𝚲πk,πk+1𝐜πk+1∗⋅𝚲πk+1,πk+1])).\
 displaystyle\dim(\mathcal{V})=\dim\left(\im\left(\begin{bmatrix}\mathbf{\Lambda}_{\pi_{k}}\\
 \mathbf{\Lambda}_{\pi_{k+1}}\end{bmatrix}\cdot\mathbf{c}^{*}\right)\right)=\dim\left(\im\     (74)
 left(\begin{bmatrix}\mathbf{c}^{*}_{\pi_{k}}\cdot\mathbf{\Lambda}_{\pi_{k},\pi_{k}}+\mathbf
 {c}^{*}_{\pi_{k+1}}\cdot\mathbf{\Lambda}_{\pi_{k},\pi_{k+1}}\\ \mathbf{c}^{*}_{\pi_{k+1}}\
 cdot\mathbf{\Lambda}_{\pi_{k+1},\pi_{k+1}}\end{bmatrix}\right)\right)\ .

Using (68) again, we have 𝐜πk+1∗≠0\mathbf{c}^{*}_{\pi_{k+1}}\neq 0. Then, if πk∈pa⁡(πk+1)\pi_{k}\in
\pa(\pi_{k+1}), interventional regularity ensures that the ratio of the two entries in (74) is not
a constant function which implies that dim(𝒱)=2\dim(\mathcal{V})=2. Furthermore, if πk∉pa⁡(πk+1)\
pi_{k}\notin\pa(\pi_{k+1}) but 𝐜πk∗≠0\mathbf{c}^{*}_{\pi_{k}}\neq 0, based on Lemma 3, (74) reduces
to

 dim(𝒱)=dim(im⁡([𝐜πk∗⋅𝚲πk,πk𝐜πk+1∗⋅𝚲πk+1,πk+1]))=2.\displaystyle\dim(\mathcal{V})=\dim\left(\
 im\left(\begin{bmatrix}\mathbf{c}^{*}_{\pi_{k}}\cdot\mathbf{\Lambda}_{\pi_{k},\pi_{k}}\\ \    (75)
 mathbf{c}^{*}_{\pi_{k+1}}\cdot\mathbf{\Lambda}_{\pi_{k+1},\pi_{k+1}}\end{bmatrix}\right)\
 right)=2\ .

Therefore, if we have

 dim(proj𝗇𝗎𝗅𝗅⁡({𝐇i∗:i∈[k−1]})im(Δ𝐒X⋅𝐰∗))=1,\displaystyle\dim(\proj_{{\sf null}(\{\mathbf{H}^   (76)
 {*}_{i}\,:\,i\,\in\,[k-1]\})}\im(\Delta\mathbf{S}_{X}\cdot\mathbf{w}^{*}))=1\ ,

by setting [𝐖]:,k+1=𝐰∗[\mathbf{W}]_{:,k+1}=\mathbf{w}^{*}, we guarantee that [𝐃⋅𝐖]πk,k+1=0[\mathbf
{D}\cdot\mathbf{W}]_{\pi_{k},k+1}=0 if πk∉pa⁡(πk+1)\pi_{k}\notin\pa(\pi_{k+1}). Note that,
similarly to the base case of outer induction, Algorithm 1 is guaranteed to find 𝐜πn−1∗=0\mathbf{c}
^{*}_{\pi_{n-1}}=0 by searching over (α,β)∈{−∥𝐖:,k∥1,…,∥𝐖:,k∥1}×{1,…,∥𝐖:,k+1∥1}(\alpha,\beta)\in\{-
\|\mathbf{W}_{:,k}\|_{1},\dots,\|\mathbf{W}_{:,k}\|_{1}\}\times\{1,\dots,\|\mathbf{W}_{:,k+1}\|_{1}
\}. Therefore, if dim(𝒱)\dim(\mathcal{V}) is never found to be 11 for any (α,β)(\alpha,\beta), it
means πk∈pa⁡(πk+1)\pi_{k}\in\pa(\pi_{k+1}), and the algorithm adds the edge t→jt\to j, which
concludes the proof of the base case for the inner induction.

Induction hypothesis for the inner induction.

Next, assume that for all j∈{k+1,…,u}j\in\{k~+~1,\dots,u\},

 [𝐃⋅𝐖]πk,j\displaystyle[\ ={0πk∉an¯⁡(πj)1πk=πjandπk∈an(πj)⇔k∈pa^(j).\displaystyle=\begin
 mathbf{D}\cdot\mathbf    {cases}0&\pi_{k}\notin\anplus(\pi_{j})\\ 1&\pi_{k}=\pi_{j}\end       (77)
 {W}]_{\pi_{k},j}         {cases}\quad\mbox{and}\quad\pi_{k}\in\an(\pi_{j})\iff k\in\hat{\pa}
                          (j)\ .

We will prove that (77) holds for j=u+1j=u+1 as well. We work with ℳk,u+1=[u−1]∖{ch^​(k)∪{k}}\
mathcal{M}_{k,u+1}=[u-1]\setminus\{\hat{\ch}(k)\cup\{k\}\}. Consider some 𝐰∗=α[𝐖]:,k+β[𝐖]:,u+1\
mathbf{w}^{*}=\alpha[\mathbf{W}]_{:,k}+\beta[\mathbf{W}]_{:,u+1} in which β≠0\beta\neq 0 and let 𝐜∗
=𝐃⋅𝐰∗\mathbf{c}^{*}=\mathbf{D}\cdot\mathbf{w}^{*}. Due to the assumption in (77), ancestors of any
node in ℳk,u+1\mathcal{M}_{k,u+1} are contained in ℳk,u+1\mathcal{M}_{k,u+1}. Then, using Lemma 2
again, we have

 𝗇𝗎𝗅𝗅⁡({𝐇i∗:i∈ℳk,u+1})=𝗇𝗎𝗅𝗅⁡({[𝐆†]πi:i∈ℳk,u+1}).\displaystyle{\sf null}(\{\mathbf{H}^{*}_{i}
 \,:\,i\in\mathcal{M}_{k,u+1}\})={\sf null}(\{[\mathbf{G}^{{\dagger}}]_{\pi_{i}}\,:\,i\in\     (78)
 mathcal{M}_{k,u+1}\})\ .

Then, we have

 𝒱\           =proj𝗇𝗎𝗅𝗅⁡({𝐇i∗:i∈ℳk,u+1})im(Δ𝐒X⋅𝐰∗)\displaystyle=\proj_{{\sf null}(\{\mathbf
 displaystyle {H}^{*}_{i}\,:\,i\in\mathcal{M}_{k,u+1}\})}\im(\Delta\mathbf{S}_{X}\cdot\mathbf  (79)
 \mathcal{V}  {w}^{*})
              =proj𝗇𝗎𝗅𝗅⁡({[𝐆†]πi:i∈ℳk,u+1})([𝐆†]⊤⋅im(𝚲⋅𝐜∗)),\displaystyle=\proj_{{\sf null}(\
              {[\mathbf{G}^{{\dagger}}]_{\pi_{i}}\,:\,i\in\mathcal{M}_{k,u+1}\})}([\mathbf{G}  (80)
              ^{{\dagger}}]^{\top}\cdot\im(\mathbf{\Lambda}\cdot\mathbf{c}^{*}))\ ,

and

 dim(𝒱)=dim(im⁡([𝚲][n]∖ℳk,u+1⋅𝐜∗))≥dim(im⁡([𝚲πk𝚲πu+1]⋅𝐜∗)).\dim(\mathcal{V})=\dim\big(\im([\
 mathbf{\Lambda}]_{[n]\setminus\mathcal{M}_{k,u+1}}\cdot\mathbf{c}^{*})\big)\geq\dim\left(\im  (81)
 \left(\begin{bmatrix}\mathbf{\Lambda}_{\pi_{k}}\\ \mathbf{\Lambda}_{\pi_{u+1}}\end{bmatrix}\
 cdot\mathbf{c}^{*}\right)\right)\ .

We will investigate dim(𝒱)\dim(\mathcal{V}) through the ratio

 𝚲πk⋅𝐜∗𝚲πu+1⋅𝐜∗.\displaystyle\frac{\mathbf{\Lambda}_{\pi_{k}}\cdot\mathbf{c}^{*}}{\mathbf{\    (82)
 Lambda}_{\pi_{u+1}}\cdot\mathbf{c}^{*}}\ .

Note that, using (77) and the fact that π\pi is a causal order, we know that

 𝐜πi\displaystyle\mathbf{c}_{\pi_ =0,∀i∈{u+2,…,n}∪{{k+1,…,u}∖an^​(u+1)},\displaystyle=0\ ,\
 {i}}                             quad\forall\,i\,\in\{u+2,\dots,n\}\cup\{\{k+1,\dots,u\}\     (83)
                                  setminus\hat{\an}(u+1)\}\ ,
 and𝚲πj,πi\displaystyle\mbox{and} =0,∀πj∉pa¯⁡(πi).\displaystyle=0\ ,\quad\forall\,\pi_{j}\
 \qquad\mathbf{\Lambda}_{\pi_{j}, notin\paplus(\pi_{i})\ .                                     (84)
 \pi_{i}}

Then, using (83) and (84), we have

 𝚲πu+1⋅𝐜∗\displaystyle =∑πi∈ch¯⁡(πu+1)𝐜πi∗⋅𝚲πu+1,πi=𝐜πu+1∗⋅𝚲πu+1,πu+1,\displaystyle=\sum_{\
 \mathbf{\Lambda}_{\   pi_{i}\in\chplus(\pi_{u+1})}\mathbf{c}^{*}_{\pi_{i}}\cdot\mathbf{\      (85)
 pi_{u+1}}\cdot\mathbf Lambda}_{\pi_{u+1},\pi_{i}}=\mathbf{c}^{*}_{\pi_{u+1}}\cdot\mathbf{\
 {c}^{*}               Lambda}_{\pi_{u+1},\pi_{u+1}}\ ,
                       =𝐜∗πk⋅𝚲πk,πk+𝐜∗πu+1⋅𝚲πk,πu+1+∑i:πi∈ch⁡(πk)​ and ​i∈an^​(u+1)
 𝚲πk⋅𝐜∗\displaystyle\  𝐜∗πi⋅𝚲πk,πi.\displaystyle=\mathbf{c}^{*}_{\pi_{k}}\cdot\mathbf{\
 mathbf{\Lambda}_{\pi_ Lambda}_{\pi_{k},\pi_{k}}+\mathbf{c}^{*}_{\pi_{u+1}}\cdot\mathbf{\      (86)
 {k}}\cdot\mathbf{c}^  Lambda}_{\pi_{k},\pi_{u+1}}+\sum_{i\,:\,\pi_{i}\in\ch(\pi_{k})\text{
 {*}                   and }i\in\hat{\an}(u+1)}\mathbf{c}^{*}_{\pi_{i}}\cdot\mathbf{\Lambda}_
                       {\pi_{k},\pi_{i}}\ .

First, note that if there exists ℓ\ell such that πk∈an⁡(πℓ)\pi_{k}\in\an(\pi_{\ell}) and πℓ∈an⁡
(πu+1)\pi_{\ell}\in\an(\pi_{u+1}), then by the assumptions in (68) and (77), we already have that
k∈pa^​(ℓ)k\in\hat{\pa}(\ell) and ℓ∈pa^​(u+1)\ell\in\hat{\pa}(u+1) which implies that k∈pa^​(u+1)k\
in\hat{\pa}(u+1). Then, we only need to consider the case where there does not exist such ℓ\ell. In
this case, the summation in (86) is zero and we have

 𝚲πk⋅𝐜∗𝚲πu+1⋅𝐜∗=𝐜πk∗⋅𝚲πk,πk+𝐜πu+1∗⋅𝚲πk,πu+1𝐜πu+1∗⋅𝚲πu+1,πu+1.\displaystyle\frac{\mathbf{\
 Lambda}_{\pi_{k}}\cdot\mathbf{c}^{*}}{\mathbf{\Lambda}_{\pi_{u+1}}\cdot\mathbf{c}^{*}}=\frac
 {\mathbf{c}^{*}_{\pi_{k}}\cdot\mathbf{\Lambda}_{\pi_{k},\pi_{k}}+\mathbf{c}^{*}_{\pi_{u+1}}\  (87)
 cdot\mathbf{\Lambda}_{\pi_{k},\pi_{u+1}}}{\mathbf{c}^{*}_{\pi_{u+1}}\cdot\mathbf{\Lambda}_{\
 pi_{u+1},\pi_{u+1}}}\ .

Next, if πk∈pa⁡(πu+1)\pi_{k}\in\pa(\pi_{u+1}), interventional regularity ensures that this ratio is
not a constant function which implies that dim(𝒱)≥2\dim(\mathcal{V})\geq 2. Furthermore, if πk∉an⁡
(πu+1)\pi_{k}\notin\an(\pi_{u+1}) but 𝐜πk∗≠0\mathbf{c}^{*}_{\pi_{k}}\neq 0, then (87) reduces to

 𝚲πk⋅𝐜∗𝚲πu+1⋅𝐜∗=𝐜πk∗⋅𝚲πk,πk𝐜πu+1∗⋅𝚲πu+1,πu+1,\displaystyle\frac{\mathbf{\Lambda}_{\pi_{k}}\
 cdot\mathbf{c}^{*}}{\mathbf{\Lambda}_{\pi_{u+1}}\cdot\mathbf{c}^{*}}=\frac{\mathbf{c}^{*}_{\  (88)
 pi_{k}}\cdot\mathbf{\Lambda}_{\pi_{k},\pi_{k}}}{\mathbf{c}^{*}_{\pi_{u+1}}\cdot\mathbf{\
 Lambda}_{\pi_{u+1},\pi_{u+1}}}\ ,

which is not constant due to Lemma 3 and subsequently dim(𝒱)≥2\dim(\mathcal{V})\geq 2. Therefore,
dim(𝒱)=1\dim(\mathcal{V})=1 implies that πk∉an⁡(πu+1)\pi_{k}\notin\an(\pi_{u+1}) and 𝐜πk∗=[𝐃⋅𝐖]
πk,u+1=0\mathbf{c}^{*}_{\pi_{k}}=[\mathbf{D}\cdot\mathbf{W}]_{\pi_{k},u+1}=0. Finally, similarly to
the previous cases, Algorithm 1 is guaranteed to find such (α,β)(\alpha,\beta) that makes 𝐜πk∗=0\
mathbf{c}^{*}_{\pi_{k}}=0 by searching over (α,β)∈{−∥𝐖:,k∥1,…,∥𝐖:,k∥1}×{1,…,∥𝐖:,u+1∥1}(\alpha,\
beta)\in\{-\|\mathbf{W}_{:,k}\|_{1},\dots,\|\mathbf{W}_{:,k}\|_{1}\}\times\{1,\dots,\|\mathbf{W}_
{:,u+1}\|_{1}\}, e.g., (α,β)=(−[𝐃⋅𝐖]k,u+1,[𝐃⋅𝐖]k,k)(\alpha,\beta)=(-[\mathbf{D}\cdot\mathbf{W}]_
{k,u+1},[\mathbf{D}\cdot\mathbf{W}]_{k,k}). Then, the proof of the inner induction step, and
subsequently, the outer induction step is concluded, and (49) holds true. Consequently, the proof
of the theorem is completed.

A.3 Proof of Theorem 4

We start with a short synopsis of the proof. Stage 4 of Algorithm 1 consists of two steps. The
first step resolves the mixing with ancestors in recovered latent variables and the second step
refines the estimated graph to the edges from non-parent ancestors to children nodes. The proof of
the first step uses similar ideas to that of [7, Lemma 10]. Specifically, we use zero-covariance as
a surrogate for independence and search for unmixing vectors to eliminate the mixing with
ancestors. Since we do not have SN interventions unlike the setting in [7], we use additional proof
techniques to identify an environment in which a certain node is intervened. In the graph recovery
stage, we leverage the knowledge of ancestral relationships and use a small number of conditional
independence tests to remove the edges from non-parent ancestors to the children nodes.

A.3.1 Recovery of the latent variables

First, by Theorem 3, for the output 𝐇∗\mathbf{H}^{*} of Stage 3 of Algorithm 1 we have

 [𝐇∗⋅𝐆]=𝐏π⋅𝐋,\displaystyle[\mathbf{H}^{*}\cdot\mathbf{G}]=\mathbf{P}_{\pi}\cdot\mathbf{L}\ ,  (89)

for some lower triangular matrix 𝐋∈ℝn×n\mathbf{L}\in{\mathbb{R}}^{n\times n} such that 𝐋\mathbf{L}
has non-zero diagonal entries and 𝐋i,j=0\mathbf{L}_{i,j}=0 for all j∉an¯⁡(i)j\notin\anplus(i), and
π\pi is a causal order. We will show that the output of Stage 4 satisfies that

 𝟙​([𝐇∗⋅𝐆]t)=𝐞πt⊤,∀t∈[n],\displaystyle\mathds{1}\big([\mathbf{H}^{*}\cdot\mathbf{G}]_{t}\big)  (90)
 =\mathbf{e}_{\pi_{t}}^{\top}\ ,\quad\forall t\in[n]\ ,

which will imply

 Z^​(X,𝐇∗)=𝐇∗⋅X=𝐇∗⋅𝐆⋅Z=𝐏π⋅𝐂s⋅Z,\hat{Z}(X;\mathbf{H}^{*})=\mathbf{H}^{*}\cdot X=\mathbf{H}^{*}  (91)
 \cdot\mathbf{G}\cdot Z=\mathbf{P}_{\pi}\cdot\mathbf{C}_{\rm s}\cdot Z\ ,

where 𝐏π\mathbf{P}_{\pi} is the row permutation matrix of π\pi and 𝐂s\mathbf{C}_{\rm s} is a
constant diagonal matrix with nonzero diagonal entries. Note that this is the definition of perfect
identifiability for latent variables, specified in Definition 1. We prove that Stage 4 output 𝐇∗\
mathbf{H}^{*} satisfies (90) as follows.

We start by noting that since π1\pi_{1} is a root node in 𝒢\mathcal{G} as shown in Lemma 2, (89)
implies

 [𝐇∗⋅𝐆]1=𝐋π1,π1⋅𝐞π1⊤and𝟙​([𝐇∗⋅𝐆]1)=𝐞π1⊤,[\mathbf{H}^{*}\cdot\mathbf{G}]_{1}=\mathbf{L}_{\pi_
 {1},\pi_{1}}\cdot\mathbf{e}_{\pi_{1}}^{\top}\quad\mbox{and}\quad\mathds{1}\big([\mathbf{H}^   (92)
 {*}\cdot\mathbf{G}]_{1}\big)=\mathbf{e}_{\pi_{1}}^{\top}\ ,

and (90) is satisfied for t=1t=1. Next, assume that (90) is satisfied for all t∈{1,…,k−1}t\in\{1,\
dots,k-1\}, i.e.,

 𝟙​([𝐇∗⋅𝐆]t)=𝐞πt⊤,∀t∈[k−1].\displaystyle\mathds{1}\big([\mathbf{H}^{*}\cdot\mathbf{G}]_{t}\    (93)
 big)=\mathbf{e}_{\pi_{t}}^{\top}\ ,\quad\forall t\in[k-1]\ .

Consider kk-th iteration of the algorithm in which we update 𝐇k∗\mathbf{H}^{*}_{k} where {𝐇i∗:i∈
[k−1]}\{\mathbf{H}^{*}_{i}:i\in[k-1]\} already satisfy (90). We will prove that (90) will be
satisfied in four steps. Consider any environment ℰm\mathcal{E}^{m} and use shorthand Z^m\hat{Z}^
{m} for Z^m​(X,𝐇∗)\hat{Z}^{m}(X;\mathbf{H}^{*}).

Step 1: Cov⁡(𝐮⋅Z^an^​(k)m+Z^km,Z^an^​(k)m)=𝟎\Cov(\mathbf{u}\cdot\hat{Z}^{m}_{\hat{\an}(k)}+\hat{Z}^
{m}_{k},\hat{Z}^{m}_{\hat{\an}(k)})=\boldsymbol{0} has a unique solution for 𝐮\mathbf{u}.

For any i∈[k−1]i\in[k-1], (93) implies

 Z^im=[𝐇∗⋅𝐆]⋅Zm=𝐜i⋅Zπim,\hat{Z}^{m}_{i}=[\mathbf{H}^{*}\cdot\mathbf{G}]\cdot Z^{m}=\mathbf{c}  (94)
 _{i}\cdot Z^{m}_{\pi_{i}}\ ,

for some nonzero constants {𝐜1,…,𝐜k−1}\{\mathbf{c}_{1},\dots,\mathbf{c}_{k-1}\}. Specifically, by
defining

 an^​(k)={γ1,…,γr}\displaystyle\hat{\an}(k)=\{\gamma_{1},\dots,\gamma_{r}\}  (95)

in which the nodes are topologically ordered, then

 Z^an^​(k)m=[Z^γ1m,…,Z^γrm]=[𝐜γ1⋅Zπγ1m,…,𝐜γr⋅Zπγrm].\displaystyle\hat{Z}^{m}_{\hat{\an}(k)}=\
 big[\hat{Z}^{m}_{\gamma_{1}},\dots,\hat{Z}^{m}_{\gamma_{r}}\big]=[\mathbf{c}_{\gamma_{1}}\    (96)
 cdot Z^{m}_{\pi_{\gamma_{1}}},\dots,\mathbf{c}_{\gamma_{r}}\cdot Z^{m}_{\pi_{\gamma_{r}}}]\
 .

Note that Cov⁡(Z^an^​(k)m)\Cov(\hat{Z}^{m}_{\hat{\an}(k)}) is invertible since the causal
relationships among the entries in Z^an^​(k)m\hat{Z}^{m}_{\hat{\an}(k)} are not deterministic.
Then, we have

 Cov(𝐮⋅Z^an^​(k)m+Z^km,Z^an^​(k)m)=0⇔𝐮=−Cov(Z^km,Z^an^​(k)m)⋅[Cov(Z^an^​(k)m)]−1.\
 displaystyle\Cov(\mathbf{u}\cdot\hat{Z}^{m}_{\hat{\an}(k)}+\hat{Z}^{m}_{k},\hat{Z}^{m}_{\hat  (97)
 {\an}(k)})=0\quad\iff\quad\mathbf{u}=-\Cov(\hat{Z}^{m}_{k},\hat{Z}^{m}_{\hat{\an}(k)})\cdot
 [\Cov(\hat{Z}^{m}_{\hat{\an}(k)})]^{-1}\ .

For the next step, using the additive noise model specified in (3), under hard interventions we
have

 Zπkm=fπkm​(Zpa⁡(πk))+Nπkm,where​(fπkm,Nπkm)≜{(fπk,Nπk),πk∉Im(0,N¯πk),πk∈Im,\displaystyle Z^
 {m}_{\pi_{k}}=f^{m}_{\pi_{k}}(Z_{\pa(\pi_{k})})+N^{m}_{\pi_{k}}\ ,\quad\mbox{where}\;\;(f^    (98)
 {m}_{\pi_{k}},N^{m}_{\pi_{k}})\triangleq\begin{cases}(f_{\pi_{k}},N_{\pi_{k}})\ ,&\pi_{k}\
 notin I^{m}\\ (0,\bar{N}_{\pi_{k}})\ ,&\pi_{k}\in I^{m}\end{cases}\ ,

where N¯πk\bar{N}_{\pi_{k}} denotes the exogenous noise term under intervention. Let us use ff and
NN as shorthands for fπkmf^{m}_{\pi_{k}} and NπkmN^{m}_{\pi_{k}}.

Step 2: Cov⁡(𝐮⋅Z^an^​(k)m+Z^km,Z^an^​(k)m)=0\Cov(\mathbf{u}\cdot\hat{Z}^{m}_{\hat{\an}(k)}+\hat{Z}^
{m}_{k},\hat{Z}^{m}_{\hat{\an}(k)})=0 and 𝐮⋅Z^an^​(k)m⟂⟂Z^an^​(k)m\mathbf{u}\cdot\hat{Z}^{m}_{\hat
{\an}(k)}\perp\!\!\!\perp\hat{Z}^{m}_{\hat{\an}(k)} implies that fπkmf^{m}_{\pi_{k}} cannot be
nonlinear.

Define random variable U≜(𝐮⋅Z^an^​(k)m+Z^km)U\triangleq(\mathbf{u}\cdot\hat{Z}^{m}_{\hat{\an}(k)}+\
hat{Z}^{m}_{k}). Suppose that Cov⁡(U,Z^an^​(k)m)=0\Cov(U,\hat{Z}^{m}_{\hat{\an}(k)})=0 and U⟂⟂Z^an^
​(k)mU\perp\!\!\!\perp\hat{Z}^{m}_{\hat{\an}(k)}. Recall that using (89), Z^km\hat{Z}^{m}_{k} is a
linear combination of the variables {Zπim:i∈an⁡(k)}\{Z^{m}_{\pi_{i}}:i\in\an(k)\} and ZπkmZ^{m}_{\
pi_{k}}, i.e.,

 Z^km=𝐜′⋅Z^an^​(k)m+𝐜0′⋅Zπkm,\hat{Z}^{m}_{k}=\mathbf{c}^{\prime}\cdot\hat{Z}^{m}_{\hat{\an}    (99)
 (k)}+\mathbf{c}^{\prime}_{0}\cdot Z^{m}_{\pi_{k}}\ ,

where 𝐜′∈ℝ|an^​(k)|\mathbf{c}^{\prime}\in{\mathbb{R}}^{|\hat{\an}(k)|} and 𝐜0′\mathbf{c}^{\prime}_
{0} is a non-zero scalar. Then, UU can be restated as

 U=𝐮⋅Z^an^​(k)m+Z^km=(𝐮+𝐜′)⋅Z^an^​(k)m+𝐜0′⋅(f⁡(Zpa⁡(k)m)+N).\displaystyle U=\mathbf{u}\cdot\
 hat{Z}^{m}_{\hat{\an}(k)}+\hat{Z}^{m}_{k}=(\mathbf{u}+\mathbf{c}^{\prime})\cdot\hat{Z}^{m}_  (100)
 {\hat{\an}(k)}+\mathbf{c}^{\prime}_{0}\cdot(f(Z^{m}_{\pa(k)})+N)\ .

Note that U⟂⟂Z^an^​(k)mU\perp\!\!\!\perp\hat{Z}^{m}_{\hat{\an}(k)} and (96) together imply that UU
is independent of any function of the variables in {Zπim:i∈an⁡(k)}\{Z^{m}_{\pi_{i}}:i\in\an(k)\}.
Hence,

 (𝐮+𝐜′)⋅Z^an^​(k)m+𝐜0′⋅f⁡(Zpa⁡(k))⟂⟂(𝐮+𝐜′)⋅Z^an^​(k)m+𝐜0′​(f⁡(Zpa⁡(k))+N),\displaystyle(\
 mathbf{u}+\mathbf{c}^{\prime})\cdot\hat{Z}^{m}_{\hat{\an}(k)}+\mathbf{c}^{\prime}_{0}\cdot   (101)
 f(Z_{\pa(k)})\perp\!\!\!\perp(\mathbf{u}+\mathbf{c}^{\prime})\cdot\hat{Z}^{m}_{\hat{\an}
 (k)}+\mathbf{c}^{\prime}_{0}(f(Z_{\pa(k)})+N)\ ,

where the right-hand size is UU and the left-hand side is a function of {Zπim:i∈an⁡(k)}\{Z^{m}_{\
pi_{i}}:i\in\an(k)\}. Also, since NN is the exogenous noise variable associated with ZπkZ_{\pi_
{k}}, we have

 (𝐮+𝐜′)⋅Z^an^​(k)m+𝐜0′⋅f⁡(Zpa⁡(k))⟂⟂𝐜0′⋅N.\displaystyle(\mathbf{u}+\mathbf{c}^{\prime})\cdot
 \hat{Z}^{m}_{\hat{\an}(k)}+\mathbf{c}^{\prime}_{0}\cdot f(Z_{\pa(k)})\perp\!\!\!\perp\       (102)
 mathbf{c}^{\prime}_{0}\cdot N\ .

(101) and (102) imply that (𝐮+𝐜′)⋅Z^an^​(k)m+𝐜0′⋅f⁡(Zpa⁡(k))(\mathbf{u}+\mathbf{c}^{\prime})\cdot\
hat{Z}^{m}_{\hat{\an}(k)}+\mathbf{c}^{\prime}_{0}\cdot f(Z_{\pa(k)}) is a constant function of Z^an
^​(k)m\hat{Z}^{m}_{\hat{\an}(k)}. Since (𝐮+𝐜′)⋅Z^an^​(k)m(\mathbf{u}+\mathbf{c}^{\prime})\cdot\hat
{Z}^{m}_{\hat{\an}(k)} is a linear function (or constant zero) and 𝐜0′≠0\mathbf{c}^{\prime}_{0}\neq
0, ff cannot be a nonlinear function which concludes the proof of this step. Next, we consider two
possible cases, ff is zero, i.e., πk∈Im\pi_{k}\in I^{m} case, and ff is a linear function.

Step 3: πk∈Im\pi_{k}\in I^{m} and f=0f=0.

In this case, we have Zπkm=NZ^{m}_{\pi_{k}}=N. Note that for 𝐮=−𝐜′\mathbf{u}=-\mathbf{c}^{\prime},
(100) becomes

 U=𝐜0′⋅N=𝐜0′⋅Zπkm,U=\mathbf{c}^{\prime}_{0}\cdot N=\mathbf{c}^{\prime}_{0}\cdot Z^{m}_{\pi_   (103)
 {k}}\ ,

which implies

 (𝐇k∗+𝐮⋅𝐇an^​(k)∗)⋅𝐆⋅Zm\displaystyle(\mathbf{H} =Z^km+𝐮⋅Z^an^​(k)m=U=𝐜0′⋅Zπkm\displaystyle=\
 ^{*}_{k}+\mathbf{u}\cdot\mathbf{H}^{*}_{\hat{\ hat{Z}^{m}_{k}+\mathbf{u}\cdot\hat{Z}^{m}_{\  (104)
 an}(k)})\cdot\mathbf{G}\cdot Z^{m}             hat{\an}(k)}=U=\mathbf{c}^{\prime}_{0}\cdot
                                                Z^{m}_{\pi_{k}}
 and𝟙​((𝐇k∗+𝐮⋅𝐇an^​(k)∗)⋅𝐆)\displaystyle\mbox
 {and}\qquad\mathds{1}\big((\mathbf{H}^{*}_{k}+ =𝐞πk⊤.\displaystyle=\mathbf{e}_{\pi_{k}}^{\   (105)
 \mathbf{u}\cdot\mathbf{H}^{*}_{\hat{\an}(k)})\ top}\ .
 cdot\mathbf{G}\big)

Also note that, any 𝐮\mathbf{u} vector that satisfies U⟂⟂Z^an^​(k)mU\perp\!\!\!\perp\hat{Z}^{m}_{\
hat{\an}(k)} also satisfies Cov⁡(U,Z^an^​(k)m)=𝟎\Cov(U,\hat{Z}^{m}_{\hat{\an}(k)})=\boldsymbol{0}.
In Step 1, we have shown that Cov⁡(U,Z^an^​(k)m)=𝟎\Cov(U,\hat{Z}^{m}_{\hat{\an}(k)})=\boldsymbol{0}
has a unique solution. Therefore, U⟂⟂Z^an^​(k)mU\perp\!\!\!\perp\hat{Z}^{m}_{\hat{\an}(k)} also has
a unique solution, which we have found in (97). Hence, if πk∈Im\pi_{k}\in I^{m}, then Algorithm 1
updates 𝐇k∗\mathbf{H}^{*}_{k} correctly.

Step 4: πk∉Im\pi_{k}\notin I^{m} and ff is linear.

Only remaining case to check is πk∉Im\pi_{k}\notin I^{m} and ff is a linear function. Let

 f⁡(Zpa⁡(πk))≜𝐜′′⋅Z^an^​(k)m,f(Z_{\pa(\pi_{k})})\triangleq\mathbf{c}^{\prime\prime}\cdot\hat  (106)
 {Z}^{m}_{\hat{\an}(k)}\ ,

in which 𝐜′′∈ℝ|an^​(k)|\mathbf{c}^{\prime\prime}\in{\mathbb{R}}^{|\hat{\an}(k)|} has nonzero
entries only at the coordinates corresponding to pa⁡(πk)\pa(\pi_{k}). Then, for (𝐮+𝐜′)⋅Z^an^​(k)
m+𝐜0′⋅f⁡(Zpa⁡(k))(\mathbf{u}+\mathbf{c}^{\prime})\cdot\hat{Z}^{m}_{\hat{\an}(k)}+\mathbf{c}^{\
prime}_{0}\cdot f(Z_{\pa(k)}) to be a constant function, we need to have (𝐮+𝐜′+𝐜0′⋅𝐜′′)=𝟎(\mathbf
{u}+\mathbf{c}^{\prime}+\mathbf{c}^{\prime}_{0}\cdot\mathbf{c}^{\prime\prime})=\boldsymbol{0}. Note
that, for 𝐜′′≠𝟎\mathbf{c}^{\prime\prime}\neq\boldsymbol{0} case, i.e., f≠0f\neq 0 and πk∉Im\pi_{k}\
notin I^{m}, we have already found 𝐮obs=−𝐜′−𝐜0′⋅𝐜′′\mathbf{u}^{\rm obs}=-\mathbf{c}^{\prime}-\
mathbf{c}^{\prime}_{0}\cdot\mathbf{c}^{\prime\prime} using the observational environment. Since we
are searching for 𝐮=−𝐜′\mathbf{u}=-\mathbf{c}^{\prime}, which is required to achieve scaling
consistency, we compare the solution 𝐮m\mathbf{u}^{m} at environment ℰm\mathcal{E}^{m} to 𝐮obs\
mathbf{u}^{\rm obs} and if they are distinct, we update 𝐇k∗\mathbf{H}^{*}_{k}.

To sum up, Stage 4 updates 𝐇k∗\mathbf{H}^{*}_{k} correctly by identifying an environment ℰm\mathcal
{E}^{m} in which ZπkZ_{\pi_{k}} is intervened and eliminating the effect of 𝐇an^​(k)∗\mathbf{H}^{*}
_{\hat{\an}(k)}. Finally, we note that such an environment is guaranteed to exist among {ℰm:𝐖m,k≠0}
\{\mathcal{E}^{m}:\mathbf{W}_{m,k}\neq 0\}. To see this, recall that [𝐃⋅𝐖]πk,k≠0[\mathbf{D}\cdot\
mathbf{W}]_{\pi_{k},k}\neq 0 due to (49), proven in Theorem 3. This implies that there exists mm
such that 𝐃πk,m=1\mathbf{D}_{\pi_{k},m}=1 and 𝐖m,k≠0\mathbf{W}_{m,k}\neq 0, and 𝐃πk,m=1\mathbf{D}_
{\pi_{k},m}=1 implies that πk∈Im\pi_{k}\in I^{m}. This completes the proof of (90), and
subsequently the proof of the perfect latent recovery.

A.3.2 Recovery of the latent graph

After the first step of Stage 4, we have

 Z^t=𝐜t⋅Zπt,∀t∈[n],\hat{Z}_{t}=\mathbf{c}_{t}\cdot Z_{\pi_{t}}\ ,\quad\forall t\in[n]\ ,  (107)

where {𝐜t:t∈[n]}\{\mathbf{c}_{t}:t\in[n]\} are nonzero constants. Then,

 Z^t⟂⟂Z^j∣{Z^i:i∈𝒮}⇔Zπt⟂⟂Zπj∣{Zπi:i∈𝒮}.\hat{Z}_{t}\perp\!\!\!\perp\hat{Z}_{j}\mid\{\hat{Z}_
 {i}:i\in\mathcal{S}\}\quad\iff\quad Z_{\pi_{t}}\perp\!\!\!\perp Z_{\pi_{j}}\mid\{Z_{\pi_     (108)
 {i}}:i\in\mathcal{S}\}\ .

Consider node t∈[n]t\in[n] and node j∈ch^​(t)j\in\hat{\ch}(t). If πt∈pa⁡(πj)\pi_{t}\in\pa(\pi_{j}),
given the adjacency-faithfulness assumption, (108) implies that Z^t\hat{Z}_{t} and Z^j\hat{Z}_{j}
cannot be made conditionally independent for any conditioning set. On the other hand, note that for
any set 𝒮\mathcal{S} that contains all the nodes in pa⁡(πj)\pa(\pi_{j}) and does not contain a node
in de⁡(πj)\de(\pi_{j}) satisfies

 Zπt⟂⟂Zπj|{Zπi:i∈𝒮},Z_{\pi_{t}}\perp\!\!\!\perp Z_{\pi_{j}}\mid\{Z_{\pi_{i}}:i\in\mathcal{S}  (109)
 \}\ ,

and subsequently,

 Z^t⟂⟂Z^j|{Z^i:i∈𝒮}.\hat{Z}_{t}\perp\!\!\!\perp\hat{Z}_{j}\mid\{\hat{Z}_{i}:i\in\mathcal{S}   (110)
 \}\ .

Finally, if πt∉pa⁡(πj)\pi_{t}\notin\pa(\pi_{j}), then pa^​(j)∖{t}\hat{\pa}(j)\setminus\{t\}
contains all the nodes in pa⁡(πj)\pa(\pi_{j}) and does not contain a node in de⁡(πj)\de(\pi_{j}).
Hence, the second stage of Step 4 of Algorithm 1 successfully eliminates all spurious edges between
tt and j∈ch^​(t)j\in\hat{\ch}(t).

A.4 Proofs of Theorem 1 and Theorem 2

Under Assumption 1 and interventional regularity, Lemma 2 and Theorem 3 show that using UMN soft
interventions, outputs of Algorithm 1 satisfy identifiability up to ancestors. Hence,
identifiability up to ancestors is possible using UMN soft interventions.

Furthermore, note that Lemma 2 and Theorem 3 are valid for both soft and hard interventions. Then,
Theorem 4 shows that using UMN hard interventions, Algorithm 1 outputs satisfy perfect
identifiability. Hence, perfect identifiability is possible using UMN hard interventions.

A.5 Proof of Lemma 3

We start by showing that [Λ⁡(z)]i,i[\Lambda(z)]_{i,i} cannot be a constant function in zz. This is
shown in the proof of [5, Lemma 7]. For our paper to be self-contained, we repeat the proof steps
in [5] as follows. For i∈[n]i\in[n] define

 h⁡(zi,zpa⁡(i))≜pi​(zi∣zpa⁡(i))qi​(zi∣zpa⁡(i)).h(z_{i},z_{\pa(i)})\triangleq\frac{p_{i}(z_    (111)
 {i}\mid z_{\pa(i)})}{q_{i}(z_{i}\mid z_{\pa(i)})}\ .

We prove by contradiction that h⁡(zi,zpa⁡(i))h(z_{i},z_{\pa(i)}) varies with ziz_{i}. Assume the
contrary, i.e., let h⁡(zi,zpa⁡(i))=h⁡(zpa⁡(i))h(z_{i},z_{\pa(i)})=h(z_{\pa(i)}). By rearranging (
111) we have

 pi​(zi∣zpa⁡(i))=h⁡(zpa⁡(i))⋅qi​(zi∣zpa⁡(i)).\displaystyle p_{i}(z_{i}\mid z_{\pa(i)})=h(z_   (112)
 {\pa(i)})\cdot q_{i}(z_{i}\mid z_{\pa(i)})\ .

Fix a realization of zpa⁡(i)=zpa⁡(i)∗z_{\pa(i)}=z_{\pa(i)}^{*}, and integrate both sides of (112)
with respect to ziz_{i}. Since both pip_{i} and qiq_{i} are pdfs, we have

 1\           =∫ℝpi​(zi∣zpa⁡(i)∗)​d​zi=∫ℝh⁡(zpa⁡(i)∗)⋅qi​(zi∣zpa⁡(i)∗)​d​zi\displaystyle=\
 displaystyle int_{{\mathbb{R}}}p_{i}(z_{i}\mid z_{\pa(i)}^{*})\mathrm{d}{z_{i}}=\int_{{\     (113)
 1            mathbb{R}}}h(z_{\pa(i)}^{*})\cdot q_{i}(z_{i}\mid z_{\pa(i)}^{*})\mathrm{d}{z_
              {i}}
              =h⁡(zpa⁡(i)∗)​∫ℝqi​(zi∣zpa⁡(i)∗)​d​zi\displaystyle=h(z_{\pa(i)}^{*})\int_{{\    (114)
              mathbb{R}}}q_{i}(z_{i}\mid z_{\pa(i)}^{*})\mathrm{d}{z_{i}}
              =h⁡(zpa⁡(i)∗).\displaystyle=h(z_{\pa(i)}^{*})\ .                                (115)

This identity implies that pi​(zi∣zpa⁡(i)∗)=qi​(zi∣zpa⁡(i)∗)p_{i}(z_{i}\mid z_{\pa(i)}^{*})=q_{i}
(z_{i}\mid z_{\pa(i)}^{*}) for any arbitrary realization zpa⁡(i)∗z_{\pa(i)}^{*}, which contradicts
with the premise that observational and interventional causal mechanisms are distinct.
Consequently,

 [Λ⁡(z)]i,i=∂∂zi​log⁡pi​(zi∣zpa⁡(i))qi​(zi∣zpa⁡(i))[\Lambda(z)]_{i,i}=\frac{\partial}{\       (116)
 partial z_{i}}\log\frac{p_{i}(z_{i}\mid z_{\pa(i)})}{q_{i}(z_{i}\mid z_{\pa(i)})}

is not a constant in zz. Note that for a fixed realization of zj=zj∗z_{j}=z^{*}_{j}, zpa⁡(i)=zpa⁡
(i)∗z_{\pa(i)}=z^{*}_{\pa(i)}, and zpa⁡(j)=zpa⁡(j)∗z_{\pa(j)}=z^{*}_{\pa(j)}, [𝚲⁡(z)]j,j[\mathbf{\
Lambda}(z)]_{j,j} becomes constant whereas [𝚲⁡(z)]i,i[\mathbf{\Lambda}(z)]_{i,i} varies with ziz_
{i}. Hence, their ratio is not a constant in zz. Finally, note that 𝚲⁡(z)\mathbf{\Lambda}(z) is an
upper-triangular matrix since (1,…,n)(1,\dots,n) is a valid causal order and for all i∈[n]i\in[n],

 ∇log⁡pi​(zi∣zpa⁡(i))qi​(zi∣zpa⁡(i))\nabla\log\frac{p_{i}(z_{i}\mid z_{\pa(i)})}{q_{i}(z_{i}  (117)
 \mid z_{\pa(i)})}

is a function of only {zk:k∈pa⁡(i)∪{i}}\{z_{k}:k\in\pa(i)\cup\{i\}\}. Together with the fact that
diagonal entries of 𝚲⁡(z)\mathbf{\Lambda}(z) are not constantly zero, the columns (and rows) of 𝚲\
mathbf{\Lambda} are linearly independent vector-valued functions.

A.6 Insufficiency of strongly separating sets

Background.

Recently, [16] has shown the identifiability of latent representations under a linear
transformation using do interventions. Specifically, they have shown that a strongly separating set
of multi-node interventions are sufficient for identifiability. It is well-known that strongly
separating sets can be constructed using 2​⌈log2⁡n⌉2\lceil\log_{2}n\rceil elements. Hence,
identifiability can be achieved using 2​⌈log⁡n⌉2\lceil\log n\rceil do interventions. In this
section, we show that a similar result is impossible when using stochastic hard interventions.

Lemma 4 (Impossibility).

A strongly separated set of 2​⌈log2⁡n⌉2\lceil\log_{2}n\rceil stochastic hard interventions are not
guaranteed to be sufficient for perfect identifiability. In fact, they are not even sufficient for
identifiability up to ancestors.

Proof: We prove the claim for n=2n=2 nodes. The smallest strongly separating set for two nodes is
{{1},{2}}\{\{1\},\{2\}\}. We will consider two distinct models of latent variables and latent
graphs that are not distinguishable using interventional data of I1={1}I^{1}=\{1\} and I2={2}I^{2}=
\{2\} (without observational data). The key idea is that after the linear transformation in the
first model is fixed, we can design the linear transformation in the second model such that
observed variables in both models will have the same distributions. We construct a pair of
indistinguishable models as follows.

First model.

Let 𝒢\mathcal{G} consists of the edge 1→21\rightarrow 2. Consider a linear Gaussian latent model
with the edge weight of Z1Z_{1} on Z2Z_{2} is set to 11, and consider identity mapping, i.e.,

 𝒢:1→2,𝐆=[1001],Z1=[N1∗N1∗+N2],Z2=[N1N2∗],\displaystyle\mathcal{G}:1\rightarrow 2\ ,\qquad\
 mathbf{G}=\begin{bmatrix}1&0\\ 0&1\end{bmatrix}\ ,\quad Z^{1}=\begin{bmatrix}N_{1}^{*}\\ N_  (118)
 {1}^{*}+N_{2}\end{bmatrix}\ ,\quad Z^{2}=\begin{bmatrix}N_{1}\\ N_{2}^{*}\end{bmatrix}\ ,
 N1∼𝒩⁡(0,V1),N1∗∼𝒩⁡(0,V1∗),N2∼𝒩⁡(0,V2),N2∗∼𝒩⁡(0,V2∗).\displaystyle N_{1}\sim\mathcal{N}(0,V_
 {1})\ ,\;\;N_{1}^{*}\sim\mathcal{N}(0,V_{1}^{*})\ ,\;\;N_{2}\sim\mathcal{N}(0,V_{2})\ ,\;\;  (119)
 N_{2}^{*}\sim\mathcal{N}(0,V_{2}^{*})\ .

where V1,V1∗,V2,V2∗V_{1},V_{1}^{*},V_{2},V_{2}^{*} are nonzero variances of the exogenous noise
terms such that V¯1≠V¯1∗\bar{V}_{1}\neq\bar{V}_{1}^{*} to ensure that interventional and
observational mechanisms of node 11 are distinct. Then, the observed variables XX in two
environments are given by

 X1\displaystyle X^ =𝐆⋅Z1∼𝒩⁡(0,[V1∗V1∗V1∗V1∗+V2]),\displaystyle=\mathbf{G}\cdot Z^{1}\sim\
 {1}                mathcal{N}\left(0,\begin{bmatrix}V_{1}^{*}&V_{1}^{*}\\ V_{1}^{*}&V_{1}^   (120)
                    {*}+V_{2}\end{bmatrix}\right)\ ,
 andX2\displaystyle =𝐆⋅Z2∼𝒩⁡(0,[V100V2∗]).\displaystyle=\mathbf{G}\cdot Z^{2}\sim\mathcal{N}
 \mbox{and}\quad X^ \left(0,\begin{bmatrix}V_{1}&0\\ 0&V_{2}^{*}\end{bmatrix}\right)\ .       (121)
 {2}

Second model.

Let 𝒢¯\bar{\mathcal{G}} be the empty graph. Consider a linear Gaussian latent model under a
non-identity mapping parameterized by

 𝒢¯:empty,𝐆¯=[abcd],Z¯1=[N¯1∗N¯2],Z2=[N¯1N¯2∗],\displaystyle\bar{\mathcal{G}}:\mbox{empty}\
 ,\qquad\bar{\mathbf{G}}=\begin{bmatrix}a&b\\ c&d\end{bmatrix}\ ,\quad\bar{Z}^{1}=\begin      (122)
 {bmatrix}\bar{N}_{1}^{*}\\ \bar{N}_{2}\end{bmatrix}\ ,\quad Z^{2}=\begin{bmatrix}\bar{N}_
 {1}\\ \bar{N}_{2}^{*}\end{bmatrix}\ ,
 N¯1∼𝒩⁡(0,V¯1),N¯1∗∼𝒩⁡(0,V¯1∗),N¯2∼𝒩⁡(0,V¯2),N¯2∗∼𝒩⁡(0,V¯2∗).\displaystyle\bar{N}_{1}\sim\
 mathcal{N}(0,\bar{V}_{1})\ ,\;\;\bar{N}_{1}^{*}\sim\mathcal{N}(0,\bar{V}_{1}^{*})\ ,\;\;\    (123)
 bar{N}_{2}\sim\mathcal{N}(0,\bar{V}_{2})\ ,\;\;\bar{N}_{2}^{*}\sim\mathcal{N}(0,\bar{V}_{2}
 ^{*})\ .

where V¯1,V¯1∗,V¯2,V¯2∗\bar{V}_{1},\bar{V}_{1}^{*},\bar{V}_{2},\bar{V}_{2}^{*} are nonzero
variances of the exogenous noise terms such that V¯1≠V¯1∗\bar{V}_{1}\neq\bar{V}_{1}^{*} and
V¯2≠V¯2∗\bar{V}_{2}\neq\bar{V}_{2}^{*} to ensure that interventional mechanisms are distinct from
the observational ones. Then, the observed variables X¯\bar{X} in two environments are given by

                 =𝐆¯⋅Z¯1∼𝒩⁡(0,
 X¯1\            [a2​V¯1∗+b2​V¯2a​c​V¯1∗+b​d​V¯2a​c​V¯1∗+b​d​V¯2c2​V¯1∗+d2​V¯2]),\
 displaystyle\   displaystyle=\bar{\mathbf{G}}\cdot\bar{Z}^{1}\sim\mathcal{N}\left(0,\begin   (124)
 bar{X}^{1}      {bmatrix}a^{2}\bar{V}_{1}^{*}+b^{2}\bar{V}_{2}&ac\bar{V}_{1}^{*}+bd\bar{V}_
                 {2}\\ ac\bar{V}_{1}^{*}+bd\bar{V}_{2}&c^{2}\bar{V}_{1}^{*}+d^{2}\bar{V}_{2}
                 \end{bmatrix}\right)\ ,
                 =𝐆¯⋅Z¯2∼𝒩⁡(0,
 andX¯2\         [a2​V¯1+b2​V¯2∗a​c​V¯1+b​d​V¯2∗a​c​V¯1+b​d​V¯2∗c2​V¯1+d2​V¯2∗]).\
 displaystyle\   displaystyle=\bar{\mathbf{G}}\cdot\bar{Z}^{2}\sim\mathcal{N}\left(0,\begin   (125)
 mbox{and}\quad\ {bmatrix}a^{2}\bar{V}_{1}+b^{2}\bar{V}_{2}^{*}&ac\bar{V}_{1}+bd\bar{V}_{2}^
 bar{X}^{2}      {*}\\ ac\bar{V}_{1}+bd\bar{V}_{2}^{*}&c^{2}\bar{V}_{1}+d^{2}\bar{V}_{2}^{*}
                 \end{bmatrix}\right)\ .

Non-identifiability.

We will show a nontrivial construction that ensures X1=X¯1X^{1}=\bar{X}^{1} and X2=X¯2X^{2}=\bar{X}
^{2}, which implies the non-identifiability from intervention set {{1},{2}}\{\{1\},\{2\}\}. First,
using (120), (121), (124), and (125), we write all requirements for X1=X¯1X^{1}=\bar{X}^{1} and X2=
X¯2X^{2}=\bar{X}^{2} to hold:

 V1∗\displaystyle V_  =a2​V¯1∗+b2​V¯2,\displaystyle=a^{2}\bar{V}_{1}^{*}+b^{2}\bar{V}_{2}\ ,  (126)
 {1}^{*}
 V1∗\displaystyle V_  =a​c​V¯1∗+b​d​V¯2,\displaystyle=ac\bar{V}_{1}^{*}+bd\bar{V}_{2}\ ,      (127)
 {1}^{*}
 V2\displaystyle V_   =(c2−a​c)​V¯1∗+(d2−b​d)​V¯2,\displaystyle=(c^{2}-ac)\bar{V}_{1}^{*}+(d  (128)
 {2}                  ^{2}-bd)\bar{V}_{2}\ ,
 V1\displaystyle V_   =a2​V¯1+b2​V¯2∗,\displaystyle=a^{2}\bar{V}_{1}+b^{2}\bar{V}_{2}^{*}\ ,  (129)
 {1}
 V2∗\displaystyle V_  =c2​V¯1+d2​V¯2∗,\displaystyle=c^{2}\bar{V}_{1}+d^{2}\bar{V}_{2}^{*}\ ,  (130)
 {2}^{*}
 0\displaystyle 0     =a​c​V¯1+b​d​V¯2∗.\displaystyle=ac\bar{V}_{1}+bd\bar{V}_{2}^{*}\ .      (131)

Now, let V¯1,V¯1∗,V¯2,V¯2∗{\bar{V}_{1},\bar{V}_{1}^{*},\bar{V}_{2},\bar{V}_{2}^{*}} take any values
such that V¯1​V¯2≠V¯1∗​V¯2∗\bar{V}_{1}\bar{V}_{2}\neq\bar{V}_{1}^{*}\bar{V}_{2}^{*}. We want to
show that there exist {a,b,c,d}\{a,b,c,d\} and {V1,V1∗,V2,V2∗}\{V_{1},V_{1}^{*},V_{2},V_{2}^{*}\}
values that satisfy all the six equations. Note that, the values of V1,V2V_{1},V_{2}, and V2∗V_{2}^
{*} are not constrained by multiple equations or additional conditions. Hence, for any given
{a,b,c,d}\{a,b,c,d\} and {V¯1,V¯1∗,V¯2,V¯2∗}\{\bar{V}_{1},\bar{V}_{1}^{*},\bar{V}_{2},\bar{V}_{2}^
{*}\}, we can readily set V1,V2V_{1},V_{2}, and V2∗V_{2}^{*} to satisfy (129), (128), and (130),
respectively. Therefore, we only need to ensure that we can choose {a,b,c,d}\{a,b,c,d\} such that
the identities in (126),(127), and (131) are satisfied. Next, after substituting d=1d=1, and
rearranging, we only need to choose {a,b,c}\{a,b,c\} that satisfy

 a​c​V¯1+b​V¯2∗=0,\displaystyle ac\bar{V}_{1}+b\bar{V}_{2}^{*}=0\ ,                           (132)
 (a2−a​c)​V¯1∗+(b2−b)​V¯2=0.\displaystyle(a^{2}-ac)\bar{V}_{1}^{*}+(b^{2}-b)\bar{V}_{2}=0\ .  (133)

Substituting a​c=−b​V¯2∗V¯1ac=-b\dfrac{\bar{V}_{2}^{*}}{\bar{V}_{1}} into (133), we require

 b2⋅V¯2−b⁡(V¯2−V¯2∗​V¯1∗V¯1)+a2​V¯1∗=0.\displaystyle b^{2}\cdot\bar{V}_{2}-b(\bar{V}_{2}-\    (134)
 bar{V}_{2}^{*}\frac{\bar{V}_{1}^{*}}{\bar{V}_{1}})+a^{2}\bar{V}_{1}^{*}=0\ .

(134) has a real solution for bb if and only if

 a2≤V¯24​V¯1∗​(1−V¯1∗​V¯2∗V¯1​V¯2)2.\displaystyle a^{2}\leq\frac{\bar{V}_{2}}{4\bar{V}_{1}^   (135)
 {*}}\left(1-\frac{\bar{V}_{1}^{*}\bar{V}_{2}^{*}}{\bar{V}_{1}\bar{V}_{2}}\right)^{2}\ .

This implies that, if V¯1​V¯2≠V¯1∗​V¯2∗\bar{V}_{1}\bar{V}_{2}\neq\bar{V}_{1}^{*}\bar{V}_{2}^{*}, we
can choose aa and bb that satisfies (134), and cc is determined by (132). This means that, for any
given {V¯1,V¯1∗,V¯2,V¯2∗}\{\bar{V}_{1},\bar{V}_{1}^{*},\bar{V}_{2},\bar{V}_{2}^{*}\} such that
V¯1​V¯2≠V¯1∗​V¯2∗\bar{V}_{1}\bar{V}_{2}\neq\bar{V}_{1}^{*}\bar{V}_{2}^{*}, there exists {a,b,c,d}\
{a,b,c,d\} and {V1,V1∗,V2,V2∗}\{V_{1},V_{1}^{*},V_{2},V_{2}^{*}\} that ensures X1=X¯1X^{1}=\bar{X}^
{1} and X2=X¯2X^{2}=\bar{X}^{2}. Hence, interventions on the strongly separating set {{1},{2}}\{\{1
\},\{2\}\} are not sufficient to distinguish the first and second models, which completes the proof
of non-identifiability.  □\square

A.7 Analysis of interventional regularity

Lemma 5.

Consider additive noise models under hard interventions. Interventional regularity is satisfied if
the post-intervention score function of NiN_{i}, denoted by r¯\bar{r}, is analytic and one of the
following is true.

 1. 1.
   
    ∂fi​(zpa⁡(i))∂zj\dfrac{\partial f_{i}(z_{\pa(i)})}{\partial z_{j}} is not constant and there do
    not exist constants α1≠1\alpha_{1}\neq 1, α2,α3∈ℝ\alpha_{2},\alpha_{3}\in{\mathbb{R}} such that

     r¯​(y)=α1⋅r¯​(y+α2)+α3,∀y∈ℝ.\bar{r}(y)=\alpha_{1}\cdot\bar{r}(y+\alpha_{2})+\alpha_{3}\  (136)
     ,\quad\forall y\in{\mathbb{R}}\ .

 2. 2.
   
    ∂fi​(zpa⁡(i))∂zj\dfrac{\partial f_{i}(z_{\pa(i)})}{\partial z_{j}} is constant and noise term 
    NiN_{i} remains unaltered after the intervention.

Proof: The additive noise model for nodes ii and jj are given by

 Zi=fi​(Zpa⁡(i))+Ni,andZj=fj​(Zpa⁡(j))+Nj.Z_{i}=f_{i}(Z_{\pa(i)})+N_{i}\ ,\quad\mbox{and}\    (137)
 quad Z_{j}=f_{j}(Z_{\pa(j)})+N_{j}\ .

When nodes ii and jj are intervened, respectively, ZiZ_{i} and ZjZ_{j} are generated according to

 Zi=N¯iandZj=N¯j,Z_{i}=\bar{N}_{i}\quad\mbox{and}\quad Z_{j}=\bar{N}_{j}\ ,  (138)

in which N¯i\bar{N}_{i} and N¯j\bar{N}_{j} correspond to exogenous noise terms for nodes ii and jj
under intervention. Then, denoting the pdfs of Ni,N¯i,Nj,N¯jN_{i},\bar{N}_{i},N_{j},\bar{N}_{j} by
hi,h¯i,hj,h¯jh_{i},\bar{h}_{i},h_{j},\bar{h}_{j}, respectively, we have

 pi​(zi∣zpa⁡(i))\displaystyle p_{i}(z_{i} =hi​(zi−fi​(zpa⁡(i))),\displaystyle=h_{i}(z_{i}-f_  (139)
 \mid z_{\pa(i)})                         {i}(z_{\pa(i)}))\ ,
 qi​(zi)\displaystyle q_{i}(z_{i})        =h¯i​(zi),\displaystyle=\bar{h}_{i}(z_{i})\ ,       (140)
 pj​(zj∣zpa⁡(j))\displaystyle p_{j}(z_{j} =hj​(zj−fj​(zpa⁡(j))),\displaystyle=h_{j}(z_{j}-f_  (141)
 \mid z_{\pa(j)})                         {j}(z_{\pa(j)}))\ ,
 qj​(zj)\displaystyle q_{j}(z_{j})        =h¯j​(zj).\displaystyle=\bar{h}_{j}(z_{j})\ .       (142)

Then, by denoting the score functions associated with hi,h¯i,hj,h¯jh_{i},\bar{h}_{i},h_{j},\bar{h}_
{j} by ri,r¯i,rj,r¯jr_{i},\bar{r}_{i},r_{j},\bar{r}_{j}, respectively, we have

 ∂∂zi​log⁡pi​(zi∣zpa⁡(i))qi​(zi)\displaystyle\    =ri​(ni)−r¯i​(ni+fi​(zpa⁡(i))),\
 frac{\partial}{\partial z_{i}}\log\frac{p_{i}(z_ displaystyle=r_{i}(n_{i})-\bar{r}_{i}(n_    (143)
 {i}\mid z_{\pa(i)})}{q_{i}(z_{i})}               {i}+f_{i}(z_{\pa(i)}))\ ,
 ∂∂zj​log⁡pi​(zi∣zpa⁡(i))qi​(zi)\displaystyle\    =−ri(ni)⋅∂fi​(zpa⁡(i))∂zj,\displaystyle=
 frac{\partial}{\partial z_{j}}\log\frac{p_{i}(z_ -r_{i}(n_{i})\cdot\frac{\partial f_{i}(z_   (144)
 {i}\mid z_{\pa(i)})}{q_{i}(z_{i})}               {\pa(i)})}{\partial z_{j}}\ ,
 ∂∂zj​log⁡pj​(zj∣zpa⁡(j))qj​(zj)\displaystyle\    =rj​(nj)−r¯j​(nj+fj​(zpa⁡(j))).\
 frac{\partial}{\partial z_{j}}\log\frac{p_{j}(z_ displaystyle=r_{j}(n_{j})-\bar{r}_{j}(n_    (145)
 {j}\mid z_{\pa(j)})}{q_{j}(z_{j})}               {j}+f_{j}(z_{\pa(j)}))\ .

Next, assume the contrary and let the ratio in (12) be a constant α∈ℝ\alpha\in{\mathbb{R}}. Then,
substituting (143), (144), and (145) into (12) and rearranging the terms, we have

 (α+∂fi​(zpa⁡(i))∂zj)⋅ri​(ni)−α⋅r¯i​(ni+fi​(zpa⁡(i)))=c⋅(rj​(nj)−r¯j​(nj+fj​(zpa⁡(j)))).\
 left(\alpha+\frac{\partial f_{i}(z_{\pa(i)})}{\partial z_{j}}\right)\cdot r_{i}(n_{i})-\     (146)
 alpha\cdot\bar{r}_{i}(n_{i}+f_{i}(z_{\pa(i)}))=c\cdot\left(r_{j}(n_{j})-\bar{r}_{j}(n_{j}
 +f_{j}(z_{\pa(j)}))\right)\ .

Case 1: ∂fi​(zpa⁡(i))∂zj\frac{\partial f_{i}(z_{\pa(i)})}{\partial z_{j}} is not constant.

Consider two distinct realizations of Zpa⁡(i)∪pa⁡(j)Z_{\pa(i)\cup\pa(j)} such that ∂∂zj​fi​(zpa⁡
(i))\frac{\partial}{\partial z_{j}}f_{i}(z_{\pa(i)}) takes values of β1\beta_{1} and β2\beta_{2}
where β1≠β2\beta_{1}\neq\beta_{2}. Then, (146) implies

 (α+β1)⋅ri​(ni)−α⋅r¯i​(ni+γ1)=c⋅u1,\displaystyle(\alpha+\beta_{1})\cdot r_{i}(n_{i})-\alpha\  (147)
 cdot\bar{r}_{i}(n_{i}+\gamma_{1})=c\cdot u_{1}\ ,
 (α+β2)⋅ri​(ni)−α⋅r¯i​(ni+γ2)=c⋅u2,\displaystyle(\alpha+\beta_{2})\cdot r_{i}(n_{i})-\alpha\  (148)
 cdot\bar{r}_{i}(n_{i}+\gamma_{2})=c\cdot u_{2}\ ,

for some constants γ1,γ2,u1,u2\gamma_{1},\gamma_{2},u_{1},u_{2} for all ni∈ℝn_{i}\in{\mathbb{R}},
and β1≠β2\beta_{1}\neq\beta_{2}. By rearranging the terms, we get rid of ri​(ni)r_{i}(n_{i}) terms
and obtain

 α⋅((α+β2)⋅r¯i​(ni+γ1)−(α+β1)⋅r¯i​(ni+γ2))=c⋅((α+β1)⋅u2−(α+β2)⋅u1).\displaystyle\alpha\cdot\
 left((\alpha+\beta_{2})\cdot\bar{r}_{i}(n_{i}+\gamma_{1})-(\alpha+\beta_{1})\cdot\bar{r}_    (149)
 {i}(n_{i}+\gamma_{2})\right)=c\cdot\left((\alpha+\beta_{1})\cdot u_{2}-(\alpha+\beta_{2})\
 cdot u_{1}\right)\ .

Since β1≠β2\beta_{1}\neq\beta_{2}, (149) implies that

 r¯i​(y)=α1⋅r¯i​(y+α2)+α3,\displaystyle\bar{r}_{i}(y)=\alpha_{1}\cdot\bar{r}_{i}(y+\alpha_    (150)
 {2})+\alpha_{3}\ ,

where α1,α2,α3\alpha_{1},\alpha_{2},\alpha_{3} are constants and α1≠1\alpha_{1}\neq 1. Therefore,
if there does not exist such constants, then the ratio in (12) cannot be a constant.

Case 2: ∂fi​(zpa⁡(i))∂zj=β\frac{\partial f_{i}(z_{\pa(i)})}{\partial z_{j}}=\beta for some nonzero
constant β\beta.

In this case, (146) becomes

 (α+β)⋅ri​(ni)−α⋅r¯i​(ni+fi​(zpa⁡(i)))=c⋅(rj​(nj)−r¯j​(nj+fj​(zpa⁡(j)))).(\alpha+\beta)\cdot
 r_{i}(n_{i})-\alpha\cdot\bar{r}_{i}(n_{i}+f_{i}(z_{\pa(i)}))=c\cdot\left(r_{j}(n_{j})-\bar   (151)
 {r}_{j}(n_{j}+f_{j}(z_{\pa(j)}))\right)\ .

Note that the right-hand side of (151) does not contain nin_{i}. Also note that since fi​(zpa⁡(i))
f_{i}(z_{\pa(i)}) is continuous, there exists an open interval Θ⊆ℝ\Theta\subseteq{\mathbb{R}} such
that fi​(zpa⁡(i))f_{i}(z_{\pa(i)}) can take all values θ∈Θ\theta\in\Theta. Then, by taking the
derivative of both sides with respect to nin_{i} and varying fi​(zpa⁡(i))f_{i}(z_{\pa(i)}) in Θ\
Theta, we find that r¯i′​(ni+θ)\bar{r}_{i}^{\prime}(n_{i}+\theta) is constant for all θ∈Θ\theta\in\
Theta. Since r¯i\bar{r}_{i} is analytic, this means that r¯i​(y)=α1⋅y+α2\bar{r}_{i}(y)=\alpha_{1}\
cdot y+\alpha_{2} for some constants α1,α2\alpha_{1},\alpha_{2} for all y∈ℝy\in{\mathbb{R}}. Then,
since the noise term is invariant under intervention, we have ri=r¯ir_{i}=\bar{r}_{i}. Substituting
this into (151), we obtain

 (α+β)⋅(α​ni+α2)−α​α1⋅(ni+fi​(zpa⁡(i)))=c⋅(rj​(nj)−r¯j​(nj+fj​(zpa⁡(j)))).\displaystyle(\
 alpha+\beta)\cdot(\alpha n_{i}+\alpha_{2})-\alpha\alpha_{1}\cdot(n_{i}+f_{i}(z_{\pa(i)}))=c  (152)
 \cdot\left(r_{j}(n_{j})-\bar{r}_{j}(n_{j}+f_{j}(z_{\pa(j)}))\right)\ .

Since β≠0\beta\neq 0, the left-hand side is a function of nin_{i} whereas the right-hand side is
not, which is invalid. Hence, the ratio in (12) cannot be constant in this case.  □\square

A.8 Computational complexity of UMNI-CRL algorithm

UMNI-CRL (Algorithm 1) consists of four stages that we elaborate on as follows.

Stage 1:
   
    The score difference estimation is only performed once before the subsequent main algorithm
    steps. Since our results and the algorithm do not rely on a specific score difference
    estimation technique, studying the computational complexity of this step is out of scope.

Stage 2:
   
    In this step, we check the dimension of 𝒱\mathcal{V}, a projection of a subspace (see line 6 of
    Algorithm 1) at most n×(2​κ+1)nn\times(2\kappa+1)^{n} times. Here, κ\kappa denotes the maximum
    possible determinant of a matrix in {0,1}(n−1)×(n−1)\{0,1\}^{(n-1)\times(n-1)}. In the proof of
    Lemma 2 in Appendix A.1, we discuss why this choice facilitates the identifiability guarantees.

Stage 3:
   
    In this step, we check the dimension of 𝒱\mathcal{V} (see line 23 of Algorithm 1) at most
    ∥𝐖:,j∥1×∥𝐖:,t∥1\|\mathbf{W}_{:,j}\|_{1}\times\|\mathbf{W}_{:,t}\|_{1} times for every (t,j)
    (t,j) in Stage 3. Note that 𝐖\mathbf{W} is updated within the steps of Stage 3. Therefore, the
    exact computational complexity would be a function of the graph structure and the outputs of
    Stage 2.

Stage 4:
   
    Unmixing procedure for hard interventions essentially operates as a post-processing step that
    does not pose additional computational challenges. For instance, the total number of
    conditional independence tests in Stage 4 is 𝒪⁡(n2)\mathcal{O}(n^{2}).

Bounds on κ\kappa.

In Section 4, we defined κ\kappa as the maximum determinant of a binary matrix {0,1}(n−1)×(n−1)\
{0,1\}^{(n-1)\times(n-1)} and noted that the computational complexity of UMNI-CRL algorithm depends
on nn and κ\kappa as seen in Stage 2 above. In general, using the well-known bound for the
determinant of {0,1}\{0,1\} matrices [32], we have

 κ≤⌊2​(n/4)n⌋.|[adj⁡(𝐃)]i,j|≤nn/22n−1=2​(n4)n.\kappa\leq\lfloor 2(n/4)^{n}\rfloor\ .\Big|\
 big[\operatorname{adj}(\mathbf{D})\big]_{i,j}\Big|\leq\frac{n^{n/2}}{2^{n-1}}=2\left(\frac   (153)
 {n}{4}\right)^{n}\ .

However, for specific cases, we have much smaller upper bounds for κ\kappa. For instance, for the
choices of nn being 2,3,4,5,6,72,3,4,5,6,7, κ\kappa is known to be upper bounded by
1,1,2,3,5,91,1,2,3,5,9, respectively [32]. Furthermore, [33] shows that the determinant of a matrix
in {0,1}n×n\{0,1\}^{n\times n} with n+kn+k nonzero entries is bounded by 2k/32^{k/3}. Hence, if 𝐃\
mathbf{D} has n+kn+k nonzero entries, then

 κ≤⌊2k/3⌋.\kappa\leq\lfloor 2^{k/3}\rfloor\ .  (154)

This implies that κ\kappa can be quite small for sparse UMN interventions. For instance, if we take
the exhaustive set of SN interventions and only add two additional intervened nodes in total, then
we have κ=1\kappa=1.

Empirical tricks.

Finally, we note that various empirical tricks can be used to reduce the algorithm’s computational
complexity. For instance, after every update to 𝐖\mathbf{W}, we can divide the columns of 𝐖\mathbf
{W} by the greatest common divisor of its entries. Furthermore, even though κ\kappa can grow
quickly as nn becomes larger, in our experiments with up to n=8n=8 nodes, we observe that setting κ
=2\kappa=2 usually suffices for a good performance. Hence, we set κ=2\kappa=2 in the simulations in
Section 5.

Appendix B Additional simulations

Details of evaluation metrics.

For the graph recovery via hard interventions, we use conditional independence tests in Stage 4 of
Algorithm 1. Since we adopt a linear Gaussian SEM latent model, we use a partial correlation test
and set the significance level to α=0.05\alpha=0.05. For the latent variable recovery error metrics
ℓ𝗁𝖺𝗋𝖽\ell_{\sf hard} and ℓ𝗌𝗈𝖿𝗍\ell_{\sf soft} defined in (19), we first pass the entries of 𝐇∗⋅𝐆\
mathbf{H}^{*}\cdot\mathbf{G} through a threshold of 0.10.1, then compute the incorrect mixing
metrics.

B.1 Simulations with nonlinear latent causal models and sensitivity to noisy scores


(a) Graph recovery


(b) Latent variable recovery


Figure 1: Sensitivity analysis of UMNI-CRL algorithm for quadratic latent causal models. The
results are for n=5n=5 latent nodes and d=20d=20 observed variables, 10410^{4} samples, and for
average of 100 runs. (a): SHD⁡(𝒢tc,𝒢^){\rm SHD}(\mathcal{G}_{\rm tc},\hat{\mathcal{G}}) versus SNR
(soft) and SHD⁡(𝒢,𝒢^){\rm SHD}(\mathcal{G},\hat{\mathcal{G}}) versus SNR (hard). (b): Incorrect
mixing ratio ℓsoft\ell_{\rm soft} versus SNR (soft) and ℓhard\ell_{\rm hard} versus SNR (hard).

In Section 5, we have adopted linear Gaussian SEMs as latent causal models for which the score
estimation can be done via estimating the precision matrices of the observed variables. In this
section, we perform additional simulations to study nonlinear latent causal models to investigate
the relation between the algorithm’s performance and the accuracy of the score function estimates.
To this end, we adopt a quadratic latent causal model under varying amounts of noise in score
functions. Specifically, we follow the experimental setup in [7] and adopt a quadratic latent
causal model with additive noise as follows

 Zi=Zpa⁡(i)⊤⋅𝐀i⋅Zpa⁡(i)+Ni,\displaystyle Z_{i}=\sqrt{Z_{\pa(i)}^{\top}\cdot\mathbf{A}_{i}\    (155)
 cdot Z_{\pa(i)}}+N_{i}\ ,

where {𝐀i:i∈[n]}\{\mathbf{A}_{i}:i\in[n]\} are positive-definite matrices, and the noise terms are
zero-mean Gaussian variables with variances σi2\sigma_{i}^{2} sampled randomly from Unif⁡
([0.5,1.5]){\rm Unif}([0.5,1.5]). For an intervention on node ii, ZiZ_{i} is set to Ni/2N_{i}/2.
This causal model admits a closed-form score function (see [7, Appendix E.2] for details), which
enables us to obtain a score oracle. In our MN intervention setup, we use this score oracle and
introduce varying levels of artificial noise according to

 s^X​(x,σ2)=sX​(x)⋅(1+Ξ),whereΞ∼𝒩⁡(0,σ2⋅𝐈d×d)\hat{s}_{X}(x;\sigma^{2})=s_{X}(x)\cdot\big(1+\  (156)
 Xi\big)\ ,\quad\mbox{where}\quad\Xi\sim\mathcal{N}(0,\sigma^{2}\cdot\mathbf{I}_{d\times d})

to test the behavior of our algorithm under different noise regimes σ∈[10−3,10−1.5]\sigma\in[10^
{-3},10^{-1.5}]. Figure 1 demonstrates the results for the latent graph recovery and latent
variable recovery.

Experimental support, please view the build logs for errors. Generated by L A T E xml [LOGO] .

Instructions for reporting errors

We are continuing to improve HTML versions of papers, and your feedback helps enhance accessibility
and mobile support. To report errors in the HTML that will help us improve conversion and
rendering, choose any of the methods listed below:

  • Click the "Report Issue" ( ) button, located in the page header.

Tip: You can select the relevant text first, to include it in your report.

Our team has already identified the following issues. We appreciate your time reviewing and
reporting rendering errors we may not have found yet. Your efforts will help us improve the HTML
versions for all readers, because disability should not be a barrier to accessing research. Thank
you for your continued support in championing open access for all.

Have a free development cycle? Help support accessibility at arXiv! Our collaborators at LaTeXML
maintain a list of packages that need conversion, and welcome developer contributions.

We gratefully acknowledge support from our major funders, member institutions, , and all
contributors.
About · Help · Contact · Subscribe · Copyright · Privacy · Accessibility · Operational Status
(opens in new tab)
Major funding support from
Simons Foundation Simons Foundation International Schmidt Sciences
 
