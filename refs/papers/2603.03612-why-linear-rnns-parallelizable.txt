source: https://arxiv.org/html/2603.03612
arxiv: https://arxiv.org/abs/2603.03612
note: HTML dump via w3m; not a PDF.
========================================================================

Report GitHub Issue

×
Title: [                    ]

Content selection saved. Describe the issue below:

Description: [                    ]
Submit without GitHub Submit in GitHub
[smileybone] arXiv is now an independent nonprofit! Learn more ×
arXiv logo Back to arXiv
Why HTML? Report Issue Back to Abstract Download PDF   

 1. Abstract
 2. 1 Introduction
 3. 2 Preliminaries
     1. 2.1 Datatypes
     2. 2.2 Nonlinear RNNs
     3. 2.3 Linear RNNs
     4. 2.4 Multilayer RNNs
         1. Precision.
     5. 2.5 Circuit Complexity
 4. 3 Parallelizability Limits of Nonlinear RNNs
     1. 3.1 Poly-Precision Nonlinear RNNs Are 𝖯\mathsf{P}-Complete
     2. 3.2 Log-Precision Nonlinear RNNs Are 𝖫\mathsf{L}-Complete
 5. 4 Simulating LRNNs with Parallel Circuits
 6. 5 Expressivity Differences Between LRNNs
     1. 5.1 DPLR LRNNs are 𝖯𝖭𝖢1\mathsf{PNC}^{1}-Complete
     2. 5.2 PD LRNNs are 𝖭𝖢1\mathsf{NC}^{1}-Complete
 7. 6 Experiments
     1. Experimental Setup.
     2. Sorted Deterministic Graph Connectivity.
     3. Iterated Matrix Multiplication.
 8. 7 Conclusion
 9. References
10. A Nonlinear RNNs
     1. A.1 Polynomial Precision
     2. A.2 Logarithmic Precision
11. B Lower Bounds for DPLR LRNNS
     1. B.1 Preliminaries
     2. B.2 Simulating WFAs with RWKV-7
         1. B.2.1 Simulation theorem
             1. Block parameters.
             2. Offline factorization of block products.
             3. Router (layers 1–3).
             4. Arithmetic layer (layer 4).
             5. Completion vector and correctness.
     3. B.3 Iterated 3×33\times 3 Matrix Multiplication with RWKV-7
         1. Step 1: reduce 3×33\times 3 matrix multiplication to a 99-dimensional linear
            recurrence.
         2. Step 2: implement x↦x​B​(A)x\mapsto xB(A) with dot-product overwrites in dimension
            1818.
         3. Step 3: align the 99 overwrites with the token stream using a 3-layer router.
         4. Step 4: arithmetic in layer 4 and correctness.
         5. Completion readout at t=T=9​Nt=T=9N.
     4. B.4 Simulating WFAs with DeltaNet
         1. B.4.1 DeltaNet memory update and the multiplicative step matrix
         2. B.4.2 Router primitives: position modulo and token buffering
             1. Notation.
             2. Computing tmod2​mt\bmod 2m.
             3. Exact token buffering by column overwrite.
             4. Buffer layout.
             5. Finite lookup.
         3. B.4.3 Arithmetic primitives realizable by multiplicative DeltaNet steps
             1. Applying an arbitrary matrix with scratch coordinates.
             2. Phase 1 (clear scratch and temp).
             3. Phase 2 (accumulate x​PxP into scratch).
             4. Phase 3 (clear main).
             5. Phase 4 (copy scratch back to main).
         4. B.4.4 Simulation theorem: a 44-layer DeltaNet simulates any WFA
             1. Offline factorization of block products.
             2. Layer 1 (parity / first-position features).
             3. Layer 2 (compute tmod2​mt\bmod 2m).
             4. Layer 3 (buffer last 2​m2m tokens and perform a finite lookup).
             5. Layer 4 (arithmetic: purely multiplicative DeltaNet simulation).
             6. Correctness via a completion vector.
             7. Readout.
     5. B.5 Iterated 3×33\times 3 Matrix Multiplication with DeltaNet
         1. Step 1: reduce right-multiplication to a 99-dimensional linear update.
         2. Step 2: choose a superblock length and step budget.
         3. Step 3: define superblock transition matrices.
         4. Step 4: offline factorization of each Πb\Pi_{b} into multiplicative DeltaNet steps.
         5. Step 5: the 4-layer DeltaNet construction (router + arithmetic).
         6. Final readout via completion vectors.
12. C Upper Bounds on PD LRNNs
13. D Single-Layer LRNNs
     1. D.1 Inexpressibility of Deterministic Graph Connectivity
     2. D.2 Single-Layer PD LRNNs and Deterministic WFAs
14. E Arithmetic Circuits over ℚ\mathbb{Q}
15. F Additional Details for Experiments
     1. Benchmark.

License: arXiv.org perpetual non-exclusive license
arXiv:2603.03612v3 [cs.LG] 01 Jun 2026

Why Are Linear RNNs More Parallelizable?

William Merrill Affiliation: Allen Institute for AI Correspondence to: willm@allenai.org    
Hongjian Jiang Affiliation: Rheinland-Pfälzische Technische Universität    Yanhong Li Affiliation: 
Allen Institute for AI    Anthony Lin Affiliation: Rheinland-Pfälzische Technische Universität 
Affiliation: Max-Planck Institute for Software Systems    Ashish Sabharwal Affiliation: Allen
Institute for AI

Abstract

The community is increasingly exploring linear RNNs (LRNNs) as language models, motivated by their
expressive power and parallelizability. While prior work establishes the expressivity benefits of
LRNNs over transformers, it is unclear what makes LRNNs—but not traditional, nonlinear RNNs—as easy
to parallelize in practice as transformers. We answer this question by providing a tight connection
between types of RNNs and standard complexity classes. We show that LRNNs can be viewed as
log-depth (bounded fan-in) arithmetic circuits, which represents only a slight depth overhead
relative to log-depth boolean circuits that transformers admit. Furthermore, we show that nonlinear
RNNs can solve 𝖫\mathsf{L}-complete problems (and even 𝖯\mathsf{P}-complete ones, under polynomial
precision), revealing a fundamental barrier to parallelizing them as efficiently as transformers.
Our theory also identifies fine-grained expressivity differences between recent popular LRNN
variants: permutation-diagonal LRNNs are 𝖭𝖢1\mathsf{NC}^{1}-complete whereas diagonal-plus-low-rank
LRNNs are more expressive (𝖯𝖭𝖢1\mathsf{PNC}^{1}-complete). We provide further insight by
associating each type of RNN with a corresponding automata-theoretic model that it can simulate.
Together, our results reveal fundamental tradeoffs between nonlinear RNNs and different variants of
LRNNs, providing a foundation for designing LLM architectures that achieve an optimal balance
between expressivity and parallelism.

Keywords: 

Machine Learning, ICML

1 Introduction

Parallelism and expressive power are both desirable properties in an LLM architecture that are,
unfortunately, at odds (Merrill and Sabharwal, 2023). RNN architecture design reflects this
tradeoff: while older RNNs were nonlinear and highly sequential (Elman, 1990; Hochreiter and
Schmidhuber, 1997), more recent RNN architectures prefer a linear state update (Bradbury et al.,
2017; Katharopoulos et al., 2020; Gu and Dao, 2024, inter alia) to enable parallel processing of
long sequences (Blelloch, 1990). Linear RNNs (LRNNs) have been shown to be surprisingly
theoretically expressive relative to transformers (Merrill et al., 2024; Grazzi et al., 2025) and
practically performant (Beck et al., 2024; Yang et al., 2025; Peng et al., 2025), but it remains
unclear how LRNNs and nonlinear RNNs compare in expressive power, or, conversely, whether
fundamental barriers prevent parallelizing nonlinear RNNs to the same degree as LRNNs.

In this work, we theoretically compare the parallelizability and expressive power of nonlinear RNNs
and LRNNs through the lens of computational complexity theory. A core result is a conditional
separation in expressive power between the two classes: whereas LRNNs can only solve tasks that are
in the circuit complexity class 𝖯𝖭𝖢1\mathsf{PNC}^{1} (Theorem 3), nonlinear RNNs can express 𝖯\
mathsf{P}-complete problems with polynomial precision (Corollary 2) or 𝖫\mathsf{L}-complete
problems when restricted to log precision (Theorem 2). In either regime, this suggests nonlinear
RNNs can represent substantially less parallelizable computation that’s beyond the capabilities of
LRNNs, assuming 𝖯𝖭𝖢1≠𝖯\mathsf{PNC}^{1}\neq\mathsf{P} with poly precision, or 𝖯𝖭𝖢1≠𝖫\mathsf{PNC}^{1}
\neq\mathsf{L} with log precision.

Conversely, these expressivity results have implications for the parallelizability of different
types of RNNs. It follows that LRNNs—regardless of precision—can be simulated by 𝖭𝖢\mathsf{NC}
circuits of depth O⁡(log⁡n​log∗​n)O(\log n\log^{*}n), i.e., with a negligible depth overhead of O⁡
(log∗⁡n)O(\log^{*}n) beyond the wall-clock time we would expect for simulating a transformer. In
contrast, our results imply that nonlinear RNNs with log precision likely require Ω⁡(log2⁡n)\Omega
(\log^{2}n) depth, constituting a O⁡(log⁡n)O(\log n) overhead relative to a transformer. Moreover,
with polynomial precision, nonlinear RNNs require more than polylog depth to simulate with bounded
fan-in circuits, assuming 𝖭𝖢≠𝖯\mathsf{NC}\neq\mathsf{P}.

𝖳𝖢0\mathsf{TC}^{0}Ungated / Diag.(S4, Mamba)𝖭𝖢1\mathsf{NC}^{1}PD LRNN(PD-SSM)𝖯𝖭𝖢1\mathsf{PNC}^{1}
DPLR LRNN(DeltaNet, RWKV-7)𝖫\mathsf{L}Nonlinear RNNwith log precision𝖯\mathsf{P}Nonlinear RNN
Complexity class:RNN model class:Specific models:𝖭𝖢\mathsf{NC} circuit depth (upper bound):log⁡(n)\
log(n)log⁡(n)\log(n)log⁡(n)​log∗⁡(n)\log(n)\log^{*}(n)log2⁡(n)\log^{2}(n)beyond polylog⁡(n)\mathrm
{polylog}(n)Automaton class (lower bound):—DWFAWFACounter machineTuring machine

Figure 1: Main results, summarized as a hierarchy of increasingly expressive RNN classes with
popular models within each class. Each RNN shown is “tight” for the respective complexity class 𝖢\
mathsf{C} (𝖯𝖭𝖢1\mathsf{PNC}^{1}, 𝖫\mathsf{L}, etc.) in the sense that both falls in 𝖢\mathsf{C} and
can solve a 𝖢\mathsf{C}-complete problem. LRNNs are in 𝖯𝖭𝖢1\mathsf{PNC}^{1}, implying they can be
nearly as efficiently parallelized as transformers, incurring only a small O⁡(log∗⁡(n))O(\log^{*}
(n)) depth overhead in terms of bounded fan-in boolean circuits. Nonlinear RNNs, in contrast, can
solve 𝖫\mathsf{L}-complete and even 𝖯\mathsf{P}-complete problems, but this comes at the cost of
being less parallelizable, requiring notably deeper circuits. Bottom row lists the classes of
automata that each RNN model class can simulate, where WFA stands for weighted finite automaton and
DWFA stands for deterministic WFA.

Beyond comparing nonlinear RNNs and LRNNs, we also compare the fine-grained expressivity of
different LRNN variants within 𝖯𝖭𝖢1\mathsf{PNC}^{1}. In particular, we consider differences between
several different parameterizations that can express 𝖭𝖢1\mathsf{NC}^{1}-complete problems, showing
that diagonal-plus-low-rank (DPLR) LRNNs like DeltaNet (Yang et al., 2024; Grazzi et al., 2025) and
RWKV-7 (Peng et al., 2025) can express 𝖯𝖭𝖢1\mathsf{PNC}^{1}-complete problems, whereas
permutation-diagonal (PD) LRNNs (Terzic et al., 2025) are in 𝖭𝖢1\mathsf{NC}^{1}. These results,
combined with our other results about nonlinear RNNs, yield a comprehensive hierarchy of the
expressivity landscape for RNNs that is summarized in Figure 1.

Beyond our complexity characterization, we also associate each RNN and LRNN class with a
corresponding automata-theoretic model from prior work (Peng et al., 2018; Weiss et al., 2018;
Merrill et al., 2020) that this class can simulate.

Finally, we find empirically that our results ^1^1 1 Code link: https://
arg-git.informatik.uni-kl.de/pub/LinearRNN about RNNs’ expressivity predict their behavior when
trained on synthetic tasks. On an 𝖫\mathsf{L}-complete graph connectivity task, only nonlinear RNNs
achieve good performance, as theory predicts. In addition, both nonlinear RNNs and DPLR LRNNs learn
iterated matrix multiplication problem (𝖯𝖭𝖢1\mathsf{PNC}^{1}-complete), but transformers and Mamba
cannot, in line with theory. These tasks provide potential synthetic benchmarks for LRNN
architecture development beyond the traditional state tracking and recall tasks that are currently
popular (Arora et al., 2024; Beck et al., 2024; Peng et al., 2025, e.g.,).

Overall, our theoretical results reveal the fundamental tradeoffs in expressivity and parallelism
between linear and nonlinear RNNs. Moreover, we show fine-grained differences in expressivity
between LRNNs. Taken together, we hope our results can help the community continue to refine LRNN
architectures to push the frontier between expressivity and parallelism as much as possible up to
fundamental barriers.

2 Preliminaries

We first introduce the datatypes we use to model bounded-precision arithmetic, as well as various
LRNN architectures. We then turn to introducing computational models from circuit complexity and
and formal language theory that will be relevant in our analysis of LRNNs.

2.1 Datatypes

Analyzing LLM architectures via circuit complexity requires making assumptions about the underlying
datatype. Prior work has considered various options while trying to ensure nice numerical
properties like associativity and commutativity (Merrill et al., 2024). In this work, guided by the
theory of weighted finite automata, we make these desiderata more explicit by asserting that the
numeric datatype should be a semiring, denoted ⟨𝕂,⊕,⊗⟩\langle\mathbb{K},\oplus,\otimes\rangle. That
is, addition and multiplication should be associative with identities, addition should commute, and
multiplication should distribute over addition This makes the output of LRNNs more cleanly defined
in an implementation-agnostic way, and also makes WFA and matrix multiplication computations
well-defined for these datatypes (which will be important).

In particular, we will work with the standard rational semiring used in prior analysis, (i.e.,
polynomial-precision rational numbers; Chiang, 2025), though other semirings are possible in
principle.

Definition 1.

Rational numbers, ℚ\mathbb{Q}, form the rational semiring with the standard arithmetic semantics
for ⊕\oplus and ⊗\otimes.

We assume ℚ\mathbb{Q} admits an order >> in the standard way. Additionally, we equip ℚ\mathbb{Q}
with division in order to capture layer normalization Finally, we define non-linear functions ⋅\
sqrt{\cdot} and exp\exp via their Taylor series approximation in terms of ⊕\oplus and ⊗\otimes to
O⁡(poly⁡(n))O(\mathrm{poly}(n)) terms (Chiang, 2025, cf.). Each of these operations can be given
poly- or log-precision variants, which return an (nc)(n^{c})- or (c​log⁡n)(c\log n)-bit
approximation using this technique, respectively (see Definition 10 ).

A natural notion for describing “simple” arithmetic functions will be the following:

Definition 2 (Arithmetic Closed Form).

f:𝕂k→𝕂f:\mathbb{K}^{k}\to\mathbb{K} has an arithmetic closed form if it can be written as a finite
composition of ⊕,⊗,max,exp\oplus,\otimes,\max,\exp, and ⋅\sqrt{\cdot} as defined for 𝕂\mathbb{K}.

The following property about closed forms will be useful:

Lemma 1 (Chiang, 2025).

If f:ℚk→ℚf:\mathbb{Q}^{k}\to\mathbb{Q} has an arithmetic closed form, then ff can be computed in 𝖥𝖮
\mathsf{FO}-uniform 𝖳𝖢0\mathsf{TC}^{0}.

2.2 Nonlinear RNNs

An RNN over a datatype 𝕂\mathbb{K} (generally ℚ\mathbb{Q}) is a neural network component that takes
as input a sequence of vectors x1,…,xn∈𝕂dx_{1},\ldots,x_{n}\in\mathbb{K}^{d} and outputs a new
sequence y1,…,yn∈𝕂dy_{1},\ldots,y_{n}\in\mathbb{K}^{d} that sequentially mixes them. Classical RNNs
(Elman, 1990; Hochreiter and Schmidhuber, 1997) were defined by updating a recurrent state vector
via the application of some (generally nonlinear) function:

Definition 3 (Nonlinear RNN).

Given 𝐱1,…,𝐱n∈𝕂d\mathbf{x}_{1},\ldots,\mathbf{x}_{n}\in\mathbb{K}^{d}, an RNN computes a a sequence
of recurrent states 𝐡1,…,𝐡n∈𝕂d\mathbf{h}_{1},\ldots,\mathbf{h}_{n}\in\mathbb{K}^{d} via 𝐡t=f⁡
(𝐡t−1,𝐱t)\mathbf{h}_{t}=f(\mathbf{h}_{t-1},\mathbf{x}_{t}), where ff is a closed-form arithmetic
function (cf. Definition 2).

We consider two nonlinear RNN parameterizations. First, ReLU RNNs are nonlinear RNNs with f⁡
(𝐡t−1,𝐱t)=max⁡{𝐀𝐡t−1+𝐁𝐱t,0}f(\mathbf{h}_{t-1},\mathbf{x}_{t})=\max\{\mathbf{A}\mathbf{h}_{t-1}+\
mathbf{B}\mathbf{x}_{t},0\}, where 𝐀\mathbf{A} and 𝐁\mathbf{B} are matrices 𝕂d×d\mathbb{K}^{d\times
d}. More generally, we will sometimes consider the more general MLP RNN where ff can be a
multi-layer feedforward network using ReLU activations.

2.3 Linear RNNs

Going beyond classical RNNs, there has recently been interest in RNNs with linear state updates due
to the advantages for parallelizing on the sequence dimension. These linear RNNs (LRNNs) can be
defined as follows:

Definition 4 (LRNN).

Given 𝐱1,…,𝐱n∈𝕂d\mathbf{x}_{1},\ldots,\mathbf{x}_{n}\in\mathbb{K}^{d}, an LRNN head computes a
sequence of matrix valued states 𝐒1,…,𝐒n∈𝕂d×d\mathbf{S}_{1},\ldots,\mathbf{S}_{n}\in\mathbb{K}^{d\
times d} via

 𝐒t=At​(𝐱t)​𝐒t−1+bt​(𝐱t),\mathbf{S}_{t}=A_{t}(\mathbf{x}_{t})\mathbf{S}_{t-1}+b_{t}(\mathbf{x}_
 {t}),

where At​(𝐱t)∈𝕂d×dA_{t}(\mathbf{x}_{t})\in\mathbb{K}^{d\times d} and bt​(𝐱t)∈𝕂db_{t}(\mathbf{x}_
{t})\in\mathbb{K}^{d} have arithmetic closed forms independent of tt.

Definition 4 leaves open the specific parameterization of AtA_{t} and btb_{t} as a function of 𝐱t\
mathbf{x}_{t}. The choice of parameterization is crucial for theoretical expressivity (Merrill et
al., 2024) as well practical concerns like memory. For instance, constrained parameterizations
(time-invariant or diagonal) were explored in early LRNNs, but these were shown to limit
expressivity to 𝖳𝖢0\mathsf{TC}^{0} (Merrill et al., 2024). Since then, newer LRNNs have adopted
richer parameterizations that are non-diagonal but still efficient. Two broad classes of ‘‘slightly
non-diagonal’’ parameterizations are diagonal-plus-low-rank (DPLR) and permutation-diagonal (PD)
LRNNs.^2^2 2 Henceforth, for brevity, we will write 𝐀t\mathbf{A}_{t}, 𝐤t\mathbf{k}_{t}, etc. to
mean At​(𝐱t)A_{t}(\mathbf{x}_{t}), kt​(𝐱t)k_{t}(\mathbf{x}_{t}), etc., leaving the dependence on 𝐱t
\mathbf{x}_{t} implicit.

Definition 5 (DPLR).

An LRNN is DPLR if it satisfies 𝐀t=𝐃t−𝐤t⊤​𝐯t\mathbf{A}_{t}=\mathbf{D}_{t}-\mathbf{k}_{t}^{\top}\
mathbf{v}_{t}, where 𝐃t\mathbf{D}_{t} is diagonal, 𝐤t,𝐯t\mathbf{k}_{t},\mathbf{v}_{t} are vectors,
and all have an arithmetic closed form in terms of 𝐱t\mathbf{x}_{t}.

In particular, we consider specific DPLR variants:

  • •
   
    RWKV-7 (Peng et al., 2025):

     𝐀t\          =diag⁡(𝐰t)−λt​κt​(𝐚t⊙κt)⊤\displaystyle=\    𝐛t\          =𝐯t​𝐤~t⊤,\displaystyle=
     displaystyle diag(\mathbf{w}_{t})-\lambda_{t}\kappa_{t}  displaystyle \mathbf{v}_{t}\tilde{\
     \mathbf{A}_  (\mathbf{a}_{t}\odot\mathbf{\kappa}_{t})^{\ \ \mathbf{b} mathbf{k}}_{t}^{\top},
     {t}          top}                                        _{t}

    for per-token vectors 𝐰t,𝐚t,κt,𝐯t,λt,𝐤~t∈𝕂d\mathbf{w}_{t},\mathbf{a}_{t},\kappa_{t},\mathbf{v}_
    {t},\lambda_{t},\tilde{\mathbf{k}}_{t}\in\mathbb{K}^{d} are produced from 𝐱t\mathbf{x}_{t} via
    linear projection.

  • •
   
    DeltaNet (Yang et al., 2024):

     𝐀t\           =𝐈−βt​𝐤t⊤​𝐤t\displaystyle=\mathbf{I}- 𝐛t\            =𝐯t​𝐤t⊤,\displaystyle=\
     displaystyle\ \beta_{t}\mathbf{k}_{t}^{\top}\mathbf displaystyle\  mathbf{v}_{t}\mathbf{k}_
     mathbf{A}_{t} {k}_{t}                               \mathbf{b}_{t} {t}^{\top},

    where 𝐤t,𝐯t,βt\mathbf{k}_{t},\mathbf{v}_{t},\beta_{t} depend on 𝐱t\mathbf{x}_{t} via
    projection.

A key difference between RWKV-7 and DeltaNet is that the state update is symmetric in DeltaNet.
Related to this, Grazzi et al. (2025) show how, for fixed-precision DeltaNet, the range of βt\beta_
{t} is crucial for expressivity: restricting βt∈(0,1)\beta_{t}\in(0,1) prevents recognizes parity,
but βt∈(0,2)\beta_{t}\in(0,2) allows it. Both architectures have recently been trained at large
scale (Peng et al., 2025; Kimi Team, 2025).

Definition 6 (PD; Terzic et al., 2025).

An LRNN is PD if it satisfies 𝐀t=𝐏t​𝐃t\mathbf{A}_{t}=\mathbf{P}_{t}\mathbf{D}_{t} for a column
one-hot 0-1 matrix 𝐏t\mathbf{P}_{t} and a diagonal matrix 𝐃t\mathbf{D}_{t}, both with an arithmetic
closed form in terms of 𝐱t\mathbf{x}_{t}.

𝐏t\mathbf{P}_{t} represents a function π:{1,2,…,d}→{1,2,…,d}\pi:\{1,2,\ldots,d\}\to\{1,2,\ldots,d
\}. Despite the name, 𝐏t\mathbf{P}_{t} need not represent a permutation matrix, though it will be a
permutation when 𝐏t\mathbf{P}_{t} is full rank.

2.4 Multilayer RNNs

In line with current practice (Yang et al., 2025), we will imagine that these RNN components (
Sections 2.2 and 2.3, respectively) are used as “heads” within a larger architecture resembling the
transformer. A head is computed by passing 𝐱1,…,𝐱n\mathbf{x}_{1},\ldots,\mathbf{x}_{n} to each
head; the specific parameterizations mentioned above will compute quantities resembling keys and
values in the transformer. After this, the head output 𝐲t\mathbf{y}_{t} is determined as to be 𝐡t\
mathbf{h}_{t} in a nonlinear RNN and 𝐪t⊤​𝐒t−1\mathbf{q}_{t}^{\top}\mathbf{S}_{t-1} in an LRNN,
where 𝐪t=𝐐𝐱t\mathbf{q}_{t}=\mathbf{Q}\mathbf{x}_{t} is analogous to a query.

The overall architecture of a multilayer RNN then works as follows. The first layer maps each token
to an embedding. This is followed by alternating LRNN sublayers, which mix information across
tokens, and feedforward sublayers, which apply local computation at each token. More formally,
these sublayers have the form:

Definition 7 (Multihead RNN Sublayer).

Given 𝐱1,…,𝐱n∈𝕂d\mathbf{x}_{1},\ldots,\mathbf{x}_{n}\in\mathbb{K}^{d}, we first apply layer-norm,
compute heads 𝐲1,…,𝐲h\mathbf{y}_{1},\ldots,\mathbf{y}_{h} in parallel, and then aggregate the
outputs. Formally, the sublayer computes the mapping 𝐱t↦𝐱t+𝐎⋅⟨𝐲1,…,𝐲h⟩\mathbf{x}_{t}\mapsto\mathbf
{x}_{t}+\mathbf{O}\cdot\langle\mathbf{y}_{1},\ldots,\mathbf{y}_{h}\rangle.

Definition 8 (Feedforward Sublayer).

Given 𝐱1,…,𝐱n∈𝕂d\mathbf{x}_{1},\ldots,\mathbf{x}_{n}\in\mathbb{K}^{d}, we first apply layer-norm
and then apply a standard two-layer ReLU network. Letting 𝐱~=lnorm⁡(𝐱)\tilde{\mathbf{x}}=\mathrm
{lnorm}(\mathbf{x}), the feedforward sublayer computes 𝐱t↦𝐱t+𝐖⋅ReLU⁡(𝐔​𝐱~t)\mathbf{x}_{t}\mapsto\
mathbf{x}_{t}+\mathbf{W}\cdot\mathrm{ReLU}(\mathbf{U}\tilde{\mathbf{x}}_{t}).

A full RNN network consists of serial composition of multihead RNN and feedforward sublayers.

Definition 9 (Language Recognition).

Given a datatype 𝕂\mathbb{K}, we define the language recognized by an RNN as follows. Let $∈Σ\$\in\
Sigma be a beginning-of-sequence symbol. For every string w∈Σnw\in\Sigma^{n}, we pass $w\$w through
the RNN and apply a linear transformation 𝐨\mathbf{o} to the final layer output 𝐲nℓ\mathbf{y}^{\
ell}_{n} at the last token and accept if and only if 𝐨⊤​𝐲nℓ>0\mathbf{o}^{\top}\mathbf{y}^{\ell}_
{n}>0.

Precision.

Let f:Σ∗→𝕂df:\Sigma^{*}\to\mathbb{K}^{d} be a representation in a neural sequence model (e.g., a
transformer or RNN). We can define the precision of this representation as follows:

Definition 10.

Let encode\mathrm{encode} be the function that serializes each value in a vector 𝐱∈𝕂d\mathbf{x}\in\
mathbb{K}^{d} in binary. The precision of 𝐡:Σ∗→𝕂d\mathbf{h}:\Sigma^{*}\to\mathbb{K}^{d} is p𝐡​(n)=
maxw∈Σn⁡|encode⁡(𝐡⁡(w))|p_{\mathbf{h}}(n)=\max_{w\in\Sigma^{n}}\;\lvert\mathrm{encode}(\mathbf{h}
(w))\rvert.

We define the precision of an RNN as the maximum precision over all components in its computation
graph. By definition, all RNN over ℚ\mathbb{Q} have a poly-size computation graph and hence at most
poly-precision, i.e., O⁡(nc)O(n^{c}) for some cc. Some RNNs attain log precision, i.e., satisfy O⁡
(log⁡n)O(\log n). When talking about log-precision networks, we will assume that log-precision
variants of exp\exp and ⋅\sqrt{\cdot} are used in the computation graph, so that extra precision is
not added to intermediate values needlessly.

2.5 Circuit Complexity

We will leverage circuit complexity (Vollmer, 1999) to formalize the tradeoff between
parallelizability and expressivity of RNNs. We first recall standard circuit complexity classes. A
circuit is a directed acyclic graph where leaf nodes represent input variables and internal nodes
represent logic gates (e.g., AND, OR, NOT) applied to their children; a circuit CnC_{n} define a
boolean function {0,1}n→{0,1}\{0,1\}^{n}\to\{0,1\}, or, more generally, a function Σn→{0,1}\Sigma^
{n}\to\{0,1\} for some finite vocabulary Σ\Sigma, in the standard way. A circuit family 𝒞={Cn}n=0∞\
mathcal{C}=\{C_{n}\}_{n=0}^{\infty} is then a collection of circuits indexed by input length that
defines a formal language L⊆Σ∗L\subseteq\Sigma^{*} where w∈Lw\in L if and only if C|w|​(w)=1C_{\
lvert w\rvert}(w)=1. 𝖭𝖢d\mathsf{NC}^{d} is the class of languages recognized by bounded-fan-in
boolean circuits of polynomial size and depth O⁡(logd⁡n)O(\log^{d}n). Write 𝖭𝖢\mathsf{NC} for
⋃d≥0𝖭𝖢d\bigcup_{d\geq 0}\mathsf{NC}^{d}. 𝖠𝖢d\mathsf{AC}^{d} is the extension of 𝖭𝖢d\mathsf{NC}^{d}
that allows unbounded fan in, and 𝖳𝖢d\mathsf{TC}^{d} extends 𝖠𝖢d\mathsf{AC}^{d} by allowing
majority (MAJ) gates in addition to AND, OR, and NOT.

In general, circuit families are a non-uniform model of computation: since there is a different
circuit for each input length, there is no finite description of the overall computation. To
rectify this issue, we can define uniform variants of circuit classes where the structure of CnC_
{n} is determined as as a function of nn via some kind of computable procedure. There are different
natural variants of circuit classes with different levels of uniformity; unless otherwise
specified, we work with first-order (𝖥𝖮\mathsf{FO}) uniformity. See Strobl et al. (2024) for more
background on these classes.

We will focus on the bounded fan-in classes, which are more aligned with real hardware. The
expressivity of transformers (Hao et al., 2022; Merrill and Sabharwal, 2023) and simple LRNNs like
Mamba (Merrill et al., 2024) is upper bounded by 𝖳𝖢0\mathsf{TC}^{0}, which is an unbounded fan-in
class but known to be contained in the log-depth bounded fan-in class 𝖭𝖢1\mathsf{NC}^{1}. This
latter class is efficiently parallelizable in the sense that problems in it can be solved by an
appropriate hardware implementation in time proportional to the depth of the corresponding
circuits, i.e., in logarithmic wall-clock time. With today’s LLMs often trained for context lengths
of 64K to 1M tokens, log depth is proportional to 16 to 20 whereas the log-squared depth of 𝖭𝖢2\
mathsf{NC}^{2} circuits is proportional to 256 to 400. Thus, log-squared depth increases sequential
runtime by a factor of 16 to 20 relative to log depth, rendering the resulting computation
significantly less parallelizable. We thus treat log-depth as the boundary of efficient
parallelization.

A more liberal notion of efficient parallelizability allows for arithmetic circuits instead of
boolean ones. For our purpose, an arithmetic circuit is defined with respect to a ring
(R,⊕,⊖,⊗,0,1)(R,\oplus,\ominus,\otimes,0,1). In particular, it uses gates that correspond to
⊕,⊖,⊗:R×R→R\oplus,\ominus,\otimes:R\times R\to R. Such a circuit gives rise to a function f:Σn→Rf:\
Sigma^{n}\to R, where nn is the number of input nodes.

Central to our results will be the class 𝖯𝖭𝖢1\mathsf{PNC}^{1}, which corresponds to the class of
languages LL for which there is a family 𝒞1={Cn}n≥0\mathcal{C}_{1}=\{C_{n}\}_{n\geq 0} of log-depth
arithmetic circuits with CnC_{n} represents a function with nn input nodes such that, for all w∈Σnw
\in\Sigma^{n}, we have w∈Lw\in L iff Cn​(w)>0C_{n}(w)>0 (positivity check). If we replace the
positivity check Cn​(w)>0C_{n}(w)>0 with the zero or equality check Cn​(w)=0C_{n}(w)=0, we obtain
the class 𝖤𝖭𝖢1\mathsf{ENC}^{1}.^3^3 3 While this class is often denoted as 𝖢=​𝖭𝖢1\mathsf{C}_{=}\
mathsf{NC}^{1}, we use the simplified notation 𝖤𝖭𝖢1\mathsf{ENC}^{1} for succinctness. The following
relations are known between these classes (assuming some level of uniformity for the circuit
classes when compared with 𝖫\mathsf{L} or 𝖯\mathsf{P}):

 𝖳𝖢0\displaystyle\    ⊆𝖭𝖢1⊆𝖤𝖭𝖢1⊆𝖯𝖭𝖢1⊆𝖫\displaystyle\subseteq\mathsf{NC}^{1}\subseteq\mathsf{ENC}^
 mathsf{TC}^{0}       {1}\subseteq\mathsf{PNC}^{1}\subseteq\mathsf{L}
                      ⊆𝖭𝖢2⊆⋯⊆𝖭𝖢⊆𝖯.\displaystyle\subseteq\mathsf{NC}^{2}\subseteq\cdots\subseteq\
                      mathsf{NC}\subseteq\mathsf{P}.

Whether these relations are proper is a long-standing open question in complexity theory (just as
the question of 𝖯\mathsf{P} vs. 𝖭𝖯\mathsf{NP}). That said, we will proceed as in complexity theory,
namely, associate a neural model with one of the above complexity class by showing that it captures
a complete problem in the class and is subsumed by the class. We define complete problems for these
circuit classes in the standard way, i.e., under first-order (𝖥𝖮\mathsf{FO}) reductions. For 𝖭𝖢1\
mathsf{NC}^{1}, natural complete problems include simulating a DFA (i.e., the word problem for S5S_
{5}), multiplying matrices over the boolean semiring, and evaluating boolean formulas (Buss, 1987).
For 𝖯𝖭𝖢1\mathsf{PNC}^{1}, complete problems can be defined that, informally, extend these 𝖭𝖢1\
mathsf{NC}^{1}-complete problems with arithmetic values (cf. Proposition 3; Caussinus et al., 1998
).

We note that 𝖯𝖭𝖢1\mathsf{PNC}^{1} is generally thought to be “just above” 𝖭𝖢1\mathsf{NC}^{1} in the
sense that it can be simulated by 𝖭𝖢\mathsf{NC} circuits of depth O⁡(log⁡n​log∗​n)O(\log n\log^{*}
n).^4^4 4 Mereghetti and Palano (2000) mention that the iterated matrix multiplication problem is
in 𝖭𝖢1\mathsf{NC}^{1}. However, they do not provide a proof or reference, and this turns out to be
not quite accurate. The best known simulation of 𝖯𝖭𝖢1\mathsf{PNC}^{1} by 𝖭𝖢\mathsf{NC} circuits
recursively uses the Chinese Remainder Theorem and incurs a small overhead of O⁡(log∗⁡n)O(\log^{*}
n), requiring total depth O⁡(log⁡n​log∗​n)O(\log n\log^{*}n) (Jung, 1985). For sequence of length
64K to 1M, the overhead factor in parallel runtime relative to 𝖭𝖢1\mathsf{NC}^{1} is just 3. We
will therefore think of the class 𝖯𝖭𝖢1\mathsf{PNC}^{1} as nearly as efficiently parallelizable as
𝖭𝖢1\mathsf{NC}^{1} in general and as transformers in particular.

3 Parallelizability Limits of Nonlinear RNNs

We start with a folklore upper bound on the expressivity of nonlinear RNNs (proof in Appendix A for
completeness):

Proposition 1 (Upper Bound).

Let MM be an RNN over ℚ\mathbb{Q} with s=O⁡(poly⁡(n))s=O(\mathrm{poly}(n)) precision. Then the
language recognized by MM is also recognizable by a Turing machine that uses O⁡(max⁡{s,log⁡n})O(\
max\{s,\log n\}) space and poly⁡(n)\mathrm{poly}(n) time.

Corollary 1.

Poly-precision RNNs can be simulated in 𝖯\mathsf{P} and log-precision RNNs can be simulated in 𝖫\
mathsf{L}.

This result gives a fully sequential simulation for poly-precision RNNs in 𝖯\mathsf{P}. On the
other hand, since 𝖫⊆𝖭𝖢2\mathsf{L}\subseteq\mathsf{NC}^{2}, log-precision nonlinear RNNs can be
parallelized to depth O⁡(log2⁡n)O(\log^{2}n), incurring a factor of O⁡(log⁡n)O(\log n) in circuit
depth compared to transformers. We will next show that both of these upper bounds are essentially
tight, barring major breakthroughs in complexity theory. That is, poly-precision RNNs likely cannot
be parallelized at all to poly-logarithmic depth, and, for log-precision RNNs, the O⁡(log⁡n)O(\log
n) penalty relative to transformers likely cannot be removed.

3.1 Poly-Precision Nonlinear RNNs Are 𝖯\mathsf{P}-Complete

We first show that nonlinear RNNs with polynomial precision can simulate Turing machines, taking
inspiration from the classical construction of Siegelmann and Sontag (1995). It will follow that
they can solve 𝖯\mathsf{P}-complete problems, which means they cannot be parallelized (to poly-log
depth) unless 𝖭𝖢=𝖯\mathsf{NC}=\mathsf{P}. Towards showing this, we prove that RNNs whose gating
function is an MLP can simulate a multi-stack machine, which is Turing-complete (proof in Appendix 
A):

Theorem 1 (Turing-Completeness).

For any multi-stack machine MM, there exists a one-layer MLP RNN that recognizes the same language
as MM.

Proof sketch.

The idea follows Siegelmann and Sontag (1995). We use a poly-precision scalar in the RNN state to
represent each stack and leverage the fact that a ReLU network (used in the recurrence) can update
the stack by pushing and popping. ∎

Since a multi-stack machine is Turing complete, it follows from Theorem 1 that poly-precision
nonlinear RNNs can solve a 𝖯\mathsf{P}-complete problem (proof in Appendix A):

Corollary 2 (𝖯\mathsf{P}-Completeness).

There is a one-layer MLP RNN whose language is 𝖯\mathsf{P}-complete under 𝖥𝖮\mathsf{FO} reductions.

It follows that, under standard complexity conjectures, poly-precision nonlinear RNNs cannot be
parallelized effectively:

Corollary 3 (Poly-Precision Depth Lower Bound).

If 𝖭𝖢≠𝖯\mathsf{NC}\neq\mathsf{P}, there is a nonlinear RNN that, for any kk, cannot be simulated by
an 𝖭𝖢\mathsf{NC} circuit family of depth O⁡(logk⁡n)O(\log^{k}n).

3.2 Log-Precision Nonlinear RNNs Are 𝖫\mathsf{L}-Complete

We have established that poly-precision nonlinear RNNs cannot be parallelized effectively assuming
𝖭𝖢≠𝖯\mathsf{NC}\neq\mathsf{P}. However, in practice, LLMs use bounded precision, which motivates
similarly analyzing nonlinear RNNs with log precision. Corollary 1 shows such RNNs are in 𝖫\mathsf
{L}; we now show they can also solve an 𝖫\mathsf{L}-complete problem, establishing that they likely
cannot be parallelized as effectively as transformers or linear RNNs under standard conjectures.

Prior work (Merrill and Sabharwal, 2025a) has used the graph connectivity problem as a prototypical
𝖫\mathsf{L}-complete problem when analyzing models such as log-depth transformers. This problem,
however, does not naturally lend to a left-to-right single-pass solution that would be suitable for
formalizing the additional expressive power of nonlinear RNNs. To work around this hurdle, we
consider the following sorted and deterministic variant of the problem where each graph node has
exactly one outgoing edge and the nodes are presented in a topologically sorted order:

Definition 11.

Sorted deterministic graph connectivity is the problem that takes as input a source node ss,
sequence of directed edges (i1,j1),…​(in,jn)(i_{1},j_{1}),\ldots(i_{n},j_{n}), and target node tt,
all encoded in unary. The edges are deterministic — meaning that each ii has at most one outgoing
edge (i,j)(i,j) — and topologically sorted — meaning i1<⋯<ini_{1}<\cdots<i_{n}. The output is 1 if
and only if there is a path of edges from ss to tt.

Perhaps surprisingly, this simpler variant of deterministic graph connectivity remains 𝖫\mathsf{L}
-complete:

Proposition 2.

Sorted deterministic graph connectivity is 𝖫\mathsf{L}-complete under 𝖥𝖮\mathsf{FO}-reductions.

We defer the proof to Section A.2. Intuitively, this is achieved by reduction from deterministic
(but not necessarily sorted) graph connectivity. The latter is well-known to be 𝖫\mathsf{L}
-complete (Jones, 1975; Cook and McKenzie, 1987; Sipser, 1997, cf.), and is in fact also complete
under 𝖥𝖮\mathsf{FO}-reductions just like the general graph connectivity problem (Immerman, 1998).
The reduction makes O⁡(n)O(n) copies of the deterministic graph and connects the ii-th copy to
(i+1)(i+1)-st copy. In this way, the resulting deterministic graph is polynomial in the size of the
original graph and at the same time topologically sorted.

The topological sortedness condition allows a logspace machine to solve the sorted deterministic
graph connectivity problem by reading the graph in one direction (from left to right). This is also
the reason that a single-layer nonlinear RNN can solve the sorted deterministic graph connectivity
problem. Since the indices of the vertices (given in unary) can be encoded in binary with O⁡(log⁡n)
O(\log n) bits, nonlinear RNNs can solve this problem with only log precision.

Theorem 2 (𝖫\mathsf{L}-Complete).

There exists a one-layer log-precision MLP RNN that solves sorted deterministic graph connectivity,
an 𝖫\mathsf{L}-complete problem.

Proof sketch.

The proof leverages the computational model of counter machines (Fischer et al., 1968; Weiss et
al., 2018; Merrill, 2021), a generalization of finite automata augmented with a fixed number of
integer-valued registers that has been heavily used to analyze RNNs (Weiss et al., 2018; Merrill,
2019). Lemma 2 shows a counter machine can solve sorted deterministic graph connectivity. Moreover,
Lemma 3 shows a log-precision MLP RNN can simulate a counter machine.^5^5 5 While counter machines
have been leveraged extensively to analyze RNNs in the past (Weiss et al., 2018; Merrill et al.,
2020), Lemma 3 is novel and interesting in its own right. Thus, we conclude that an MLP RNN can
also solve sorted deterministic graph connectivity. ∎

It is a longstanding open problem (Allender, 2023) if any 𝖫\mathsf{L}-complete problem has a
boolean circuit family of depth o⁡(log2⁡n)o(\log^{2}n). Thus, we have:

Corollary 4 (Log-Precision Parallelism Limit).

Unless every problem in 𝖫\mathsf{L} admits an 𝖭𝖢\mathsf{NC} circuit family of o⁡(log2⁡n)o(\log^{2}
n) depth, there exists a log-precision nonlinear RNN that requires 𝖭𝖢\mathsf{NC} circuit families
of depth Ω⁡(log2⁡n)\Omega(\log^{2}n) to simulate.

Corollary 4 relates to recent work on parallelizing nonlinear RNNs via Newton’s method, i.e.,
exactly computing the nonlinear RNN through iterated linear approximation (Danieli et al., 2026;
Gonzalez et al., 2026). Gonzalez et al. (2025) show that this method runs in time O⁡(log2⁡n)O(\log^
{2}n) for predictable dynamical systems, matching our theoretical expectation for parallel runtime
of nonlinear RNNs.

4 Simulating LRNNs with Parallel Circuits

Having established the limits of parallelizing nonlinear RNNs, we turn to understanding the degree
to which linear RNNs can be parallelized and, conversely, their limitations for expressing
inherently sequential computation. Prior theoretical work has also studied the expressivity of
linear RNNs from a circuit complexity perspective, revealing expressivity advantages of some linear
RNNs over transformers (Merrill et al., 2024). In particular, whereas linear RNNs with simple
parameterizations like S4 and Mamba fall into 𝖳𝖢0\mathsf{TC}^{0}, the same complexity class as
transformers, more complex parameterizations like DeltaNet (Grazzi et al., 2025) and RWKV (Peng et
al., 2025) can express 𝖭𝖢1\mathsf{NC}^{1}-complete finite state tracking problems. This past work
suggests a key advantage of well-parameterized linear RNN architectures over transformers, but
leaves open a precise understanding of whether this additional expressivity implies drawbacks in
parallelism, i.e, additional overhead in the sequential runtime required to simulate LRNNs relative
to transformers.

Transformers only compute functions in 𝖳𝖢0\mathsf{TC}^{0}, which implies they can be simulated by
𝖭𝖢1\mathsf{NC}^{1} circuits of depth O⁡(log⁡n)O(\log n). It is known that linear RNNs can be
parallelized to near log depth using the parallel SCAN algorithm (Blelloch, 1990), but this does
not immediately yield a circuit complexity characterization comparable to that of transformers. We
address this gap by showing that all linear RNNs, independent of parameterization details, can be
simulated in the complexity class 𝖯𝖭𝖢1\mathsf{PNC}^{1} of bounded fan-in log-depth circuits with
arithmetic gates:

Theorem 3 (LRNN Upper Bound).

The language recognized by any LRNN over ℚ\mathbb{Q} is in 𝖯𝖭𝖢1\mathsf{PNC}^{1}.

Proof.

Since 𝖥𝖮\mathsf{FO}-uniform polynomial-size circuit families are closed under finite composition (
Merrill and Sabharwal, 2025b, Proposition 3), the proof proceeds by simulating each component in
the network in 𝖥𝖮\mathsf{FO}-uniform 𝖯𝖭𝖢1\mathsf{PNC}^{1}.

As a building block, we need gates that take as input bit representations for (s,t)(s,t) and output
the integer-pair representation (s,t)(s,t) for the rational number s/ts/t. This can be done by an
𝖥𝖮\mathsf{FO}-uniform poly-sized log-depth arithmetic ℤ\mathbb{Z}-circuit (here, the measurement is
with respect to the number of bits of ss and tt), as follows: if ss is represented by bn−1​…​b0∈
{0,1}∗b_{n-1}\ldots b_{0}\in\{0,1\}^{*}, then s=∑i=0n−1bi⋅2is=\sum_{i=0}^{n-1}b_{i}\cdot 2^{i}.
Iterated sum, fast exponentiation, and multiplication by a constant can all be done by a uniform
arithmetic ℤ\mathbb{Z}-circuit with depth O⁡(log⁡n)O(\log n) and size polynomial in nn.

Next, we write the output in a convolutional form (Merrill and Sabharwal, 2023; Merrill et al.,
2024):

 𝐲t=𝐱t+∑j=0i𝐪t⊤​(∏k=0j𝐀j−k)​𝐛i−j.\mathbf{y}_{t}=\mathbf{x}_{t}+\sum_{j=0}^{i}\mathbf{q}_{t}^{\  (1)
 top}\left(\prod_{k=0}^{j}\mathbf{A}_{j-k}\right)\mathbf{b}_{i-j}.

By Lemma 1, there is an 𝖥𝖮\mathsf{FO}-uniform 𝖳𝖢0\mathsf{TC}^{0}, and hence 𝖭𝖢1\mathsf{NC}^{1},
circuit family that maps the inputs x¯\bar{x} to each 𝐀t\mathbf{A}_{t} and 𝐛t\mathbf{b}_{t}. Using
the previously described bits-to-rational conversion gates, we turn these into rational
representations. Each of these matrices has bounded dimension dd. Iterated matrix multiplication,
as well as iterated matrix addition, for matrices of a bounded dimension over any semiring 𝕂\mathbb
{K} (here 𝕂=ℚ\mathbb{K}=\mathbb{Q}) can be done using a log depth poly-sized 𝕂\mathbb{K}-arithmetic
circuit (Vollmer, 1999). Since ℚ\mathbb{Q}-arithmetic circuits can be simulated using ℤ\mathbb{Z}
-circuits incurring only a constant number of operations for each gate (see Proposition 4), we
obtain a 𝖦𝖺𝗉𝖭𝖢1\mathsf{GapNC}^{1} circuit for outputting 𝐰⊤​𝐲t\mathbf{w}^{\top}{\mathbf{y}_{t}}.
The last step is to perform a positivity check, 𝐰⊤​𝐲t>0\mathbf{w}^{\top}{\mathbf{y}_{t}}>0, which
puts the problem in 𝖯𝖭𝖢1\mathsf{PNC}^{1}. ∎

Theorem 3 applies generally to LRNNs over ℚ\mathbb{Q} (i.e., poly-precision LRNNs). For the
subclass of log-precision LRNNs, it is in fact possible to obtain a tighter characterization. Let
𝖠𝖢0​[𝖤𝖭𝖢1]\mathsf{AC}^{0}[\mathsf{ENC}^{1}] denote languages that can be solved by boolean 𝖠𝖢0\
mathsf{AC}^{0} circuits that can make use (as a blackbox) of any boolean circuit that comes from an
𝖤𝖭𝖢1\mathsf{ENC}^{1}-arithmetic circuit. It is known (Datta et al., 2012, cf.) that 𝖤𝖭𝖢1⊆𝖠𝖢0​[𝖤𝖭𝖢1]
⊆𝖯𝖭𝖢1\mathsf{ENC}^{1}\subseteq\mathsf{AC}^{0}[\mathsf{ENC}^{1}]\subseteq\mathsf{PNC}^{1}.

Theorem 4 (Log-Precision LRNN Upper Bound).

The language recognized by any log-precision LRNN over ℚ\mathbb{Q} is in 𝖠𝖢0​[𝖤𝖭𝖢1]\mathsf{AC}^{0}
[\mathsf{ENC}^{1}].

Proof.

Let CC denote the 𝖥𝖮\mathsf{FO}-uniform 𝖦𝖺𝗉𝖭𝖢1\mathsf{GapNC}^{1} circuit constructed in the proof
of Theorem 3 that computes 𝐰⊤​𝐲t>0\mathbf{w}^{\top}\mathbf{y}_{t}>0. Owing to log precision of the
LRNN, this output can take only polynomially many possible values vv. Thus, instead of performing a
positivity check on 𝐰⊤​𝐲t\mathbf{w}^{\top}\mathbf{y}_{t} (as in construction in Theorem 3), we
leverage log-precision to simulate the positivity check with polynomially many equality checks.
Specifically, for each possible vv, we extend CC to build an 𝖥𝖮\mathsf{FO}-uniform 𝖤𝖭𝖢1\mathsf{ENC}
^{1}-circuit CvC_{v} that checks whether the output 𝐰⊤​𝐲t\mathbf{w}^{\top}\mathbf{y}_{t} equals vv.
The positivity check is then simply an unbounded fan-in OR, and hence an 𝖠𝖢0\mathsf{AC}^{0}
circuit, whose inputs are the outputs of CvC_{v} for v>0v>0. ∎

As noted in Section 2.5, 𝖯𝖭𝖢1\mathsf{PNC}^{1} can be simulated by O⁡(log⁡n​log∗​n)O(\log n\log^{*}
n) depth 𝖭𝖢\mathsf{NC} circuits. We therefore have:

Corollary 5.

The language recognized by any LRNN over ℚ\mathbb{Q} is also recognized by an 𝖭𝖢\mathsf{NC} circuit
family of depth O⁡(log⁡n​log∗​n)O(\log n\log^{*}n).

It follows that LRNNs are nearly as efficiently parallelizable on hardware as transformers, paying
only an O⁡(log∗⁡n)O(\log^{*}n) depth penalty beyond the O⁡(log⁡n)O(\log n) depth needed to
implement transformers with 𝖭𝖢\mathsf{NC} circuits. On the other hand, we will show next that
well-parameterized LRNNs significantly gain (over transformers) on the side of expressivity.

As a by-product of our above results, we also obtain the following boundary between linear RNNs and
nonlinear RNNs. Recall that (sorted, deterministic) graph connectivity problem is 𝖫\mathsf{L}
-complete.

Corollary 6.

Assuming 𝖫\mathsf{L} does not admit 𝖭𝖢\mathsf{NC} circuits of depth O⁡(log⁡n​log∗​n)O(\log n\log^
{*}n), LRNNs over ℚ\mathbb{Q} cannot solve (sorted, deterministic) graph connectivity, but
nonlinear RNNs can.

It is only known that 𝖫⊆𝖭𝖢2\mathsf{L}\subseteq\mathsf{NC}^{2}. It is still a long-standing open
problem (Allender, 2023) whether any 𝖫\mathsf{L}-complete problem admits 𝖭𝖢\mathsf{NC} circuits
with depth o⁡(log2⁡n)o(\log^{2}n).

5 Expressivity Differences Between LRNNs

In Section 4, we showed all linear RNNs can only solve problems in 𝖯𝖭𝖢1\mathsf{PNC}^{1}. A natural
question is whether this upper bound is tight, i.e., whether each linear RNN variant can solve hard
problems in 𝖯𝖭𝖢1\mathsf{PNC}^{1}. For some variants like S4 and Mamba, we already know a tighter
upper bound of 𝖳𝖢0\mathsf{TC}^{0} can be given (Merrill et al., 2024), meaning these architectures
do not achieve full expressivity in 𝖯𝖭𝖢1\mathsf{PNC}^{1}. However, several newer LRNN
parameterizations of LRNNs that can express 𝖭𝖢1\mathsf{NC}^{1}-complete problems have been
proposed: in particular several flavors of DLPR LRNNs as well as PD LRNNs (Section 2.3). We now
analyze the expressivity of these different parameterizations, showing a conditional separation:
while DPLR LRNNs attain 𝖯𝖭𝖢1\mathsf{PNC}^{1}-complete expressivity, we can show that PD LRNNs are
contained within 𝖭𝖢1\mathsf{NC}^{1}.

5.1 DPLR LRNNs are 𝖯𝖭𝖢1\mathsf{PNC}^{1}-Complete

Two common popular DPLR LRNNs are RWKV-7 (Peng et al., 2025) and DeltaNet (Schlag et al., 2021). We
will show both of these architectures can solve 𝖯𝖭𝖢1\mathsf{PNC}^{1}-complete problems, achieving
the upper bound on LRNN expressivity from Section 4. We begin by defining a 𝖯𝖭𝖢1\mathsf{PNC}^{1}
-complete problem, namely iterated matrix multiplication:

Definition 12 (Iterated 3×33\times 3 Matrix Multiplication).

The input is a token stream of length 9​n9n over {−1,0,1}\{-1,0,1\}, partitioned into nn
consecutive blocks of length 99. Block tt encodes a matrix 𝐀t∈𝕂3×3\mathbf{A}_{t}\in\mathbb{K}^{3\
times 3}. The iterated matrix multiplication problem is to compute 𝐏n=∏t=1n𝐀t∈ℤ3×3\mathbf{P}_{n}=\
prod_{t=1}^{n}\mathbf{A}_{t}\in\mathbb{Z}^{3\times 3}.

Proposition 3 (Caussinus et al., 1998, Corollary 3.4).

The 3×33\times 3 iterated multiplication problem is 𝖦𝖺𝗉𝖭𝖢1\mathsf{GapNC}^{1}-complete under 𝖥𝖮\
mathsf{FO} reductions, and thus checking if the (0,0)(0,0) entry of 𝐏n\mathbf{P}_{n} is positive is
𝖯𝖭𝖢1\mathsf{PNC}^{1} complete under 𝖥𝖮\mathsf{FO} reductions.

We show RWKV-7 and DeltaNet can express this iterated matrix multiplication problem, establishing
that they can solve 𝖯𝖭𝖢1\mathsf{PNC}^{1}-complete problems:

Theorem 5 (DPLR 𝖯𝖭𝖢1\mathsf{PNC}^{1}-Completeness).

There exist a 44-layer RWKV-7 and a 44-layer DeltaNet that solves the iterated 3×33\times 3 matrix
multiplication problem and, in particular, checks if the (0,0)(0,0) entry of 𝐏n\mathbf{P}_{n} is
positive.

Proof sketch.

For a full proof, see Section B.3 for RWKV-7 and Section B.5 for DeltaNet. The goal is to multiply
a sequence of arbitrary matrices. If the transition matrices were allowed to have an arbitrary
form, the construction would be easy with a single RNN layer. However, since the transition
matrices are constrained to have DPLR form, we need a few extra layers to factor blocks of the
given 3×33\times 3 matrices into blocks of DPLR matrices. To this end, we generalize an idea from
Peng et al. (2025): use layers 1–3 to factor blocks of arbitrary matrices into DPLR matrices with
the same product, and then multiply all of these DPLR matrices to get the overall product 𝐏n\mathbf
{P}_{n}. ∎

Theorem 5 shows that RWKV-7 and DeltaNet, two DPLR LRNNs, can indeed solve a 𝖯𝖭𝖢1\mathsf{PNC}^{1}
-complete problem, providing a lower bound on their expressivity that matches the upper bound from
Theorem 3. This however leaves open the possibility that even such LRNNs may not be able to solve
much simpler problems within 𝖯𝖭𝖢1\mathsf{PNC}^{1}. To address this gap, we extend the argument to
also derive a more general lower bound, stating that RWKV-7 can, in fact, solve every problem
solvable by a Weighted Finite Automaton (WFA) over the semiring ℤ\mathbb{Z} (WFA; see Definition 13
for a definition). WFA subsume the problem of iterated 3-by-3 matrix multiplications with matrices
over {−1,0,1}\{-1,0,1\}, which we already remarked (see Exercise 5.10 of Vollmer, 1999 and
Caussinus et al., 1998) to be 𝖦𝖺𝗉𝖭𝖢1\mathsf{GapNC}^{1}-complete. In other words, the set of
functions f:Σ∗→ℤf:\Sigma^{*}\to\mathbb{Z} definable by WFAs contains a 𝖦𝖺𝗉𝖭𝖢1\mathsf{GapNC}^{1}
-complete problem. Thus, considering “acceptance” by a WFA of an input string ww with the condition
f⁡(w)>0f(w)>0, there is a 𝖯𝖭𝖢1\mathsf{PNC}^{1}-complete WFA language.

Theorem 6 (DPLR WFA Simulation).

For any nn-state WFA 𝒜\mathcal{A} over ℚ\mathbb{Q}, there exist a 44-layer RWKV-7 and a 4-layer
DeltaNet that both compute f𝒜​(w)f_{\mathcal{A}}(w).

A proof sketch and details are deferred to Section B.2 for RWKV-7 and to Section B.4 for DeltaNet.

5.2 PD LRNNs are 𝖭𝖢1\mathsf{NC}^{1}-Complete

PD LRNNs are another recently proposed LRNN variant that can succinctly represent the 𝖭𝖢1\mathsf
{NC}^{1}-complete state tracking problem (Terzic et al., 2025). We analyze the expressive power,
showing that, in contrast to DPLR LRNNs, PD LRNNs are constrained to 𝖭𝖢1\mathsf{NC}^{1}:

Theorem 7.

Let MM be a multi-layer PD LRNN over ℚ\mathbb{Q}. Then the language recognized by MM is in 𝖥𝖮\
mathsf{FO}-uniform 𝖭𝖢1\mathsf{NC}^{1}.

Proof Sketch.

The theorem reduces to showing that the product of a sequence of matrices 𝐏1​𝐃1,…,𝐏n​𝐃n\mathbf{P}_
{1}\mathbf{D}_{1},\ldots,\mathbf{P}_{n}\mathbf{D}_{n} can be computed in 𝖥𝖮\mathsf{FO}-uniform 𝖭𝖢1\
mathsf{NC}^{1}. Terzic et al. (2025) observe PD matrices are closed under multiplication, so this
product also has PD form. Let ∏i=1n𝐏i​𝐃i=𝐏~n​𝐃~n\prod_{i=1}^{n}\mathbf{P}_{i}\mathbf{D}_{i}=\tilde
{\mathbf{P}}_{n}\tilde{\mathbf{D}}_{n}. We derive a closed form for 𝐏~i\mathbf{\tilde{P}}_{i} and
𝐃~i\tilde{\mathbf{D}}_{i}, and show these can be computed in uniform 𝖭𝖢1\mathsf{NC}^{1}; full proof
in Appendix C. ∎

Theorem 7 establishes a conditional expressivity gap between DPLR and PD LRNNs: while DPLR
architectures like DeltaNet and RWKV-7 can express some 𝖯𝖭𝖢1\mathsf{PNC}^{1}-complete problems, PD
LRNNs are restricted to 𝖭𝖢1\mathsf{NC}^{1}. Moreover, since it has previously been shown that PD
LRNNs can recognize regular languages (Terzic et al., 2025), we see that they can solve 𝖭𝖢1\mathsf
{NC}^{1}-complete problems as well.

In the one-layer case, we can show that deterministic WFAs can be simulated by PD LRNNS in the
following sense:

Theorem 8.

Any language recognized by a deterministic WFA with a zero threshold over ℚ\mathbb{Q} can also be
recognized by a one-layer PD LRNN over ℚ\mathbb{Q}.

Proof in Section D.2. From Theorems 7 and 8, we have:

Corollary 7.

Any language defined by a deterministic WFA with a zero threshold can be simulated in 𝖭𝖢1\mathsf
{NC}^{1}. Moreover, there exists an 𝖭𝖢1\mathsf{NC}^{1}-complete language that can be recognized by
a deterministic WFA with a zero threshold.

6 Experiments

We have provided a concise theoretical characterization of the expressive strengths and limitations
of (non)linear RNNs. We now empirically evaluate the learnable capacity of recurrent models on the
Deterministic Graph Connectivity and Iterated 3×33\times 3 Matrix Multiplication. Specifically, we
compare nonlinear RNNs with linear RNN models (DeltaNet and RWKV-7), the linear RNN variant Mamba,
and include Transformers as an additional baseline.

RNNRWKV-7DeltaNetMambaTransformer00252550507575100100Accuracy(%)(a) Sorted Deterministic Graph
ConnectivityRNNRWKV-7DeltaNetMambaTransformer00252550507575100100Accuracy(%)(b) Matrix
Multiplication over ℤm\mathbb{Z}_{m}RNNRWKV-7DeltaNetMambaTransformer00252550507575100100Accuracy
(%)(c) Unbounded Matrix Multiplication[1,100][101,200][201,300]

Figure 2: Accuracy across size/length ranges. (a) Deterministic graph connectivity over graph-size
ranges. (b) Iterated 3×33\times 3 matrix multiplication over ℤm\mathbb{Z}_{m}. (c) Iterated 3×33\
times 3 matrix multiplication over ℤ\mathbb{Z}. Models are evaluated on in-distribution ranges
[1,100][1,100] and out-of-distribution ranges [101,200][101,200] and [201,300][201,300].

Experimental Setup.

To better evaluate learning capacity, we construct balanced datasets with length generalization.
The train set contains 70K samples uniformly drawn from N∈[1,100]N\in[1,100] and the validation set
contains 20K samples drawn from the same range. We evaluated on three test splits, each containing
10K samples, drawn from [1,100][1,100], [101,200][101,200], and [201,300][201,300]. Here, NN
denotes the number of nodes in the graph task and the size of the matrix in the matrix
multiplication task.

We used the nonlinear RNN and transformer (Sarrof et al., 2024), and linear models from Yang and
Zhang (2024). All models are trained using the BCEWithLogitsLoss function. All models are trained
using AdamW with learning rate 1​e−41e^{-4} and weight decay 10−410^{-4}, batch size 64, gradient
clipping at norm 1.0, and early stopping either when the accuracy of the in-distribution data
reaches 100%100\% for three consecutive evaluations or when a maximum of 60K training steps are
reached. Full benchmark details and training configurations are provided in the Appendix.

Sorted Deterministic Graph Connectivity.

As shown in Figure 2 (a), all models achieve high in-distribution accuracy, indicating that sorted
deterministic graph connectivity is learnable under matched training and test lengths. However,
performance diverges markedly under length extrapolation. The nonlinear RNN maintains near-perfect
accuracy across all bins, demonstrating strong length generalization. In contrast, Transformer,
RWKV-7, Mamba, and DeltaNet exhibit substantial degradation as graph size increases, despite
competitive performance. These results highlight the importance of recurrent inductive bias for
length-generalizable algorithmic reasoning.

Iterated Matrix Multiplication.

RWKV-7, the nonlinear RNN, and DeltaNet achieve near-perfect accuracy on the in-distribution data
and degrade only moderately on the longer out-of-distribution sequences in (b). In contrast,
Transformer and Mamba perform poorly across all lengths, suggesting limited capacity to capture the
underlying algebraic structure even within the training regime. Architectural differences are more
pronounced in the non-modular (c). RWKV-7, the nonlinear RNN, and DeltaNet retain perfect or
near-perfect accuracy across both in-distribution and out-of-distribution lengths, despite
unbounded integer growth. Mamba improves with increasing sequence length but remains substantially
below the top-performing models, while the Transformer degrades sharply beyond training lengths.
Overall, these results indicate that architectures with explicit recurrence or state-space
structure are substantially better suited for iterated linear-algebraic computations than standard
attention-based Transformers.

7 Conclusion

We have given a comprehensive characterization of the expressive power and parallelizability of
different RNNs and LRNNs. First, we reveal fundamental expressivity gaps between linear and
nonlinear RNNs, highlighting the deterministic graph connectivity problem as a simple separator.
This circuit complexity perspective refines an older line of work on rational recurrences (Peng et
al., 2018; Merrill et al., 2020), formalizing a fundamental tradeoff between the expressivity of
the state update and the parallelizability of an RNN. Second, we compare the expressivity of
different types of linear RNN. Whereas both PD and DPLR parameterizations have been shown to be
capable of regular language recognition in prior work, our theory reveals finegrained differences
between these variants, with PD being 𝖭𝖢1\mathsf{NC}^{1}-complete while DPLR LRNNs are 𝖯𝖭𝖢1\mathsf
{PNC}^{1}-complete. Experiments agree with our theoretical predictions about the expressivity
differences between nonlinear RNNs and LRNNs and different LRNN variants.

Hybrid transformer–LRNN models have recently seen wide adoption (Waleffe et al., 2024; Kimi Team,
2025; Qwen Team, 2026, e.g.,) informed by emerging understanding of LRNN expressivity (Merrill et
al., 2024; Grazzi et al., 2025; Peng et al., 2025; Merrill et al., 2026). Thus, we hope our theory
can guide the design of LRNN and hybrid architectures that effectively balance expressivity and
parallelism. Our results also suggest recent methods to parallelize nonlinear RNNs (Danieli et al.,
2026; Gonzalez et al., 2025) are one pathway to more expressive hybrid models, though nonlinear
RNNs must fundamentally pay a Θ⁡(log⁡n)\Theta(\log n) cost in parallel runtime under standard
conjectures (cf. Corollary 4). It is an open question whether the additional expressivity of
nonlinear RNNs justifies this overhead in practice.

Impact Statement

This paper presents foundational work whose goal is to advance the field of machine learning. There
are many potential societal consequences of our work, none of which we feel must be specifically
highlighted here.

Acknowledgments

WM thanks Mehryar Mohri for insightful discussions related to RNNs and WFAs as well as Brian Kitano
and Aleksandar Terzić for finding issues in early drafts.

This material is based in part upon work supported by the European Union (ERC, LASD, 101089343,
https://doi.org/10.3030/101089343).

References

  • Allender (2023) E. Allender Discussion on parallelizing logspace problems. Note: https://
    cstheory.stackexchange.com/questions/52459/
    what-circuit-depth-is-enough-to-compute-a-log-space-complete-problem[Online; accessed 27 Jan
    2026] Cited by: §3.2, §4.
  • Arora et al. (2024) S. Arora, S. Eyuboglu, M. Zhang, A. Timalsina, S. Alberti, J. Zou, A.
    Rudra, and C. Re Simple linear attention language models balance the recall-throughput tradeoff
    . In ICLR 2024 Workshop on Mathematical and Empirical Understanding of Foundation Models, 
    External Links: Link Cited by: §1.
  • Barcelo et al. (2026) P. Barcelo, D. Chiang, G. Cybenko, L. Strobl, and A. Yang Theory of
    Neural Language Models (Dagstuhl Seminar 25282). Dagstuhl Reports 15 (7), pp. 22–52. Note: 
    Keywords: Dagstuhl Seminar, Neural Networks, Language Models, Automata, Logic, Model Theory,
    Circuit Complexity External Links: ISSN 2192-5283, Link, Document Cited by: §A.1.
  • Beck et al. (2024) M. Beck, K. Pöppel, M. Spanring, A. Auer, O. Prudnikova, M. K. Kopp, G.
    Klambauer, J. Brandstetter, and S. Hochreiter XLSTM: extended long short-term memory. In 
    NeurIPS, External Links: Link Cited by: §1, §1.
  • Blelloch (1990) G. E. Blelloch Prefix sums and their applications. Technical report Technical
    Report CMU-CS-90-190, School of Computer Science, Carnegie Mellon University. Cited by: §1, §4.
  • Bradbury et al. (2017) J. Bradbury, S. Merity, C. Xiong, and R. Socher Quasi-recurrent neural
    networks. In ICLR, External Links: Link Cited by: §1.
  • Buss (1987) S. R. Buss The boolean formula value problem is in alogtime. In Proceedings of the
    Nineteenth Annual ACM Symposium on Theory of Computing, STOC ’87, New York, NY, USA, 
    pp. 123–131. External Links: ISBN 0897912217, Link, Document Cited by: §2.5.
  • Carlyle and Paz (1971) J. W. Carlyle and A. Paz Realizations by stochastic finite automata. 
    JCSS 5 (1), pp. 26–40. Cited by: §D.1.
  • Caussinus et al. (1998) H. Caussinus, P. McKenzie, D. Thérien, and H. Vollmer Nondeterministic
    NC1{}^{1} computation. Journal of Computer and System Sciences 57 (2), pp. 200–212. External
    Links: ISSN 0022-0000, Document, Link Cited by: §2.5, §5.1, Proposition 3.
  • Chiang (2025) D. Chiang Transformers in uniform TC0{}^{0}. TMLR. Note: External Links: ISSN
    2835-8856, Link Cited by: §2.1, §2.1, Lemma 1.
  • Cook and McKenzie (1987) S. A. Cook and P. McKenzie Problems complete for deterministic
    logarithmic space. Journal of Algorithms 8 (5), pp. 385–394. External Links: ISSN 0196-6774,
    Link, Document Cited by: §A.2, §3.2.
  • Danieli et al. (2026) F. Danieli, P. Rodriguez, M. Sarabia, X. Suau, and L. Zappella ParaRNN:
    unlocking parallel training of nonlinear RNNs for large language models. In The Fourteenth
    International Conference on Learning Representations, External Links: Link Cited by: §3.2, §7.
  • Datta et al. (2012) S. Datta, M. Mahajan, B. V. R. Rao, M. Thomas, and H. Vollmer Counting
    classes and the fine structure between NC1{}^{1} and L. Theor. Comput. Sci. 417, pp. 36–49. 
    External Links: Link, Document Cited by: §4.
  • Elman (1990) J. L. Elman Finding structure in time. Cognitive Science 14 (2), pp. 179–211. 
    External Links: Document, Link, https://onlinelibrary.wiley.com/doi/pdf/10.1207/
    s15516709cog1402_1 Cited by: §1, §2.2.
  • Fischer et al. (1968) P. C. Fischer, A. R. Meyer, and A. L. Rosenberg Counter machines and
    counter languages. Mathematical systems theory 2, pp. 265–283. External Links: Link Cited by:
    §A.2, §3.2.
  • Gonzalez et al. (2026) X. Gonzalez, E. K. Buchanan, H. D. Lee, J. W. Liu, K. A. Wang, D. M.
    Zoltowski, L. Kozachkov, C. Re, and S. Linderman A unifying framework for parallelizing
    sequential models with linear dynamical systems. TMLR. External Links: Link Cited by: §3.2.
  • Gonzalez et al. (2025) X. Gonzalez, L. Kozachkov, D. M. Zoltowski, K. L. Clarkson, and S.
    Linderman Predictability enables parallelization of nonlinear state space models. In NeurIPS, 
    External Links: Link Cited by: §3.2, §7.
  • Grazzi et al. (2025) R. Grazzi, J. Siems, J. K.H. Franke, A. Zela, F. Hutter, and M. Pontil 
    Unlocking state-tracking in linear RNNs through negative eigenvalues. In ICLR, External Links: 
    Link Cited by: §1, §1, §2.3, §4, §7.
  • Gu and Dao (2024) A. Gu and T. Dao Mamba: linear-time sequence modeling with selective state
    spaces. External Links: Link Cited by: §1.
  • Hao et al. (2022) Y. Hao, D. Angluin, and R. Frank Formal language recognition by hard
    attention transformers: perspectives from circuit complexity. TACL 10, pp. 800–810. Cited by:
    §2.5.
  • Hochreiter and Schmidhuber (1997) S. Hochreiter and J. Schmidhuber Long short-term memory. 
    Neural Comput. 9 (8), pp. 1735–1780. External Links: ISSN 0899-7667, Link, Document Cited by:
    §1, §2.2.
  • Immerman (1998) N. Immerman Descriptive complexity. Springer Verlag. Cited by: §A.2, §3.2.
  • Immerman and Landau (1992) N. Immerman and S. Landau The complexity of iterated multiplication.
    Technical report University of Massachusetts, University of Massachusetts, USA. Cited by:
    Appendix C.
  • Jones (1975) N. D. Jones Space-bounded reducibility among combinatorial problems. JCSS 11 (1), 
    pp. 68–85. External Links: ISSN 0022-0000, Link, Document Cited by: §A.2, §3.2.
  • Jung (1985) H. Jung Depth efficient transformations of arithmetic into boolean circuits. In 
    Proceedings of Fundamentals of Computation Theory, External Links: Document, Link Cited by:
    footnote 4.
  • Katharopoulos et al. (2020) A. Katharopoulos, A. Vyas, N. Pappas, and F. Fleuret Transformers
    are RNNs: fast autoregressive transformers with linear attention. In ICML, Cited by: §1.
  • Kimi Team (2025) Kimi Team Kimi Linear: an expressive, efficient attention architecture. 
    External Links: 2510.26692, Link Cited by: §2.3, §7.
  • Mereghetti and Palano (2000) C. Mereghetti and B. Palano Threshold circuits for iterated matrix
    product and powering. RAIRO - Theoretical Informatics and Applications 34 (1), pp. 39–46. 
    External Links: Document Cited by: footnote 4.
  • Merrill et al. (2026) W. Merrill, Y. Li, T. Romero, A. Svete, C. Costello, P. Dasigi, D.
    Groeneveld, D. Heineman, B. Kuehl, N. Lambert, C. Li, K. Lo, S. Malik, D. Matusz, B.
    Minixhofer, J. Morrison, L. Soldaini, F. Timbers, P. Walsh, N. A. Smith, H. Hajishirzi, and A.
    Sabharwal Olmo hybrid: from theory to practice and back. External Links: 2604.03444, Link Cited
    by: §7.
  • Merrill et al. (2024) W. Merrill, J. Petty, and A. Sabharwal The illusion of state in
    state-space models. In Forty-first International Conference on Machine Learning, External
    Links: Link Cited by: §1, §2.1, §2.3, §2.5, §4, §4, §5, §7.
  • Merrill and Sabharwal (2023) W. Merrill and A. Sabharwal The parallelism tradeoff: limitations
    of log-precision transformers. TACL 11, pp. 531–545. External Links: Link, Document Cited by:
    §1, §2.5, §4.
  • Merrill and Sabharwal (2025a) W. Merrill and A. Sabharwal A little depth goes a long way: the
    expressive power of log-depth transformers. In NeurIPS, External Links: Link Cited by: §3.2.
  • Merrill and Sabharwal (2025b) W. Merrill and A. Sabharwal Exact expressive power of
    transformers with padding. In NeurIPS, External Links: Link Cited by: §4.
  • Merrill et al. (2020) W. Merrill, G. Weiss, Y. Goldberg, R. Schwartz, N. A. Smith, and E. Yahav
    A formal hierarchy of RNN architectures. In Proceedings of the 58th Annual Meeting of the
    Association for Computational Linguistics, D. Jurafsky, J. Chai, N. Schluter, and J. Tetreault
    (Eds.), Online, pp. 443–459. External Links: Link, Document Cited by: §D.1, §D.1, §1, §7,
    footnote 5.
  • Merrill (2019) W. Merrill Sequential neural networks as automata. In Proceedings of the
    Workshop on Deep Learning and Formal Languages: Building Bridges, J. Eisner, M. Gallé, J.
    Heinz, A. Quattoni, and G. Rabusseau (Eds.), Florence, pp. 1–13. External Links: Link, Document
    Cited by: §A.2, §3.2.
  • Merrill (2021) W. Merrill On the linguistic capacity of real-time counter automata. External
    Links: 2004.06866, Link Cited by: §A.2, §3.2.
  • Mohri (2026) M. Mohri Rational transductors. External Links: 2602.07599, Link Cited by: §D.1.
  • Peng et al. (2025) B. Peng, R. Zhang, D. Goldstein, E. Alcaide, X. Du, H. Hou, J. Lin, J. Liu,
    J. Lu, W. Merrill, G. Song, K. Tan, S. Utpala, N. Wilce, J. S. Wind, T. Wu, D. Wuttke, and C.
    Zhou-Zheng RWKV-7 ”goose” with expressive dynamic state evolution. In COLM, External Links: 
    Link Cited by: §B.2.1, §B.2.1, §B.2.1, §B.3, §B.3, §B.3, §1, §1, §1, 1st item, §2.3, §4, §5.1,
    §5.1, §7, Definition 15.
  • Peng et al. (2018) H. Peng, R. Schwartz, S. Thomson, and N. A. Smith Rational recurrences. In 
    EMNLP, External Links: Link, Document Cited by: §D.1, §1, §7.
  • Qwen Team (2026) Qwen Team Qwen3.5-omni technical report. External Links: 2604.15804, Link 
    Cited by: §7.
  • Sarrof et al. (2024) Y. Sarrof, Y. Veitsman, and M. Hahn The expressive capacity of state space
    models: a formal language perspective. In Proceedings of the 38th International Conference on
    Neural Information Processing Systems, NIPS ’24, Red Hook, NY, USA. External Links: ISBN
    9798331314385 Cited by: §6.
  • Schlag et al. (2021) I. Schlag, K. Irie, and J. Schmidhuber Linear transformers are secretly
    fast weight programmers. In ICML, pp. 9355–9366. External Links: Link Cited by: §5.1.
  • Siegelmann and Sontag (1995) H.T. Siegelmann and E.D. Sontag On the computational power of
    neural nets. Journal of Computer and System Sciences 50 (1), pp. 132–150. External Links: ISSN
    0022-0000, Document, Link Cited by: §3.1, §3.1.
  • Sipser (1997) M. Sipser Introduction to the theory of computation. PWS Publishing Company. 
    Cited by: §A.2, §3.2.
  • Strobl et al. (2024) L. Strobl, W. Merrill, G. Weiss, D. Chiang, and D. Angluin What formal
    languages can transformers express? a survey. TACL 12, pp. 543–561. External Links: Link,
    Document Cited by: §2.5.
  • Terzic et al. (2025) A. Terzic, N. Menet, M. Hersche, T. Hofmann, and A. Rahimi Structured
    sparse transition matrices to enable state tracking in state-space models. In The Thirty-ninth
    Annual Conference on Neural Information Processing Systems, External Links: Link Cited by:
    Appendix C, §1, §5.2, §5.2, §5.2, Definition 6.
  • Vollmer (1999) H. Vollmer Introduction to circuit complexity. Springer. Cited by: §2.5, §4,
    §5.1.
  • Waleffe et al. (2024) R. Waleffe, W. Byeon, D. Riach, B. Norick, V. Korthikanti, T. Dao, A. Gu,
    A. Hatamizadeh, S. Singh, D. Narayanan, G. Kulshreshtha, V. Singh, J. Casper, J. Kautz, M.
    Shoeybi, and B. Catanzaro An empirical study of mamba-based language models. External Links: 
    2406.07887, Link Cited by: §7.
  • Weiss et al. (2018) G. Weiss, Y. Goldberg, and E. Yahav On the practical computational power of
    finite precision RNNs for language recognition. In Proceedings of the 56th Annual Meeting of
    the Association for Computational Linguistics (Volume 2: Short Papers), I. Gurevych and Y.
    Miyao (Eds.), Melbourne, Australia, pp. 740–745. External Links: Link, Document Cited by: §A.2,
    §1, §3.2, footnote 5.
  • Yang et al. (2026) A. Yang, C. Watson, A. Xue, S. Bhattamishra, J. Llarena, W. Merrill, E. D.
    S. Ferreira, A. Svete, and D. Chiang The transformer cookbook. TMLR. External Links: Link Cited
    by: §A.1, §A.2.
  • Yang et al. (2025) S. Yang, J. Kautz, and A. Hatamizadeh Gated delta networks: improving mamba2
    with delta rule. In The Thirteenth International Conference on Learning Representations, 
    External Links: Link Cited by: §1, §2.4.
  • Yang et al. (2024) S. Yang, B. Wang, Y. Zhang, Y. Shen, and Y. Kim Parallelizing linear
    transformers with the delta rule over sequence length. In NeurIPS, External Links: Link Cited
    by: §1, 2nd item.
  • Yang and Zhang (2024) S. Yang and Y. Zhang FLA: a triton-based library for hardware-efficient
    implementations of linear attention mechanism. External Links: Link Cited by: §6.

Appendix A Nonlinear RNNs

See 1

Proof.

Given a log-precision RNN of depth dd, we will simulate it left-to-right using a Turing machine TT.
Let s′=max⁡{s,log⁡n}s^{\prime}=\max\{s,\log n\}. At each step tt, for each layer, TT stores the
hidden state hth_{t} at that layer using ss bits. Given ht−1h_{t-1} and input xtx_{t}, by Lemma 1,
hth_{t} can be computed in 𝖥𝖮\mathsf{FO}-uniform 𝖳𝖢0\mathsf{TC}^{0}, which is contained in 𝖫\mathsf
{L}. Thus, TT can compute hth_{t} using O⁡(max⁡{s,log⁡n})O(\max\{s,\log n\}) space^6^6 6 Here log⁡n
\log n space is for intermediate computation of the 𝖫\mathsf{L} machine, and ss space is for its
output. and poly⁡(n)\mathrm{poly}(n) time. This space can be reused for computing ht+1h_{t+1},
ht+2h_{t+2}, etc. Overall, the Turing machine uses d⋅O⁡(max⁡{s,log⁡n})d\cdot O(\max\{s,\log n\})
bits of storage space and runs in poly⁡(n)\mathrm{poly}(n) time, as desired. ∎

A.1 Polynomial Precision

See 1

Proof.

The idea is the same as the proof of Lemma 3, except that we will simulate an automaton with stacks
of binary values rather than counters. The stacks allow update operations 𝗉𝗎𝗌𝗁0,𝗉𝗎𝗌𝗁1,𝗉𝗈𝗉,\mathsf
{push}_{0},\mathsf{push}_{1},\mathsf{pop}, and 𝗇𝗈𝗈𝗉\mathsf{noop}. Additionally, it has a 𝗁𝖾𝖺𝖽\
mathsf{head}, which is the top element. We view each stack ss as a positive number s∈[0,2]s\in[0,2]
that starts off as s=1s=1 and is modified using the following update operations:

 𝗁𝖾𝖺𝖽⁡(s)\displaystyle\ =𝟙[s≥1]\displaystyle=\mathbbm{1}[s\geq 1]
 mathsf{head}(s)
 𝗉𝗎𝗌𝗁v​(s)\displaystyle =v+12​s\displaystyle=v+\frac{1}{2}s
 \mathsf{push}_{v}(s)
 𝗉𝗈𝗉⁡(s)\displaystyle\  ={2​(s−𝗁𝖾𝖺𝖽​(s))if​s≠1𝗇𝗈𝗈𝗉⁡(s)otherwise\displaystyle=\begin{cases}2(s-\
 mathsf{pop}(s)         mathsf{head}(s))&\textrm{if}\;s\neq 1\\ \mathsf{noop}(s)&\textrm
                        {otherwise}\end{cases}
 𝗇𝗈𝗈𝗉⁡(s)\displaystyle\ =s.\displaystyle=s.
 mathsf{noop}(s)

Moreover, we will let st∈[0,2]ks_{t}\in[0,2]^{k} denote the vector of stacks at time tt. The update
operation ut∈{𝗁𝖾𝖺𝖽,𝗉𝗎𝗌𝗁,𝗉𝗈𝗉,𝗇𝗈𝗈𝗉}u_{t}\in\{\mathsf{head},\mathsf{push},\mathsf{pop},\mathsf{noop}\}
is determined via a finite lookup table uu as a function of qtq_{t}, σt\sigma_{t}, and 𝗁𝖾𝖺𝖽⁡(st)\
mathsf{head}(s_{t}). The state transition δ\delta depends on the same inputs.

The implementation of these operations with a ReLU network follows the same idea as the proof of
Lemma 3. We first construct a ReLU network to compute 𝗁𝖾𝖺𝖽⁡(s)\mathsf{head}(s) from ss as a
threshold with ϵ=1/3\epsilon=1/3 (Yang et al., 2026, Section 4.7). Next, we compute qt=δ⁡
(qt−1,σt,𝗁𝖾𝖺𝖽⁡(st))q_{t}=\delta(q_{t-1},\sigma_{t},\mathsf{head}(s_{t})) and ut=u⁡(qt−1,σt,𝗁𝖾𝖺𝖽⁡
(st−1))u_{t}=u(q_{t-1},\sigma_{t},\mathsf{head}(s_{t-1})) as finite lookup tables (Yang et al.,
2026, Section 4.8). At this point, we have the correct next state, and it just remains to be shown
that we can update the stacks. Let 𝗉𝗈𝗉1​(s)=2​s−2\mathsf{pop}_{1}(s)=2s-2 and 𝗉𝗈𝗉0​(s)=2​s\mathsf
{pop}_{0}(s)=2s. We compute the possible updated stacks 𝗉𝗎𝗌𝗁v​(st−1),𝗉𝗈𝗉v​(st−1),\mathsf{push}_{v}
(s_{t-1}),\mathsf{pop}_{v}(s_{t-1}), and 𝗇𝗈𝗈𝗉⁡(st−1)\mathsf{noop}(s_{t-1}) with parallel ReLU
networks, using the fact that each is a linear function. For each stack [st]i[s_{t}]_{i}, we then
compute its new value from these u⁡(st−1)u(s_{t-1})’s using a selector gadget based on [ut]i[u_{t}]
_{i}, 𝗁𝖾𝖺𝖽​(st−1)i\mathsf{head}(s_{t-1})_{i}, and whether s=1s=1 (Yang et al., 2026, Section 4.9).
Thus, we can correctly compute qtq_{t} and sts_{t} at each step. ∎

See 2

Proof.

We leverage the same idea as Barcelo et al. (2026, Section 4.8). Let LL be any 𝖯\mathsf{P}-complete
language under 𝖥𝖮\mathsf{FO} reductions. By construction 𝖫\mathsf{L} can be recognized by a
poly-time Turing machine and thus also by a multi-stack machine running in time O⁡(nc)O(n^{c}) for
some cc. Define a new padded language L′={w​□|w|c∣w∈L}L^{\prime}=\{w\square^{\lvert w\rvert^{c}}\
mid w\in L\}. Then L′L^{\prime} is 𝖯\mathsf{P}-complete and can be solved by a multi-stack machine,
and thus also by an MLP RNN by Theorem 1. ∎

A.2 Logarithmic Precision

See 2

Proof.

We describe a simple 𝖥𝖮\mathsf{FO} reduction from deterministic (but not necessarily sorted) graph
connectivity. The latter is well-known to be 𝖫\mathsf{L}-complete (Jones, 1975; Cook and McKenzie,
1987; Sipser, 1997, cf.), and is in fact also complete under 𝖥𝖮\mathsf{FO}-reductions just like the
general graph connectivity problem (Immerman, 1998). We are given a deterministic graph GG with
source s=1s=1, target t=nt=n, and edges

 (i1,j1),…,(im,jm)(i_{1},j_{1}),\ldots,(i_{m},j_{m})

with ik,jk∈{1,…,n}i_{k},j_{k}\in\{1,\ldots,n\}, for each k=1,…,mk=1,\ldots,m, and i1=si_{1}=s and
jm=t=nj_{m}=t=n. We create a new deterministic graph G′G^{\prime} with edges

 (i1+h​n,j1+(h+1)​n),…,(im+h​n,jm+(h+1)​n)(i_{1}+hn,j_{1}+(h+1)n),\ldots,(i_{m}+hn,j_{m}+(h+1)n)

for each h=0,…,m−1h=0,\ldots,m-1. We also add an additional node vF=n​m+1v_{F}=nm+1 and edges
(n+h​n,vF)(n+hn,v_{F}). Note that nn has no outgoing edge in GG, so G′G^{\prime} remains
deterministic. That is, v0,…,vsv_{0},\ldots,v_{s} is a path in GG iff v0,v1+n,v2+2​n,…,vs+s​nv_
{0},v_{1}+n,v_{2}+2n,\ldots,v_{s}+sn is a path in G′G^{\prime}. Thus, tt is reachable from ss in GG
iff vFv_{F} is reachable from ss in G′G^{\prime}.

The above reduction can easily be seen to be implementable by a logspace-uniform 𝖠𝖢0\mathsf{AC}^{0}
reduction. The values h​nhn (for each h=1,…,mh=1,\ldots,m) can be precomputed by a logspace machine
since this depends only on the input size (more precisely, one can enumerate enough of h​nhn for up
to m=O⁡(n)m=O(n)). The 𝖠𝖢0\mathsf{AC}^{0} circuit simply needs to perform simple additions. ∎

See 2

Our proof of this theorem will leverage the computational model of counter machines (Fischer et
al., 1968; Weiss et al., 2018; Merrill, 2021, CMs;). Also called counter automata, CMs are a
generalization of finite automata augmented with a fixed number of integer-valued registers. CMs
been heavily used to analyze the expressive power of RNNs (Weiss et al., 2018; Merrill, 2019).
Lemma 2 shows a counter machine can solve sorted deterministic graph connectivity. Moreover, Lemma 
3 shows a log-precision RNN with a ReLU MLP gating function can simulate a counter machine. Thus,
we conclude that a log-precision RNN with ReLU MLP gating can also solve sorted deterministic graph
connectivity.

Lemma 2.

Sorted deterministic graph connectivity can be solved by counter machine.

Proof.

Fix a unary encoding of an instance

 (s,(i1,j1),…,(in,jn),t)(s,(i_{1},j_{1}),\dots,(i_{n},j_{n}),t)

with separators between ss, each imi_{m}, each jmj_{m}, and tt. We assume that vertices are
numbered in a topological order and encoded in unary by their numbers, so for every edge (im,jm)(i_
{m},j_{m}) we have jm≥imj_{m}\geq i_{m}. The machine uses three counters S,I,TS,I,T and O⁡(1)O(1)
states to maintain the invariant that, after kk edges, SS encodes the furthest state reachable from
ss.

The first step is to process the initial block 0s0^{s}. The machine starts in a state qsq_{s} and
increments SS on each symbol (leaving I,TI,T at 00), so after this block S=sS=s.

We next enter a state q𝖾𝖽𝗀𝖾q_{\mathsf{edge}} and process each edge (im,jm)(i_{m},j_{m}) in turn,
maintaining the invariant that after mm edges we have S=smS=s_{m} (the node reached from ss after
the first mm edges) and I=0I=0. For edge (im,jm)(i_{m},j_{m}), in a state qiq_{i} the machine reads
the unary block 0im0^{i_{m}} and on each symbol performs

 S←S−1,I←I+1.S\leftarrow S-1,\qquad I\leftarrow I+1.

At the following separator it tests whether S=0S=0 using the zero-mask. If S=0S=0 (i.e. sm−1=ims_
{m-1}=i_{m}), it moves to a state qj=q_{j}^{=} and on each symbol of the jmj_{m}-block simply does
S←S+1S\leftarrow S+1 (leaving II unchanged); since SS starts at 00, after 0jm0^{j_{m}} we have S=
jmS=j_{m}, and at the next separator it resets I←0I\leftarrow 0 and returns to q𝖾𝖽𝗀𝖾q_{\mathsf
{edge}}, so sm=jms_{m}=j_{m}. If instead S≠0S\neq 0, the machine moves to a state qj≠q_{j}^{\neq}.
On each symbol of the jmj_{m}-block it now performs

 S←S+1,I←I−1if ​I≠0,and does nothing if ​I=0,S\leftarrow S+1,\ I\leftarrow I-1\quad\text{if }I\neq
 0,\qquad\text{and does nothing if }I=0,

where the choice again uses only the zero-mask bit of II. Initially I=imI=i_{m}, so after min⁡
{im,jm}\min\{i_{m},j_{m}\} symbols we have added that many units back to SS and subtracted them
from II. Because jm≥imj_{m}\geq i_{m}, by the end of the jmj_{m}-block we have I=0I=0 and

 S=(sm−1−im)+im=sm−1,S=(s_{m-1}-i_{m})+i_{m}=s_{m-1},

and at the next separator we return to q𝖾𝖽𝗀𝖾q_{\mathsf{edge}} with S=sm=sm−1S=s_{m}=s_{m-1} and I=
0I=0. Thus the invariant is preserved across every edge.

After the last edge the machine enters a state qtq_{t} with S=snS=s_{n} and I=0I=0. On the target
block 0t0^{t} it updates

 S←S−1,T←T+1S\leftarrow S-1,\qquad T\leftarrow T+1

on each symbol, so at the end S=sn−tS=s_{n}-t. On the end-of-input marker it moves to an accepting
state iff the zero-mask bit of SS is 00, i.e. iff S=0S=0, which is equivalent to sn=ts_{n}=t. ∎

Lemma 3.

Let MM be a real-time counter machine. There exists a one-layer MLP RNN with log precision that
recognizes the same language as MM.

Proof.

We divide partition the hidden state hth_{t} into three parts: sts_{t}, which encodes a finite
state as a one-hot vector, ct+c^{+}_{t}, which encodes the value of positive counters, and ct−c^{-}
_{t}, which encodes the absolute value of negative counters. Define qtq_{t} as the state encoded by
sts_{t} and ct=ct+−ct−c_{t}=c^{+}_{t}-c^{-}_{t}. We will show how to construct an MLP ff such that,
applying ht=f⁡(ht−1,xt)h_{t}=f(h_{t-1},x_{t}), we will have st,ct+,ct−s_{t},c^{+}_{t},c^{-}_{t}
such that

 qt\displaystyle q_  =δ⁡(qt−1,σt,z⁡(ct−1))\displaystyle=\delta(q_{t-1},\sigma_{t},z(c_{t-1}))
 {t}
 ct\displaystyle c_  =u⁡(qt−1,σt,z⁡(ct−1))​(ct−1).\displaystyle=u(q_{t-1},\sigma_{t},z(c_{t-1}))
 {t}                 (c_{t-1}).

We first construct a ReLU network to compute z⁡(ct−1)z(c_{t-1}) as a function of ct−1+c^{+}_{t-1}
and ct−1−c^{-}_{t-1} using the standard construction for equality checks with a tolerance of ϵ=1/3\
epsilon=1/3 (Yang et al., 2026, Section 4.7). We then construct ReLU networks to compute δ\delta
and uu (with output encoded as one-hot vectors) given qt−1,σt,z⁡(ct−1)q_{t-1},\sigma_{t},z(c_
{t-1}), which is possible since both are finite lookup tables (Yang et al., 2026, Section 4.8). We
compute zz with δ\delta to get a one-hot encoding of qt=δ⁡(qt−1,σt,z⁡(ct−1))q_{t}=\delta(q_{t-1},\
sigma_{t},z(c_{t-1})), i.e., our updated state vector sts_{t}. Similarly, we compose zz with uu to
get a one-hot encoding of the update function ut=u⁡(qt−1,σt,z⁡(ct−1))u_{t}=u(q_{t-1},\sigma_{t},z
(c_{t-1})) to apply to the counters. The new values of ct+c^{+}_{t} and ct−c^{-}_{t} can be
obtained by a ReLU network that computes several continuous piecewise-linear functions of
ct−1+,ct−1−c^{+}_{t-1},c^{-}_{t-1} in parallel for different updates u∈{×0,+0,+1,−1}u\in\{{\times}
0,{+}0,{+}1,{-}1\} (Yang et al., 2026, Section 4.1) and then applies a conditional selector for u=
utu=u_{t} (Yang et al., 2026, Section 4.9).

We can then accept or reject a string of length nn based on sn,cn+,cn−s_{n},c^{+}_{n},c^{-}_{n}
using an MLP. The construction uses log precision because qtq_{t} is in a finite set and ct+,ct−=O⁡
(n)c^{+}_{t},c^{-}_{t}=O(n) . ∎

Appendix B Lower Bounds for DPLR LRNNS

This section contains four simulation results, organized by architecture and target computation:

  • •
   
    RWKV-7 simulates WFAs over ℚ\mathbb{Q} (Subsection B.2).

  • •
   
    RWKV-7 computes iterated 3×33\times 3 matrix multiplication over ℚ\mathbb{Q} (Subsection B.3).

  • •
   
    DeltaNet simulates WFAs over ℚ\mathbb{Q} (Subsection B.4).

  • •
   
    DeltaNet computes iterated 3×33\times 3 matrix multiplication over ℚ\mathbb{Q} (Subsection B.5
    ).

B.1 Preliminaries

Definition 13 (WFA Path Form).

Let ⟨𝕂,⊕,⊗⟩\langle\mathbb{K},\oplus,\otimes\rangle be a semiring. A weighted finite automaton (WFA)
is a tuple 𝒜=(Q,Σ,{Mσ}σ∈Σ,α,ω)\mathcal{A}=(Q,\Sigma,\{M_{\sigma}\}_{\sigma\in\Sigma},\alpha,\omega)
where Q={1,…,n}Q=\{1,\dots,n\} is a finite state set, each Mσ∈𝕂n×nM_{\sigma}\in\mathbb{K}^{n\times
n} is a transition matrix, and α,ω∈𝕂n\alpha,\omega\in\mathbb{K}^{n} are initial/final weight
vectors, respectively. The WFA assigns a score to a string w=w1⋯wn∈Σ∗w=w_{1}\cdots w_{n}\in\Sigma^
{\ast} via

 f𝒜​(w)=⨁π={(sk,tk)}k=1nαπ1⊗(⨂i=1[Mwi]πk,πk+1)⊗ωπn,f_{\mathcal{A}}(w)=\bigoplus_{\pi=\{(s_{k},t_
 {k})\}_{k=1}^{n}}\alpha_{\pi_{1}}\otimes\left(\bigotimes_{i=1}[M_{w_{i}}]_{\pi_{k},\pi_{k+1}}\
 right)\otimes\omega_{\pi_{n}},

where π\pi iterates over all paths of states of length nn.

Equivalently, defining matrix multiplication over ⟨𝕂,⊕,⊗⟩\langle\mathbb{K},\oplus,\otimes\rangle,
the computation of a WFA can be defined according to

 f𝒜​(w)=α⊤⊗Mw1⊗…⊗Mwn⊗ω∈𝕂.f_{\mathcal{A}}(w)=\alpha^{\top}\otimes M_{w_{1}}\otimes\ldots\otimes M_
 {w_{n}}\otimes\omega\in\mathbb{K}.

Definition 14 (Streaming iterated 3×33\times 3 matrix multiplication over ℤ\mathbb{Z}).

The input is a token stream of length 9​N9N over alphabet ℤ\mathbb{Z}, partitioned into NN
consecutive blocks of length 99. The tt-th block encodes a matrix A(t)∈ℤ3×3A^{(t)}\in\mathbb{Z}^{3\
times 3} in row-major order:

 (A1,1(t),A1,2(t),A1,3(t),A2,1(t),A2,2(t),A2,3(t),A3,1(t),A3,2(t),A3,3(t)).\bigl(A^{(t)}_{1,1},A^
 {(t)}_{1,2},A^{(t)}_{1,3},\;A^{(t)}_{2,1},A^{(t)}_{2,2},A^{(t)}_{2,3},\;A^{(t)}_{3,1},A^{(t)}_
 {3,2},A^{(t)}_{3,3}\bigr).

The task is to output the product

 PN:=A(1)A(2)⋯A(N)∈ℤ3×3(equivalently, its 9 entries).P_{N}:=A^{(1)}A^{(2)}\cdots A^{(N)}\in\mathbb
 {Z}^{3\times 3}\qquad\text{(equivalently, its $9$ entries).}

B.2 Simulating WFAs with RWKV-7

Let 𝐞i∈𝕂d\mathbf{e}_{i}\in\mathbb{K}^{d} denote the iith standard basis column vector.

Definition 15 (RWKV-style transition matrix (Peng et al., 2025)).

Fix a dimension dd. Given w,a,κ∈𝕂dw,a,\kappa\in\mathbb{K}^{d} and a scalar λ∈𝕂\lambda\in\mathbb{K},
define

 A⁡(w,a,κ,λ):=diag⁡(w)−λ​κ​(a⊙κ)⊤,A(w,a,\kappa;\lambda)\;:=\;\mathrm{diag}(w)\;-\;\lambda\,\kappa
 \,(a\odot\kappa)^{\top},

where ⊙\odot is elementwise product.

To handle WFA transition matrices, we use a primitive that overwrites one coordinate by a dot
product of the current state against a prescribed coefficient vector.

Definition 16 (Dot-product overwrite matrix).

Fix a dimension dd, an index dst∈{1,…,d}\mathrm{dst}\in\{1,\dots,d\}, and a coefficient vector
c∈𝕂dc\in\mathbb{K}^{d} with cdst=0c_{\mathrm{dst}}=0. Define

 U⁡(dst,c):=I−𝐞dst​𝐞dst⊤+c​𝐞dst⊤∈𝕂d×d.U(\mathrm{dst};c):=I-\mathbf{e}_{\mathrm{dst}}\mathbf{e}_{\
 mathrm{dst}}^{\top}+c\,\mathbf{e}_{\mathrm{dst}}^{\top}\in\mathbb{K}^{d\times d}.

Equivalently, U⁡(dst,c)U(\mathrm{dst};c) is the identity matrix with its dst\mathrm{dst}-th column
replaced by cc.

Lemma 4 (Effect on row vectors).

For any row vector r∈𝕂1×dr\in\mathbb{K}^{1\times d},

 (rU(dst;c))dst=∑i=1drici,(rU(dst;c))j=rj(j≠dst).(r\,U(\mathrm{dst};c))_{\mathrm{dst}}=\sum_{i=1}^
 {d}r_{i}c_{i},\qquad(r\,U(\mathrm{dst};c))_{j}=r_{j}\ \ (j\neq\mathrm{dst}).

Lemma 5 (Dot-product overwrite as an RWKV transition).

Fix dd, dst\mathrm{dst}, and c∈𝕂dc\in\mathbb{K}^{d} with cdst=0c_{\mathrm{dst}}=0. Set

 w=𝟏,a=𝐞dst,κ=𝐞dst−c,λ=1.w=\mathbf{1},\qquad a=\mathbf{e}_{\mathrm{dst}},\qquad\kappa=\mathbf{e}_
 {\mathrm{dst}}-c,\qquad\lambda=1.

Then A⁡(w,a,κ,1)=U⁡(dst,c)A(w,a,\kappa;1)=U(\mathrm{dst};c).

Proof.

Since a=𝐞dsta=\mathbf{e}_{\mathrm{dst}}, we have a⊙κ=κdst​𝐞dsta\odot\kappa=\kappa_{\mathrm{dst}}\
mathbf{e}_{\mathrm{dst}}. Because cdst=0c_{\mathrm{dst}}=0, κdst=1\kappa_{\mathrm{dst}}=1, so a⊙κ=
𝐞dsta\odot\kappa=\mathbf{e}_{\mathrm{dst}}. Thus

 A=I−κ​𝐞dst⊤=I−(𝐞dst−c)​𝐞dst⊤=I−𝐞dst​𝐞dst⊤+c​𝐞dst⊤=U⁡(dst,c).∎A=I-\kappa\,\mathbf{e}_{\mathrm
 {dst}}^{\top}=I-(\mathbf{e}_{\mathrm{dst}}-c)\mathbf{e}_{\mathrm{dst}}^{\top}=I-\mathbf{e}_{\
 mathrm{dst}}\mathbf{e}_{\mathrm{dst}}^{\top}+c\,\mathbf{e}_{\mathrm{dst}}^{\top}=U(\mathrm{dst};
 c).\qed

We now show that any n×nn\times n matrix can be applied to a row vector using Θ⁡(n)\Theta(n)
overwrite steps, provided we have nn additional scratch coordinates.

Lemma 6 (Apply an arbitrary n×nn\times n matrix using 2​n2n overwrites).

Let P∈𝕂n×nP\in\mathbb{K}^{n\times n}. Consider row vectors in dimension 2​n2n written as [x∣s][x\
mid s] with x,s∈𝕂1×nx,s\in\mathbb{K}^{1\times n}. There exist m:=2​nm:=2n overwrite matrices G1​
(P),…,Gm​(P)∈𝕂2​n×2​nG_{1}(P),\dots,G_{m}(P)\in\mathbb{K}^{2n\times 2n} such that for all x,sx,s,

 [x∣s]G1(P)⋯Gm(P)=[xP∣xP].[x\mid s]\;G_{1}(P)\cdots G_{m}(P)\;=\;[xP\mid xP].

Proof.

Phase 1 (compute x​PxP into scratch). For each j∈{1,…,n}j\in\{1,\dots,n\}, define c(j)∈𝕂2​nc^{(j)}\
in\mathbb{K}^{2n} by ci(j)=Pi,jc^{(j)}_{i}=P_{i,j} for 1≤i≤n1\leq i\leq n and ci(j)=0c^{(j)}_{i}=0
for n+1≤i≤2​nn+1\leq i\leq 2n. Let Gj​(P):=U⁡(n+j,c(j))G_{j}(P):=U(n+j;c^{(j)}). By Lemma 4, this
overwrites scratch coordinate n+jn+j with ∑i=1nxi​Pi,j=(x​P)j\sum_{i=1}^{n}x_{i}P_{i,j}=(xP)_{j},
leaving all other coordinates unchanged. After nn steps, [x∣s]G1(P)⋯Gn(P)=[x∣xP][x\mid s]G_{1}(P)\
cdots G_{n}(P)=[x\mid xP].

Phase 2 (copy scratch back to main). For each j∈{1,…,n}j\in\{1,\dots,n\} let d(j):=𝐞n+j∈𝕂2​nd^
{(j)}:=\mathbf{e}_{n+j}\in\mathbb{K}^{2n} and set Gn+j​(P):=U⁡(j,d(j))G_{n+j}(P):=U(j;d^{(j)}).
This overwrites main coordinate jj with the dot product against 𝐞n+j\mathbf{e}_{n+j}, i.e. the
current scratch coordinate n+j=(x​P)jn+j=(xP)_{j}, leaving scratch unchanged. Thus after nn more
steps, [x∣xP]Gn+1(P)⋯G2​n(P)=[xP∣xP][x\mid xP]G_{n+1}(P)\cdots G_{2n}(P)=[xP\mid xP]. ∎

B.2.1 Simulation theorem

Theorem 9 (RWKV-7 simulates weighted finite automata over ℚ\mathbb{Q}).

Fix 𝕂=ℚ\mathbb{K}=\mathbb{Q}. Let 𝒜=(Q,Σ,{Mσ},α,ω)\mathcal{A}=(Q,\Sigma,\{M_{\sigma}\},\alpha,\
omega) be an nn-state WFA over 𝕂\mathbb{K} (Definition 13), and let w=w1⋯wTw=w_{1}\cdots w_{T} be
an input word. Then there exists a 44-layer RWKV-7 network whose output at position TT equals f𝒜(w)
=αMw1⋯MwTω∈ℚf_{\mathcal{A}}(w)=\alpha M_{w_{1}}\cdots M_{w_{T}}\omega\in\mathbb{Q}.

Proof.

We use the “3-layer router + 1-layer multiplicative simulator” template of Peng et al. (2025)
(Appendix D), instantiated with a block length m:=2​nm:=2n and arithmetic dimension 2​n2n.

Block parameters.

Let m:=2​nm:=2n. Write each position as t=ℓ​m+τt=\ell m+\tau with ℓ≥0\ell\geq 0 and 1≤τ≤m1\leq\tau\
leq m. Introduce a padding token ⊥\bot and set wt:=⊥w_{t}:=\bot for all t≤0t\leq 0, with M⊥:=IM_{\
bot}:=I. Let the previous block string be

 w~(ℓ):=w(ℓ−1)​m+1⋯wℓ​m∈(Σ∪{⊥})m,\tilde{w}^{(\ell)}:=w_{(\ell-1)m+1}\cdots w_{\ell m}\in(\Sigma\
 cup\{\bot\})^{m},

which is well-defined for all ℓ\ell due to padding.

Offline factorization of block products.

For each length-mm block string w~=w~1⋯w~m\tilde{w}=\tilde{w}_{1}\cdots\tilde{w}_{m}, define the
block product

 P(w~):=Mw~1⋯Mw~m∈𝕂n×n.P(\tilde{w}):=M_{\tilde{w}_{1}}\cdots M_{\tilde{w}_{m}}\in\mathbb{K}^{n\
 times n}.

Apply Lemma 6 to obtain overwrite matrices Gw~,1,…,Gw~,m∈𝕂2​n×2​nG_{\tilde{w},1},\dots,G_{\tilde
{w},m}\in\mathbb{K}^{2n\times 2n} such that for all x,s∈𝕂1×nx,s\in\mathbb{K}^{1\times n},

 [x∣s]​∏i=1mGw~,i=[x​P​(w~)∣x​P​(w~)].[x\mid s]\;\prod_{i=1}^{m}G_{\tilde{w},i}\;=\;[xP(\tilde{w})
 \mid xP(\tilde{w})].

Fix one such factorization for each w~\tilde{w}.

Router (layers 1–3).

Define a lookup table Ξ\Xi whose key is

 (tmod2​m,wt,wt−1,…,wt−(2​m−1)),\Bigl(\;t\bmod 2m,\;w_{t},w_{t-1},\dots,w_{t-(2m-1)}\;\Bigr),

and whose outputs are:

 1. 1.
   
    the next factor Gw~(ℓ),τG_{\tilde{w}^{(\ell)},\tau} to apply at time tt (encoded so that the
    arithmetic layer can set (w,a,κ)(w,a,\kappa) accordingly, using Lemma 5);

 2. 2.
   
    a “completion” readout vector ω^t\hat{\omega}_{t} (defined below).

Both depend only on the key: w~(ℓ)\tilde{w}^{(\ell)} is contained in the last 2​m2m tokens whenever
ℓ≥1\ell\geq 1 (and is all padding when ℓ=0\ell=0), and τ\tau is determined by tmod2​mt\bmod 2m. By
the finite-window lookup construction of Peng et al. (2025) (Appendix D; instantiated with block
length mm), a 3-layer RWKV-7 can compute Ξ\Xi at every position and write its outputs into the
residual stream.

Arithmetic layer (layer 4).

Use a single wkv head of dimension 2​n2n. Initialize the first row of the wkv matrix state using a
single additive update on the first token: set v1=𝐞1v_{1}=\mathbf{e}_{1} and k~1=[α∣α]\tilde{k}_{1}
=[\alpha\mid\alpha], so the state becomes 𝐞1⊤​k~1\mathbf{e}_{1}^{\top}\tilde{k}_{1}. For all t≥2t\
geq 2, set the additive term to 00 (i.e. vt=k~t=𝟎v_{t}=\tilde{k}_{t}=\mathbf{0}), so evolution is
purely multiplicative: St=St−1​AtS_{t}=S_{t-1}\,A_{t}.

At each time tt, the router specifies the factor Gw~(ℓ),τG_{\tilde{w}^{(\ell)},\tau}. Choose RWKV
transition parameters so that At=Gw~(ℓ),τA_{t}=G_{\tilde{w}^{(\ell)},\tau}; since each factor is an
overwrite matrix, this is possible by Lemma 5.

Completion vector and correctness.

Let ptp_{t} denote the first row of the arithmetic layer’s wkv state StS_{t}. As in Peng et al.
(2025) (Appendix D), one checks by induction that for t=ℓ​m+τt=\ell m+\tau,

 pt=[α∣α]​(∏b=0ℓ−2∏i=1mGw~(b),i)​(∏i=1τGw~(ℓ−1),i),p_{t}=[\alpha\mid\alpha]\;\Bigl(\prod_{b=0}^{\
 ell-2}\prod_{i=1}^{m}G_{\tilde{w}^{(b)},i}\Bigr)\;\Bigl(\prod_{i=1}^{\tau}G_{\tilde{w}^{(\
 ell-1)},i}\Bigr),

with the convention that empty products are II.

Define the within-block prefix product

 Rt:=Mwℓ​m+1⋯Mwt,vt:=Rtω∈𝕂n×1.R_{t}:=M_{w_{\ell m+1}}\cdots M_{w_{t}},\qquad v_{t}:=R_{t}\,\omega\
 in\mathbb{K}^{n\times 1}.

Now define the completion vector:

 ω^t:=(∏i=τ+1mGw~(ℓ−1),i)⋅[vt𝟎]∈𝕂2​n×1.\hat{\omega}_{t}:=\left(\prod_{i=\tau+1}^{m}G_{\tilde{w}^
 {(\ell-1)},i}\right)\cdot\begin{bmatrix}v_{t}\\ \mathbf{0}\end{bmatrix}\in\mathbb{K}^{2n\times
 1}.

This ω^t\hat{\omega}_{t} is a fixed function of the router key (position mod 2​m2m and last 2​m2m
tokens), hence it can be returned by the lookup table Ξ\Xi.

Finally, compute the scalar readout pt​ω^tp_{t}\hat{\omega}_{t}:

 ptω^t=αMw1⋯Mwtωp_{t}\hat{\omega}_{t}=\alpha\,M_{w_{1}}\cdots M_{w_{t}}\,\omega

using Lemma 6 on each completed block. In particular, at t=Tt=T the RWKV-7 output equals f𝒜​(w)f_{\
mathcal{A}}(w). ∎

B.3 Iterated 3×33\times 3 Matrix Multiplication with RWKV-7

Theorem 10 (RWKV-7 computes iterated 3×33\times 3 matrix products over ℚ\mathbb{Q}).

Fix 𝕂=ℚ\mathbb{K}=\mathbb{Q}. There exists a 44-layer RWKV-7 network such that, given the
length-9​N9N stream encoding A(1),…,A(N)A^{(1)},\dots,A^{(N)}, the network outputs the 99 entries
of PN:=A(1)A(2)⋯A(N)P_{N}:=A^{(1)}A^{(2)}\cdots A^{(N)} (equivalently, vec⁡(PN)∈ℚ9\operatorname
{vec}(P_{N})\in\mathbb{Q}^{9}) at the final position t=9​Nt=9N.

Proof sketch; full proof follows later.

Layers 1–2 compute the position modulo 1818. This yields (i) the within-block offset τ∈{1,…,9}\tau\
in\{1,\dots,9\} and (ii) a block-parity bit b∈{0,1}b\in\{0,1\} that we use to alternate between two
halves of an arithmetic state. Layer 3 stores the last 1818 tokens (two consecutive length-99
blocks) and, from the key (tmod18,wt,…,wt−17)(t\bmod 18,\;w_{t},\dots,w_{t-17}), look up the RWKV
transition parameters needed at time tt. Concretely, at each token it outputs (in the residual
stream) the destination index and coefficient vector for a single dot-product overwrite update
implementing one coordinate of the map x↦x​B​(A(ℓ−1))x\mapsto x\,B(A^{(\ell-1)}), where A(ℓ−1)A^{(\
ell-1)} is the previous block’s matrix. (We treat the pre-sequence padding as “block 00” encoding A
(0):=I3A^{(0)}:=I_{3}.) Layer 4 maintains an 1818-dimensional row state [x(0)∣x(1)]∈ℚ1×18[x^{(0)}\
mid x^{(1)}]\in\mathbb{Q}^{1\times 18} inside the first row of a wkv matrix state, where each half
has length 99. During each length-99 block, layer 4 applies nine dot-product overwrites that
compute a new vector xnew=xold​B​(A(ℓ−1))x_{\text{new}}=x_{\text{old}}\,B(A^{(\ell-1)}) in the
inactive half, using the parity bit bb to select which half is source vs. destination. At the final
position t=9​Nt=9N, the destination half contains vec(A(1)⋯A(N−1))\operatorname{vec}(A^{(1)}\cdots
A^{(N-1)}). A completion readout (computed by the router from the final finite-window key, which
contains the entire last block and hence determines A(N)A^{(N)}) outputs the 99 coordinates of vec
(A(1)⋯A(N))\operatorname{vec}(A^{(1)}\cdots A^{(N)}) via nine dot products. ∎

Proof.

We follow the same “router (layers 1–3) + multiplicative arithmetic (layer 4)” template used in the
RWKV-7 regular-language construction (Appendix D of Peng et al. (2025)).

Step 1: reduce 3×33\times 3 matrix multiplication to a 99-dimensional linear recurrence.

Let vec:ℚ3×3→ℚ1×9\operatorname{vec}:\mathbb{Q}^{3\times 3}\to\mathbb{Q}^{1\times 9} be row-major
vectorization:

 vec⁡(P):=(P1,1,P1,2,P1,3,P2,1,P2,2,P2,3,P3,1,P3,2,P3,3).\operatorname{vec}(P):=(P_{1,1},P_
 {1,2},P_{1,3},P_{2,1},P_{2,2},P_{2,3},P_{3,1},P_{3,2},P_{3,3}).

Define the block-diagonal embedding

 B⁡(A):=[A000A000A]∈ℚ9×9.B(A)\;:=\;\begin{bmatrix}A&0&0\\[2.0pt] 0&A&0\\[2.0pt] 0&0&A\end{bmatrix}
 \in\mathbb{Q}^{9\times 9}.

Right-multiplication by AA acts independently on the three rows of PP, hence

 vec⁡(P​A)=vec⁡(P)​B​(A)for all ​P,A∈ℚ3×3.\operatorname{vec}(PA)=\operatorname{vec}(P)\,B(A)\   (2)
 qquad\text{for all }P,A\in\mathbb{Q}^{3\times 3}.

Let Pt:=A(1)⋯A(t)P_{t}:=A^{(1)}\cdots A^{(t)} and xt:=vec⁡(Pt)x_{t}:=\operatorname{vec}(P_{t}).
Then

 x0=vec⁡(I3),xt=xt−1​B​(A(t)).x_{0}=\operatorname{vec}(I_{3}),\qquad x_{t}=x_{t-1}\,B(A^{(t)}).

Thus it suffices to maintain the 99-dimensional state xtx_{t} and repeatedly apply the linear map
x↦x​B​(A)x\mapsto xB(A).

Step 2: implement x↦x​B​(A)x\mapsto xB(A) with dot-product overwrites in dimension 1818.

We use the dot-product overwrite primitive: for any destination coordinate dst\mathrm{dst} and
coefficient vector cc with cdst=0c_{\mathrm{dst}}=0, the overwrite matrix U⁡(dst,c)U(\mathrm{dst};
c) replaces exactly one coordinate of a row vector with a dot product (Lemmas 4 and 5). In
particular, each overwrite matrix can be realized as a single RWKV multiplicative transition
(Lemma 5).

We maintain an arithmetic row state in dimension 1818 written as

 p=[x(0)∣x(1)],x(0),x(1)∈ℚ1×9,p=[x^{(0)}\mid x^{(1)}],\qquad x^{(0)},x^{(1)}\in\mathbb{Q}^{1\times
 9},

together with a bit b∈{0,1}b\in\{0,1\} indicating the active half x(b)x^{(b)} (source for dot
products). Let π⁡(i,j):=3​(i−1)+j\pi(i,j):=3(i-1)+j be the row-major index map for i,j∈{1,2,3}i,j\
in\{1,2,3\}.

Fix a 3×33\times 3 matrix AA. The coordinates of x​B​(A)xB(A) are length-33 dot products:

 (x​B​(A))π⁡(i,j)=∑k=13xπ⁡(i,k)​Ak,j.(xB(A))_{\pi(i,j)}=\sum_{k=1}^{3}x_{\pi(i,k)}\,A_{k,j}.  (3)

We compute these 99 values into the inactive half x(1−b)x^{(1-b)} using 99 overwrite steps. For
each pair (i,j)∈{1,2,3}2(i,j)\in\{1,2,3\}^{2}, define:

  • •
   
    Destination coordinate in the inactive half:

     dst⁡(b,i,j):= 9​(1−b)+π⁡(i,j)∈{1,…,18}.\mathrm{dst}(b;i,j)\;:=\;9(1-b)+\pi(i,j)\in\{1,\
     dots,18\}.

  • •
   
    Coefficient vector c(b,i,j)​(A)∈ℚ18c^{(b;i,j)}(A)\in\mathbb{Q}^{18} supported only on the three
    active coordinates corresponding to row ii:

     c(b,i,j)​(A) 9​b+π⁡(i,k):=Ak,j(k=1,2,3),c(b,i,j)​(A)u:=0otherwise.c^{(b;i,j)}(A)_{\,9b+\pi
     (i,k)}\;:=\;A_{k,j}\quad(k=1,2,3),\qquad c^{(b;i,j)}(A)_{u}:=0\ \ \text{otherwise}.

Since dst⁡(b,i,j)\mathrm{dst}(b;i,j) lies in the inactive half, we have c(b,i,j)​(A)dst⁡(b,i,j)=0c^
{(b;i,j)}(A)_{\mathrm{dst}(b;i,j)}=0, so the overwrite is well-formed. Applying U⁡(dst⁡(b,i,j),c
(b,i,j)​(A))U(\mathrm{dst}(b;i,j);c^{(b;i,j)}(A)) overwrites precisely the destination coordinate
with the dot product of the current row state against c(b,i,j)​(A)c^{(b;i,j)}(A) (Lemma 4), which
equals the right-hand side of (3). Moreover, because each coefficient vector is supported only on
the active half, these overwrites do not depend on (and hence do not interfere with)
already-written coordinates in the inactive half.

If we apply these overwrites for all (i,j)(i,j) (in any order, e.g. row-major order), then the
inactive half becomes x(b)​B​(A)x^{(b)}B(A) while the active half remains unchanged:

 [x(0)∣x(1)]⟼[x(0)∣x(1)]⋅(∏(i,j)U⁡(dst⁡(b,i,j),c(b,i,j)​(A)))=[x(0)∣x(1)]​with ​x(1−b)←x(b)​B​
 (A).[x^{(0)}\mid x^{(1)}]\;\longmapsto\;[x^{(0)}\mid x^{(1)}]\cdot\Bigl(\prod_{(i,j)}U(\       (4)
 mathrm{dst}(b;i,j);c^{(b;i,j)}(A))\Bigr)=[x^{(0)}\mid x^{(1)}]\ \text{with }x^{(1-b)}\
 leftarrow x^{(b)}B(A).

Step 3: align the 99 overwrites with the token stream using a 3-layer router.

We now explain how layers 1–3 produce, at each time tt, exactly the overwrite parameters needed by
layer 4.

Partition the input stream into NN blocks of length 99. For convenience, define an additional
“block 00” consisting of padding symbols before the sequence; the router will interpret this as
encoding I3I_{3} and we write A(0):=I3A^{(0)}:=I_{3}. Let A(ℓ)A^{(\ell)} be the matrix encoded by
block ℓ\ell for ℓ=1,…,N\ell=1,\dots,N. At any position tt in block ℓ\ell (so ℓ∈{1,…,N}\ell\in\{1,\
dots,N\}), the model will apply the update corresponding to the previous matrix A(ℓ−1)A^{(\ell-1)}
(one-block delay).

Computing (τ,b)(\tau,b) from position. Let τ∈{1,…,9}\tau\in\{1,\dots,9\} be the within-block offset
and let b∈{0,1}b\in\{0,1\} be the parity bit indicating whether the active half is the first (b=0b=
0) or second (b=1b=1) half. Both are determined by tmod18t\bmod 18: specifically, τ=1+((t−1)mod9)\
tau=1+((t-1)\bmod 9) and bb is the indicator of whether tmod18∈{10,…,18}t\bmod 18\in\{10,\dots,18
\}. By Peng et al. (2025, Lemma 6) instantiated with n=9n=9, a 2-layer RWKV-7 can output tmod18t\
bmod 18, hence can provide τ\tau and bb to deeper layers.

Recovering the previous block. At time tt in block ℓ\ell, the last 1818 tokens (wt,wt−1,…,wt−17)(w_
{t},w_{t-1},\dots,w_{t-17}) contain the entire previous block ℓ−1\ell-1 (all 99 tokens encoding A
(ℓ−1)A^{(\ell-1)}), together with a prefix of the current block. Therefore, the tuple

 (tmod18,wt,wt−1,…,wt−17)\bigl(t\bmod 18,\;w_{t},w_{t-1},\dots,w_{t-17}\bigr)

uniquely determines the matrix entries of A(ℓ−1)A^{(\ell-1)} and the current offset τ\tau. By Peng
et al. (2025, Lemma 7) instantiated with n=9n=9, a 3-layer RWKV-7 can implement an arbitrary lookup
table on this finite key and write its outputs into the residual stream.

What the router outputs. Define a lookup table Ξ\Xi keyed by (tmod18,wt,…,wt−17)(t\bmod 18,\;w_{t},
\dots,w_{t-17}) whose output at time tt is:

 1. 1.
   
    the destination index dst⁡(b,i,j)\mathrm{dst}(b;i,j) and coefficient vector c(b,i,j)​(A(ℓ−1))c^
    {(b;i,j)}(A^{(\ell-1)}) for the overwrite corresponding to τ=π⁡(i,j)\tau=\pi(i,j); and

 2. 2.
   
    (optionally) the bit bb for the final readout.

The existence of such a lookup table is immediate because the key space is finite. By the previous
paragraph, layers 1–3 can compute Ξ\Xi at every position.

Finally, using the weight-preparation linear maps in the time-mixing block, layer 4 converts
(dst,c)(\mathrm{dst},c) into RWKV transition parameters realizing the overwrite matrix U⁡(dst,c)U(\
mathrm{dst};c) via Lemma 5.

Step 4: arithmetic in layer 4 and correctness.

Layer 4 uses a single wkv head of head dimension 1818. Initialize the first row of the wkv matrix
state at the first token by setting

 v1=𝐞1,k~1=[vec⁡(I3)∣𝟎]∈ℚ1×18,v_{1}=\mathbf{e}_{1},\qquad\tilde{k}_{1}=[\operatorname{vec}(I_{3})\
 mid\mathbf{0}]\in\mathbb{Q}^{1\times 18},

so the wkv state becomes 𝐞1⊤​k~1\mathbf{e}_{1}^{\top}\tilde{k}_{1} and its first row equals p1=
[vec⁡(I3)∣𝟎]p_{1}=[\operatorname{vec}(I_{3})\mid\mathbf{0}]. For all subsequent positions t≥2t\geq
2, set the additive term to 00 (i.e. vt=k~t=𝟎v_{t}=\tilde{k}_{t}=\mathbf{0}), so the first row
evolves purely multiplicatively as pt=pt−1​Atp_{t}=p_{t-1}A_{t}.

At each position tt in block ℓ\ell, the router provides the overwrite parameters corresponding to
the matrix A(ℓ−1)A^{(\ell-1)} and the overwrite index τ\tau. By the discussion above, we choose At=
U⁡(dst,c)A_{t}=U(\mathrm{dst};c) so that exactly one coordinate of pt−1p_{t-1} is overwritten.
After consuming all 99 tokens of block ℓ\ell, equation (4) implies that the inactive half has been
filled with

 x(1−b)=x(b)​B​(A(ℓ−1)).x^{(1-b)}\;=\;x^{(b)}\,B(A^{(\ell-1)}).

By (2), this corresponds to right-multiplying the current 3×33\times 3 product by A(ℓ−1)A^{(\
ell-1)}.

By induction over blocks, after finishing block ℓ\ell the destination half contains

 vec(A(1)A(2)⋯A(ℓ−1)).\operatorname{vec}\!\left(A^{(1)}A^{(2)}\cdots A^{(\ell-1)}\right).

In particular, after finishing the final block ℓ=N\ell=N, the destination half contains vec⁡(PN−1)\
operatorname{vec}(P_{N-1}) where PN−1:=A(1)⋯A(N−1)P_{N-1}:=A^{(1)}\cdots A^{(N-1)}. We now remove
the need for an additional padding block by applying the final multiplication by A(N)A^{(N)} in the
readout at the last token.

Completion readout at t=T=9​Nt=T=9N.

Let T:=9​NT:=9N. Let pT=[xT(0)∣xT(1)]∈ℚ1×18p_{T}=[x_{T}^{(0)}\mid x_{T}^{(1)}]\in\mathbb{Q}^{1\
times 18} denote the arithmetic layer’s row state at time TT, and let bT∈{0,1}b_{T}\in\{0,1\} be
the block-parity bit computed from Tmod18T\bmod 18. Define the destination-half index hT:=1−bT∈
{0,1}h_{T}:=1-b_{T}\in\{0,1\} (equivalently, hT=Nmod2h_{T}=N\bmod 2). Then xT(hT)=vec⁡(PN−1)x_{T}^
{(h_{T})}=\operatorname{vec}(P_{N-1}).

Since TT is the last token of block NN, the router key (Tmod18,wT,…,wT−17)(T\bmod 18,\;w_{T},\
dots,w_{T-17}) contains the entire block NN and hence determines A(N)A^{(N)}. For each (i,j)∈
{1,2,3}2(i,j)\in\{1,2,3\}^{2}, define the completion vector

 ω^T(i,j):=∑k=13Ak,j(N)​e 9​hT+π⁡(i,k)∈ℚ18,where ​π​(i,j)=3​(i−1)+j.\hat{\omega}_{T}^{(i,j)}:=\
 sum_{k=1}^{3}A^{(N)}_{k,j}\,e_{\,9h_{T}+\pi(i,k)}\in\mathbb{Q}^{18},\qquad\text{where }\pi(i,j)=3
 (i-1)+j.

Then

 pT​ω^T(i,j)=∑k=13xT,π⁡(i,k)(hT)​Ak,j(N)=vec⁡(PN)π⁡(i,j).p_{T}\hat{\omega}_{T}^{(i,j)}=\sum_{k=1}^
 {3}x^{(h_{T})}_{T,\pi(i,k)}\,A^{(N)}_{k,j}=\operatorname{vec}(P_{N})_{\pi(i,j)}.

Each ω^T(i,j)\hat{\omega}_{T}^{(i,j)} is a fixed function of the router key, so it can be returned
by the finite-window lookup in layer 3 at time TT. Using nine parallel readouts at the final
position (one per (i,j)(i,j)), the network outputs the nine scalars pT​ω^T(i,j)p_{T}\hat{\omega}_
{T}^{(i,j)} in row-major order, which equal vec⁡(PN)\operatorname{vec}(P_{N}). ∎

B.4 Simulating WFAs with DeltaNet

This subsection shows that a 44-layer stacked DeltaNet can simulate any weighted finite automaton
over ℚ\mathbb{Q}. Throughout this subsection we fix 𝕂=ℚ\mathbb{K}=\mathbb{Q} and interpret all
arithmetic in ℚ\mathbb{Q}.

B.4.1 DeltaNet memory update and the multiplicative step matrix

We consider a DeltaNet layer with matrix state St∈𝕂d×dS_{t}\in\mathbb{K}^{d\times d} updated by

 St=St−1​(I−βt​kt​kt⊤)+βt​vt​kt⊤,S_{t}\;=\;S_{t-1}\bigl(I-\beta_{t}k_{t}k_{t}^{\top}\bigr)\;+   (5)
 \;\beta_{t}v_{t}k_{t}^{\top},

where βt∈𝕂\beta_{t}\in\mathbb{K} and kt,vt∈𝕂dk_{t},v_{t}\in\mathbb{K}^{d} are computed from the
layer input xtx_{t} (via linear maps / an MLP, as in standard stacked DeltaNet blocks).

We will repeatedly use the multiplicative step matrix

 H⁡(β,k):=I−β​k​k⊤∈𝕂d×d.H(\beta,k)\;:=\;I-\beta kk^{\top}\in\mathbb{K}^{d\times d}.  (6)

When vt=𝟎v_{t}=\mathbf{0}, the update is purely multiplicative:

 St=St−1​H​(βt,kt).S_{t}\;=\;S_{t-1}H(\beta_{t},k_{t}).

B.4.2 Router primitives: position modulo and token buffering

Notation.

Let eie_{i} denote the standard basis column vector. We assume the input stream contains an
explicit BOS token at t=1t=1.

Computing tmod2​mt\bmod 2m.

The router needs a cyclic write index t~≡t(mod2​m)\tilde{t}\equiv t\pmod{2m} to address a
2​m2m-slot token buffer.

Lemma 7 (DeltaNet can compute position modulo 2​m2m).

For any positive integer mm, there exists a 22-layer stacked DeltaNet whose output includes a
one-hot encoding of t~:=tmod2​m\tilde{t}:=t\bmod 2m at every position tt.

Proof.

The construction mirrors a standard “two-reflections-make-a-rotation” argument. Layer 1 produces
the parity bit of tt (and can also detect t=1t=1 via the BOS token). Layer 2 maintains a
22-dimensional continuous state that advances by a fixed angle every two steps: it alternates
between two Householder reflections of the form I−2​κ​κ⊤I-2\kappa\kappa^{\top}, which is exactly H⁡
(β,k)H(\beta,k) with β=2\beta=2 and k=κk=\kappa (and vt=𝟎v_{t}=\mathbf{0}). The state therefore
cycles through 2​m2m distinct values, one for each residue class modulo 2​m2m. A finite MLP on top
of a bank of linear readouts can map these 2​m2m discrete states to a one-hot encoding of t~\tilde
{t}. ∎

Exact token buffering by column overwrite.

We store the last 2​m2m tokens by overwriting one designated column per step.

Lemma 8 (Exact column overwrite).

Fix dd and an index dst∈{1,…,d}\mathrm{dst}\in\{1,\dots,d\}. If βt=1\beta_{t}=1 and kt=edstk_{t}=e_
{\mathrm{dst}}, then

 St=St−1​(I−edst​edst⊤)+vt​edst⊤,S_{t}\;=\;S_{t-1}(I-e_{\mathrm{dst}}e_{\mathrm{dst}}^{\top})+v_
 {t}e_{\mathrm{dst}}^{\top},

so all columns of St−1S_{t-1} are unchanged except column dst\mathrm{dst}, which becomes exactly
vtv_{t}.

Proof.

Right-multiplying by (I−edst​edst⊤)(I-e_{\mathrm{dst}}e_{\mathrm{dst}}^{\top}) zeros column dst\
mathrm{dst} and leaves all other columns unchanged; adding vt​edst⊤v_{t}e_{\mathrm{dst}}^{\top}
writes vtv_{t} into that column. ∎

Buffer layout.

Let L:=2​mL:=2m. In the buffer layer, use a DeltaNet head dimension

 dbuf:=(|Σ|+1)+L.d_{\text{buf}}\;:=\;(|\Sigma|+1)+L.

We interpret:

  • •
   
    the first |Σ|+1|\Sigma|+1 coordinates as a one-hot token space (including padding/BOS as
    needed);

  • •
   
    the last LL coordinates as buffer-slot selectors.

At time tt, let t~∈{1,…,L}\tilde{t}\in\{1,\dots,L\} be the residue class from Lemma 7. Write the
current token into the t~\tilde{t}-th buffer slot by setting

 βt=1,kt=e(|Σ|+1)+t~,vt=ewt∈𝕂dbuf.\beta_{t}=1,\qquad k_{t}=e_{(|\Sigma|+1)+\tilde{t}},\qquad v_{t}
 =e_{w_{t}}\in\mathbb{K}^{d_{\text{buf}}}.

By Lemma 8, column (|Σ|+1)+t~(|\Sigma|+1)+\tilde{t} stores exactly the one-hot token vector ewte_
{w_{t}}. Using LL read heads with fixed queries q(j)=e(|Σ|+1)+jq^{(j)}=e_{(|\Sigma|+1)+j}, the
layer can read out all LL stored tokens (each as a one-hot vector in the first |Σ|+1|\Sigma|+1
coordinates) into the residual stream.

Finite lookup.

Given the one-hot encoding of t~\tilde{t} and the last 2​m2m tokens (read from the buffer), the key
space is finite. Thus, an (exponentially wide) feedforward network can implement an arbitrary
lookup table on this key and output: (i) the next arithmetic-step parameters (β,k)(\beta,k) for
Layer 4, and (ii) a readout vector used to decode the desired scalar output.

B.4.3 Arithmetic primitives realizable by multiplicative DeltaNet steps

We now show that the multiplicative matrix H⁡(β,k)=I−β​k​k⊤H(\beta,k)=I-\beta kk^{\top} can realize
the linear operations needed to apply arbitrary matrices.

Lemma 9 (Coordinate scaling / clearing).

Fix j∈{1,…,d}j\in\{1,\dots,d\} and s∈𝕂s\in\mathbb{K}. Let k=ejk=e_{j} and β=1−s\beta=1-s. Then

 H⁡(β,ej)=I−(1−s)​ej​ej⊤H(\beta,e_{j})\;=\;I-(1-s)e_{j}e_{j}^{\top}

scales coordinate jj by ss and leaves all other coordinates unchanged. In particular, s=0s=0 clears
coordinate jj.

Proof.

For any row vector r∈𝕂1×dr\in\mathbb{K}^{1\times d},

 r​H​(β,ej)=r−(1−s)​(r​ej)​ej⊤,rH(\beta,e_{j})=r-(1-s)(re_{j})e_{j}^{\top},

so (r​H)j=rj−(1−s)​rj=s​rj(rH)_{j}=r_{j}-(1-s)r_{j}=sr_{j} and (r​H)ℓ=rℓ(rH)_{\ell}=r_{\ell} for
ℓ≠j\ell\neq j. ∎

Definition 17 (Unit transvection).

For src≠dst\mathrm{src}\neq\mathrm{dst} define the (column) unit transvection

 T⁡(src→dst):=I+esrc​edst⊤.T(\mathrm{src}\to\mathrm{dst})\;:=\;I+e_{\mathrm{src}}e_{\mathrm{dst}}^
 {\top}.

Right-multiplication by T⁡(src→dst)T(\mathrm{src}\to\mathrm{dst}) updates a row vector rr by
rdst←rdst+rsrcr_{\mathrm{dst}}\leftarrow r_{\mathrm{dst}}+r_{\mathrm{src}} and leaves all other
coordinates unchanged.

Lemma 10 (Unit transvection as three multiplicative DeltaNet steps).

Assume 12,13∈𝕂\tfrac{1}{2},\tfrac{1}{3}\in\mathbb{K}. Fix src≠dst\mathrm{src}\neq\mathrm{dst} and
define (in dimension dd)

 u:=esrc+edst,w:=esrc+2​edst.u:=e_{\mathrm{src}}+e_{\mathrm{dst}},\qquad w:=e_{\mathrm{src}}+2e_{\
 mathrm{dst}}.

Then

 H⁡(2,u)​H​(12,esrc)​H​(13,w)=T⁡(src→dst)=I+esrc​edst⊤.H(2,u)\;H\!\left(\tfrac{1}{2},e_{\mathrm
 {src}}\right)\;H\!\left(\tfrac{1}{3},w\right)\;=\;T(\mathrm{src}\to\mathrm{dst})\;=\;I+e_{\mathrm
 {src}}e_{\mathrm{dst}}^{\top}.

Proof.

All three factors act as the identity outside the 2D subspace spanned by esrc,edste_{\mathrm
{src}},e_{\mathrm{dst}}, so it suffices to verify the 2×22\times 2 restriction. In the ordered
basis (esrc,edst)(e_{\mathrm{src}},e_{\mathrm{dst}}), we have:

 H⁡(2,(1,1))=[−1−2−2−1],H⁡(12,(1,0))=[12001],H⁡(13,(1,2))=[23−23−23−13].H(2,(1,1))=\begin{bmatrix}
 -1&-2\\ -2&-1\end{bmatrix},\quad H(\tfrac{1}{2},(1,0))=\begin{bmatrix}\tfrac{1}{2}&0\\ 0&1\end
 {bmatrix},\quad H(\tfrac{1}{3},(1,2))=\begin{bmatrix}\tfrac{2}{3}&-\tfrac{2}{3}\\[2.0pt] -\tfrac
 {2}{3}&-\tfrac{1}{3}\end{bmatrix}.

Multiplying gives

 [−1−2−2−1]​[12001]​[23−23−23−13]=[1101]=I+esrc​edst⊤.\begin{bmatrix}-1&-2\\ -2&-1\end{bmatrix}\
 begin{bmatrix}\tfrac{1}{2}&0\\ 0&1\end{bmatrix}\begin{bmatrix}\tfrac{2}{3}&-\tfrac{2}{3}\\[2.0pt]
 -\tfrac{2}{3}&-\tfrac{1}{3}\end{bmatrix}=\begin{bmatrix}1&1\\ 0&1\end{bmatrix}=I+e_{\mathrm{src}}
 e_{\mathrm{dst}}^{\top}.

Embedding back into the full dd-dimensional space proves the claim. ∎

Lemma 11 (Scaled add via a temp register).

Assume 12,13∈𝕂\tfrac{1}{2},\tfrac{1}{3}\in\mathbb{K}. Fix distinct indices src,dst,tmp\mathrm{src},
\mathrm{dst},\mathrm{tmp} and a scalar λ∈𝕂\lambda\in\mathbb{K}. There exists a length-88 sequence
of multiplicative matrices of the form H⁡(β,k)H(\beta,k) such that for every row vector rr with
rtmp=0r_{\mathrm{tmp}}=0, the resulting row vector r′r^{\prime} satisfies:

 rdst′=rdst+λ​rsrc,rj′=rj​(j≠dst),rtmp′=0.r^{\prime}_{\mathrm{dst}}=r_{\mathrm{dst}}+\lambda r_{\
 mathrm{src}},\qquad r^{\prime}_{j}=r_{j}\ (j\neq\mathrm{dst}),\qquad r^{\prime}_{\mathrm{tmp}}=0.

Proof.

Use the following four-stage program, each stage implementable by multiplicative DeltaNet steps:

 1. 1.
   
    Copy src\mathrm{src} into tmp\mathrm{tmp} by a unit transvection T⁡(src→tmp)T(\mathrm{src}\to\
    mathrm{tmp}), implemented using 33 steps via Lemma 10.

 2. 2.
   
    Scale tmp\mathrm{tmp} by λ\lambda using one coordinate scaling: apply H⁡(1−λ,etmp)H(1-\
    lambda,e_{\mathrm{tmp}}) (Lemma 9).

 3. 3.
   
    Add tmp\mathrm{tmp} into dst\mathrm{dst} by a unit transvection T⁡(tmp→dst)T(\mathrm{tmp}\to\
    mathrm{dst}), implemented using 33 steps via Lemma 10.

 4. 4.
   
    Clear tmp\mathrm{tmp} by scaling it by 00: apply H⁡(1,etmp)H(1,e_{\mathrm{tmp}}) (Lemma 9).

Starting from rtmp=0r_{\mathrm{tmp}}=0, the net effect is rdst←rdst+λ​rsrcr_{\mathrm{dst}}\
leftarrow r_{\mathrm{dst}}+\lambda r_{\mathrm{src}} and tmp\mathrm{tmp} returns to 00. The total
number of multiplicative steps is 3+1+3+1=83+1+3+1=8. ∎

Applying an arbitrary matrix with scratch coordinates.

We now show that any n×nn\times n matrix can be applied to a row vector using O⁡(n2)O(n^{2})
multiplicative DeltaNet steps, given nn scratch coordinates and one additional temp coordinate.

Lemma 12 (Apply any n×nn\times n matrix with scratch in O⁡(n2)O(n^{2}) multiplicative steps).

Assume 12,13∈𝕂\tfrac{1}{2},\tfrac{1}{3}\in\mathbb{K}. Let P∈𝕂n×nP\in\mathbb{K}^{n\times n}.
Consider row vectors in dimension 2​n+12n+1 written as

 [x​∣s∣​t],x,s∈𝕂1×n,t∈𝕂.[x\mid s\mid t],\qquad x,s\in\mathbb{K}^{1\times n},\ t\in\mathbb{K}.

There exists an explicit sequence of

 m:= 8​n2+5​n+1m\;:=\;8n^{2}+5n+1

matrices G1​(P),…,Gm​(P)∈𝕂(2​n+1)×(2​n+1)G_{1}(P),\dots,G_{m}(P)\in\mathbb{K}^{(2n+1)\times(2n+1)},
each of the form H⁡(β,k)H(\beta,k), such that for all x,s,tx,s,t,

 [x∣s∣t]G1(P)⋯Gm(P)=[xP∣xP∣0].[x\mid s\mid t]\;G_{1}(P)\cdots G_{m}(P)\;=\;[xP\mid xP\mid 0].

Proof.

Let the coordinates be indexed as:

 main: 1,…,n,scratch: n+1,…,2n,temp: 2n+1.\text{main: }1,\dots,n,\quad\text{scratch: }n+1,\
 dots,2n,\quad\text{temp: }2n+1.

Phase 1 (clear scratch and temp).

For each scratch coordinate n+jn+j apply a clear:

 H⁡(1,en+j)(j=1,…,n),H(1,e_{n+j})\quad(j=1,\dots,n),

and clear temp by H⁡(1,e2​n+1)H(1,e_{2n+1}). This uses n+1n+1 steps and yields [x​∣0∣​0][x\mid 0\
mid 0].

Phase 2 (accumulate x​PxP into scratch).

For each destination j∈{1,…,n}j\in\{1,\dots,n\} and source i∈{1,…,n}i\in\{1,\dots,n\}, update
scratch coordinate n+jn+j by

 (scratch ​j)←(scratch ​j)+Pi,j⋅(main ​i)(\text{scratch }j)\ \leftarrow\ (\text{scratch }j)+P_
 {i,j}\cdot(\text{main }i)

using Lemma 11 with

 src=i,dst=n+j,tmp=2​n+1,λ=Pi,j.\mathrm{src}=i,\qquad\mathrm{dst}=n+j,\qquad\mathrm{tmp}=2n+1,\
 qquad\lambda=P_{i,j}.

Each scaled add costs 88 multiplicative steps and leaves temp equal to 00. After all n2n^{2}
updates, scratch equals x​PxP while main remains xx.

Phase 3 (clear main).

Clear the main coordinates by applying H⁡(1,ei)H(1,e_{i}) for i=1,…,ni=1,\dots,n. This uses nn
steps and yields [0​∣x​P∣​0][0\mid xP\mid 0].

Phase 4 (copy scratch back to main).

For each j=1,…,nj=1,\dots,n, add scratch coordinate n+jn+j into main coordinate jj using a unit
transvection T⁡(n+j→j)T(n+j\to j), implemented by 33 multiplicative steps via Lemma 10. Since main
is 00, after these 3​n3n steps we have main =x​P=xP and scratch remains x​PxP.

Summing the step counts gives

 (n+1)+8​n2+n+3​n=8​n2+5​n+1.(n+1)+8n^{2}+n+3n=8n^{2}+5n+1.

∎

B.4.4 Simulation theorem: a 44-layer DeltaNet simulates any WFA

Theorem 11 (DeltaNet simulates weighted finite automata).

Fix 𝕂=ℚ\mathbb{K}=\mathbb{Q}. Let 𝒜=(Q,Σ,{Mσ},α,ω)\mathcal{A}=(Q,\Sigma,\{M_{\sigma}\},\alpha,\
omega) be an nn-state WFA over 𝕂\mathbb{K} (Definition 13), and let w=w1⋯wTw=w_{1}\cdots w_{T} be
an input word. Then there exists a 44-layer stacked DeltaNet whose scalar output at every position
tt equals

 αMw1Mw2⋯Mwtω∈𝕂.\alpha\,M_{w_{1}}M_{w_{2}}\cdots M_{w_{t}}\,\omega\in\mathbb{K}.

In particular, at t=Tt=T the output equals f𝒜​(w)f_{\mathcal{A}}(w).

Proof sketch; full proof follows later.

We use the same finite-window router + arithmetic template as in Theorem 9 (compute tmod2​mt\bmod
2m, buffer the last 2​m2m tokens, and look up the next arithmetic step), but implement these router
components using DeltaNet primitives.

The arithmetic layer now uses only multiplicative DeltaNet steps H⁡(β,k)=I−β​k​k⊤H(\beta,k)=I-\beta
kk^{\top} (Definition • ‣ 2.3) over 𝕂=ℚ\mathbb{K}=\mathbb{Q}, which guarantees 12,13∈𝕂\tfrac{1}{2},
\tfrac{1}{3}\in\mathbb{K}. Lemma 9 shows that H⁡(β,k)H(\beta,k) can implement coordinate scalings,
and Lemma 10 shows that constant-length products of such steps implement unit transvections
rdst←rdst+rsrcr_{\mathrm{dst}}\leftarrow r_{\mathrm{dst}}+r_{\mathrm{src}}. Combining these
primitives, Lemma 12 gives an explicit program applying an arbitrary n×nn\times n matrix in m=
8​n2+5​n+1m=8n^{2}+5n+1 multiplicative steps (using scratch space and one temporary coordinate).
The router streams the corresponding (βt,kt)(\beta_{t},k_{t}) parameters to the arithmetic layer
exactly as in Theorem 9, and the same completion-vector decoding yields f𝒜​(w)f_{\mathcal{A}}(w). ∎

Proof.

Let

 m:= 8​n2+5​n+1m\;:=\;8n^{2}+5n+1

be the step budget from Lemma 12. For each position t≥1t\geq 1, write t=ℓ​m+τt=\ell m+\tau with ℓ=⌊
(t−1)/m⌋≥0\ell=\lfloor(t-1)/m\rfloor\geq 0 and 1≤τ≤m1\leq\tau\leq m.

Offline factorization of block products.

Introduce a padding symbol ⊥\bot and set wt=⊥w_{t}=\bot for t≤0t\leq 0 with M⊥:=IM_{\bot}:=I. For
any length-mm block string w~=w~1⋯w~m∈(Σ∪{⊥})m\tilde{w}=\tilde{w}_{1}\cdots\tilde{w}_{m}\in(\Sigma\
cup\{\bot\})^{m}, define the block product

 P(w~):=Mw~1Mw~2⋯Mw~m∈𝕂n×n.P(\tilde{w})\;:=\;M_{\tilde{w}_{1}}M_{\tilde{w}_{2}}\cdots M_{\tilde{w}
 _{m}}\in\mathbb{K}^{n\times n}.

Apply Lemma 12 to P⁡(w~)P(\tilde{w}) to obtain a fixed sequence

 Gw~,1,…,Gw~,m∈𝕂(2​n+1)×(2​n+1),Gw~,τ=H⁡(βw~,τ,kw~,τ),G_{\tilde{w},1},\dots,G_{\tilde{w},m}\in\
 mathbb{K}^{(2n+1)\times(2n+1)},\qquad G_{\tilde{w},\tau}=H(\beta_{\tilde{w},\tau},k_{\tilde{w},\
 tau}),

such that for all [x​∣s∣​t][x\mid s\mid t],

 [x​∣s∣​t]​∏τ=1mGw~,τ=[x​P​(w~)​∣x​P​(w~)∣​0].[x\mid s\mid t]\;\prod_{\tau=1}^{m}G_{\tilde{w},\
 tau}\;=\;[xP(\tilde{w})\mid xP(\tilde{w})\mid 0].

Fix one such factorization for each possible w~\tilde{w}.

Layer 1 (parity / first-position features).

Layer 1 produces a finite-valued encoding that distinguishes the first position (via BOS) and
encodes parity. This can be done by a 1D multiplicative recurrence that toggles sign each step,
together with the BOS token.

Layer 2 (compute tmod2​mt\bmod 2m).

By Lemma 7, Layer 2 outputs a one-hot encoding of t~:=tmod2​m\tilde{t}:=t\bmod 2m in the residual
stream.

Layer 3 (buffer last 2​m2m tokens and perform a finite lookup).

Set L:=2​mL:=2m and build a 2​m2m-slot cyclic token buffer using Lemma 8, with the buffer layout
described earlier. At time tt, the buffer contains the last 2​m2m tokens

 (wt,wt−1,…,wt−(2​m−1)),(w_{t},w_{t-1},\dots,w_{t-(2m-1)}),

and Layer 2 provides t~=tmod2​m\tilde{t}=t\bmod 2m. Define a lookup table Ξ\Xi whose key is

 (t~,wt,wt−1,…,wt−(2​m−1)),\Bigl(\tilde{t},\;w_{t},w_{t-1},\dots,w_{t-(2m-1)}\Bigr),

and whose outputs are:

 1. 1.
   
    the arithmetic parameters (βt(4),kt(4))(\beta^{(4)}_{t},k^{(4)}_{t}) specifying the matrix Gw~
    (ℓ−1),τG_{\tilde{w}^{(\ell-1)},\tau} to apply in Layer 4 (defined below);

 2. 2.
   
    a completion/readout vector ω^t∈𝕂2​n+1\hat{\omega}_{t}\in\mathbb{K}^{2n+1} (also defined
    below).

Since the key space is finite, an MLP can implement Ξ\Xi exactly and write these outputs into the
residual stream.

Layer 4 (arithmetic: purely multiplicative DeltaNet simulation).

Layer 4 uses one DeltaNet head of dimension 2​n+12n+1. We maintain only the first row of StS_{t} as
the arithmetic row vector.

Initialization. Let p0=[α​∣0∣​0]∈𝕂1×(2​n+1)p_{0}=[\alpha\mid 0\mid 0]\in\mathbb{K}^{1\times(2n+1)}.
On the first token, set S0=0S_{0}=0 and write S1=e1​p0S_{1}=e_{1}p_{0} by choosing

 β1=1,v1=e1,k1=p0⊤.\beta_{1}=1,\qquad v_{1}=e_{1},\qquad k_{1}=p_{0}^{\top}.

Then S1=β1​v1​k1⊤=e1​p0S_{1}=\beta_{1}v_{1}k_{1}^{\top}=e_{1}p_{0}.

Updates. For all t≥2t\geq 2, set vt=𝟎v_{t}=\mathbf{0}. Let w~(ℓ−1):=w(ℓ−1)​m+1⋯wℓ​m\tilde{w}^{(\
ell-1)}:=w_{(\ell-1)m+1}\cdots w_{\ell m} be the previous completed block string (using ⊥\bot
padding when ℓ=0\ell=0), and let τ\tau be the within-block offset of tt. Layer 3 outputs (βt(4),kt
(4))(\beta^{(4)}_{t},k^{(4)}_{t}) so that

 H⁡(βt(4),kt(4))=Gw~(ℓ−1),τ.H(\beta^{(4)}_{t},k^{(4)}_{t})\;=\;G_{\tilde{w}^{(\ell-1)},\tau}.

Thus, for t≥2t\geq 2 we have St=St−1​Gw~(ℓ−1),τS_{t}=S_{t-1}G_{\tilde{w}^{(\ell-1)},\tau}. Let ptp_
{t} denote the first row of StS_{t}; then pt=pt−1​Gw~(ℓ−1),τp_{t}=p_{t-1}G_{\tilde{w}^{(\ell-1)},\
tau}.

Correctness via a completion vector.

Unrolling the recurrence, if t=ℓ​m+τt=\ell m+\tau then

 pt=[α​∣0∣​0]​(∏b=0ℓ−2∏s=1mGw~(b),s)​(∏s=1τGw~(ℓ−1),s),p_{t}=[\alpha\mid 0\mid 0]\;\Bigl(\prod_{b=
 0}^{\ell-2}\prod_{s=1}^{m}G_{\tilde{w}^{(b)},s}\Bigr)\;\Bigl(\prod_{s=1}^{\tau}G_{\tilde{w}^{(\
 ell-1)},s}\Bigr),

with the convention that empty products are identity.

Define the current-block prefix product

 Rt:=Mwℓ​m+1⋯Mwt∈𝕂n×n,vt:=Rtω∈𝕂n×1.R_{t}:=M_{w_{\ell m+1}}\cdots M_{w_{t}}\in\mathbb{K}^{n\times
 n},\qquad v_{t}:=R_{t}\,\omega\in\mathbb{K}^{n\times 1}.

Define the completion vector

 ω^t:=(∏s=τ+1mGw~(ℓ−1),s)​[vt𝟎0]∈𝕂2​n+1.\hat{\omega}_{t}:=\left(\prod_{s=\tau+1}^{m}G_{\tilde{w}^
 {(\ell-1)},s}\right)\begin{bmatrix}v_{t}\\ \mathbf{0}\\ 0\end{bmatrix}\in\mathbb{K}^{2n+1}.

This depends only on the router key (position mod 2​m2m and the last 2​m2m tokens), hence can be
output by Ξ\Xi.

Now compute the scalar pt​ω^tp_{t}\hat{\omega}_{t}: multiplying ptp_{t} by the remaining factors
Gw~(ℓ−1),τ+1⋯Gw~(ℓ−1),mG_{\tilde{w}^{(\ell-1)},\tau+1}\cdots G_{\tilde{w}^{(\ell-1)},m} completes
the application of the previous block product. By Lemma 12, the full block product ∏s=1mGw~(ℓ−1),s\
prod_{s=1}^{m}G_{\tilde{w}^{(\ell-1)},s} maps the first nn coordinates xx to x​P​(w~(ℓ−1))xP(\tilde
{w}^{(\ell-1)}) (and duplicates it into scratch), so telescoping over completed blocks yields

 ptω^t=αMw1⋯Mwtω.p_{t}\hat{\omega}_{t}\;=\;\alpha\,M_{w_{1}}\cdots M_{w_{t}}\,\omega.

Readout.

At position tt, set a DeltaNet query vector qt:=ω^tq_{t}:=\hat{\omega}_{t} (routed from Layer 3)
and read ot=St​qto_{t}=S_{t}q_{t}. Because St=e1​ptS_{t}=e_{1}p_{t} has only the first row nonzero,
we have ot=e1⋅(pt​ω^t)o_{t}=e_{1}\cdot(p_{t}\hat{\omega}_{t}). An output head returns the first
coordinate, which equals αMw1⋯Mwtω\alpha M_{w_{1}}\cdots M_{w_{t}}\omega. ∎

B.5 Iterated 3×33\times 3 Matrix Multiplication with DeltaNet

We now show that DeltaNet can solve the streaming iterated 3×33\times 3 matrix multiplication
problem (Definition 14) over a bounded-precision modulus, using the same router+arithmetic template
as Theorem 11. The main difference is that the arithmetic state dimension is constant (n=9n=9 after
vectorization), so the required per-superblock step budget is a constant.

Theorem 12 (DeltaNet computes iterated 3×33\times 3 matrix products over ℚ\mathbb{Q}).

Fix 𝕂=ℚ\mathbb{K}=\mathbb{Q}. There exists a 44-layer stacked DeltaNet such that, given the stream
encoding A(1),…,A(N)∈ℚ3×3A^{(1)},\dots,A^{(N)}\in\mathbb{Q}^{3\times 3} (Definition 14), the
network outputs the 99 entries of

 PN:=A(1)A(2)⋯A(N)∈ℚ3×3P_{N}:=A^{(1)}A^{(2)}\cdots A^{(N)}\in\mathbb{Q}^{3\times 3}

at the final position t=9​Nt=9N.

Proof.

Work over 𝕂=ℚ\mathbb{K}=\mathbb{Q}.

Step 1: reduce right-multiplication to a 99-dimensional linear update.

Let vec:ℚ3×3→ℚ1×9\operatorname{vec}:\mathbb{Q}^{3\times 3}\to\mathbb{Q}^{1\times 9} be row-major
vectorization. Define the block-diagonal embedding

 B⁡(A):=[A000A000A]∈ℚ9×9.B(A)\;:=\;\begin{bmatrix}A&0&0\\[2.0pt] 0&A&0\\[2.0pt] 0&0&A\end{bmatrix}
 \in\mathbb{Q}^{9\times 9}.

Then for all P,A∈ℚ3×3P,A\in\mathbb{Q}^{3\times 3},

 vec⁡(P​A)=vec⁡(P)​B​(A).\operatorname{vec}(PA)=\operatorname{vec}(P)\,B(A).

Let xt:=vec(A(1)⋯A(t))x_{t}:=\operatorname{vec}(A^{(1)}\cdots A^{(t)}). Then

 x0=vec⁡(I3),xt=xt−1​B​(A(t)).x_{0}=\operatorname{vec}(I_{3}),\qquad x_{t}=x_{t-1}\,B(A^{(t)}).

Thus it suffices to maintain a 99-dimensional row state and repeatedly apply the linear map x↦x​B​
(A)x\mapsto xB(A).

Step 2: choose a superblock length and step budget.

We will apply Lemma 12 with n=9n=9, i.e. we maintain an arithmetic state of dimension 2​n+1=192n+1=
19 (main 99, scratch 99, temp 11), and can apply any 9×99\times 9 matrix in

 marith:=8​n2+5​n+1= 8⋅81+45+1= 694m_{\text{arith}}:=8n^{2}+5n+1\;=\;8\cdot 81+45+1\;=\;694

multiplicative steps.

Each input matrix A(t)A^{(t)} arrives as 99 tokens. We group matrices into superblocks of length

 L:=78,so each superblock has ​9​L=702​ tokens.L:=78,\qquad\text{so each superblock has }9L=702\
 text{ tokens}.

Since 702≥694702\geq 694, we can schedule the 694694 arithmetic steps for a superblock and use the
remaining 702−694=8702-694=8 token times as identity steps (i.e. apply H⁡(0,k)=IH(0,k)=I).

We handle the one-superblock delay by a completion readout at the final position T=9​NT=9N,
analogous to the RWKV completion-vector trick: the router computes a readout vector that (i)
finishes the remaining multiplicative steps for the previous superblock and (ii) applies the final
superblock product in the readout.

Step 3: define superblock transition matrices.

Partition the matrix sequence into superblocks of length L=78L=78, except that the final superblock
may be shorter. Let B:=⌈N/L⌉B:=\lceil N/L\rceil and let

 r:=N−(B−1)​L∈{1,…,L}.r:=N-(B-1)L\in\{1,\dots,L\}.

For each superblock index b<Bb<B define the full-superblock product

 Πb:=B(A((b−1)​L+1))⋯B(A(b​L))∈ℚ9×9.\Pi_{b}\;:=\;B(A^{((b-1)L+1)})\cdots B(A^{(bL)})\in\mathbb{Q}^
 {9\times 9}.

For the final superblock define the (possibly partial) product

 ΠB:=B(A((B−1)​L+1))⋯B(A(N))∈ℚ9×9.\Pi_{B}\;:=\;B(A^{((B-1)L+1)})\cdots B(A^{(N)})\in\mathbb{Q}^{9\
 times 9}.

Then

 vec(PN)=x0Π1Π2⋯ΠB,x0=vec(I3).\operatorname{vec}(P_{N})\;=\;x_{0}\,\Pi_{1}\Pi_{2}\cdots\Pi_{B},\
 qquad x_{0}=\operatorname{vec}(I_{3}).

Partition the matrix sequence into superblocks of length L=78L=78, except that the final superblock
may be shorter. Let B:=⌈N/L⌉B:=\lceil N/L\rceil and let

 r:=N−(B−1)​L∈{1,…,L}.r:=N-(B-1)L\in\{1,\dots,L\}.

For each superblock index b<Bb<B define the full-superblock product

 Πb:=B(A((b−1)​L+1))⋯B(A(b​L))∈ℚ9×9.\Pi_{b}\;:=\;B(A^{((b-1)L+1)})\cdots B(A^{(bL)})\in\mathbb{Q}^
 {9\times 9}.

For the final superblock define the (possibly partial) product

 ΠB:=B(A((B−1)​L+1))⋯B(A(N))∈ℚ9×9.\Pi_{B}\;:=\;B(A^{((B-1)L+1)})\cdots B(A^{(N)})\in\mathbb{Q}^{9\
 times 9}.

Step 4: offline factorization of each Πb\Pi_{b} into multiplicative DeltaNet steps.

By Lemma 12 (with n=9n=9), for each full superblock product Πb\Pi_{b} with b<Bb<B there exists a
length-694694 sequence of 19×1919\times 19 matrices of the form H⁡(β,k)H(\beta,k). Extend this to a
length-702702 sequence by appending 88 identity steps. Thus each full superblock b<Bb<B is
associated with a fixed sequence

 Gb,1,…,Gb,702∈ℚ19×19,Gb,τ=H⁡(βb,τ,kb,τ).G_{b,1},\dots,G_{b,702}\in\mathbb{Q}^{19\times 19},\qquad
 G_{b,\tau}=H(\beta_{b,\tau},k_{b,\tau}).

(We also define G0,τ:=IG_{0,\tau}:=I for all τ\tau, corresponding to Π0:=I\Pi_{0}:=I.)

By Lemma 12 (with n=9n=9), for each Πb\Pi_{b} there exists a length-694694 sequence of 19×1919\
times 19 matrices of the form H⁡(β,k)H(\beta,k) that maps

 [x​∣s∣​t]↦[x​Πb​∣x​Πb∣​0].[x\mid s\mid t]\;\mapsto\;[x\Pi_{b}\mid x\Pi_{b}\mid 0].

Extend this to a length-702702 sequence by appending 88 identity steps. Thus each superblock bb is
associated with a fixed sequence

 Gb,1,…,Gb,702∈ℚ19×19,Gb,τ=H⁡(βb,τ,kb,τ)G_{b,1},\dots,G_{b,702}\in\mathbb{Q}^{19\times 19},\qquad
 G_{b,\tau}=H(\beta_{b,\tau},k_{b,\tau})

such that after applying all 702702 steps,

 [x​∣s∣​t]​∏τ=1702Gb,τ=[x​Πb​∣x​Πb∣​0].[x\mid s\mid t]\;\prod_{\tau=1}^{702}G_{b,\tau}\;=\;[x\Pi_
 {b}\mid x\Pi_{b}\mid 0].

Fix one such factorization for each possible superblock token string.

Step 5: the 4-layer DeltaNet construction (router + arithmetic).

Layers 1–2 (position modulo). Let mtok:=702m_{\text{tok}}:=702 be the token length per superblock.
Use Layers 1–2 to compute a one-hot encoding of tmod(2​mtok)=tmod1404t\bmod(2m_{\text{tok}})=t\bmod
1404 (Lemma 7 with m=mtokm=m_{\text{tok}}) and write it to the residual stream.

Layer 3 (token buffer + lookup). Maintain a cyclic buffer of the last 2​mtok=14042m_{\text{tok}}=
1404 tokens using the column overwrite mechanism (Lemma 8) with L=1404L=1404 slots. At time tt, the
last 14041404 tokens contain:

  • •
   
    the entire previous superblock (the last 702702 tokens), encoding the L=78L=78 matrices whose
    product is Πb−1\Pi_{b-1},

  • •
   
    and a prefix of the current superblock.

Define a lookup table Ξ\Xi keyed by

 (tmod1404,wt,wt−1,…,wt−1403)\Bigl(t\bmod 1404,\;w_{t},w_{t-1},\dots,w_{t-1403}\Bigr)

whose output is the next arithmetic-step parameters (βt(4),kt(4))(\beta^{(4)}_{t},k^{(4)}_{t})
specifying the factor Gb−1,τG_{b-1,\tau} to apply at this time (one-superblock delay). Because the
key space is finite (and of size at most m1404≤NO⁡(1)m^{1404}\leq N^{O(1)} under m≤Ncm\leq N^{c}),
an MLP can implement Ξ\Xi exactly.

Layer 4 (arithmetic in dimension 1919). Layer 4 uses one DeltaNet head of dimension 1919 and
interprets the first row of StS_{t} as the arithmetic row vector [x​∣s∣​t][x\mid s\mid t], where
x,s∈ℚ1×9x,s\in\mathbb{Q}^{1\times 9} and t∈ℚt\in\mathbb{Q}.

Initialization (first token). Write [vec⁡(I3)​∣0∣​0][\operatorname{vec}(I_{3})\mid 0\mid 0] into
the first row of S1S_{1} using one additive write: set β1=1\beta_{1}=1, v1=e1v_{1}=e_{1}, and k1=
[vec⁡(I3)​∣0∣​0]⊤k_{1}=[\operatorname{vec}(I_{3})\mid 0\mid 0]^{\top}.

Purely multiplicative evolution (all later tokens). For all t≥2t\geq 2, set vt=𝟎v_{t}=\mathbf{0} so
St=St−1​H​(βt,kt)S_{t}=S_{t-1}H(\beta_{t},k_{t}). Layer 3 provides (βt,kt)(\beta_{t},k_{t}) so that
H⁡(βt,kt)=Gb−1,τH(\beta_{t},k_{t})=G_{b-1,\tau}, i.e. the τ\tau-th factor for the previous
superblock’s product Πb−1\Pi_{b-1}. During the first superblock, take Π0=I\Pi_{0}=I and apply
identity steps.

Let T:=9​NT:=9N be the final position. Write

 T=(B−1)​mtok+τ,mtok:=702,1≤τ≤mtok.T=(B-1)\,m_{\text{tok}}+\tau,\qquad m_{\text{tok}}:=702,\qquad
 1\leq\tau\leq m_{\text{tok}}.

(So τ=9​r\tau=9r, where r=N−(B−1)​Lr=N-(B-1)L is the number of matrices in the final superblock.)
By construction, at time TT the arithmetic layer has applied the first τ\tau factors of the
previous full superblock GB−1,1,…,GB−1,τG_{B-1,1},\dots,G_{B-1,\tau} (or identity factors if B=1B=
1), but it has not applied ΠB\Pi_{B}. We therefore decode vec(PN)=x0Π1⋯ΠB\operatorname{vec}(P_{N})=
x_{0}\Pi_{1}\cdots\Pi_{B} using a completion readout at time TT.

Final readout via completion vectors.

Let pT∈ℚ1×19p_{T}\in\mathbb{Q}^{1\times 19} denote the arithmetic layer’s first-row state at time
TT. For each output coordinate j∈{1,…,9}j\in\{1,\dots,9\} define the completion vector

 ω^T(j):=(∏s=τ+1mtokGB−1,s)​[ΠB​ej𝟎0]∈ℚ19×1,\hat{\omega}_{T}^{(j)}\;:=\;\left(\prod_{s=\tau+1}^{m_
 {\text{tok}}}G_{B-1,s}\right)\begin{bmatrix}\Pi_{B}e_{j}\\[2.0pt] \mathbf{0}\\[2.0pt] 0\end
 {bmatrix}\in\mathbb{Q}^{19\times 1},

where the product is interpreted as identity when B=1B=1 or τ=mtok\tau=m_{\text{tok}}. This ω^T(j)\
hat{\omega}_{T}^{(j)} is a fixed function of the finite router key (Tmod1404,wT,wT−1,…,wT−1403),(T\
bmod 1404,\;w_{T},w_{T-1},\dots,w_{T-1403}), so it can be returned by the layer-3 lookup.

The network outputs the nine scalars

 pTω^T(j)=(vec(PN))j,j=1,…,9,p_{T}\hat{\omega}_{T}^{(j)}\;=\;\bigl(\operatorname{vec}(P_{N})\bigr)
 _{j},\qquad j=1,\dots,9,

i.e. the 99 entries of PNP_{N} in row-major order, at the final position T=9​NT=9N. ∎

Appendix C Upper Bounds on PD LRNNs

See 7

Proof.

Similar to the proof of Theorem 3, specifically the convolutional form of LRNNs in Equation 1, this
proof reduces to showing that the product of a sequence P1​D1,…,Pn​DnP_{1}D_{1},\ldots,P_{n}D_{n}
of PD matrices can be computed in 𝖥𝖮\mathsf{FO}-uniform 𝖭𝖢1\mathsf{NC}^{1}. Terzic et al. (2025)
observe PD matrices are closed under multiplication, so this product also has PD form. Let ∏i=
1nPi​Di=P~n​D~n\prod_{i=1}^{n}P_{i}D_{i}=\tilde{P}_{n}\tilde{D}_{n}. We will provide a closed form
for P~i\tilde{P}_{i} and D~i\tilde{D}_{i}, and then argue that it can be easily computed using 𝖥𝖮\
mathsf{FO}-uniform 𝖭𝖢1\mathsf{NC}^{1} circuits.

Recall that DiD_{i} is a diagonal matrix. Each PiP_{i} is a column one-hot matrix in {0,1}d×d\{0,1
\}^{d\times d} and thus represents a function πi:{1,2,…,d}→{1,2,…,d}\pi_{i}:\{1,2,\ldots,d\}\to\
{1,2,\ldots,d\}. When PiP_{i} is full rank, πi\pi_{i} represents a permutation of dd elements. In
general, πi\pi_{i} may map two inputs to the same output, i.e., collapse two elements into one. We
will thus refer to it as a relaxed permutation.

We first observe that for any d×dd\times d matrix AA, both A​PiAP_{i} and A​DiAD_{i} have a very
simple form: A​PiAP_{i} is the matrix obtained by applying the relaxed permutation πi\pi_{i} to the
columns of AA, and A​DiAD_{i} is the matrix obtained by scaling the jj-th column of AA by the jj-th
diagonal entry of DiD_{i}. It follows that A​P1​D1,…,Pn​DnAP_{1}D_{1},\ldots,P_{n}D_{n} is nothing
but an alternating sequence of relaxed permutations and scaling of columns of AA. Intuitively, as
long as we adjust the scaling terms appropriately, we can switch the order, i.e., first apply all
relaxed permutations to the columns of AA and then apply appropriate scaling; this, as we will
shortly see, will make the matrix product easy to compute using uniform 𝖭𝖢1\mathsf{NC}^{1}
circuits. Formalizing this, we will indeed show that P~n=∏i=1nPi\tilde{P}_{n}=\prod_{i=1}^{n}P_{i}
and D~n=∏j=1nπ~n,j​(Dj)\tilde{D}_{n}=\prod_{j=1}^{n}\tilde{\pi}_{n,j}(D_{j}), where π~n,j=∏i=nj+1πi
\tilde{\pi}_{n,j}=\prod_{i=n}^{j+1}\pi_{i} is the composition of the last n−jn-j relaxed
permutations (by convention, π~n,n\tilde{\pi}_{n,n} is the identity permutation) and π⁡(Dj)\pi(D_
{j}) denotes the relaxed permutation of the diagonal entries of DjD_{j} according to π\pi, leading
to another diagonal matrix.

Conveniently, it is well-known that the product of nn fixed size matrices that take only finitely
many values (in particular fixed size 0-1 matrices) can be computed in 𝖥𝖮\mathsf{FO}-uniform 𝖭𝖢1\
mathsf{NC}^{1} (Immerman and Landau, 1992). Thus P~n\tilde{P}_{n} can be computed using such
circuits. The relaxed permutation π~j\tilde{\pi}_{j} can also be similarly computed, and applied to
DjD_{j}. Lastly, D~n\tilde{D}_{n}, being a product of nn diagonal matrices, consists of nn products
of nn rationals each, which again can be computed in 𝖥𝖮\mathsf{FO}-uniform 𝖭𝖢1\mathsf{NC}^{1}.

All that remains to show is that P~n=∏i=1nPi\tilde{P}_{n}=\prod_{i=1}^{n}P_{i} and D~n=∏j=1nπ~n,j​
(Dj)\tilde{D}_{n}=\prod_{j=1}^{n}\tilde{\pi}_{n,j}(D_{j}). We prove this by induction on nn. The
base case of n=1n=1 holds trivially. For n≥2n\geq 2, assume by induction that ∏i=1n−1Pi​Di=
P~n−1​D~n−1\prod_{i=1}^{n-1}P_{i}D_{i}=\tilde{P}_{n-1}\tilde{D}_{n-1} where P~n−1=∏i=1n−1Pi\tilde
{P}_{n-1}=\prod_{i=1}^{n-1}P_{i} and D~n−1=∏j=1n−1π~n−1,j​(Dj)\tilde{D}_{n-1}=\prod_{j=1}^{n-1}\
tilde{\pi}_{n-1,j}(D_{j}). Then ∏i=1nPi​Di=P~n−1​D~n−1​Pn​Dn\prod_{i=1}^{n}P_{i}D_{i}=\tilde{P}_
{n-1}\tilde{D}_{n-1}P_{n}D_{n}.

We will use a “swap” property of P and D matrices, as well as the column one-hot nature of the P
matrices, to show that D~n−1​Pn​Dn=Pn​D~n\tilde{D}_{n-1}P_{n}D_{n}=P_{n}\tilde{D}_{n}, which will
finish the proof.

For this last part, observe that left-multiplying PnP_{n} by the diagonal matrix D~n−1\tilde{D}_
{n-1} amounts to scaling the jj-th row of PnP_{n} by the jj-th diagonal entry of D~n−1\tilde{D}_
{n-1}. Since PnP_{n} is column one-hot and thus has at most one non-zero entry in each column, this
operation is the same as right-multiplying PnP_{n} with a relax-permuted version of the diagonal
matrix, namely πn​(D~n−1)\pi_{n}(\tilde{D}_{n-1}). That is,

 D~n−1​Pn\
 displaystyle\tilde   =Pn​πn​(D~n−1)\displaystyle=P_{n}\,\pi_{n}(\tilde{D}_{n-1})
 {D}_{n-1}P_{n}
                      =Pn​πn​(∏j=1n−1π~n−1,j​(Dj))\displaystyle=P_{n}\,\pi_{n}\left(\prod_{j=1}^
                      {n-1}\tilde{\pi}_{n-1,j}(D_{j})\right)
                      =Pn​∏j=1n−1πn​π~n−1,j​(Dj)=Pn​∏j=1n−1π~n,j​(Dj)\displaystyle=P_{n}\,\prod_{j
                      =1}^{n-1}\pi_{n}\tilde{\pi}_{n-1,j}(D_{j})\,\,=\,\,P_{n}\,\prod_{j=1}^{n-1}\
                      tilde{\pi}_{n,j}(D_{j})

Right-multiplying both sides by DnD_{n}, we obtain:

 D~n−1​Pn​Dn\           =Pn​(∏j=1n−1π~n,j​(Dj))​Dn=Pn​∏j=1nπ~n,j​(Dj)=Pn​D~n\displaystyle=P_{n}\,\
 displaystyle\tilde{D}_ left(\prod_{j=1}^{n-1}\tilde{\pi}_{n,j}(D_{j})\right)D_{n}=P_{n}\prod_{j=
 {n-1}P_{n}D_{n}        1}^{n}\tilde{\pi}_{n,j}(D_{j})=P_{n}\tilde{D}_{n}

as desired, finishing the proof. ∎

Appendix D Single-Layer LRNNs

D.1 Inexpressibility of Deterministic Graph Connectivity

Our main results (Section 3) show that, assuming 𝖯𝖭𝖢1≠𝖫\mathsf{PNC}^{1}\neq\mathsf{L}, multilayer
linear RNNs cannot solve the sorted deterministic graph connectivity problem. The same result holds
unconditionally in the single layer case, since a single-layer linear RNN is a WFA and no WFA can
represent the sorted deterministic graph connectivity problem (cf. Section 3):

Theorem 13.

No weighted finite automaton can recognize sorted deterministic graph connectivity.

Proof.

Assume by way of contradiction that we can represent sorted deterministic graph connectivity. Then
the Hankel matrix for this language has finite rank (Carlyle and Paz, 1971). But we can easily
construct an unbounded-rank subblock of the Hankel matrix where the rows are ss and columns are tt,
and there are no edges in the graph (this yields the identity). Thus, we have reached a
contradiction and this problem must not be representable. ∎

A single-layer linear RNN can be simulated by a weighted finite automaton (Merrill et al., 2020,
Theorem 4). Therefore, no linear RNN with just one layer can solve sorted deterministic graph
connectivity, in the following sense:

Corollary 8.

The hidden state of a single linear RNN layer cannot solve sorted deterministic graph connectivity.

The connection between LRNNs and WFAs shown here motivates considering a connection with an older
line of work on rational recurrences (Peng et al., 2018). The central idea here, before the
emergence of transformer, was that constraining RNNs so that their state was computable by a WFA
was desirable because it made them more computationally efficient. Our results here reveal why:
one-layer LRNNs are a kind of rational recurrence, and all rational recurrences can be computed in
𝖯𝖭𝖢1\mathsf{PNC}^{1}, i.e., in a highly parallel fashion. While stacking multiple LRNN layers may
not remain a WFA (Merrill et al., 2020; Mohri, 2026), it will remain in 𝖯𝖭𝖢1\mathsf{PNC}^{1} by
Theorem 3.

D.2 Single-Layer PD LRNNs and Deterministic WFAs

We say that a WFA (Definition 13) is deterministic if the initial state vector α\alpha and each
column over every transition matrix AσA_{\sigma} have at most one non-zero entry.

See 8

Proof.

We can represent an arbitrary deterministic WFA transition matrix in PD form as follows. Let dd be
the number of states. We set btb_{t} to be a one-hot encoding of the initial state at $\$ and the
zero vector elsewhere. For each σ\sigma and state qq, we set PσP_{\sigma} to be 1 at the unique q′q
^{\prime} to which qq transitions. We set the entry in DσD_{\sigma} corresponding to qq to have the
weight of this transition. Thus, a single-layer PD LRNN is equivalent to a deterministic WFA. ∎

Appendix E Arithmetic Circuits over ℚ\mathbb{Q}

We define arithmetic circuits over the rational field ⟨ℚ;+,−,×,0,1⟩\langle\mathbb{Q};+,-,\times,0,1
\rangle, which we often write simply as ℚ\mathbb{Q}. We represent a rational number a/ba/b as a
pair (a,b)(a,b) of integers. An arithmetic circuit CC over ℚ\mathbb{Q} with nn input nodes gives
rise to a function f:{0,1}n→ℤ×ℤf:\{0,1\}^{n}\to\mathbb{Z}\times\mathbb{Z}. That is, the circuit
will output a representation a/ba/b of a rational number, but it might not be normalized. We show
below that no problems arise in this way.

Proposition 4.

Any arithmetic circuit family 𝒞={Cn}n≥1\mathcal{C}=\{C_{n}\}_{n\geq 1} over ℚ\mathbb{Q} with
logarithmic depth gives rise to a 𝖦𝖺𝗉𝖭𝖢1\mathsf{GapNC}^{1} function. Therefore, it can be expressed
as a boolean bounded-fan-in circuit family with O⁡(log⁡n​log∗​n)O(\log n\log^{*}n) depth.

Proof.

To prove this, simply note that over ℚ\mathbb{Q}:

 ab+cd=a​d+b​cb​c\frac{a}{b}+\frac{c}{d}=\frac{ad+bc}{bc}

and

 ab×cd=a​cb​d.\frac{a}{b}\times\frac{c}{d}=\frac{ac}{bd}.

Similarly, −ab=−ab-\frac{a}{b}=\frac{-a}{b}. Therefore, each ℚ\mathbb{Q}-operation can be simulated
with at most four ℤ\mathbb{Z}-operations. Therefore, if 𝒞\mathcal{C} has log depth, then so are the
ℤ\mathbb{Z}-circuits simulating 𝒞\mathcal{C}.

So, as a corollary, 𝒞\mathcal{C} can be expressed as a boolean bounded-fan-in circuit family with
O⁡(log⁡n​log∗​n)O(\log n\log^{*}n) depth. ∎

Appendix F Additional Details for Experiments

Benchmark.

We evaluate models on a suite of synthetic algorithmic tasks designed to test length
generalization.

Deterministic graph reachability. We construct a synthetic reachability dataset over directed
graphs with nn vertices. Vertices are assigned to two buckets under label-dependent constraints:
for positive samples, vertices 00 and nn lie in the same bucket, while for negative samples, they
lie in different buckets. All remaining vertices are assigned independently with probability pp.
Directed edges are then added deterministically by connecting consecutive vertices within each
bucket, yielding two disjoint directed paths. Each instance encodes a reachability query from
vertex 00 to vertex nn as a binary-serialized sequence.

Iterated matrix multiplication with modulus. This task requires computing prefix products of a
sequence of 3×33\times 3 matrices over the finite ring ℤm\mathbb{Z}_{m}, where mm is a fixed prime.
Each example has the form SOS T|m​∣qk∣​M1​∣⋯∣​MTT\mid m\mid q_{k}\mid M_{1}\mid\cdots\mid M_{T} EOS
, where TT denotes the sequence length, qkq_{k} is a fixed query index, and each matrix Mt∈{−1,0,1}
3×3M_{t}\in\{-1,0,1\}^{3\times 3} is sampled independently and rejected until invertible modulo mm.
Let Pt=M1⋯MtmodmP_{t}=M_{1}\cdots M_{t}\bmod m denote the prefix product at step tt. The target
sequence is v1​∣⋯∣​vTv_{1}\mid\cdots\mid v_{T}, where vtv_{t} equals the qkq_{k}-th entry of PtP_
{t} modulo mm. Models are trained with a stepwise classification loss to predict vtv_{t} at each
step. The minimum sequence length for this task is 11.

Iterated matrix multiplication without modulus. In this task, models multiply a sequence of 3×33\
times 3 integer matrices over ℤ\mathbb{Z} and predict a single binary label. Each example has the
form SOS T|M1​∣⋯∣​MTT\mid M_{1}\mid\cdots\mid M_{T} EOS, where TT is the number of matrices and
each Mt∈{−1,0,1}3×3M_{t}\in\{-1,0,1\}^{3\times 3} is sampled i.i.d. with probabilities
(0.45,0.10,0.45)(0.45,0.10,0.45) for entries (−1,0,1)(-1,0,1). Let PT=M1M2⋯MTP_{T}=M_{1}M_{2}\cdots
M_{T} denote the product over ℤ\mathbb{Z}; the target label is defined as y=1y=1 iff (PT)0,0=0(P_
{T})_{0,0}=0, and y=0y=0 otherwise. We optionally construct balanced splits via rejection sampling
to mitigate length-dependent artifacts. Since integer products may overflow for large TT, labeling
can be performed with an optional clipping cap that truncates intermediate values during
multiplication. The minimum sequence length of this task is 11.

Across both deterministic graph connectivity and iterated matrix multiplication, we use a largely
standardized training setup to enable fair comparison across architectures. As shown in Table 1,
all models are trained with AdamW, a fixed batch size of 128, and gradient clipping at 1.0.
Learning rates and weight decay are kept consistent within each task, with minor adjustments for
architectures that require stronger regularization, such as DeltaNet. Training is capped at 60k
steps for deterministic graph connectivity and 60k steps for iterated matrix multiplication,
ensuring comparable optimization budgets while isolating architectural inductive biases as the
primary source of performance differences.

Task    Model                         Configuration                               LR          Steps
     RNN         dm=256d_{m}{=}256, L=2L{=}2, dh=256d_{h}=256,           ×10−43\!\times\!10^  60k
                                                                         {-4}
     Transformer dm=256d_{m}{=}256, L=2L{=}2, h=4h=4                     ×10−43\!\times\!10^  60k
                                                                         {-4}
DGC  Mamba       dm=256d_{m}{=}256, L=2L{=}2, dstate=128d_{\text{state}} ×10−43\!\times\!10^  60k
                 {=}128                                                  {-4}
     RWKV-7      dm=256d_{m}{=}256, L=2L{=}2, h=4h=4                     ×10−43\!\times\!10^  60k
                                                                         {-4}
     DeltaNet    dm=256d_{m}{=}256, L=2L{=}2, h=4h=4                     ×10−43\!\times\!10^  60k
                                                                         {-4}
     RNN         dm=256d_{m}{=}256, L=2L{=}2, dh=256d_{h}=256,           ×10−43\!\times\!10^  60k
                                                                         {-4}
     Transformer dm=256d_{m}{=}256, L=2L{=}2, h=4h=4                     ×10−43\!\times\!10^  60k
                                                                         {-4}
IMM  Mamba       dm=256d_{m}{=}256, L=2L{=}2, dstate=64d_{\text{state}}  ×10−43\!\times\!10^  60k
                 {=}64                                                   {-4}
     RWKV-7      dm=256d_{m}{=}256, L=2L{=}2, h=4h=4                     ×10−43\!\times\!10^  60k
                                                                         {-4}
     DeltaNet    dm=256d_{m}{=}256, L=2L{=}2, h=4h=4                     ×10−43\!\times\!10^  60k
                                                                         {-4}


Table 1: Training hyperparameters for deterministic graph connectivity (DGC) and iterated matrix
multiplication (IMM). dmd_{m}, LL, dhd_{h}, hh, dstated_{\text{state}}, and L​RLR denote the model
dimension, number of layers, hidden size, number of attention heads, SSM state expansion factor,
and learning rate, respectively. All models are trained with AdamW, batch size 128, and gradient
clipping at 1.0.

Experimental support, please view the build logs for errors. Generated by L A T E xml [LOGO] .

Instructions for reporting errors

We are continuing to improve HTML versions of papers, and your feedback helps enhance accessibility
and mobile support. To report errors in the HTML that will help us improve conversion and
rendering, choose any of the methods listed below:

  • Click the "Report Issue" ( ) button, located in the page header.

Tip: You can select the relevant text first, to include it in your report.

Our team has already identified the following issues. We appreciate your time reviewing and
reporting rendering errors we may not have found yet. Your efforts will help us improve the HTML
versions for all readers, because disability should not be a barrier to accessing research. Thank
you for your continued support in championing open access for all.

Have a free development cycle? Help support accessibility at arXiv! Our collaborators at LaTeXML
maintain a list of packages that need conversion, and welcome developer contributions.

We gratefully acknowledge support from our major funders, member institutions, , and all
contributors.
About · Help · Contact · Subscribe · Copyright · Privacy · Accessibility · Operational Status
(opens in new tab)
Major funding support from
Simons Foundation Simons Foundation International Schmidt Sciences
 
