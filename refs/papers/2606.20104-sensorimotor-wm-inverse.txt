source: https://arxiv.org/html/2606.20104
arxiv: https://arxiv.org/abs/2606.20104
note: HTML dump via w3m; not a PDF.
========================================================================

Report GitHub Issue

×
Title: [                    ]

Content selection saved. Describe the issue below:

Description: [                    ]
Submit without GitHub Submit in GitHub
[smileybone] arXiv is now an independent nonprofit! Learn more ×
arXiv logo Back to arXiv
Why HTML? Report Issue Back to Abstract Download PDF   

 1. Abstract
 2. 1 Introduction
     1. Setting and contributions.
 3. 2 Background and related work
     1. Latent world models.
     2. Representation collapse.
     3. Inverse dynamics.
     4. Other directions.
 4. 3 Method
     1. Dataset and setting.
     2. Forward dynamics in embedding space.
     3. Inverse dynamics regularization.
     4. Joint training objective.
 5. 4 Learned latent structure
     1. A controlled testbed.
     2. Recovered intrinsic dimension.
     3. Forward model.
     4. Equivariance and latent action composition.
     5. Controllable degrees of freedom.
     6. Visualizing control-dependent perception.
 6. 5 Planning in latent space
     1. Setup.
     2. Environments.
     3. Baselines.
     4. Architecture.
     5. Planning performance.
     6. Probing physical state.
     7. Latent geometry.
     8. Takeaway.
 7. 6 Discussion
     1. Limitations and future work.
 8. References
 9. A Implementation details
     1. A.1 Training objective
     2. A.2 Architecture
         1. Encoder fθf_{\theta}.
         2. Forward model gϕg_{\phi}.
         3. Inverse model hψh_{\psi}.
     3. A.3 Optimization details
         1. Choosing λ\lambda.
     4. A.4 Planning protocol
     5. A.5 Probing protocol.
     6. A.6 Compute.
10. B Environments and datasets
     1. (a) TwoRoom.
     2. (b) Push-T.
     3. (c) OGBench-Cube.
     4. (d) Reacher.
11. C Planning performance on extended goal offsets
12. D SIGReg latent geometry
13. E Dot world details
     1. E.1 Environment
     2. E.2 Architecture and training
     3. E.3 Collapse without the inverse loss
14. F Sprite world reconstruction experiment
     1. F.1 Environment and control settings
     2. F.2 Training and reconstruction probe
     3. F.3 Interpretation
15. G Equivariance and latent action composition
16. H Extended related work
     1. H.1 Inverse dynamics for representation learning
         1. H.1.1 Intrinsic Curiosity Module (ICM)
         2. H.1.2 Multi-step inverse identifiability theory
             1. AC-State [29].
             2. Refinements and known failure modes.
             3. Implication for our work.
     2. H.2 Inverse-dynamics-regularized world models
         1. DynaMo [10].
         2. LAPO and APV.
         3. World-Action Model [22].
         4. EB-JEPA [52].
         5. Auxiliary-task JEPA theory [57].
     3. H.3 Alternative anti-collapse mechanisms in latent world models
         1. H.3.1 Reconstruction-based
         2. H.3.2 EMA latent targets
         3. H.3.3 Augmentation and contrastive methods
         4. H.3.4 JEPA-style regularizers

License: CC BY 4.0
arXiv:2606.20104v1 [cs.LG] 18 Jun 2026

Sensorimotor World Models: Perception for Action via Inverse Dynamics

Petr Ivashkov ^ 1  Randall Balestriero^ 2  Bernhard Schölkopf^ 1,3,4 ^1Max Planck Institute for
Intelligent Systems, Tübingen, Germany ^2Department of Computer Science, Brown University,
Providence, RI, USA ^3ELLIS Institute, Tübingen, Germany ^4ETH Zürich, Zürich, Switzerland ^†^†
thanks: Correspondence to: petr.ivashkov@tuebingen.mpg.de.

Abstract

Perception for action suggests that representations of the world should be shaped not by visual
fidelity alone, but by their relevance for actions. At the same time, latent JEPA-style world
models advocate learning compact predictive states from high-dimensional observations to facilitate
the prediction of future states, but end-to-end training of these models is nontrivial because
representations may collapse if our only goal is to construct a latent state that is easy to
predict. We introduce a sensorimotor world model (SMWM): a latent world model trained end-to-end
with inverse dynamics regularization. This single regularizer addresses both issues: it prevents
representation collapse and induces action-aligned representations. By forcing latent states to
preserve information about the action underlying a transition, it biases the model toward the
controllable degrees of freedom of the environment while discarding uncontrollable distractors.
This yields stable latent world models trained from offline, reward-free trajectories, without
frozen encoders, exponential moving averages, or complex latent regularizers. Empirically, SMWM
learns compact, interpretable latent spaces and enables competitive planning performance across
simple 2D and 3D control tasks.

 Webpage  Code

1 Introduction

World models that predict future states given actions are central to intelligent agents [16, 18, 46
]. Learning such models directly in pixel space is difficult because observations are
high-dimensional and pixel-level reconstruction encourages the model to capture background details
that are often irrelevant for control. Latent world models avoid these issues by learning dynamics
in a representation space rather than in observation space [18, 21, 24, 23], and more recently,
Joint Embedding Predictive Architectures (JEPAs) [31, 2, 4] dispense with reconstruction altogether
and predict directly in embedding space.


Figure 1: Method overview. We train an encoder fθf_{\theta}, a forward dynamics model gϕg_{\phi},
and an inverse dynamics model hψh_{\psi} jointly from an offline dataset of transitions
(ot,at,ot+1)(o_{t},a_{t},o_{t+1}). The encoder maps each observation to a compact embedding, zt=fθ​
(ot)z_{t}=f_{\theta}(o_{t}) and zt+1=fθ​(ot+1)z_{t+1}=f_{\theta}(o_{t+1}). The forward model
predicts the next embedding from the current embedding and action, z^t+1=gϕ​(zt,at)\hat{z}_{t+1}=g_
{\phi}(z_{t},a_{t}), and is supervised by the mean-squared forward loss ℒfwd\mathcal{L}_{\text
{fwd}} between z^t+1\hat{z}_{t+1} and zt+1z_{t+1}. In parallel, the inverse model predicts the
executed action from the two embeddings, a^t=hψ​(zt,zt+1)\hat{a}_{t}=h_{\psi}(z_{t},z_{t+1}),
supervised by the mean-squared inverse loss ℒinv\mathcal{L}_{\text{inv}} between a^t\hat{a}_{t} and
ata_{t}. Both losses backpropagate into the encoder through the joint objective ℒ=ℒfwd+λ​ℒinv\
mathcal{L}=\mathcal{L}_{\text{fwd}}+\lambda\,\mathcal{L}_{\text{inv}}. The inverse loss acts as an
anti-collapse mechanism: to enable action recovery, fθf_{\theta} must preserve the action-relevant
information contained in (ot,ot+1)(o_{t},o_{t+1}).

JEPA-style world models from pixels are appealing but susceptible to representation collapse: when
the encoder and the latent dynamics model are trained jointly to make correct predictions in
embedding space, the encoder may learn to map all observations to a constant embedding, making the
dynamics prediction task trivial while rendering the learned model useless for other tasks [15, 5].
Recent work approaches this problem from several angles. DINO-WM [59] sidesteps the problem by
freezing a pretrained encoder. PLDM [49] adds a multi-term variance–covariance regularizer on the
embeddings. LeWorldModel [33, 3] matches the embedding distribution to an isotropic Gaussian via
SIGReg. V-JEPA 2 [1] uses stop-gradient targets from an exponential-moving-average target encoder.

While representation collapse is an important practical problem, it also points to the basic
question of what a useful world model representation should retain. It has been argued that this
connects to causality: a causal representation [45] should not merely encode predictive
correlations, but also actions and their effects, to provide an interventional world model [46]. In
cognitive psychology and neuroscience, the term perception for action captures the view that
perception ultimately subserves action [14]. Common coding proposes that perceived events and
planned actions share a unified representational domain, with actions represented partly in terms
of their anticipated perceptual effects [40], rendering the content of a representation dependent
on the organism’s action repertoire.^1^1 1 This also relates to Gibson’s notion of affordances:
organisms perceive the environment in terms of possibilities for action, such as grasping, walking,
avoiding, or manipulating [13]. Going further back, von Uexküll’s concept of the Umwelt describes
the organism-specific world disclosed by a creature’s sensory and motor capacities [55].
Ultimately, the world should thus look rather different to different creatures, even if they appear
to inhabit the same environment. Representations should thus not only be judged by how much
information they preserve about observations, but by whether they preserve the distinctions that
matter for the actions available to the agent and for the consequences of those actions.

Enactive perception is the theory that perception is not a passive reception of sensory input, but
an active bodily exploration of the world [54]. A recent instantiation of this view focuses on 
sensorimotor contingencies as the lawful relationships between an agent’s actions and the resulting
changes in sensory input [36]. With this in mind, we return to our practical problem, i.e.,
collapse: can we train a latent world model whose representation respects these contingencies by
retaining information sufficient to recover the agent’s actions? We call the resulting object a
sensorimotor world model (SMWM): a predictive latent model whose state is shaped by the coupling
between motor commands and sensory change.

We use a simple and principled mechanism to instantiate this idea: inverse dynamics regularization.
We add a single-step inverse dynamics head that predicts the action taken between two consecutive
observations from their embeddings, and we propagate gradients from this head into the encoder, as
illustrated in Fig. 1. The intuition is direct: if two consecutive embeddings contain enough
information to recover the action that produced the transition, they must preserve features of the
observations that are relevant for controllable dynamics. This encourages the encoder to retain
action-relevant information while discarding the rest. Unlike distributional regularizers that
prescribe the geometry of the embedding space, inverse dynamics anchors the representation to a
task-grounded quantity—the action.

Setting and contributions.

We consider offline datasets of trajectories consisting of video frames and corresponding
continuous actions, without rewards, task labels, or knowledge of the data-collecting policy. This
setting broadly covers demonstration data, action-annotated video datasets, and logged interaction
data. Our contributions are:

 1. 1.
   
    SMWM, a simple sensorimotor world model trained end-to-end from pixels, using inverse dynamics
    regularization as the sole anti-collapse mechanism with one additional hyperparameter,

 2. 2.
   
    evidence that the learned representations are not merely non-collapsed, but track the
    controllable degrees of freedom, preserve spatial geometry, and filter out uncontrollable
    distractors,

 3. 3.
   
    competitive planning performance against SIGReg-regularized baselines across 2D and 3D control
    tasks, showing that this structural advantage carries over to downstream control.

2 Background and related work

Latent world models.

Learning dynamics models in learned latent spaces has a rich history. The Dreamer family [18, 20,
21] and PlaNet [19] learn latent dynamics with a pixel decoder under a variational objective.
TD-MPC [24, 23] drops the decoder and trains a joint encoder and latent dynamics model end-to-end
with a temporal-difference objective. JEPAs [31, 2, 4] push this further, dispensing with both
reconstruction and reward signal and predicting directly in embedding space. Our work belongs to
the JEPA family and operates in the offline, reward-free regime.

Representation collapse.

The self-supervised learning literature has charted a design space of remedies for representation
collapse: contrastive losses with negative samples [53, 8], asymmetric architectures with
stop-gradients [15, 9], and regularizers on the embedding distribution [58, 5]. Recent JEPA world
models inherit choices from this design space: DINO-WM [59] freezes a pretrained encoder, PLDM [49]
adds multiple auxiliary loss terms including variance–covariance regularization, temporal
smoothness, and inverse dynamics, LeWorldModel [33, 3] matches embeddings to an isotropic Gaussian,
and V-JEPA 2 [1] uses stop-gradient and exponential moving average target networks. We share the
JEPA training framework and the offline pixel-action setting with these works but use
inverse-dynamics as the standalone anti-collapse mechanism, as described in Sec. 3.

Inverse dynamics.

The use of inverse dynamics for representation learning is not new. Pathak et al. [39] use it to
learn features that capture controllable aspects of the environment for intrinsic-reward
computation. A line of theoretical work shows that, in finite rich-observation settings with
exogenous distractors, multi-step inverse objectives can recover control-relevant latent state
information under certain assumptions [11, 29, 26, 35]. While none of these results transfers to
our continuous-state, continuous-action setting with a single-step inverse, we treat them as
motivation for using action prediction as an inductive bias toward controllable state structure.
Concurrent work has begun to combine inverse dynamics with world-model objectives in related but
distinct settings [22, 52, 57]; we discuss our differences from each of these works in App. H.

Other directions.

Several adjacent lines share architectural ingredients but address different problems. Action-free
or latent-action world models infer pseudo-actions from video and use them to supervise dynamics:
LAPO [44] and DynaMo [10] use a learned inverse dynamics model to extract latent actions from
action-free demonstrations, and APV [48] pretrains an action-free latent video predictor for
downstream action-conditional world-model learning. Visual RL methods share encoders between
auxiliary objectives and policies but do not learn explicit world models [30, 28, 56]. Bisimulation
metrics offer a complementary lens on which states should map to similar embeddings [12, 7], and
causal abstraction [42] formalizes a related consistency between structural models via a
commutative diagram on states and interventions. App. H provides further detail.

3 Method

Dataset and setting.

We assume access to an offline dataset 𝒟={(ot,at,ot+1)}\mathcal{D}=\{(o_{t},a_{t},o_{t+1})\} of
transitions, where ot,ot+1∈𝒪o_{t},o_{t+1}\in\mathcal{O} are consecutive observations (e.g., video
frames) and at∈𝒜⊆ℝma_{t}\in\mathcal{A}\subseteq\mathbb{R}^{m} is a continuous action taken in
between. We do not require a behavior policy or a reward function. Our goal is to learn an encoder
fθ:𝒪→𝒵f_{\theta}:\mathcal{O}\rightarrow\mathcal{Z} that maps observations to a compact embedding
space 𝒵⊆ℝd\mathcal{Z}\subseteq\mathbb{R}^{d}, together with a forward dynamics model gϕ:𝒵×𝒜→𝒵g_{\
phi}:\mathcal{Z}\times\mathcal{A}\rightarrow\mathcal{Z} that predicts the next embedding from the
current embedding and action.

Forward dynamics in embedding space.

Given a transition (ot,at,ot+1)(o_{t},a_{t},o_{t+1}), we encode both observations and train the
dynamics model to match the embedding of the next observation,

 zt=fθ​(ot),zt+1=fθ​(ot+1),z^t+1=gϕ​(zt,at),z_{t}=f_{\theta}(o_{t}),\quad z_{t+1}=f_{\theta}    (1)
 (o_{t+1}),\quad\hat{z}_{t+1}=g_{\phi}(z_{t},a_{t}),

by minimizing the mean-squared forward loss

 ℒfwd=𝔼(ot,at,ot+1)∼𝒟​[‖z^t+1−zt+1‖22].\mathcal{L}_{\text{fwd}}=\mathbb{E}_{(o_{t},a_{t},o_     (2)
 {t+1})\sim\mathcal{D}}\left[\|\hat{z}_{t+1}-z_{t+1}\|_{2}^{2}\right].

This objective alone admits a trivial solution: if fθf_{\theta} maps every observation to a
constant embedding z⋆z^{\star} and gϕg_{\phi} outputs z⋆z^{\star} regardless of its inputs, then
ℒfwd=0\mathcal{L}_{\text{fwd}}=0. Such a collapsed representation carries no information about the
observation and is useless for downstream planning or control.

Inverse dynamics regularization.

To prevent this collapse, we introduce an inverse dynamics model hψ:𝒵×𝒵→𝒜h_{\psi}:\mathcal{Z}\times
\mathcal{Z}\rightarrow\mathcal{A} that predicts the action taken between two consecutive
observations from their embeddings, a^t=hψ​(zt,zt+1)\hat{a}_{t}=h_{\psi}(z_{t},z_{t+1}), trained
with the mean-squared action loss

 ℒinv=𝔼(ot,at,ot+1)∼𝒟​[‖a^t−at‖22].\mathcal{L}_{\text{inv}}=\mathbb{E}_{(o_{t},a_{t},o_{t+1})\  (3)
 sim\mathcal{D}}\left[\|\hat{a}_{t}-a_{t}\|_{2}^{2}\right].

Under squared loss, a collapsed encoder would give the inverse model identical inputs for every
transition, so the best inverse predictor is a constant action, with risk 𝔼​‖at−𝔼⁡[at]‖22\mathbb{E}
\|a_{t}-\mathbb{E}[a_{t}]\|_{2}^{2}. Any reduction below this constant-predictor risk requires
(zt,zt+1)(z_{t},z_{t+1}) to preserve information that is predictive of ata_{t}. Such a nontrivial
inverse prediction thus rules out a fully collapsed representation. In contrast to distributional
priors such as SIGReg’s isotropic-Gaussian regularizer [33], this mechanism does not prescribe the
geometry of the embedding space: it only requires that the encoder preserve whatever information
about ata_{t} is contained in the pair (ot,ot+1)(o_{t},o_{t+1}).

Joint training objective.

We jointly optimize the encoder, forward model, and inverse model by minimizing

 ℒ=ℒfwd+λ​ℒinv,\mathcal{L}=\mathcal{L}_{\text{fwd}}+\lambda\,\mathcal{L}_{\text{inv}},  (4)

where λ>0\lambda>0 controls the strength of the inverse dynamics regularization. The forward loss
updates fθf_{\theta} and gϕg_{\phi}; the inverse loss updates fθf_{\theta} and hψh_{\psi}. The
encoder, therefore, receives gradients from both objectives, producing embeddings that are
simultaneously predictive under forward dynamics and action-informative under inverse dynamics.

4 Learned latent structure

To build intuition for what SMWM learns and to assess which structure of the world the resulting
representations reflect, we instantiate the method on a minimal testbed with known ground-truth
state, intrinsic dimension, and action geometry.

A controlled testbed.

We consider a dot world: a single-dot environment whose state is fully described by the 2D position
(x,y)(x,y) of a colored dot rendered on a 64×6464{\times}64 white canvas; the observation
ot∈ℝ3×64×64o_{t}\in\mathbb{R}^{3\times 64\times 64} is the rendered image. Actions at=(Δ​x,Δ​y)
∈𝒜⊂ℝ2a_{t}=(\Delta x,\Delta y)\in\mathcal{A}\subset\mathbb{R}^{2} displace the dot, so the dynamics
are st+1=st+ats_{t+1}=s_{t}+a_{t} in the state space. The dataset 𝒟\mathcal{D} is constructed by
sampling, for each transition, a position and a random displacement. The encoder fθf_{\theta} is a
small CNN with the latent dimension d=64d=64, and the forward gϕg_{\phi} and inverse hψh_{\psi}
models are two-layer MLPs. The exact architecture, dataset, and training details are deferred to
App. E.

Recovered intrinsic dimension.

First, we show that fθf_{\theta} identifies the true 2D structure of the world from pixels and
actions alone. Fig. 2 reports the PCA spectrum of the embeddings {fθ​(o)}\{f_{\theta}(o)\} over a
uniform grid of dot positions. Two principal components carry essentially all the variance in the
embedding space, and the spectrum drops sharply at the true intrinsic dimension dtrue=2d_{\text
{true}}=2; the remaining 6262 directions are effectively collapsed. Projecting the embeddings onto
the top two principal components reveals that the latent geometry is not only low-dimensional but
also spatially faithful: the grid of world states maps to a near-square grid in latent space. A
mild deformation occurs only near the boundary, which corresponds to out-of-distribution states. As
a result, spatial neighbors in the observation space remain neighbors in the latent space. The
encoder thus recovers an approximately 2-dimensional, topology-preserving representation of the
state space without any ground-truth state supervision.

Refer to caption
Figure 2: Dot world latent geometry. Left: PCA spectrum of the learned embeddings; the
explained-variance ratio drops sharply past the true intrinsic dimension dtrue=2d_{\text{true}}=2
(red dashed line). Center: grid of probe world states (x,y)(x,y), color-coded by position. Right:
the same probes embedded by fθf_{\theta} and projected onto the top two principal components.
Despite no state supervision, the encoder recovers an effectively 2-dimensional representation
inside a 64-dimensional ambient latent space. Moreover, the recovered representation is spatially
faithful to the true state space. The latent grid appears rotated relative to (x,y)(x,y), as
expected: PC axes are identifiable only up to rotation and sign.

Forward model.

The encoder and forward model should jointly satisfy a consistency condition: encoding the
observation reached after taking action aa should agree with first encoding the current observation
and then applying gϕ​(⋅,a)g_{\phi}(\cdot,a). For ease of notation, we drop the indices and write
ga​(z):=gϕ​(z,a)g_{a}(z):=g_{\phi}(z,a) and ff for fθf_{\theta}, rendering the consistency
condition

 f⁡(a⁡(o))​=!​ga​(f⁡(o)).f(a(o))\overset{!}{=}g_{a}(f(o)).  (5)

In other words, the diagram on the left of Fig. 3 must commute [25, 42]. To test it, we encode an
initial observation o1o_{1}, unroll gg autoregressively along a 5-step action sequence a1,…​a5a_
{1},\dots a_{5}, and compare the predicted embeddings against the encoded ground-truth embeddings
zt=f⁡(ot)z_{t}=f(o_{t}). The predicted and encoded trajectories agree closely along the entire
rollout (Fig. 3, right), illustrating that Eq. 5 approximately holds on the trained model. Each
at∈𝒜a_{t}\in\mathcal{A} induces a corresponding displacement ρ⁡(at)\rho(a_{t}) in the relevant
subspace of the latent space (with ρ⁡(a)\rho(a) approximately independent of zz), so the forward
model behaves as ga​(z)≈z+ρ⁡(a)g_{a}(z)\approx z+\rho(a). The trained model thus delivers, in
addition to the state representation ff, a representation of actions as latent translations gag_
{a}, without any term in ℒ\mathcal{L} enforcing such a structure.

Refer to caption
Figure 3: Encoder and forward model commute. Left: Equivariance that should be satisfied by the
learned representation: f∘a=ga∘ff\circ a=g_{a}\circ f. Center: a 5-step trajectory in world-state
space with actions a1,…,a5a_{1},\dots,a_{5}. Right: the corresponding rollout in latent space;
predictions z^t\hat{z}_{t} (filled red) obtained by autoregressive application of gg track the
encoded ground-truth embeddings zt=f⁡(ot)z_{t}=f(o_{t}) (open blue) along the entire rollout.
Therefore, the encoder and forward model approximately commute, and world actions act as
translations in latent space.

Equivariance and latent action composition.

The above approximate equivariance condition is encouraged by the forward prediction loss. Denote
the effect of action aa on the observation oo by o↦a⁡(o)o\mapsto a(o).^2^2 2 We overload notation:
in some cases, a∈𝒜⊆ℝma\in\mathcal{A}\subseteq\mathbb{R}^{m} denotes an action vector. The forward
loss minimizes, in our simplified notation,

 ‖f⁡(a⁡(o))−ga​(f⁡(o))‖22.\left\|f(a(o))-g_{a}(f(o))\right\|_{2}^{2}.

On the support of the training distribution and up to approximation error, this encourages

 f⁡(a⁡(o))≈ga​(f⁡(o)).f(a(o))\approx g_{a}(f(o)).

This means that encoding after applying the intervention approximately agrees with first encoding
and then applying the learned latent intervention gag_{a}.^3^3 3 The encoder thus intertwines the
physical intervention with the learned latent intervention, making this a statement of equivariance
. In physics terminology one might also say that the latent state transforms covariantly with the
intervention.

This one-step equivariance has a compositional consequence. If actions form a semigroup under
composition and equivariance holds exactly, then physical action composition must be represented by
composition of the learned latent interventions, at least on the encoded data manifold:

Theorem 1.

Let 𝒜\mathcal{A} be a semigroup. If ff is equivariant, i.e., f⁡(a⁡(o))=ga​(f⁡(o))f(a(o))=g_{a}(f
(o)) for all a∈𝒜a\in\mathcal{A} and o∈𝒪o\in\mathcal{O}, then a↦gaa\mapsto g_{a} is a homomorphism
on f⁡(𝒪)f(\mathcal{O}). That is, for all a1,a2∈𝒜a_{1},a_{2}\in\mathcal{A} and z∈f⁡(𝒪)z\in f(\
mathcal{O}),

 ga2∘a1​(z)=ga2​(ga1​(z)).g_{a_{2}\circ a_{1}}(z)=g_{a_{2}}\!\left(g_{a_{1}}(z)\right).  (6)

We prove theorem 1 in App. G. In the learned model, this identity should be interpreted
approximately.

Equivariance alone, however, admits degenerate solutions, including collapse. Useful
representations, therefore, additionally require some form of faithfulness of gg on f⁡(𝒪)f({\cal
O}), i.e., every a∈𝒜a\in\cal A that acts nontrivially on 𝒪\cal O must also act nontrivially on f⁡
(𝒪)f({\cal O}). More strongly, we can require that the latent action is identifiable from
observations: for every pair (z,z′)∈f⁡(𝒪)×f⁡(𝒪)(z,z^{\prime})\in f({\cal O})\times f({\cal O}) with
z′=ga​(z)z^{\prime}=g_{a}(z), the action aa is recoverable from (z,z′)(z,z^{\prime}). We achieve
this by training an inverse model that predicts aa from (z,z′)(z,z^{\prime}).

Beyond a homomorphism into latent transformations under composition, the experiments in Fig. 3
suggest that in our case the homomorphism may (approximately) go into the additive group, i.e., the
learned interventions appear to act approximately as translations,

 ga​(z)≈z+ρ⁡(a),g_{a}(z)\approx z+\rho(a),

with ρ⁡(a)\rho(a) approximately independent of zz. It is intriguing to ask where it may stem from.
The inverse-dynamics loss provides a plausible bias toward this translation-like regime: a simple
way to make ata_{t} recoverable from (zt,zt+1)(z_{t},z_{t+1}) is to encode the action in the
displacement vector

 zt+1−zt≈ρ⁡(at).z_{t+1}-z_{t}\approx\rho(a_{t}).  (7)

If ρ\rho is injective on the action support, then the inverse hh can decode the action by applying
an approximate inverse of ρ\rho to this displacement. This mechanism does not enforce additivity,
but it renders an additive, translation-like representation a low-complexity solution.

Controllable degrees of freedom.

The 2D dot world establishes that the learned representation has the right intrinsic dimension when
it equals the action dimension. To stress-test this property, we vary the controllable degrees of
freedom and add uncontrollable distractors. Fig. 4 reports four configurations. Independent
contains two dots controlled by independent displacements, giving a 4D state and a 4D action.
Coupled contains two dots that move together under a single 2D displacement. Distractor contains
one controlled dot together with a second dot that moves randomly on its own and is not controlled
by the action. Combined merges all three: two independent dots, one coupled pair, and one
distractor, with a 6D action vector.

In every case, the number of significant principal components matches the controllable dimension of
the system, with the remaining directions effectively collapsed. One may notice that the spectra
within the controllable subspace are not exactly uniform, even though the state space is uniformly
covered under uniform coordinate sampling. However, this is expected: the encoder is free to
rescale individual dimensions and concentrate variance unevenly, so what matters is the rank of the
embedding, not the uniformity of its spectrum. Of the four configurations, the distractor case is
particularly informative: even though the random dot’s position varies in the observation, the
encoder ignores it. This is the expected outcome of Eq. 4: a distractor’s state is neither needed
to recover the action from a transition (it is independent of ata_{t}) nor predictable from past
latent state under gϕg_{\phi} (since it evolves stochastically), so encoding it would only inflate
ℒfwd\mathcal{L}_{\text{fwd}} without reducing ℒinv\mathcal{L}_{\text{inv}}.

Refer to caption
Figure 4: Effective latent dimension tracks controllable degrees of freedom. Top row: four
dot-world configurations with controllable dimensions 44, 22, 22, and 66; in Distractor and
Combined, the wavy-arrowed dot moves randomly and is not controlled by the action. Bottom row: PCA
spectra of the corresponding learned embeddings, with the true intrinsic dimension marked by the
red dashed line. The encoder allocates significant variance to exactly the controllable degrees of
freedom and filters out uncontrollable distractors, correctly recovering a minimal necessary
subspace inside the higher-dimensional ambient latent space.

Visualizing control-dependent perception.

As a complementary visualization, we ask whether the model “sees” the same object differently
depending on what is controllable. To make this explicit, we introduce an additional toy
environment in App. F: an asymmetric 2D triangular sprite whose pose is (x,y,θ)(x,y,\theta). The
sprite can translate and rotate, but we vary the action repertoire by exposing only some of the
pose changes as actions, while the remaining degrees of freedom continue to move randomly. After
training, we freeze the encoder and train a post-hoc decoder from ztz_{t} to oto_{t}. The
reconstructions show that the representation preserves the control-relevant pose variables and
averages out the uncontrolled ones: for example, when agent rotations are uncontrolled, the decoded
triangle becomes an orientation-averaged blob, whereas full x/y/θx/y/\theta control yields a sharp
oriented reconstruction.

Together, these experiments show that SMWM learns representations that are low-dimensional,
spatially faithful, and structure-preserving with respect to the action-induced dynamics.

5 Planning in latent space

The latent structure recovered above is only useful insofar as it carries over to richer
environments and supports actionable downstream control.

Setup.

We perform goal-conditioned trajectory optimization directly in the learned latent space, following
the planning setup of LeWM [33]. Given a current observation o1o_{1} and a goal observation ogo_
{g}, we encode both with the frozen encoder, z1=fθ​(o1)z_{1}=f_{\theta}(o_{1}) and zg=fθ​(og)z_{g}=
f_{\theta}(o_{g}), and roll the forward model out autoregressively from z^1=z1\hat{z}_{1}=z_{1}, z^
t+1=gϕ​(z^t,at)\hat{z}_{t+1}=g_{\phi}(\hat{z}_{t},a_{t}), to predict the latent trajectory induced
by a candidate action sequence a1:Ha_{1:H} over a planning horizon HH. Each candidate is scored by
the terminal goal-matching cost

 𝒞(a1:H)=∥z^H+1−zg∥22,\mathcal{C}(a_{1:H})=\|\hat{z}_{H+1}-z_{g}\|_{2}^{2},  (8)

which we minimize with the Cross-Entropy Method (CEM) [43]: at each iteration, CEM samples action
sequences from a Gaussian, scores them via latent rollouts under gϕg_{\phi}, and refits the
sampling distribution to the top-scoring fraction. Because autoregressive rollouts accumulate
prediction error as HH grows, we wrap CEM in a receding-horizon model-predictive control (MPC)
loop, executing only the first K≤HK\leq H optimized actions before re-encoding the resulting
observation and replanning. Throughout, fθf_{\theta} and gϕg_{\phi} remain frozen; only the action
sequence is optimized.

Environments.

We evaluate on four tasks shown in Fig. 5: TwoRoom (2D navigation), Reacher (continuous control),
Push-T (2D contact-rich manipulation), and OGBench-Cube (3D tabletop manipulation). Datasets and
environment details are deferred to App. B.

Baselines.

We compare against three baselines. SIGReg [33] is our main head-to-head: it shares the same
encoder and predictor but replaces ℒinv\mathcal{L}_{\text{inv}} with a Gaussian-matching
regularizer on the latent embeddings. Forward-only (λ=0\lambda=0) ablates the regularizer entirely,
isolating the contribution of our anti-collapse mechanism. Random samples actions uniformly from
the action space and serves as a floor reference. Full implementation details are in the appendix.

Architecture.

We adopt LeWM’s encoder and predictor [33]: the encoder is a ViT-Tiny (∼\sim5M parameters) whose
final cls token is projected to z∈ℝdz\in\mathbb{R}^{d} with d=192d=192, and gϕg_{\phi} is a small
transformer (∼\sim10M parameters) that predicts z^t+1\hat{z}_{t+1} from a single (zt,at)(z_{t},a_
{t}) pair, with ata_{t} injected via AdaLN-zero. Our only architectural addition is the inverse
model hψh_{\psi}: a 2-layer MLP of width 256256 mapping [zt;zt+1]∈ℝ2​d[z_{t};\,z_{t+1}]\in\mathbb
{R}^{2d} to a^t∈ℝm\hat{a}_{t}\in\mathbb{R}^{m}. Details are provided in App. A.

Refer to caption
Figure 5: Planning success across environments. Top: the four evaluation environments—TwoRoom (2D
navigation), Reacher (continuous control), Push-T (2D contact-rich manipulation), and OGBench-Cube
(3D tabletop manipulation). Bottom: goal-conditioned planning success rate (mean and standard error
over five seeds) under a fixed budget of 5050 environment steps and a goal placed 2525 steps ahead
of the initial state. SMWM matches the SIGReg baseline on the three 2D tasks and clearly
outperforms it on OGBench-Cube. Both regularized variants dominate the Forward-only ablation and
the Random-action baseline, confirming that an effective anti-collapse mechanism is necessary for
usable planning.

Planning performance.

All methods are evaluated with a fixed budget of 5050 environment steps per episode and goals
placed 2525 steps ahead of the initial state. Both regularized variants substantially outperform
Forward-only and Random across all four environments (Fig. 5). SMWM approximately matches SIGReg on
the three 2D tasks—TwoRoom, Reacher, and Push-T—and clearly outperforms it on OGBench-Cube, the
only environment with a 3D contact-rich manipulation, where SMWM retains 84%84\% success while
SIGReg drops to 59%59\%.

Probing physical state.

To assess what physical content the embeddings retain, we train linear and two-layer MLP probes on
frozen embeddings to regress the ground-truth state of each environment; held-out R2R^{2} values
are reported in Tab. 1.


Table 1: Probing the physical state. Held-out R2R^{2} for linear and two-layer MLP probes
regressing ground-truth physical quantities from frozen embeddings; the best score per row and
probe type is in bold. Both regularized methods (SMWM and LeWM/SIGReg) recover the underlying
physical state near-perfectly under MLP probes, with the exception of Push-T block pose.

Environment     Quantity      Linear probe           MLP probe
                              SMWM  SIGReg Fwd.-only SMWM  SIGReg Fwd.-only
  TwoRoom    agent position   1.000 0.996  0.832     1.000 1.000  0.991
  Reacher     joint angles    0.946 0.999  0.647     0.995 1.000  0.962
  Push-T     agent position   0.993 0.954  0.293     0.988 0.986  0.433
             block position   0.946 0.973  0.352     0.981 0.996  0.623
            block orientation 0.664 0.931  0.202     0.882 0.987  0.491
   Cube       cube position   0.998 0.975  0.706     0.998 0.993  0.854
            gripper position  0.999 0.986  0.902     0.999 0.995  0.979
             gripper opening  0.989 0.961  0.269     0.992 0.983  0.427

Both regularized methods recover the underlying state near-perfectly under MLP probes, and their
linear-probe scores stay near saturation across most quantities, with SMWM holding a consistent
linear advantage on the Push-T agent position and on all three Cube quantities, and SIGReg holding
a corresponding advantage on the Reacher joint angles and the Push-T block pose. The Forward-only
ablation is markedly poorer, particularly under linear probes, but is rarely fully degenerate—some
residual variance survives even in a partially collapsed encoder. Both regularized representations
therefore make the underlying physical state recoverable.

Latent geometry.

We next turn to the geometry that organizes them. Fig. 6 reports, for each environment, the PCA
spectrum of held-out embeddings (top), the distribution of a representative ground-truth quantity
in physical state space (middle), and the embeddings projected onto a 2- or 3-dimensional PC
subspace (bottom). In every environment, the spectrum drops sharply, confirming that the embeddings
concentrate on an effectively low-dimensional subspace of the 192192-dimensional ambient latent
space. The corresponding SIGReg visualizations are provided in App. D; while SIGReg also prevents
collapse, its latent geometry appears less consistently organized by the controllable coordinates.

Refer to caption
Figure 6: Latent geometry of SMWM embeddings. For each environment we show the PCA spectrum of
held-out embeddings (top), the distribution of a representative ground-truth quantity in physical
state space (middle), and the embeddings projected onto a 2- or 3-dimensional PC subspace,
color-coded by the same physical quantity (bottom). The dashed red lines mark the action dimension.
Across all four environments, the embeddings concentrate on a low-dimensional subspace whose
geometry mirrors the controllable state space: Cartesian coordinates (TwoRoom and Push-T agent,
Cube gripper) appear as approximately linear directions, while the periodic joint angles in Reacher
are encoded as a flat 2-torus, visible as a cylindrical structure in any 3D PC projection.

In TwoRoom, the encoder behaves much as in the dot world: PC1\mathrm{PC}_{1} and PC2\mathrm{PC}_{2}
recover a spatially faithful Cartesian map of the agent, and the wall—a region unreachable to the
agent—leaves an empty band in latent space, providing further evidence that the recovered
representation is spatially faithful. In Push-T, the agent position is again encoded in (PC1,PC2)(\
mathrm{PC}_{1},\mathrm{PC}_{2}), but the spectrum has heavier tails: the embedding must
additionally accommodate the position and orientation of the manipulable block (cf. Tab. 1).
Reacher reveals a richer geometry. Its state space is the torus 𝕋2\mathbb{T}^{2} of two periodic
angles (θ1,θ2)(\theta_{1},\theta_{2}), which cannot be continuously encoded into ℝ2\mathbb{R}^{2}
due to a discontinuity at θ=0∼2​π\theta=0\sim 2\pi. A natural representation is the
product-of-circles embedding (θ1,θ2)↦(cos⁡θ1,sin⁡θ1,cos⁡θ2,sin⁡θ2)(\theta_{1},\theta_{2})\mapsto(\
cos\theta_{1},\sin\theta_{1},\cos\theta_{2},\sin\theta_{2}), which lifts 𝕋2\mathbb{T}^{2} into ℝ4\
mathbb{R}^{4}. Its coordinate 3D projections are circular cylinders, which is exactly the signature
we observe in the encoded embeddings: the spectrum exposes four appreciable components, and every
3D PC subspace in Fig. 6 (bottom) shows a clear cylindrical shape. The two angles, however, spread
across all four components, which is consistent with arbitrary linear mixing of the
product-of-circles coordinates. Finally, in Cube the gripper position is recovered in (PC1,PC3)(\
mathrm{PC}_{1},\mathrm{PC}_{3}); PC2\mathrm{PC}_{2} and the heavier tail of the spectrum carry the
cube pose and gripper opening (cf. Tab. 1).

Takeaway.

Across all four environments, SMWM recovers compact latent spaces whose dimension and geometry
mirror controllable structure: Cartesian degrees of freedom form approximately linear directions,
while periodic variables form circles. The structure carries over to 2D and 3D control, with the
largest planning gain over SIGReg appearing on the 3D environment.

6 Discussion

We showed that inverse dynamics regularization addresses two coupled issues simultaneously. First,
as an anti-collapse mechanism, a single-step inverse dynamics head suffices to stabilize end-to-end
training of a JEPA-style latent world model from pixels, while introducing only one hyperparameter
and imposing no distributional prior on the embedding space. Second, the same mechanism biases the
encoder toward a compact, action-sufficient representation due to the joint pressure from the
forward and inverse dynamics loss terms. Empirically, this pressure yields representations that
track the controllable degrees of freedom of the environment, preserve both Cartesian and periodic
state geometry, and filter out uncontrollable distractors, although no term in the objective
explicitly enforces this structure. The learned representation further supports latent planning
that matches or outperforms a strong SIGReg-regularized baseline across four control tasks, with
the largest margin on the most complex 3D manipulation environment with a five-dimensional action
space. Taken together, these results support the view of the sensorimotor world model as an
effective and simple instantiation of a “perception for action” bias in latent world-model
learning.

Limitations and future work.

Our analysis assumes that actions are recoverable from consecutive observations, which may fail
when distinct actions induce the same visible change. Relatedly, because the encoder currently maps
individual frames to latent states, it will not capture quantities that are not identifiable from a
single frame, such as velocities, even when these quantities would improve forward prediction;
incorporating short observation histories into the encoder or predictor is a natural extension.
Furthermore, the action-aligned bias, while desirable, may discard information needed for
downstream tasks that depend on aspects of the environment irrelevant to the training actions (we
consider this a feature rather than a bug). In addition, biased behavior policies may create
action-correlated but uncontrollable distractors, which could be captured by the encoder. Finally,
our planning experiments inherit the usual limitations of offline world models: limited dataset
coverage constrains the reachable plans, long-horizon rollouts remain vulnerable to compounding
model error, and our empirical validation is restricted to moderate-scale simulated control tasks.
Future work should study more robust data regimes, multi-frame, and multi-step inverse objectives.
While we use inverse loss only as a regularizer, inverse models themselves could be integrated into
latent or hierarchical planners to reduce reliance on long autoregressive forward rollouts.

References

  • [1] M. Assran, A. Bardes, D. Fan, Q. Garrido, R. Howes, M. Komeili, M. Muckley, A. Rizvi, C.
    Roberts, K. Sinha, A. Zholus, S. Arnaud, A. Gejji, A. Martin, F. Robert Hogan, D. Dugas, P.
    Bojanowski, V. Khalidov, P. Labatut, F. Massa, M. Szafraniec, K. Krishnakumar, Y. Li, X. Ma, S.
    Chandar, F. Meier, Y. LeCun, M. Rabbat, and N. Ballas (2025) V-JEPA 2: self-supervised video
    models enable understanding, prediction and planning. arXiv preprint arXiv:2506.09985. External
    Links: Link Cited by: §H.3.4, §1, §2.
  • [2] M. Assran, Q. Duval, I. Misra, P. Bojanowski, P. Vincent, M. Rabbat, Y. LeCun, and N.
    Ballas (2023) Self-supervised learning from images with a joint-embedding predictive
    architecture. In CVPR, Cited by: §H.3.4, §1, §2.
  • [3] R. Balestriero and Y. LeCun (2025) LeJEPA: provable and scalable self-supervised learning
    without the heuristics. arXiv preprint arXiv:2511.08544. External Links: Link Cited by: §H.3.4,
    §1, §2.
  • [4] A. Bardes, Q. Garrido, J. Ponce, X. Chen, M. Rabbat, Y. LeCun, M. Assran, and N. Ballas
    (2024) V-jepa: latent video prediction for visual representation learning. arXiv preprint
    arXiv:2402.04252. Cited by: §H.3.4, §1, §2.
  • [5] A. Bardes, J. Ponce, and Y. LeCun (2022) VICReg: variance-invariance-covariance
    regularization for self-supervised learning. In ICLR, Cited by: §H.3.4, §1, §2.
  • [6] D. Brandfonbrener, O. Nachum, and J. Bruna (2023) Inverse dynamics pretraining learns good
    representations for multitask imitation. In Advances in Neural Information Processing Systems
    36 (NeurIPS 2023), External Links: Link Cited by: §H.1.2.
  • [7] P. S. Castro (2020) Scalable methods for computing state similarity in deterministic Markov
    decision processes. In AAAI, Cited by: §2.
  • [8] T. Chen, S. Kornblith, M. Norouzi, and G. Hinton (2020) A simple framework for contrastive
    learning of visual representations. In ICML, Cited by: §2.
  • [9] X. Chen and K. He (2021) Exploring simple siamese representation learning. In CVPR, Cited
    by: §2.
  • [10] Z. J. Cui, H. Pan, A. Iyer, S. Haldar, and L. Pinto (2024) DynaMo: in-domain dynamics
    pretraining for visuo-motor control. In The Thirty-eighth Annual Conference on Neural
    Information Processing Systems, External Links: Link Cited by: §H.2, Table 5, §2.
  • [11] Y. Efroni, D. Misra, A. Krishnamurthy, A. Agarwal, and J. Langford (2022) Provably
    filtering exogenous distractors using multistep inverse dynamics. In International Conference
    on Learning Representations (ICLR 2022), External Links: Link Cited by: §H.1.2, §H.1.2, §2.
  • [12] N. Ferns, P. Panangaden, and D. Precup (2004) Metrics for finite Markov decision processes
    . In UAI, Cited by: §2.
  • [13] J. J. Gibson (1979) The ecological approach to visual perception. Houghton Mifflin. Cited
    by: footnote 1.
  • [14] M. A. Goodale and A. D. Milner (1992) Separate visual pathways for perception and action. 
    Trends in Neurosciences 15 (1), pp. 20–25. Cited by: §1.
  • [15] J. Grill, F. Strub, F. Altché, C. Tallec, P. Richemond, E. Buchatskaya, C. Doersch, B.
    Avila Pires, Z. Guo, M. Gheshlaghi Azar, et al. (2020) Bootstrap your own latent: a new
    approach to self-supervised learning. In NeurIPS, Cited by: §H.3.2, §1, §2.
  • [16] D. Ha and J. Schmidhuber (2018) World models. arXiv preprint arXiv:1803.10122. Cited by:
    §1.
  • [17] T. Haarnoja, A. Zhou, P. Abbeel, and S. Levine (2018) Soft actor-critic: off-policy
    maximum entropy deep reinforcement learning with a stochastic actor. In International
    conference on machine learning, pp. 1861–1870. Cited by: Appendix B.
  • [18] D. Hafner, T. Lillicrap, J. Ba, and M. Norouzi (2020) Dream to control: learning behaviors
    by latent imagination. In ICLR, Cited by: §H.3.1, §1, §2.
  • [19] D. Hafner, T. Lillicrap, I. Fischer, R. Villegas, D. Ha, H. Lee, and J. Davidson (2019) 
    Learning latent dynamics for planning from pixels. In ICML, Cited by: §H.3.1, §2.
  • [20] D. Hafner, T. Lillicrap, M. Norouzi, and J. Ba (2021) Mastering Atari with discrete world
    models. In ICLR, Cited by: §H.3.1, §2.
  • [21] D. Hafner, J. Pasukonis, J. Ba, and T. Lillicrap (2023) Mastering diverse domains through
    world models. arXiv preprint arXiv:2301.04104. Cited by: §H.3.1, Table 5, §1, §2.
  • [22] Y. Han and A. Yilmaz (2026) Enhancing policy learning with world-action model. arXiv
    preprint arXiv:2603.28955. External Links: Link Cited by: §H.2, Table 5, §2.
  • [23] N. Hansen, H. Su, and X. Wang (2024) TD-mpc2: scalable, robust world models for continuous
    control. ICLR. Cited by: §H.3.2, §1, §2.
  • [24] N. Hansen, X. Wang, and H. Su (2022) Temporal difference learning for model predictive
    control. In ICML, Cited by: §H.3.2, Table 5, §1, §2.
  • [25] H. Hertz (1899) The principles of mechanics presented in a new form. Macmillan, London. 
    Cited by: §4.
  • [26] R. Islam, M. Tomar, A. Lamb, Y. Efroni, H. Zang, A. Didolkar, D. Misra, X. Li, H. van
    Seijen, R. Tachet des Combes, and J. Langford (2022) Agent-controller representations:
    principled offline RL with rich exogenous information. arXiv preprint arXiv:2211.00164. Cited
    by: §H.1.2, §H.1.2, §2.
  • [27] H. Keurti, H.-R. Pan, M. Besserve, B. F. Grewe, and B. Schölkopf (2023) Homomorphism
    AutoEncoder — learning group structured representations from observed transitions. In 
    Proceedings of the 40th International Conference on Machine Learning, Proceedings of Machine
    Learning Research, Vol. 202, pp. 16190–16215. External Links: Link Cited by: footnote 5.
  • [28] I. Kostrikov, D. Yarats, and R. Fergus (2020) Image augmentation is all you need:
    regularizing deep reinforcement learning from pixels. arXiv preprint arXiv:2004.13649. Cited
    by: §H.3.3, §2.
  • [29] A. Lamb, R. Islam, Y. Efroni, A. R. Didolkar, D. Misra, D. J. Foster, L. P. Molu, R.
    Chari, A. Krishnamurthy, and J. Langford (2023) Guaranteed discovery of control-endogenous
    latent states with multi-step inverse models. Transactions on Machine Learning Research. Note: 
    External Links: ISSN 2835-8856, Link Cited by: §H.1.2, §H.1.2, §2.
  • [30] M. Laskin, A. Srinivas, and P. Abbeel (2020) Curl: contrastive unsupervised
    representations for reinforcement learning. In International conference on machine learning, 
    pp. 5639–5650. Cited by: §H.3.3, Table 5, §2.
  • [31] Y. LeCun (2022) A path towards autonomous machine intelligence. OpenReview. Cited by:
    §H.3.4, §1, §2.
  • [32] A. Levine, P. Stone, and A. Zhang (2024) Multistep inverse is not all you need. 
    Reinforcement Learning Journal 2, pp. 884–925. Note: Presented at the Reinforcement Learning
    Conference (RLC 2024) External Links: Link Cited by: §H.1.2.
  • [33] L. Maes, Q. Le Lidec, D. Scieur, Y. LeCun, and R. Balestriero (2026) LeWorldModel: stable
    end-to-end joint-embedding predictive architecture from pixels. arXiv preprint arXiv:2603.19312
    . External Links: Link Cited by: §A.2, Appendix B, §H.3.4, Table 5, §1, §2, §3, §5, §5, §5.
  • [34] L. Maes, Q. L. Lidec, D. Haramati, N. Massaudi, D. Scieur, Y. LeCun, and R. Balestriero
    (2026) Stable-worldmodel-v1: reproducible world modeling research and evaluation. arXiv
    preprint arXiv:2602.08968. Cited by: Appendix B.
  • [35] Z. Mhammedi, D. J. Foster, and A. Rakhlin (2023) Representation learning with multi-step
    inverse kinematics: an efficient and optimal approach to rich-observation RL. In Proceedings of
    the 40th International Conference on Machine Learning (ICML 2023), Proceedings of Machine
    Learning Research, Vol. 202, pp. 24659–24700. Note: Oral presentation External Links: Link 
    Cited by: §H.1.2, §2.
  • [36] J. K. O’Regan and A. Noë (2001) A sensorimotor account of vision and visual consciousness.
    Behavioral and Brain Sciences 24 (5), pp. 939–1031. Cited by: §1.
  • [37] K. Park, Y. J. Choe, and V. Veitch (2024) The linear representation hypothesis and the
    geometry of large language models. In Proceedings of the 41st International Conference on
    Machine Learning, Proceedings of Machine Learning Research, Vol. 235, pp. 39643–39666. Cited
    by: Appendix G.
  • [38] S. Park, K. Frans, B. Eysenbach, and S. Levine (2025) OGBench: benchmarking offline
    goal-conditioned RL. In The Thirteenth International Conference on Learning Representations, 
    External Links: Link Cited by: Appendix B.
  • [39] D. Pathak, P. Agrawal, A. A. Efros, and T. Darrell (2017) Curiosity-driven exploration by
    self-supervised prediction. In ICML, Cited by: §H.1.1, Table 5, §2.
  • [40] W. Prinz (1990) A common coding approach to perception and action. In Relationships
    Between Perception and Action: Current Approaches, O. Neumann and W. Prinz (Eds.), pp. 167–201.
    Cited by: §1.
  • [41] B. Ravindran and A. G. Barto (2003) SMDP homomorphisms: an algebraic approach to
    abstraction in semi-markov decision processes. In International Joint Conference on Artificial
    Intelligence (IJCAI), pp. 1011–1016. Cited by: footnote 5.
  • [42] P. K. Rubenstein*, S. Weichwald*, S. Bongers, J. M. Mooij, D. Janzing, M. Grosse-Wentrup,
    and B. Schölkopf (2017) Causal consistency of structural equation models. In Proceedings of the
    Thirty-Third Conference on Uncertainty in Artificial Intelligence (UAI), External Links: Link 
    Cited by: §2, §4, footnote 5.
  • [43] R. Y. Rubinstein (1997) Optimization of computer simulation models with rare events. 
    European Journal of Operational Research 99 (1), pp. 89–112. Cited by: §5.
  • [44] D. Schmidt and M. Jiang (2024) Learning to act without actions. In The Twelfth
    International Conference on Learning Representations, External Links: Link Cited by: §H.2, §2.
  • [45] B. Schölkopf, F. Locatello, S. Bauer, N. R. Ke, N. Kalchbrenner, A. Goyal, and Y. Bengio
    (2021) Toward causal representation learning. Proceedings of the IEEE 109 (5), pp. 612–634. 
    Cited by: §1.
  • [46] B. Schölkopf (2019) Causality for machine learning. Note: Published in: Probabilistic and
    Causal Inference: The Works of Judea Pearl External Links: Link, 1911.10500 Cited by: §1, §1,
    footnote 5.
  • [47] M. Schwarzer, A. Anand, R. Goel, R. D. Hjelm, A. Courville, and P. Bachman (2021) 
    Data-efficient reinforcement learning with self-predictive representations. In ICLR, Cited by:
    §H.3.2.
  • [48] Y. Seo, K. Lee, S. L. James, and P. Abbeel (2022) Reinforcement learning with action-free
    pre-training from videos. In Proceedings of the 39th International Conference on Machine
    Learning (ICML 2022), Proceedings of Machine Learning Research, Vol. 162, pp. 19561–19579. 
    External Links: Link Cited by: §H.2, §2.
  • [49] V. Sobal, W. Zhang, K. Cho, R. Balestriero, T. G. J. Rudner, and Y. LeCun (2025) Learning
    from reward-free offline data: a case for planning with latent dynamics models. In Advances in
    Neural Information Processing Systems 38 (NeurIPS 2025), External Links: Link Cited by:
    Appendix B, §H.3.4, Table 5, §1, §2.
  • [50] V. Sobal, W. Zhang, K. Cho, R. Balestriero, T. G. J. Rudner, and Y. LeCun (2025) 
    Stress-testing offline reward-free reinforcement learning: a case for planning with latent
    dynamics models. In 7th Robot Learning Workshop: Towards Robots with Human-Level Abilities, 
    External Links: Link Cited by: Appendix B.
  • [51] Y. Tassa, Y. Doron, A. Muldal, T. Erez, Y. Li, D. d. L. Casas, D. Budden, A. Abdolmaleki,
    J. Merel, A. Lefrancq, et al. (2018) Deepmind control suite. arXiv preprint arXiv:1801.00690. 
    Cited by: Appendix B.
  • [52] B. Terver, R. Balestriero, M. Dervishi, D. Fan, Q. Garrido, T. Nagarajan, K. Sinha, W.
    Zhang, M. Rabbat, Y. LeCun, and A. Bar (2026) A lightweight library for energy-based
    joint-embedding predictive architectures. arXiv preprint arXiv:2602.03604. External Links: Link
    Cited by: §H.2, Table 5, §2.
  • [53] A. van den Oord, Y. Li, and O. Vinyals (2018) Representation learning with contrastive
    predictive coding. In arXiv preprint arXiv:1807.03748, Cited by: §H.3.3, §2.
  • [54] F. J. Varela, E. Rosch, and E. Thompson (1991) The embodied mind: cognitive science and
    human experience. MIT Press, Cambridge, MA. Cited by: §1.
  • [55] J. von Uexküll (1934) Streifzüge durch die umwelten von tieren und menschen: ein
    bilderbuch unsichtbarer welten. Verständliche Wissenschaft, Vol. 21, Verlag von Julius Springer
    , Berlin. Cited by: footnote 1.
  • [56] D. Yarats, R. Fergus, A. Lazaric, and L. Pinto (2021) Reinforcement learning with
    prototypical representations. In International Conference on Machine Learning, pp. 11920–11931.
    Cited by: §H.3.3, §2.
  • [57] J. Yu, S. Chen, M. Liu, N. Horiuchi, V. Braverman, Z. Xu, D. Haramati, and R. Balestriero
    (2025) Why and how auxiliary tasks improve JEPA representations. In UniReps: 3rd Edition of the
    Workshop on Unifying Representations in Neural Models, External Links: Link Cited by: §H.2, §2.
  • [58] J. Zbontar, L. Jing, I. Misra, Y. LeCun, and S. Deny (2021) Barlow twins: self-supervised
    learning via redundancy reduction. In ICML, Cited by: §2.
  • [59] G. Zhou, H. Pan, Y. LeCun, and L. Pinto (2025) DINO-WM: world models on pre-trained visual
    features enable zero-shot planning. In Proceedings of the 42nd International Conference on
    Machine Learning (ICML 2025), Proceedings of Machine Learning Research, Vol. 267, 
    pp. 79115–79135. External Links: Link Cited by: Appendix B, §H.3.4, Table 5, §1, §2.

Appendix A Implementation details

A.1 Training objective

Alg. 1 gives PyTorch-style pseudocode for the mini-batch objective used to train SMWM. The encoder
receives gradients from both the forward prediction loss and the inverse dynamics loss, which is
what prevents representation collapse.


Algorithm 1 Mini-batch loss for SMWM training.
⬇
def loss(o_t, a_t, o_tp1, lambda_inv=10.0):
"""
o_t, o_tp1: (B, C, H, W) consecutive pixel observations
a_t: (B, A) action taken between them
lambda_inv: (float) inverse dynamics loss weight
"""
z_t = encoder(o_t) # (B, D)
z_tp1 = encoder(o_tp1) # (B, D)
z_hat = fwd_model(z_t, a_t) # predict next embedding
a_hat = inv_model(z_t, z_tp1) # predict intervening action
loss_fwd = F.mse_loss(z_hat, z_tp1) # forward dynamics loss
loss_inv = F.mse_loss(a_hat, a_t) # inverse dynamics loss
return loss_fwd + lambda_inv * loss_inv

A.2 Architecture

We adopt the encoder and forward model of LeWorldModel [33] essentially unchanged, and add a small
inverse head hψh_{\psi}. Inputs are 224×224224\times 224 RGB images.

Encoder fθf_{\theta}.

A Vision Transformer Tiny (ViT-Tiny) with patch size 1414, instantiated from the Hugging Face
library. The final cls token is projected to z∈ℝdz\in\mathbb{R}^{d} with d=192d=192; the encoder
has ≈5\approx 5M parameters.

Forward model gϕg_{\phi}.

A ViT-S backbone with learned positional embeddings and causal masking over the observation
history, matching the LeWM predictor; the action ata_{t} is injected via AdaLN-zero. However, in
contrast to LeWM, we set the history length to 11 on every environment, so gϕg_{\phi} predicts z^
t+1\hat{z}_{t+1} from a single (zt,at)(z_{t},a_{t}) pair, as described in Sec. 3. LeWM instead uses
history 33 on Push-T and OGBench-Cube. The forward model has ≈10\approx 10M parameters.

Inverse model hψh_{\psi}.

Our only architectural addition. A 2-layer MLP of width 256256 with ReLU activations, mapping [zt;
zt+1]∈ℝ2​d[z_{t};\,z_{t+1}]\in\mathbb{R}^{2d} to a^t∈ℝm\hat{a}_{t}\in\mathbb{R}^{m}, without output
activation. Approximately 0.10.1M parameters.

A.3 Optimization details

All world models are trained end-to-end with the objective in Eq. 4. We use the same optimization
settings for SMWM, the forward-only ablation, and the SIGReg baseline; only the regularization term
is changed. For SMWM, the inverse-dynamics weight λ\lambda is environment-specific. The values are
listed in Tab. 2.


Table 2: Inverse-dynamics weights. Values of λ\lambda in ℒfwd+λ​ℒinv\mathcal{L}_{\text{fwd}}+\
lambda\mathcal{L}_{\text{inv}}.

 Environment  λ\lambda
TwoRoom       0.10.1
Reacher       55
Push-T        3030
OGBench-Cube  11

The remaining training hyperparameters are shared across environments unless explicitly noted in
Tab. 3. We train on two-frame snippets with a single-step forward target. Since datasets are loaded
with frameskip 55, the action encoder and inverse head receive the concatenated action over the
skipped interval. The SIGReg comparison uses the same optimizer and architecture, with λ=0\lambda=0
and SIGReg weight 0.090.09.


Table 3: Optimization hyperparameters.

  Hyperparameter      Value
    Optimizer         AdamW
  Learning rate    10−410^{-4}
   Weight decay    10−310^{-3}
    Batch size     256256
 Training epochs   1010
Gradient clipping  1.01.0

Choosing λ\lambda.

Figure 7 shows that λ\lambda requires some environment-level tuning. For example, λ=0.1\lambda=0.1
is optimal for TwoRoom but causes Reacher to collapse to near-random planning performance. The
final values in Tab. 2 were chosen from this broad sweep. The sweep contains one trained model per
λ\lambda value, so the shaded bands show binomial standard error over the 100 evaluation episodes
rather than variation across training seeds.


Figure 7: Sensitivity to inverse-dynamics weight. Goal-conditioned planning success rate as a
function of the inverse-dynamics loss weight λ\lambda at goal offset 25. Each panel corresponds to
one environment, and the red dotted line marks the value used for the main paper experiments.

A.4 Planning protocol

For each evaluation episode, the policy receives the current observation oto_{t} and a goal
observation ogo_{g}, encodes them as zt=fθ​(ot)z_{t}=f_{\theta}(o_{t}) and zg=fθ​(og)z_{g}=f_{\
theta}(o_{g}), and optimizes candidate action sequences with CEM under the terminal cost in Eq. 8.
Candidate sequences are rolled out using gϕg_{\phi} for HH latent model steps, where each
model-step action is a block of five primitive environment actions.

Planning uses the same MPC and CEM hyperparameters in all four environments (Tab. 4). We sample 100
start states without replacement from the held-out evaluation dataset, requiring that the same
episode contains a goal frame 25 steps later. The environment is reset to the sampled start state,
the target is set from the goal row, and success is measured over a budget of 50 primitive
environment steps. The sampled tasks are fixed across methods and seeds within each environment.


Table 4: Planning hyperparameters.

      Hyperparameter        Value
    Planning horizon HH     5 model steps
     Executed steps KK      5 model steps
       Action block         5 primitive actions
      CEM population        300
      CEM iterations        30
        CEM elites          30
Initial CEM variance scale  1.0
     Evaluation tasks       100
        Goal offset         25 primitive steps
     Evaluation budget      50 primitive steps

A.5 Probing protocol.

For Tab. 1, we freeze fθf_{\theta}, encode embeddings z=fθ​(o)z=f_{\theta}(o), and train probes on
25,00025{,}000 training embeddings per environment and method, reporting held-out R2R^{2} on a
disjoint 5,0005{,}000-sample validation split. The linear probe is ridge regression with α=10−3\
alpha=10^{-3}. The nonlinear probe is a two-layer MLP with widths 256,128256,128 and ReLU
activations. For vector-valued targets, including sine–cosine angle encodings, we report uniformly
averaged R2R^{2} over target coordinates.

A.6 Compute.

All training and planning runs used a single NVIDIA H100 GPU. Each world model was trained in under
4 hours.

Appendix B Environments and datasets

Our experiments use the four environments and offline datasets adopted by Maes et al. [33] (Fig. 8
). All models are trained and evaluated through the stable-worldmodel codebase [34], which
standardizes the environment interfaces, dataset loading, and downstream planning procedure.

For every environment, we use an episode-level 90/1090/10 train/validation split: fθf_{\theta},
gϕg_{\phi}, and hψh_{\psi} are trained on the training episodes, while all reported
metrics—planning (Figs. 5 and 9), probes (Tab. 1), and latent-geometry analyses—are computed on the
held-out validation split.

(a) TwoRoom.

TwoRoom [49, 50] is a 2D point-navigation task with a simple constrained geometry: the agent starts
in one room and the goal lies in the other, with a single doorway as the only passage between them.
The script moves the agent first toward the doorway and then toward the goal after crossing the
wall. The dataset contains 10,000 noisy scripted rollouts, averaging 92 steps.

(b) Push-T.

Push-T is a 2D contact-rich manipulation task in which a dot-shaped agent must push a T-shaped
object into a target pose. We use the DINO-WM expert demonstrations [59], consisting of 20,000
episodes with mean length 196.

(c) OGBench-Cube.

OGBench-Cube [38] is a tabletop manipulation task where a robot gripper must move a cube to a
specified target pose. We use the single-cube setting and the benchmark’s scripted dataset: 10,000
trajectories, each of length 200.

(d) Reacher.

Reacher is the two-link arm task from the DeepMind Control Suite [51]. We follow the DINO-WM
variant, where success is defined by matching the target joint configuration, not just by
end-effector proximity. The offline dataset contains 10,000 SAC-generated episodes [17], each 200
steps long.

Refer to caption
Figure 8: Environments. Four evaluation environments—TwoRoom (2D navigation), Reacher (continuous
control), Push-T (2D contact-rich manipulation), and OGBench-Cube (3D tabletop manipulation).

Appendix C Planning performance on extended goal offsets

Figure 9 evaluates whether the learned latent dynamics remain useful when the goal is placed
farther from the initial state. For each goal offset, the start and goal observations are sampled
from the held-out evaluation split, and the planner is given a budget of 2×2\times the offset in
primitive environment steps. Thus the evaluation becomes harder because the model must support
longer latent rollouts. The same trained models and MPC/CEM planner from Fig. 5 are used
throughout.

SMWM is stable across longer horizons on TwoRoom and OGBench-Cube, where SIGReg either degrades
sharply or remains consistently lower. On Reacher, the inverse and SIGReg curves stay close over
the tested offsets. Push-T is the main failure case: both regularized methods drop as the offset
increases.


Figure 9: Robustness to planning horizon. Goal-conditioned planning success rate as a function of
goal offset, the number of environment steps between the initial and goal observations; the
planner’s evaluation budget is fixed at 2×2\times the goal offset. Methods and environments match
Fig. 5. SMWM maintains near-flat success across horizons on TwoRoom and OGBench-Cube, while SIGReg
either degrades sharply (TwoRoom) or trails consistently (Cube). The two regularized methods are
comparable on Reacher and Push-T.

Appendix D SIGReg latent geometry

Figure 10 repeats the representation summary of Fig. 6 for the SIGReg baseline. The same held-out
embeddings and PCA protocol are used, with the bottom row showing (PC1,PC2)(\mathrm{PC}_{1},\mathrm
{PC}_{2}) for each environment. Unlike SMWM embeddings, the SIGReg spectra do not exhibit a clear
elbow. This is expected: the SIGReg regularizer explicitly encourages the embedding distribution to
match an isotropic Gaussian, spreading variance across many principal components rather than
concentrating it in a compact task-relevant subspace. As a result, although state information
remains recoverable by probes, the resulting representation is not low-dimensional, and is less
directly interpretable than the SMWM representation.

Refer to caption
Figure 10: Latent geometry of SIGReg embeddings. For each environment we show the PCA spectrum of
held-out embeddings (top), the distribution of a representative ground-truth quantity in physical
state space (middle), and the first two principal components of the encoded embeddings, color-coded
by the same physical quantity (bottom). The dashed red lines mark the action dimension. Compared
with SMWM embeddings in Fig. 6, SIGReg produces less compact and less structured latent geometry:
the spectra lack a sharp elbow, and the state-dependent structure is spread across many components
rather than appearing in a small interpretable PC subspace.

Appendix E Dot world details

E.1 Environment

Each observation is a 64×6464{\times}64 RGB image rendered on a white canvas: each dot is drawn as
a filled disc of radius r=2r=2 pixels in a fixed RGB color. Integer dot positions are sampled
uniformly in [m,64−1−m]2[m,64-1-m]^{2} with a margin of m=r+Δmax=18m=r+\Delta_{\max}=18, and
integer per-step displacements in [−Δmax,Δmax]2[-\Delta_{\max},\Delta_{\max}]^{2} with Δmax=16\
Delta_{\max}=16, so every sampled transition stays inside the canvas. Rejection sampling enforces a
minimum center-to-center separation of 2​r+1=52r+1=5 pixels at both endpoints. Each (ot,at,ot+1)(o_
{t},a_{t},o_{t+1}) is generated lazily from a seeded RNG, making the dataset deterministic and
reproducible.

The four configurations of Fig. 4 combine three motion types: Independent dots have their own
(Δ​x,Δ​y)(\Delta x,\Delta y) in the action vector, coupled dot pairs share a single (Δ​x,Δ​y)(\
Delta x,\Delta y), and random dots move at every step without contributing to the action vector.
Concretely, Independent = 2 independent dots (|a|=4|a|=4); Coupled = 1 coupled pair (|a|=2|a|=2); 
Distractor = 1 independent dot + 1 random dot (|a|=2|a|=2); Combined = 1 coupled pair + 2
independent dots + 1 random dot (|a|=6|a|=6).

E.2 Architecture and training

The encoder fθf_{\theta} stacks three stride-2 convolutions (3→32→64→1283{\to}32{\to}64{\to}128
channels, 3×33{\times}3 kernels) interleaved with ReLU, followed by a 2×22{\times}2 max-pool, a
flatten, a linear projection to ℝd\mathbb{R}^{d}, and a final tanh\tanh that bounds embeddings in
[−1,1]d[-1,1]^{d} with d=64d=64. Both gϕg_{\phi} and hψh_{\psi} are 2-hidden-layer MLPs of width
256256 with ReLU activations; gϕg_{\phi} ends in tanh\tanh to match the encoder range, while hψh_{\
psi} has no output activation. Approximate parameter counts: fθ≈220f_{\theta}\approx 220k, gϕ≈100g_
{\phi}\approx 100k, hψ≈100h_{\psi}\approx 100k.

All three networks are trained jointly with Adam (lr =5×10−4=5{\times}10^{-4}, default β\beta, no
weight decay, no LR schedule) for 100 epochs at batch size 256 on 250,000 transitions, with seed 42
and λ=10\lambda=10. Actions are normalized by Δmax\Delta_{\max} before being passed to gϕg_{\phi}
and predicted by hψh_{\psi}, so all action coordinates lie in [−1,1][-1,1]; we report ℒinv\mathcal
{L}_{\text{inv}} on these normalized actions. All metrics are computed on a held-out evaluation
split of 1,0001{,}000 transitions sampled with a disjoint seed.

E.3 Collapse without the inverse loss

To verify that the inverse loss is the active anti-collapse mechanism on dot world, we rerun the
single-dot configuration with λ=0\lambda=0 and otherwise identical hyperparameters. Figure 11
reports the resulting representation using the same probe grid as Fig. 2. Without the inverse loss,
the encoder collapses all probe observations to essentially the same embedding. Consequently, there
is no state-dependent variation for PCA to expose. The forward loss alone can therefore be
minimized by a constant, trivially predictable representation.

Refer to caption
Figure 11: Forward-only collapse on dot world. The single-dot model is trained with λ=0\lambda=0
and evaluated with the same probe grid and visualization protocol as Fig. 2. Removing the inverse
loss maps the probe states to the same embedding, so the PC projection collapses to a single point
at the origin.

Appendix F Sprite world reconstruction experiment

F.1 Environment and control settings

To make the control-dependent nature of the learned representation visually explicit, we introduce
a second toy environment with a single triangular agent moving in a 2D plane. The agent is rendered
as an asymmetric sprite on a 64×6464{\times}64 RGB canvas. Its state is its pose,

 s=(x,y,θ),s=(x,y,\theta),

where x,yx,y denote position and θ\theta denotes orientation. Because the triangle is asymmetric,
its orientation is visible from a single frame.

The key intervention in this experiment is to vary the action repertoire while keeping the
underlying visual world fixed. In all settings, the agent can move in all three degrees of freedom:
xx, yy, and θ\theta. The difference is only which changes are exposed to the model as actions.
Uncontrolled degrees of freedom are not held fixed; they continue to move randomly.

We consider the following control settings:

  • •
   
    No control: the agent translates and rotates randomly, and the action vector is empty.

  • •
   
    x/yx/y control: the action is at=(Δ​x,Δ​y)a_{t}=(\Delta x,\Delta y), while rotations remain
    random and uncontrolled.

  • •
   
    x/y/θx/y/\theta control: the action is at=(Δ​x,Δ​y,Δ​θ)a_{t}=(\Delta x,\Delta y,\Delta\theta),
    so both translation and rotation are controlled.

Thus the same triangular agent is observed under different action interfaces. This lets us ask
whether the representation changes depending on which aspects of the agent’s pose are controllable.

F.2 Training and reconstruction probe

For each control setting, we train the same inverse-dynamics-regularized world model as in the
dot-world experiments: a CNN encoder fθf_{\theta}, a forward model gϕg_{\phi}, and an inverse model
hψh_{\psi}, optimized with the objective in Eq. 4. The architecture and training procedure match
Sec. E.2. In the no-control setting, the action dimension is zero, so there is no inverse-dynamics
signal and the model reduces to the forward-only objective.

After training, we freeze the encoder and train a decoder as a post-hoc probe,

 o^t=dξ​(fθ​(ot)).\hat{o}_{t}=d_{\xi}(f_{\theta}(o_{t})).

The decoder is trained only to visualize what information is already present in the frozen
embedding zt=fθ​(ot)z_{t}=f_{\theta}(o_{t}); it is not used to train the representation.

Refer to caption
Figure 12: Control-dependent reconstruction of a triangular agent. The top row shows the
ground-truth trajectory of an asymmetric triangular agent with pose (x,y,θ)(x,y,\theta). The
remaining rows show reconstructions from frozen embeddings learned under different action
interfaces. With no control, the representation collapses and the decoder outputs an average
occupancy pattern. With x/yx/y control, the representation preserves position but averages over the
uncontrolled orientation, producing a blurred, approximately symmetric blob at the correct
location. With full x/y/θx/y/\theta control, the representation preserves both position and
orientation, yielding sharp oriented reconstructions.

F.3 Interpretation

The experiment shows that the model “sees” the same visual object differently depending on what the
agent can control. When there are no actions, there is no inverse signal that could force the
encoder to preserve state information, so the representation collapses. When only translation is
controlled, the encoder preserves where the agent is, because this is needed to recover and predict
translational effects, but it discards orientation, because rotation is random and
action-irrelevant. The decoder therefore averages over possible orientations. When rotation is also
controlled, orientation becomes action-relevant, and the encoder preserves the full pose.

This provides a direct visual counterpart to the PCA results in Fig. 4: the learned representation
retains controllable degrees of freedom and averages out uncontrolled ones.

Appendix G Equivariance and latent action composition

This appendix gives the formal statement behind the compositional claim in Sec. 4. Denote the
effect of action aa on the observation oo by o↦a⁡(o)o\mapsto a(o), and write ga​(z):=gϕ​(z,a)g_{a}
(z):=g_{\phi}(z,a). In the main text, the forward prediction loss encourages the approximate
equivariance condition

 f⁡(a⁡(o))≈ga​(f⁡(o)),f(a(o))\approx g_{a}(f(o)),  (9)

on the support of the training distribution. Here, for clarity, we consider the exact version

 f⁡(a⁡(o))=ga​(f⁡(o))f(a(o))=g_{a}(f(o))   (10)

for all a∈𝒜a\in\mathcal{A} and o∈𝒪o\in\mathcal{O}. The encoder thus intertwines the physical
intervention with the learned latent intervention, making this a statement of equivariance. In
physics terminology one might also say that the latent state transforms covariantly with the
intervention.

We consider sequences of actions, assuming that the actions form a semigroup under composition;
i.e., for a1,a2∈𝒜a_{1},a_{2}\in\mathcal{A}, the composition a2∘a1a_{2}\circ a_{1} is again in 𝒜\
mathcal{A}, and composition is associative. We say that a↦gaa\mapsto g_{a} is a homomorphism on the
latent manifold f⁡(𝒪)f(\mathcal{O}) if, for every a1,a2∈𝒜a_{1},a_{2}\in\mathcal{A}, we have

 ga2∘a1=ga2∘ga1on ​f​(𝒪),g_{a_{2}\circ a_{1}}=g_{a_{2}}\circ g_{a_{1}}\qquad\text{on }f(\      (11)
 mathcal{O}),

i.e., for every z∈f⁡(𝒪)z\in f(\mathcal{O}),

 ga2∘a1​(z)=ga2​(ga1​(z)).g_{a_{2}\circ a_{1}}(z)=g_{a_{2}}(g_{a_{1}}(z)).  (12)

Proof of theorem 1.

Choose any a1,a2∈𝒜a_{1},a_{2}\in\mathcal{A} and z∈f⁡(𝒪)z\in f(\mathcal{O}). Choose o∈𝒪o\in\mathcal
{O} with f⁡(o)=zf(o)=z. By closure, a3:=a2∘a1∈𝒜a_{3}:=a_{2}\circ a_{1}\in\mathcal{A}.

Applying equivariance three times,

 ga2∘a1​(z)\           =ga3​(f​(o))\displaystyle=g_{a_
 displaystyle g_{a_{2} {3}}\bigl(f(o)\bigr)
 \circ a_{1}}(z)
                       =f​(a3​(o))\displaystyle=f\bigl   [equivariance at ​(a3,o)​]\displaystyle\
                       (a_{3}(o)\bigr)                   text{[equivariance at }(a_{3},o)\text{]}
                       =f⁡(a2​(a1​(o)))\displaystyle=f\
                       bigl(a_{2}(a_{1}(o))\bigr)
                       =ga2​(f⁡(a1​(o)))\displaystyle=   [equivariance at ​(a2,a1​(o))​]\
                       g_{a_{2}}\!\bigl(f(a_{1}(o))\     displaystyle\text{[equivariance at }(a_
                       bigr)                             {2},a_{1}(o))\text{]}
                       =ga2​(ga1​(f⁡(o)))\displaystyle=  [equivariance at ​(a1,o)​]\displaystyle\
                       g_{a_{2}}\!\bigl(g_{a_{1}}(f(o))  text{[equivariance at }(a_{1},o)\text{]}
                       \bigr)
                       =ga2​(ga1​(z)).\displaystyle=g_
                       {a_{2}}\!\bigl(g_{a_{1}}(z)\
                       bigr).

Thus ga2∘a1​(z)=ga2​(ga1​(z))g_{a_{2}\circ a_{1}}(z)=g_{a_{2}}(g_{a_{1}}(z)) for every z∈f⁡(𝒪)z\in
f(\mathcal{O}), so a↦gaa\mapsto g_{a} is a homomorphism on f⁡(𝒪)f(\mathcal{O}). ∎

One-step equivariance thus forces physical action composition to be represented by composition of
the learned latent interventions, at least on the encoded data manifold. In the learned model, this
identity should be interpreted approximately and on-support. Note that the assumption of semigroup
closure is nontrivial: in practice, it could happen that a multi-step action sequence is not
equivalent to any single primitive action.^4^4 4 This case would correspond to using a free
semigroup of action sequences.^5^5 5 The homomorphism property established in Theorem 1 is closely
related to the notion of MDP homomorphisms [41], where abstraction maps preserve the compositional
structure of state-action transitions (cf. also [42, 27]). In our setting, however, the
homomorphism emerges from approximate equivariance induced by the forward prediction objective,
rather than being assumed as part of an abstraction framework. A related discussion can be found in
[46].

Equivariance alone admits a wide range of solutions, including two trivial extremes:

 1. 1.
   
    No encoding. Take 𝒵=𝒪\mathcal{Z}=\mathcal{O}, f=idf=\mathrm{id}, and ga=ag_{a}=a. Then both
    sides of Eq. 10 equal a⁡(o)a(o). This solution satisfies equivariance but achieves no
    compression.

 2. 2.
   
    Collapse. Take 𝒵={z}\mathcal{Z}=\{z\}, f≡zf\equiv z, and ga=idg_{a}=\mathrm{id}. Then both
    sides of Eq. 10 equal zz. This solution satisfies equivariance but discards all information
    about aa.

Useful representations therefore need more than equivariance: the latent dynamics should remain
faithful to the physical action. In particular, if an action a∈𝒜a\in\mathcal{A} changes
observations nontrivially in 𝒪\mathcal{O}, it should also induce a nontrivial transformation on f⁡
(𝒪)f(\mathcal{O}). A stronger requirement is latent action identifiability: whenever z′=ga​(z)z^{\
prime}=g_{a}(z) for (z,z′)∈f⁡(𝒪)×f⁡(𝒪)(z,z^{\prime})\in f(\mathcal{O})\times f(\mathcal{O}), the
action aa should be recoverable from the latent transition (z,z′)(z,z^{\prime}). This is exactly
the constraint imposed by the inverse model, which predicts aa from (z,z′)(z,z^{\prime}).

Beyond a homomorphism into latent transformations under composition, the experiments in Fig. 3
suggest that in our case the homomorphism may approximately go into the additive group, i.e., the
learned interventions appear to act approximately as translations,

 ga​(z)≈z+ρ⁡(a),g_{a}(z)\approx z+\rho(a),   (13)

with ρ⁡(a)\rho(a) approximately independent of zz. Here ρ⁡(a)\rho(a) is the latent displacement, or
latent effect vector, associated with action aa. Substituting this into the composition identity
yields

 z+ρ⁡(a2∘a1)≈ga2​(ga1​(z))≈z+ρ⁡(a1)+ρ⁡(a2),z+\rho(a_{2}\circ a_{1})\approx g_{a_{2}}(g_{a_     (14)
 {1}}(z))\approx z+\rho(a_{1})+\rho(a_{2}),

and therefore

 ρ⁡(a2∘a1)≈ρ⁡(a1)+ρ⁡(a2).\rho(a_{2}\circ a_{1})\approx\rho(a_{1})+\rho(a_{2}).  (15)

For pure displacement actions, where composition is itself approximately additive away from
boundaries, this means that the model has learned an approximately additive latent representation
of interventions. This stronger property is empirical rather than imposed by the objective.^6^6 6
It need not hold for contact-rich dynamics, boundary effects, periodic variables, strongly
state-dependent action effects, or noncommutative action laws.

It is intriguing to ask where this translation-like structure may stem from. The inverse-dynamics
loss provides a plausible bias toward this regime. Equivariance alone admits degenerate solutions,
including collapse; useful representations additionally require the latent action to be
identifiable from the transition. The inverse head enforces this by requiring ata_{t} to be
recoverable from (zt,zt+1)(z_{t},z_{t+1}), where zt=f⁡(ot)z_{t}=f(o_{t}) and zt+1=f⁡(ot+1)z_{t+1}=f
(o_{t+1}). A simple way to satisfy this constraint is to encode the action in the displacement
vector

 zt+1−zt≈ρ⁡(at).z_{t+1}-z_{t}\approx\rho(a_{t}).  (16)

If ρ\rho is injective on the action support, then the inverse hh can decode the action by applying
an approximate inverse of ρ\rho to this displacement. This mechanism does not enforce additivity,
but it renders an additive, translation-like representation a low-complexity solution.

This perspective connects the empirical latent geometry to an interventional version of the linear
representation hypothesis [37]. The claim is stronger than ordinary linear decodability: it is not
merely that actions can be decoded from latent states, but that applying an intervention
corresponds approximately to moving along an action-dependent direction in latent space,

 f⁡(a⁡(o))−f⁡(o)≈ρ⁡(a).f(a(o))-f(o)\approx\rho(a).  (17)

Thus the learned representation appears to linearize controllable interventions: the encoder is
approximately equivariant to action-induced transformations, and in simple displacement domains the
learned latent interventions further specialize to additive translations.

Appendix H Extended related work

This appendix expands the discussion of Sec. 2. Tab. 5 summarizes the discussed methods along the
dimensions most relevant to our contribution.


Table 5: Comparison of related methods. “Inv.” and “Fwd.” denote inverse and forward dynamics
components; “True action” indicates supervision with ground-truth actions; “WM” marks methods that
deliver a world model usable for planning. The last column lists the primary anti-collapse
mechanism. SMWM is the only entry that combines offline reward-free world-model training with
inverse dynamics as the sole anti-collapse mechanism.

     Method       Inv. Fwd. True action Reward Offline WM          Anti-collapse
ICM [39]          ✓    ✓    ✓           ×      ×       ×  Inv. dynamics on ϕθ\phi_{\theta}
CURL [30]         ×    ×    ✓           ✓      ×       ×  Contrastive
Dreamer [21]      ×    ✓    ✓           ✓      ×       ✓  Recon. ++ KL
TD-MPC [24]       ×    ✓    ✓           ✓      ×       ✓  EMA latent target
DynaMo [10]       ✓    ✓    ×           ×      ✓       ✓  Joint inv. ++ fwd. dyn.
PLDM [49]         ✓    ✓    ✓           ×      ✓       ✓  VICReg ++ inv. dyn.
DINO-WM [59]      ×    ✓    ✓           ×      ✓       ✓  Frozen DINOv2
LeWorldModel [33] ×    ✓    ✓           ×      ✓       ✓  SIGReg
WAM [22]          ✓    ✓    ✓           ✓      ×       ✓  Recon. ++ KL ++ inv. dyn.
EB-JEPA [52]      ✓    ✓    ✓           ×      ✓       ✓  VICReg ++ inv. dyn.
SMWM              ✓    ✓    ✓           ×      ✓       ✓  Inv. dynamics alone

H.1 Inverse dynamics for representation learning

H.1.1 Intrinsic Curiosity Module (ICM)

ICM [39] is the canonical inverse-dynamics-as-feature-learner construction we extend. Two
architectural differences matter for our claim. First, in ICM the encoder receives gradients only
from the inverse loss; the forward loss treats ϕθ​(ot+1)\phi_{\theta}(o_{t+1}) as a stop-gradient
target and trains only gωg_{\omega}. In our setup the encoder receives gradients from both losses,
because the forward predictor is part of the world model we deliver. Second, ICM uses a separate
A3C policy with its own encoder; the ICM encoder is used only to compute a scalar intrinsic reward.
Our encoder is the world-model encoder used downstream. Conceptually, ICM motivates inverse
dynamics as a way to ignore action-irrelevant variation, whereas we frame it as the anti-collapse
mechanism that allows joint training of an end-to-end JEPA world model without reconstruction, EMA,
or distributional regularizers.

H.1.2 Multi-step inverse identifiability theory

A line of theoretical work establishes asymptotic recovery of the control-endogenous latent state
for multi-step inverse models in finite Exogenous Block MDPs [11, 29, 26, 35]. We summarize their
assumptions precisely here.

AC-State [29].

AC-State is proved correct under: (i) finite endogenous state space SS; (ii) finite action space
AA; (iii) deterministic endogenous transition T⁡(s′∣s,a)T(s^{\prime}\mid s,a); (iv)
action-independent exogenous noise; (v) bounded endogenous diameter DD and a behavior policy that
mixes well; (vi) a multi-step inverse loss with horizon K≥DK\geq D, jointly with an information
bottleneck that selects, among loss-minimizers, the encoder of minimum output cardinality. The
conclusion is asymptotic identification of the partition induced by f∗f^{*}. The authors explicitly
note that the finite-state assumption “rules out continuous control problems”. Their Appendix B
exhibits a discrete Ex-BMDP in which a single-step inverse model cannot distinguish two distinct
endogenous states; Efroni et al. [11] and Islam et al. [26] provide further such counterexamples.

Refinements and known failure modes.

Levine et al. [32] show that even AC-State can fail in deterministic Ex-BMDPs when the witness
distance between two states exceeds DD, and fails for any finite horizon KK under periodic
endogenous dynamics; they propose adding a latent forward-dynamics term to restore guarantees.

Implication for our work.

Our setting differs from these results on every dimension that matters: continuous (rather than
finite) state and action spaces, single-step (rather than multi-step) inverse, possibly stochastic
endogenous dynamics, and no information bottleneck on the encoder’s output cardinality. We
therefore do not claim that any of these guarantees transfer to our inverse-dynamics objective. We
cite this body of work as conceptual motivation: a substantial theoretical literature confirms that
inverse-dynamics objectives of various forms learn features tied to controllable structure under
some conditions, and this informs our design choice. The single-step positive result of
Brandfonbrener et al. [6] for a linear-Gaussian multitask imitation model is the closest
theoretical endorsement of single-step inverse dynamics that we know of, but its scope
(deconfounding latent task variables in imitation) does not match ours.

H.2 Inverse-dynamics-regularized world models

DynaMo [10].

DynaMo is architecturally the closest neighbor to ours: joint latent inverse and forward dynamics
without reconstruction. The decisive difference is action supervision: DynaMo treats actions as
unobserved latents inferred jointly with the encoder, while we supervise the inverse head with the
executed action ata_{t}. Ground-truth labels turn the inverse term into a hard anti-collapse signal
that a co-learned action representation cannot provide.

LAPO and APV.

Inverse-dynamics ideas also underlie work that infers pseudo-actions from action-free video. LAPO [
44] uses inverse dynamics model to discover latent actions and trains a forward model conditioned
on these. APV [48] pretrains an action-free latent video predictor and fine-tunes for control.
These methods address the complementary regime where ground-truth actions are unavailable; with
actions in hand, we use them directly to supervise inverse-dynamics prediction.

World-Action Model [22].

WAM augments DreamerV2 with an inverse dynamics head and reports that this regularizes the latent
state toward action-relevant structure. The construction is conceptually closest to ours among
reconstruction-based methods. Two differences are decisive: WAM operates within Dreamer’s
pixel-reconstruction ELBO and requires reward supervision; we are reconstruction-free, JEPA-style,
and reward-free. WAM treats the inverse dynamics head as one auxiliary among many; we identify it
as the sufficient anti-collapse mechanism for end-to-end JEPA training.

EB-JEPA [52].

EB-JEPA is an action-conditioned JEPA with a multi-term loss combining variance, covariance,
temporal-similarity, and inverse-dynamics regularizers; their ablations report that all four terms
contribute to anti-collapse. Our claim is more specific: the inverse-dynamics term alone suffices,
removing the need for variance and covariance terms and yielding a substantially simpler training
objective.

Auxiliary-task JEPA theory [57].

Concurrent theoretical work proves a no-unhealthy-collapse theorem for JEPAs with an auxiliary
regression head: in deterministic MDPs, if both the latent prediction loss and the auxiliary
regression loss are zero, then observations with different transition dynamics or different
auxiliary values must map to distinct embeddings. Our IDR objective is the action-prediction
instantiation of this template. Our contribution complementary to theirs is empirical: we
instantiate the template on continuous pixel-action data and show that single-step IDR is
sufficient in practice.

H.3 Alternative anti-collapse mechanisms in latent world models

H.3.1 Reconstruction-based

The Dreamer family [18, 20, 21] and PlaNet [19] train recurrent state-space models with
pixel-reconstruction and reward-prediction heads under a variational objective:

 ℒDreamer=𝔼[∑t−logpθ(ot|ht,zt)−logpθ(rt|ht,zt)+βKL(qθ(zt|⋅)∥pθ(zt|ht))].\mathcal{L}_{\text
 {Dreamer}}=\mathbb{E}\!\left[\sum_{t}-\log p_{\theta}(o_{t}|h_{t},z_{t})-\log p_{\theta}(r_   (18)
 {t}|h_{t},z_{t})+\beta\,\mathrm{KL}\!\bigl(q_{\theta}(z_{t}|\cdot)\,\|\,p_{\theta}(z_{t}|h_
 {t})\bigr)\right].

DreamerV3 retains this objective family but adds engineering refinements (symlog targets, KL
balancing, two-hot value targets) for stability across domains. Reconstruction provides an implicit
anti-collapse signal but commits modelling capacity to pixel-level details that are often
irrelevant for control. Our setting differs from Dreamer’s in two ways: we train without reward
labels, and we predict in embedding space rather than reconstructing observations.

H.3.2 EMA latent targets

TD-MPC [24, 23] learns a latent encoder zt=ϕθ​(ot)z_{t}=\phi_{\theta}(o_{t}) and a latent
transition gωg_{\omega} together with reward and value heads, all trained end-to-end via TD
learning. The objective combines reward, value, and a latent-consistency term that regresses gω​
(zt,at)g_{\omega}(z_{t},a_{t}) onto sg​(ϕθ−​(ot+1))\texttt{sg}(\phi_{\theta^{-}}(o_{t+1})), where
θ−\theta^{-} is an EMA copy of θ\theta. SPR [47] applies the same idea to data-efficient Atari,
predicting kk-step latent rollouts against a BYOL-style [15] momentum target encoder. Both methods
rely on the EMA target as the primary defense against collapse: a slowly-moving target makes
constant solutions a moving target. TD-MPC additionally requires reward supervision. Our work
avoids EMA targets and reward labels, replacing both with the inverse-dynamics signal.

H.3.3 Augmentation and contrastive methods

CURL [30] adds an InfoNCE [53] contrastive auxiliary loss between augmented views of the same
observation, sharing the encoder with the policy and Q-function. DrQ [28] shows that random
image-shift augmentation alone, without an explicit auxiliary loss, suffices for state-of-the-art
visual RL. Proto-RL [56] uses prototypical clustering of embeddings as both an exploration bonus
and a representation learner. None of these methods learns a world model in our sense: they learn
encoders shared with model-free policies. Their anti-collapse mechanisms (negatives, augmentation,
prototype balance) are orthogonal to inverse-dynamics regularization and could in principle be
combined with it.

H.3.4 JEPA-style regularizers

JEPAs predict in embedding space, with the target embedding produced by an encoder applied to the
future or masked observation, typically tied to the online encoder via stop-gradient and EMA [31, 2
, 4]. In the action-conditioned regime relevant to control, several recent methods stand out. Zhou
et al. [59] (DINO-WM) freeze a pretrained DINOv2 encoder and learn only a latent dynamics model on
top. Sobal et al. [49] (PLDM) train end-to-end with a multi-term objective combining VICReg-style
regularizers [5] with an inverse-dynamics term. Maes et al. [33] (LeWorldModel) build on LeJEPA [3]
and regularize embeddings toward isotropic Gaussianity using SIGReg. Assran et al. [1] (V-JEPA 2)
scale to large internet-video corpora and rely on EMA targets and a separate action-conditioning
post-training stage. Our inverse-dynamics objective adds a distinct point on this design axis: a
task-grounded action-prediction signal anchoring the embedding to controllable structure.

Experimental support, please view the build logs for errors. Generated by L A T E xml [LOGO] .

Instructions for reporting errors

We are continuing to improve HTML versions of papers, and your feedback helps enhance accessibility
and mobile support. To report errors in the HTML that will help us improve conversion and
rendering, choose any of the methods listed below:

  • Click the "Report Issue" ( ) button, located in the page header.

Tip: You can select the relevant text first, to include it in your report.

Our team has already identified the following issues. We appreciate your time reviewing and
reporting rendering errors we may not have found yet. Your efforts will help us improve the HTML
versions for all readers, because disability should not be a barrier to accessing research. Thank
you for your continued support in championing open access for all.

Have a free development cycle? Help support accessibility at arXiv! Our collaborators at LaTeXML
maintain a list of packages that need conversion, and welcome developer contributions.

We gratefully acknowledge support from our major funders, member institutions, , and all
contributors.
About · Help · Contact · Subscribe · Copyright · Privacy · Accessibility · Operational Status
(opens in new tab)
Major funding support from
Simons Foundation Simons Foundation International Schmidt Sciences
 
