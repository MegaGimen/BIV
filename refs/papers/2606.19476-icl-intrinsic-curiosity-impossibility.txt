source: https://arxiv.org/html/2606.19476
arxiv: https://arxiv.org/abs/2606.19476
note: HTML dump via w3m; not a PDF.
========================================================================

Report GitHub Issue

×
Title: [                    ]

Content selection saved. Describe the issue below:

Description: [                    ]
Submit without GitHub Submit in GitHub
[smileybone] arXiv is now an independent nonprofit! Learn more ×
arXiv logo Back to arXiv
Why HTML? Report Issue Back to Abstract Download PDF   

 1. Abstract
 2. 1 Introduction
 3. 2 Problem Setting and Notation
     1. Environment.
     2. Intrinsic curiosity objective.
     3. In-context learning prediction errors.
 4. 3 Prior Work on Intrinsic Curiosity
     1. Surprisal.
     2. Learning progress.
     3. Bayesian information gain.
 5. 4 Computing Intrinsic Rewards Using In-Context Learners
     1. 4.1 Predictive Rewards
     2. 4.2 Limits on Intrinsic Rewards From In-Context Learners
         1. Beyond ℱ\mathcal{F}: infinite-horizon rewards.
         2. Summary.
     3. 4.3 Bayesian Experimental Design Setting
         1. Summary.
 6. 5 Experiments
     1. 5.1 Gaussian Process
         1. Predictive model.
         2. Validation.
         3. Results.
     2. 5.2 Mastermind
         1. Predictive model.
         2. Validation.
         3. Results.
     3. 5.3 Alchemy
         1. Predictive model.
         2. Validation.
         3. Results.
 7. 6 Discussion and Future Work
     1. General BAMDPs.
     2. Training the in-context learner.
     3. Online RL setting.
     4. Comparing rsumr^{\mathrm{sum}} and rdlr^{\mathrm{dl}}.
 8. References
 9. A Additional Prior Work on Intrinsic Curiosity
     1. Surprisal in a Learned Latent Space.
     2. Novelty.
     3. Empowerment.
     4. Open-endedness.
10. B Assumptions for Theoretical Results
     1. (i) Markovian, stationary dynamics given θ\theta.
     2. (ii) ρ\rho is exactly Bayesian over θ\theta.
     3. (iii) Observation-only masking.
     4. (iv) Posterior consistency.
     5. (v) Policy-generated actions.
     6. (vi) Mixing given θ\theta.
     7. B.1 The Prior p⁡(θ)p(\theta) and Meta-Learning
     8. B.2 Inexactitude of ρ\rho’s Bayesian Posterior Predictive
     9. B.3 Training ρ\rho With vs. Without Masked Inputs
         1. Standard autoregressive training.
         2. Masked training.
11. C Proofs for Theoretical Results
     1. C.1 Interventional Factorization
     2. C.2 Proof of
         1. Remark.
         2. Proof.
         3. Step 1: Unconditional reduction.
         4. Step 2: 𝔼⁡[rt]\mathbb{E}[r_{t}] depends only on the marginal trajectory distribution.
         5. Step 3: A non-degenerate kernel family with prior-dependent BAMDPs.
         6. Step 4: Expected BIG differs.
         7. Conclusion.
     3. C.3 Proof of
     4. C.4 Proof of
     5. C.5 The Log-Ratio Subclass and Its Chain-Rule Decomposition
         1. The log-ratio subclass ℱLR\mathcal{F}_{\mathrm{LR}}.
         2. Chain-rule decomposition.
         3. Reading the three terms.
         4. Relation to .
     6. C.6 NDIGO reward Decomposition
     7. C.7 Beyond Finite Touch Horizon: The Gap Reward
         1. Remarks.
     8. C.8 Necessity boundlessly growing gaps for Log-Ratio Rewards
         1. Remark (relation to ).
     9. C.9 Proof of
    10. C.10 Proof of
         1. Remark.
12. D Implementing rdlr^{\mathrm{dl}} in General BAMDPs vs. BED
     1. Why no ρ\rho-implementation in general BAMDPs.
     2. BED counterfactual identity.
13. E Experimental Details
     1. E.1 Gaussian Processes
         1. E.1.1 Environment
         2. E.1.2 Bayesian Oracle World Model
             1. Per-step Surprisal
             2. Per-step Information Gain (Bayesian Surprise)
         3. E.1.3 In-Context PFN World Model
             1. Data-Generating Process
             2. Model Architecture
             3. Training
         4. E.1.4 Policy
             1. Model Architecture
     2. E.2 Decomposition of rewards
     3. E.3 Mastermind
         1. E.3.1 Bayesian Oracle World Model
         2. E.3.2 In-Context PFN World Model
             1. Data-Generating Process
             2. Model Architecture
         3. E.3.3 Policy
             1. Model Architecture
     4. E.4 Alchemy
         1. E.4.1 Bayesian Oracle World Model
         2. E.4.2 In-Context PFN World Model
             1. Data-Generating Process
             2. Model Architecture
         3. E.4.3 Policy
             1. Model Architecture

License: CC BY 4.0
arXiv:2606.19476v1 [cs.LG] 17 Jun 2026

Can In-Context Learning Support Intrinsic Curiosity?Thanks: Equal contribution. Correspondence to: 
{eric.elmoznino,guillaume.lajoie}@gmail.com, sangnie@google.com

Eric Elmoznino    Sangnie Bhardwaj Affiliation: Google – Paradigms of Intelligence Team, Google
DeepMind    Johannes von Oswald    Rajai Nasser    Blaise Agüera y Arcas    João Sacramento    Rif
A. Saurous    Guillaume Lajoie

Abstract

Effective machine learning depends not only on how we model data, but also on what data we choose
to collect. While large sequence models have revolutionized data modeling, the problem of automated
data selection, or “intrinsic curiosity”, remains a significant challenge. Classic approaches
incentivize exploration by rewarding an agent based on its “learning progress”, which measures how
much a newly acquired observation improves a world model’s predictive ability. However, evaluating
these rewards traditionally requires expensive inner loops of gradient descent updates within each
trajectory, rendering them computationally impractical at scale. In this work, we investigate
whether the emergent in-context learning (ICL) capabilities of sequence models can eliminate this
bottleneck by serving as immediate, update-free world models. Specifically, we evaluate whether an
exploration policy can be trained to maximize learning progress, using solely the prediction errors
and counterfactual context manipulations of an in-context learner. We first prove that in general
Markov decision processes, this is in fact impossible in an unbiased way: the resulting intrinsic
rewards either suffer from nuisance terms that bias their estimation of true learning progress, or
they cannot be implemented using an in-context learner’s prediction errors. Conversely, we prove a
positive result for a broad subclass of non-temporal settings, encompassing active learning and
Bayesian Experimental Design: here, ICL-derived rewards successfully bound and asymptotically
converge to the true learning progress. We corroborate our theory with controlled experiments
across continuous and symbolic environments, demonstrating that our ICL-driven framework
successfully trains curious data-collection policies that explore optimally.

1 Introduction

How should an agent collect data in an unknown environment to ensure optimal exploration? One
foundational approach dictates that the data should maximally improve the agent’s model of its
environment (Schmidhuber, 1991b; Lindley, 1956). Such a model can subsequently support arbitrary
downstream tasks, either through explicit planning or by having acquired robust representations. To
this end, prior work has proposed utilizing “intrinsic rewards” to drive the collection of data,
which are defined strictly as a function of the trajectory of actions and observations collected by
a policy, and assume no extrinsic “task”.

Given a Bayesian prior on the world, an optimal intrinsic reward is Bayesian information gain
(BIG), which measures the expected bits gained about the true environment’s dynamics with every
action (Itti and Baldi, 2009; Lindley, 1956). However, BIG is difficult to compute in practice
because it requires explicitly parameterizing the environment’s dynamics and performing intractable
Bayesian inference. While tractable prediction-based objectives have been proposed as alternatives
(Schmidhuber, 1991b, e.g.,), they face two major hurdles: (1) their theoretical relationship to BIG
remains poorly understood and (2) computing them requires expensive gradient descent updates to a
world model that have slow credit assignment.

Overcoming this second bottleneck requires fast and data-efficient learning mechanisms. In-context
learning (ICL) has emerged as a highly effective paradigm for addressing this problem: sequence
models can act as amortized predictors that implicitly approximate the Bayesian posterior
predictive in a single forward pass, bypassing the need for explicit inference. This capability is
hypothesized to partly drive the success of large language models (Radford et al., 2019; Brown et
al., 2020), and has seen great success with Prior-Fitted Networks (PFNs) (Nagler, 2023; Müller et
al., 2022) such as TabPFN (Hollmann et al., 2022) which are pretrained on a large prior over
datasets to perform amortized learning on a new dataset at inference time.

In this work, we use ICL for evaluating prediction-based intrinsic curiosity rewards. We examine
the degree to which data collected by a policy improves a world model learned purely in-context,
and reward the policy based on this improvement. This raises several fundamental questions: First,
is this possible with ICL, and for which intrinsic rewards? Second, does the resulting reward
approximate BIG, and if so, under what circumstances? To answer these questions, we make the
following contributions:

 1. 1.
   
    By formalizing in-context learners as implicit Bayesian predictors, we prove novel mathematical
    relationships between prediction-based intrinsic rewards and BIG, which were previously
    unknown.

 2. 2.
   
    We provide a negative result for general Markov Decision Processes (MDPs), proving that
    prediction-based intrinsic rewards are biased estimators of BIG. Further, while some of these
    rewards can be implemented using ICL, we show that others, such as classic learning progress (
    Schmidhuber, 1991b), cannot.

 3. 3.
   
    In contrast, we establish a positive result for Bayesian Experimental Design (BED) settings,
    demonstrating that several intrinsic rewards can be approximated using ICL, and that it is
    possible to asymptotically approximate BIG for long trajectories with a particular reward
    structure.

 4. 4.
   
    We conduct experiments that corroborate our theoretical findings, demonstrating the practical
    viability of this ICL approach to curiosity.

2 Problem Setting and Notation

Environment.

We consider a Bayes-Adaptive MDP (BAMDP) with states sts_{t}, actions ata_{t}, horizon TT, and a
latent environment parameter θ∼p⁡(θ)\theta\sim p(\theta) fixed throughout an episode. Conditional
on θ\theta, the environment’s dynamics are Markov and stationary: st+1∼p(⋅∣st,at,θ)s_{t+1}\sim p(\
cdot\mid s_{t},a_{t},\theta). Throughout, we write ht≔(s1,a1,…,st−1,at−1)h_{t}\coloneqq(s_{1},a_
{1},\ldots,s_{t-1},a_{t-1}) for the trajectory history just before sts_{t}.

Intrinsic curiosity objective.

Our goal is to train a policy πϕ​(at∣ht,st)\pi_{\phi}(a_{t}\mid h_{t},s_{t}) under a “meta-RL”
setting (Duan et al., 2016) in which episodes are rolled out from environments sampled from p⁡(θ)p
(\theta). Crucially, there is no notion of “task” or “extrinsic” reward. We instead consider
“intrinsic” rewards that are functions of only the trajectory (s1,a1,…,sT)(s_{1},a_{1},\ldots,s_
{T}), and which are ideally maximized when the trajectory is highly informative about the latent
parameters θ\theta governing the environment’s dynamics. By training πϕ\pi_{\phi} across a broad
distribution of environments sampled from p⁡(θ)p(\theta), we aim for it to optimally explore a new
environment sampled from this distribution at inference time. In information theoretic terms, we
consider an “optimal” intrinsic reward as one that maximizes the per-step mutual information
between an observed state and the latent environment parameters, conditioned on the history. In the
literature, this is called Bayesian information gain (BIG) or Bayesian surprise (Itti and Baldi,
2009; Lindley, 1956):

 BIG:=I⁡(st;θ∣ht).\displaystyle\text{BIG}:=I(s_{t};\theta\mid h_{t}).  (1)

In-context learning prediction errors.

We consider whether a sequence model ρ\rho can be used to approximate BIG, using only its
prediction errors and manipulations of its context. We assume that ρ\rho has been pretrained
offline to perform next-state predictions on trajectories of BAMDPs sampled from prior p⁡(θ)p(\
theta) with uniform action selection — ρ\rho is then frozen when training the policy. We further
suppose that ρ\rho has been trained with sufficient capacity and data, such that it has learned to
emit the Bayesian posterior-predictive:

 ρ⁡(st∣ht)=∫p⁡(st∣st−1,at−1,θ)​p​(θ∣ht)​𝑑θ.\displaystyle\rho(s_{t}\mid h_{t})=\int p(s_{t}\mid  (2)
 s_{t-1},a_{t-1},\theta)\,p(\theta\mid h_{t})\,d\theta.

This perspective of in-context learners as amortized Bayes-optimal predictors has been formalized
in substantial prior work (Grau-Moya et al., 2024; Mikulik et al., 2020; Xie et al., 2022; Binz et
al., 2024; Nagler, 2023; Müller et al., 2022), and it serves as a starting point for our
investigations. In our setting, ρ\rho can be seen as a meta-learner that implicitly fits
environment parameters θ\theta in-context and exposes their predictions for novel transitions, all
within a single forward pass. The speed of in-context learners makes them particularly promising
for the purposes of optimizing certain intrinsic curiosity objectives, which, as we will see next,
require us to repeatedly assess the learning progress of a world model over the course of a
trajectory.

3 Prior Work on Intrinsic Curiosity

We briefly describe approaches to intrinsic curiosity relevant to our work — Aubret et al. (2023)
provides a recent review. We discuss intrinsic rewards that are less directly related to our work
in Appendix A. We assume p(⋅∣⋅,θ^t)p(\cdot\mid\cdot,{\hat{\theta}_{t}}) is a predictive world model
that has been trained on subtrajectory (a1:t−1,s1:t)(a_{1:{t-1}},s_{1:t}).

Surprisal.

A common heuristic for encouraging exploration is to maximize a world model’s surprisal, seeking
observations for which it has high prediction error. The surprisal reward is rt≔−log⁡p⁡
(st∣st−1,at−1,θ^t−1)r_{t}\coloneqq-\log\ p(s_{t}\mid s_{t-1},a_{t-1},\hat{\theta}_{t-1}). This
approach has been used at least as far back as Schmidhuber (1991a) and is simple but surprisingly
effective (Burda et al., 2019; Levy et al., 2025; Hester and Stone, 2012; Chentanez et al., 2004).
However, surprisal fails to distinguish between two sources of information: epistemic (i.e.,
learnable) and aleatoric (i.e., noise). The latter can be detrimental: a policy seeking surprisal
will learn to sit in front of a “noisy TV”, even if these observations contain no new useful
information about the environment (Schmidhuber, 1991b). Thus, while surprisal excels in
deterministic environments, it fails in stochastic settings (Burda et al., 2019).

Learning progress.

Another principled approach to intrinsic curiosity is to maximize “learning progress”, introduced
in Schmidhuber (1991b) and further explored in (Schmidhuber, 2009; Schmidhuber, 2010; Storck et
al., 1995; Oudeyer et al., 2007; Oudeyer and Kaplan, 2008; Oudeyer and Kaplan, 2007; Lopes et al.,
2012; Azar et al., 2019). It aims to reward a policy based on the degree to which the data that it
acquires improves the predictive ability of a world model. The learning progress intrinsic reward
is rt≔log⁡p⁡(st∣st−1,at−1,θ^t)−log⁡p⁡(st∣st−1,at−1,θ^t−1)r_{t}\coloneqq\log\ p(s_{t}\mid s_{t-1},a_
{t-1},\hat{\theta}_{t})-\log\ p(s_{t}\mid s_{t-1},a_{t-1},\hat{\theta}_{t-1}), i.e. the improvement
in the model’s ability to predict sts_{t} after the transition to it has been observed. pθ^tp_{\hat
{\theta}_{t}} is often parameterized using a recurrent neural network updated online with gradient
descent. Unlike surprisal, it assigns zero reward to observations that only contain aleatoric
noise, since they do not improve the model’s predictive ability.

In Section 4, we will show that while learning progress has desirable exploration properties, the
need to evaluate a model’s prediction error on transitions that it has already seen makes it
non-implementable using an in-context learner’s prediction errors, for general environments. Azar
et al. (2019) introduced an alternative that instead evaluates improvements in prediction error on
future transitions (st+K−1,at+K−1,st+K)(s_{t+K-1},a_{t+K-1},s_{t+K}), rather than on the current
one. Section 4 will show that this alternative — along with a variant that considers improvements
on all future transitions — can be computed using an in-context learner’s prediction errors.

Bayesian information gain.

Alternatively, we can seek data that maximally changes a model’s belief over possible environments
— an approach termed Bayesian information gain (BIG) or Bayesian surprise (Sun et al., 2011; Little
and Sommer, 2013; Itti and Baldi, 2009; Houthooft et al., 2016; Stadie et al., 2015; MacKay, 1992;
Fedorov, 1972; Houlsby et al., 2011; Lindley, 1956). The predictive model now considers a
distribution over possible environment dynamics, with initial prior p⁡(θ^)p(\hat{\theta}). Sun et
al. (2011) show that the optimal exploration strategy is to maximize the Kullback-Leibler
divergence between the prior and posterior over environment parameters after having observed a
novel transition: rt≔KL(p(θ^∣ht,st)||p(θ^∣ht))r_{t}\coloneqq KL(p(\hat{\theta}\mid h_{t},s_{t})\;||
\;p(\hat{\theta}\mid h_{t})), where p⁡(θ^∣ht,st)∝p⁡(θ^|ht)​p​(st|ht,θ^)p(\hat{\theta}\mid h_{t},s_
{t})\propto p(\hat{\theta}|h_{t})p(s_{t}|h_{t},\hat{\theta}). Intuitively, this objective
encourages the agent to reduce its uncertainty about the environment dynamics as quickly as
possible. Furthermore, if the initial prior p⁡(θ^)p(\hat{\theta}) is equal to the environment’s
true prior distribution over latents p⁡(θ)p(\theta), this reward is mathematically equivalent in
expectation to I⁡(st;θ∣ht)I(s_{t};\theta\mid h_{t}) from Equation 1.

While optimal in terms of yielding the fastest convergence of p⁡(θ^∣ht,st)p(\hat{\theta}\mid h_
{t},s_{t}) towards the true environment dynamics θ\theta, BIG poses significant implementation
challenges. It requires a model to do Bayesian inference over a space of possible environments — an
operation that is generally intractable and difficult to approximate. Moreover, this approach
assumes the model can explicitly parameterize the hypothesis space over θ\theta, which is
challenging in rich environments with unknown latent structure. In Section 4, we will take BIG as
the theoretically-optimal objective for intrinsic curiosity, and we will evaluate the degree to
which more tractable prediction-based rewards computed with in-context learners can approximate it.

4 Computing Intrinsic Rewards Using In-Context Learners

We now ask whether an in-context learner ρ\rho can support intrinsic curiosity rewards that
approximate BIG. Section 4.1 writes candidate rewards in ρ\rho’s interface; Section 4.2 shows none
can identify BIG in general BAMDPs at any finite horizon, and that the asymptotic limits required
to escape this barrier are practically out of reach; Section 4.3 restores tractability under a
structural restriction, namely that of Bayesian Experimental Design (BED).

4.1 Predictive Rewards

We retain the BAMDP setup of Section 2 and additionally write ht′∖th_{t^{\prime}\setminus t} for
the history ht′h_{t^{\prime}} with sts_{t} replaced by a “mask” token — a counterfactual
manipulation of ρ\rho’s context used by learning-progress-style rewards.

We assume: (i) Markovian, stationary dynamics given θ\theta, (ii) ρ\rho is exactly Bayesian over θ\
theta, (iii) the masked trajectory ht′∖th_{t^{\prime}\setminus t} removes only sts_{t}’s
contribution and ρ\rho remains exactly Bayesian on it, (iv) posterior consistency: p⁡(θ∣ht)→δθ∗p(\
theta\mid h_{t})\to\delta_{\theta^{*}} as t→∞t\to\infty, (v) actions are policy-generated; future
actions do not update the posterior over θ\theta, (vi) mixing given θ\theta: I(st;
st+k∣θ,ht,at:t+k−1)→0I(s_{t};s_{t+k}\mid\theta,h_{t},a_{t:t+k-1})\to 0 as k→∞k\to\infty. Detailed
treatments, including the consequences of approximation, masked-input training, and non-mixing
dynamics, are in Appendix B.

We want πϕ\pi_{\phi} to collect observations that are maximally informative about the world, and we
investigate how to maximize the stepwise Bayesian information gain (BIG) contributed by a new
observation sts_{t}: I⁡(st;θ∣ht)I(s_{t};\theta\mid h_{t}). Importantly, we do not allow explicit
access or manipulation of θ\theta, and consider intrinsic rewards reviewed in Section 3 that are
built off of state prediction errors, expressed in terms of ρ\rho when possible. Our theoretical
results apply to a general class of rewards, including the following common ones:

 rtsur\displaystyle
 r^{\mathrm{sur}}_  ≔−log⁡ρ⁡(st∣ht),\displaystyle\coloneqq-\log\ \rho(s_{t}\mid h_{t}),         (3)
 {t}
 rtdl\displaystyle  ≔log∫p(st∣st−1,at−1,θ)p(θ∣ht,st)dθ−logρ(st∣ht).\displaystyle\coloneqq\log\
 r^{\mathrm{dl}}_   int p(s_{t}\mid s_{t-1},a_{t-1},\theta)\,p(\theta\mid h_{t},s_{t})\,d\      (4)
 {t}                theta-\log\ \rho(s_{t}\mid h_{t}).

Here, rsurr^{\mathrm{sur}} is classical predictive surprisal. rdlr^{\mathrm{dl}} is a learning
progress reward based on description length (dl) reduction (Schmidhuber, 1991b; Schmidhuber, 2009;
Schmidhuber, 2010), which asks how much a just-observed transition would improve a model’s
predictions if included in its posterior. Adding to this list, we propose a novel reward:

 rtsum\            ≔∑t′=t+1T[log⁡ρ⁡(st′∣ht′)−log⁡ρ⁡(st′∣ht′∖t)],\displaystyle\coloneqq\sum_{t^
 displaystyle r^{\ {\prime}=t+1}^{T}\bigl[\log\ \rho(s_{t^{\prime}}\mid h_{t^{\prime}})-\log\   (5)
 mathrm{sum}}_{t}  \rho(s_{t^{\prime}}\mid h_{t^{\prime}\setminus t})\bigr],

where TT is the trajectory length. rsumr^{\mathrm{sum}} is one of our core contributions, and
experiments show it performs well (Section 5). It telescopes over the remaining trajectory and is
an extension of the NDIGO reward of Azar et al. (2019), which measured the difference in predictive
log-likelihoods for a single future observation KK steps in the future. We refer the reader to
Section C.6 for a treatment of the NDIGO reward. We illustrate our setting in Figure 1, using rsumr
^{\mathrm{sum}} as an example.


Figure 1: Our method involves using a pretrained in-context learner ρ\rho to construct intrinsic
curiosity rewards for a policy πϕ\pi_{\phi}. Trajectories unrolled by the policy are passed to the
in-context learner, and the reward can be any function of the resulting observation prediction
errors on manipulated sequence contexts. We give an example for the reward rsumr^{\mathrm{sum}},
which measures the improvement in future prediction errors when a particular state is observed
compared to when it is masked.

4.2 Limits on Intrinsic Rewards From In-Context Learners

We first characterize the class of rewards easily implementable with in-context learners ρ\rho:

1Class ℱ\mathcal{F}.

ℱ\mathcal{F} is the class of arbitrary functions of finitely many predictive likelihoods of ρ\rho
at conditioning subsets of the actual trajectory:

 ℱ={rt=f((ρ(Xi∣Yi))i∈I):Xi observation, Yi⊆trajectory, ∀i∈I, and f:[0,1]|I|→ℝ}.\displaystyle\
 mathcal{F}\;=\;\Bigl\{r_{t}=f\bigl((\rho(X_{i}\mid Y_{i}))_{i\in I}\bigr):X_{i}\text{
 observation, }Y_{i}\subseteq\text{trajectory, }\forall i\in I\,,\text{ and }f:[0,1]^{|I|}\to\
 mathbb{R}\Bigr\}.

At any finite horizon TT, rsurr^{\mathrm{sur}} and rsumr^{\mathrm{sum}} belong to ℱ\mathcal{F}.
rdlr^{\mathrm{dl}} does not: the integral in Equation 4 requires an explicit Bayesian update of ρ\
rho’s posterior — outside ρ\rho’s predictive interface in general (see Appendix D). We return to
rdlr^{\mathrm{dl}} in Section 4.3, where it does admit a ρ\rho-tractable form for a subclass of
environments. Returning to our goal of maximizing BIG, no finite-horizon reward in ℱ\mathcal{F} can
be an unbiased estimator:

1Impossibility of BIG identification in ℱ\mathcal{F}.

Let ℳ\mathcal{M} be the class of BAMDPs satisfying (i)–(v) above, whose transition kernels are
neither deterministic nor independent across time given θ\theta. Then for every rt∈ℱr_{t}\in\
mathcal{F}, there exists M∈ℳM\in\mathcal{M} with 𝔼M​[rt∣ht]≠I⁡(st;θ∣ht)\mathbb{E}^{M}[r_{t}\mid h_
{t}]\neq I(s_{t};\theta\mid h_{t}).

The proof (Section C.2) constructs two BAMDPs whose priors share enough leading moments to match 𝔼⁡
[rt]\mathbb{E}[r_{t}] for any finite rt∈ℱr_{t}\in\mathcal{F} while their expected BIG differs. We
now decompose rsurr^{\mathrm{sur}} and rsumr^{\mathrm{sum}} against I⁡(st;θ∣ht)I(s_{t};\theta\mid
h_{t}) to locate the bias.

2Decomposition of rsurr^{\mathrm{sur}}.

Under (i)–(iii),

 𝔼⁡[rtsur∣ht]=I⁡(st;θ∣ht)+H⁡(st∣ht,θ)⏟aleatoric entropy.\displaystyle\mathbb{E}[r^{\mathrm{sur}}_
 {t}\mid h_{t}]\;=\;{I(s_{t};\theta\mid h_{t})}\;+\;\underbrace{\Ent(s_{t}\mid h_{t},\theta)}_{\
 textnormal{aleatoric entropy}}.

H⁡(st∣ht,θ)=H⁡(st∣st−1,at−1,θ)\Ent(s_{t}\mid h_{t},\theta)=\Ent(s_{t}\mid s_{t-1},a_{t-1},\theta)
is non-negative and does not vanish as θ\theta becomes identified.

The proof is in Section C.3. Theorem 2 highlights the classic “noisy TV problem”, where an agent
seeks sources of unpredictable noise.

3Decomposition of rsumr^{\mathrm{sum}}.

Under (i)–(iii) and (v),

 𝔼[rtsum∣ht,at:T−1]=I(st;θ∣ht)+I(st;st+1∣θ,ht,at)⏟one-step “abductive”−I(st;θ∣ht,at:T−1,st+1:T)
 ⏟“residual”.\displaystyle\mathbb{E}[r^{\mathrm{sum}}_{t}\mid h_{t},a_{t:T-1}]\;=\;I(s_{t};\theta\
 mid h_{t})\;+\;\underbrace{I(s_{t};s_{t+1}\mid\theta,h_{t},a_{t})}_{\textnormal{one-step
 ``abductive''}}\;-\;\underbrace{I(s_{t};\theta\mid h_{t},a_{t:T-1},s_{t+1:T})}_{\textnormal
 {``residual''}}.

Both terms are non-negative. Under (iv), the residual vanishes as T→∞T\to\infty; the abductive does
not.

The proof is in Section C.4. The abductive term captures kernel-mediated coupling between sts_{t}
and the next observation st+1s_{t+1} that is not explained by their common dependence on θ\theta.
Similarly, a general treatment of rewards involving log-ratios lead to signal/abductive/residual
decomposition (Section C.5). Notably, applied to NDIGO, it produces an analogous decomposition
whose abductive and residual both fail to vanish.

Beyond ℱ\mathcal{F}: infinite-horizon rewards.

Taking T→∞T\to\infty to drive rsumr^{\mathrm{sum}}’s residual to zero already pushes rsumr^{\mathrm
{sum}} outside the finite-horizon class ℱ\mathcal{F} of Theorem 1. Are there infinite-horizon
rewards built from ρ\rho’s predictives that recover BIG in general BAMDPs? Consider the
generalization of rsumr^{\mathrm{sum}} with a gap parameter KK separating sts_{t} from the
predicted block, rtgap(K)≔logρ(st+K:T∣ht,st,at:T−1)−logρ(st+K:T∣ht,at:T−1)r^{\mathrm{gap}}_{t}(K)\
coloneqq\log\ \rho(s_{t+K:T}\mid h_{t},s_{t},a_{t:T-1})-\log\ \rho(s_{t+K:T}\mid h_{t},a_{t:T-1}).
Under the mixing assumption (vi), rgapr^{\mathrm{gap}} recovers BIG in the iterated limit
limK→∞limT→∞\lim_{K\to\infty}\lim_{T\to\infty} (Corollary 6.1), and this double limit is
structurally necessary — any log-ratio reward universally identifying BIG must place all weight at
infinite gap with blocks of unbounded size (Theorem 7). We use rgapr^{\mathrm{gap}} as an
analytical tool only: it is not feasibly tractable using in-context learners (see below; details in
Section C.7).

Summary.

The section identified compounding obstructions to building ρ\rho-based estimators of BIG:

  • •
   
    Interventional rewards — those requiring modifications of ρ\rho’s posterior — lie outside ρ\
    rho’s standard interface. The canonical example, rdlr^{\mathrm{dl}} (Equation 4), folds the
    just-observed sts_{t} into the posterior over θ\theta before re-evaluating the kernel at
    (st−1,at−1)(s_{t-1},a_{t-1}), and is not implementable through ρ\rho in general BAMDPs.

  • •
   
    Among ρ\rho-implementable rewards in ℱ\mathcal{F}, no finite-horizon estimator identifies BIG (
    Theorem 1); rsurr^{\mathrm{sur}} and rsumr^{\mathrm{sum}} exemplify the bias structure, the
    former acquiring an aleatoric “noisy TV” offset (Theorem 2) and the latter a persistent
    one-step abductive (Theorem 3).

  • •
   
    Recovery of BIG via a reward related to ℱ\mathcal{F} requires both sequence length T→∞T\to\
    infty and marginalizing gap K→∞K\to\infty, as exemplified by the theoretical reward rgapr^{\
    mathrm{gap}} (Corollaries 6.1 and 7). The latter is a substantial obstacle: significant
    unobserved intermediate states st+1:t+K−1s_{t+1:t+K-1} that ρ\rho must marginalize over are
    poorly supported in sequence models (Section B.3).

In sum, we argue there is no practical reward easily implementable with an in-context learner ρ\rho
that converges to BIG in general BAMDPs. Moreover, rsumr^{\mathrm{sum}} stands as the best ρ\
rho-implementable approximation of BIG — scaling naturally with trajectory length but nevertheless
retaining an irreducible abductive bias. We now consider BED, in which several of these
obstructions vanish.

4.3 Bayesian Experimental Design Setting

We now consider Bayesian Experimental Design (BED) environments, where the transition kernel
satisfies p⁡(st∣st−1,at−1,θ)=p⁡(st∣at−1,θ)p(s_{t}\mid s_{t-1},a_{t-1},\theta)=p(s_{t}\mid a_{t-1},\
theta) — the effect of action ata_{t} depends on θ\theta but not on the current state. This covers
any sequential experiment with trials conditionally independent given θ\theta and the action
sequence, such as active learning. Aleatoric biases remain, and rsurr^{\mathrm{sur}}’s
decomposition is unchanged (Theorem 2). But, any abductive bias of the form seen in Theorem 3
vanishes structurally, simplifying rsumr^{\mathrm{sum}}:

3.1Decomposition of rsumr^{\mathrm{sum}} in BED.

Under (i)–(v),

 𝔼[rtsum∣ht,at:T−1]=I(st;θ∣ht)−I(st;θ∣ht,at:T−1,st+1:T),\displaystyle\mathbb{E}[r^{\mathrm{sum}}_
 {t}\mid h_{t},a_{t:T-1}]\;=\;I(s_{t};\theta\mid h_{t})\;-\;I(s_{t};\theta\mid h_{t},a_{t:T-1},s_
 {t+1:T}),

i.e., the abductive of Theorem 3 vanishes structurally. As T→∞T\to\infty, 𝔼⁡[rtsum]→I⁡(st;θ∣ht)\
mathbb{E}[r^{\mathrm{sum}}_{t}]\to I(s_{t};\theta\mid h_{t}).

The proof is in Section C.9. Thus rsumr^{\mathrm{sum}} is an asymptotically unbiased BIG estimator
in BED. For rdlr^{\mathrm{dl}} in the BED setting, things also improve. The posterior predictive
term admits a ρ\rho-predictive form via a counterfactual action commitment that “copies” the
current transition (st−1,at−1,st)(s_{t-1},a_{t-1},s_{t}) into ρ\rho’s context:

 ∫p⁡(st∣st−1,at−1,θ)​p​(θ∣ht,st)​𝑑θ=ρ⁡(st+1=st|ht,st,at=at−1).\int p(s_{t}\mid s_{t-1},a_
 {t-1},\theta)\,p(\theta\mid h_{t},s_{t})\,d\theta\;=\;\rho\bigl(s_{t+1}=s_{t}\,\big|\,h_       (6)
 {t},s_{t},\,a_{t}=a_{t-1}\bigr).

The counterfactual is the action choice at=at−1a_{t}=a_{t-1} (re-using the same action), valid
because actions are policy-generated and the BED environment’s transition kernel has no st−1s_{t-1}
dependence. The identity in Equation 6 makes rdlr^{\mathrm{dl}} implementable using in-context
learners at the cost of a hypothetical extra rollout step.

4Decomposition of rdlr^{\mathrm{dl}} in BED.

Under (i)–(v), with L⁡(θ)≔p⁡(st∣at−1,θ)/ρ⁡(st∣ht)L(\theta)\coloneqq p(s_{t}\mid a_{t-1},\theta)/\
rho(s_{t}\mid h_{t}),

 𝔼⁡[rtdl∣ht]=I⁡(st;θ∣ht)+𝔼st|at−1​[log⁡𝔼p⁡(θ∣ht,st)​[L⁡(θ)]−𝔼p⁡(θ∣ht,st)​[log⁡L⁡(θ)]],\
 displaystyle\mathbb{E}[r^{\mathrm{dl}}_{t}\mid h_{t}]\;=\;I(s_{t};\theta\mid h_{t})\;+\;\mathbb
 {E}_{s_{t}\mid a_{t-1}}\!\Bigl[\log\ \mathbb{E}_{p(\theta\mid h_{t},s_{t})}[L(\theta)]-\mathbb{E}
 _{p(\theta\mid h_{t},s_{t})}[\log\ L(\theta)]\Bigr],

i.e., BIG plus a non-negative Jensen gap. Both terms in the expectation vanish as t→∞t\to\infty
under (iv).

The proof is in Section C.10. Like rsumr^{\mathrm{sum}}, rdlr^{\mathrm{dl}} converges to BIG in
BED, but with opposite-sign bias: rsumr^{\mathrm{sum}} is biased downward by a residual on future
data, rdlr^{\mathrm{dl}} upward by a Jensen gap on past data. We thus get 𝔼⁡[rtsum]≤I⁡(st;θ∣ht)≤𝔼⁡
[rtdl]\mathbb{E}[r^{\mathrm{sum}}_{t}]\leq I(s_{t};\theta\mid h_{t})\leq\mathbb{E}[r^{\mathrm{dl}}_
{t}], with the gap closing asymptotically. Importantly, however, it should be noted that rdlr^{\
mathrm{dl}}’s bias (the Jensen gap) vanishes together with BIG I⁡(st;θ∣ht)I(s_{t};\theta\mid h_{t})
itself, as t→∞t\to\infty (see Section C.9).

Summary.

BED removes the obstructions of general BAMDPs: rsumr^{\mathrm{sum}} and rdlr^{\mathrm{dl}} both
asymptotically recover BIG. However, rsumr^{\mathrm{sum}} recovers BIG as T→∞T\to\infty while rdlr^
{\mathrm{dl}} does so as t→∞t\to\infty. This means that given long sequences, rsumr^{\mathrm{sum}}
can, in principle, recover BIG at finite tt while rdlr^{\mathrm{dl}} only trivially recovers BIG in
the limit where the signal itself vanishes. Nevertheless, both rewards can still be useful at
finite TT and finite tt, as we now investigate in experiments.

5 Experiments

We evaluate our framework on three structured BED environments: Gaussian Process function
estimation, Mastermind code-breaking, and Alchemy transition-rule discovery, each requiring the
policy to actively gather observations that are informative about unknown latent variables.

We train policies with PPO or REINFORCE using curiosity-driven rewards from Section 4.1: rsurr^{\
mathrm{sur}}, rdlr^{\mathrm{dl}}, and rsumr^{\mathrm{sum}}. We also compare to rtaskr^{\mathrm
{task}}, which uses a validation metric of the environment (indicative of having learned its
dynamics) as the reward. For each environment, we pretrain an in-context learner ρ\rho by sampling
environment latents from the prior θ∼p⁡(θ)\theta\sim p(\theta) and unrolling trajectories with
uniformly-random actions; as such, we call these Prior-Fitted Networks (PFNs) in line with prior
work (Müller et al., 2022). Some environments additionally admit an exact predictive Bayesian
oracle predictive ρ∗\rho^{*}. Details and results for each environment are given below.

5.1 Gaussian Process


Figure 2: Comparison of validation log-likelihood for Gaussian Processes for the Oracle and PFN
predictive models. (Left) For rs​u​rr^{sur} validation score decreases over training steps. (Center
and right) Final validation scores: rs​u​mr^{sum} and rd​lr^{dl} achieve equivalent performance to
training on rtaskr^{\mathrm{task}}, but rs​u​rr^{sur} performs significantly worse than a random
policy.

The Gaussian Process (GP) environment models active function estimation in a continuous domain. An
unknown function ff is sampled from a GP prior with a rational quadratic kernel. The function ff is
represented by a finite set of inducing points sampled on a regular grid of resolution RR over the
domain [−xmax,xmax]2[-x_{\max},x_{\max}]^{2}; the posterior mean at any query location is obtained
by exact GP conditioning on these inducing points. More details can be found in Section E.1.1.

A fresh function ff is drawn at the start of each episode. At each step the policy selects a query
location xt∈[−xmax,xmax]2x_{t}\in[-x_{\max},x_{\max}]^{2} and observes a noisy evaluation yt=f⁡(xt)
+εty_{t}=f(x_{t})+\varepsilon_{t}, where εt∼𝒩⁡(0,σnoise2​(xt))\varepsilon_{t}\sim\mathcal{N}\bigl
(0,\,\sigma_{\mathrm{noise}}^{2}(x_{t})\bigr) is spatially varying noise. The noise is in the form
of a tiled checkerboard pattern (details in Section E.1.1), and is the same in every episode. An
optimal information-gathering policy should therefore learn to preferentially query the low-noise
tiles. Consecutive observations are conditionally independent given ff and the noise map.
Therefore, the environment follows the BED setting with θ=f\theta=f, at=xta_{t}=x_{t}, and st+1=
yts_{t+1}=y_{t}.

Predictive model.

The Bayesian oracle ρ∗\rho^{*} performs exact GP posterior inference. After observing hth_{t}, the
predictive distribution for yty_{t} is ρ∗​(yt∣ht,xt)=𝒩⁡(yt,μt​(xt),σt2​(xt))\rho^{*}(y_{t}\mid h_
{t},x_{t})=\mathcal{N}\!\bigl(y_{t};\;\mu_{t}(x_{t}),\;\sigma^{2}_{t}(x_{t})\bigr) where μt\mu_{t}
and σt2\sigma^{2}_{t} are the standard GP posterior mean and predictive variance (including
observation noise) conditioned on hth_{t}. Note ρ∗\rho^{*} has access to the true kernel and the
noise map. The learned PFN ρ\rho is a causal Transformer. Details about the training and
architecture are in Section E.1.3. We also show in Figure E.1 that ρ\rho approximates ρ∗\rho^{*}
well.

Refer to caption
Refer to caption

Figure 3: Left: The colourbar depicts noise strength of the GP. Top 10% of random paths ranked by
the rewards show that rsumr^{\mathrm{sum}} is high for the noiseless tiles, conversely rsurr^{\
mathrm{sur}} favours the noisy regions. Center: BIG across trajectories generated by trained
policies. rs​u​rr^{sur} has the lowest information gain due to the Noisy TV problem. Right: Zoomed
comparison. Higher BIG for rsumr^{\mathrm{sum}}, rdlr^{\mathrm{dl}}, and rtaskr^{\mathrm{task}}
indicates that these policies select queries that are more informative about the underlying
function.

Validation.

To measure the quality of the collected trajectory hTh_{T}, we evaluate on a set of validation
points on the grid. Let (Xv​a​l,Yv​a​l)={(x~j,y~j)}j=1M(X_{val},Y_{val})=\{(\tilde{x}_{j},\tilde{y}
_{j})\}_{j=1}^{M} be a random subset of the episode’s ground-truth GP inducing points. Regardless
of whether ρ\rho is a Bayesian oracle or a trained PFN, we validate by conditioning the oracle ρ∗\
rho^{*} on the trajectory hTh_{T} and report the mean log-likelihood V=1M​∑j=1Mlog⁡ρ∗​(y~j∣hT,x~j)V
=\frac{1}{M}\sum_{j=1}^{M}\log\rho^{*}(\tilde{y}_{j}\mid h_{T},\,\tilde{x}_{j}).

Results.

Since the environment contains aleatoric noise, we observe a stark divergence in performance
between the rewards. As shown in Figure 2, both the random baseline and the policy trained with
rsurr^{\mathrm{sur}} perform significantly worse than those trained with rdlr^{\mathrm{dl}} and
rsumr^{\mathrm{sum}}. We note that the validation log-likelihood steadily decreases over training,
despite rsurr^{\mathrm{sur}} increasing (Figure E.2). This provides strong empirical confirmation
of Theorem 2, which states that rsurr^{\mathrm{sur}} is susceptible to the “noisy TV” problem,
whereas rsumr^{\mathrm{sum}} and rdlr^{\mathrm{dl}} successfully distinguish between reducible
epistemic uncertainty and irreducible aleatoric noise.

To further confirm this, in Figure 3, we collect 1000 trajectories with a random policy. We then
calculate rsurr^{\mathrm{sur}} and rsumr^{\mathrm{sum}} for these trajectories, and in green
highlight the ones in the 90% percentile for each reward. We can observe that the trajectories with
the highest rsumr^{\mathrm{sum}} stray away from the noisy checkerboards, whereas those for rsurr^
{\mathrm{sur}} concentrate in these regions instead. We also plot the BIG across trajectories
collected by the policies trained with the different rewards. As predicted, the BIG of rsurr^{\
mathrm{sur}} is much lower than that of other rewards. Finally, in Section E.2, we show empirically
that rsumr^{\mathrm{sum}} and rdlr^{\mathrm{dl}} decompose into BIG and a shrinking finite-time
nuisance term, as predicted by Corollary 3.1 and Theorem 4.

5.2 Mastermind


Figure 4: Validation scores for Mastermind and Alchemy. In the standard variant, all rewards are
competitive. In both the noisy variants, rs​u​rr^{sur}’s performance falls below the random
baseline, whereas rs​u​mr^{sum} and rd​lr^{dl} remain robust.

Mastermind is a code-breaking game parameterized by a secret code c∈{0,…,C−1}Lc\in\{0,\dots,C{-}1\}
^{L} made of coloured pegs, where LL is the code length and CC the number of colours. At each step,
the policy submits a guess gtg_{t} and receives feedback (bt,wt)(b_{t},w_{t}), where btb_{t} counts
the number of pegs correct in both colour and position, and wtw_{t} counts the total number of pegs
correct in colour irrespective of position. The feedback depends only on the current guess and the
fixed code. This follows the BED setting with θ=c\theta=c, at=gta_{t}=g_{t}, and st+1=(bt,wt)s_
{t+1}=(b_{t},w_{t}).

We additionally consider a noisy variant of the environment in which one randomly chosen colour per
episode is designated as “evil.” Any guess position whose colour matches the evil colour has that
position’s value replaced with a uniformly random colour before feedback is computed, so the
resulting (bt,wt)(b_{t},w_{t}) may not reflect the true code. Additionally, the evil colour is
guaranteed not to appear in the secret code itself. The evil colour identity is unknown to the
policy and the learned world model ρ\rho.

Predictive model.

The oracle ρ∗\rho^{*} maintains an exact posterior over secret codes by enumerating all CLC^{L}
possibilities and filtering to those consistent with the observed history. It computes the
predictive probability of each new response as the fraction of consistent codes that would produce
that response:

 ρ∗(st∣ht)=|{c′:c′​ consistent with ​ht​ and ​(at−1,st)}||{c′:c′​ consistent with ​ht}|.\rho^{*}
 (s_{t}\mid h_{t})=\frac{|\{c^{\prime}:c^{\prime}\text{ consistent with }h_{t}\text{ and }(a_
 {t-1},s_{t})\}|}{|\{c^{\prime}:c^{\prime}\text{ consistent with }h_{t}\}|}.

ρ∗\rho^{*} is only tractable when the environment is deterministic. The learned PFN ρ\rho is a
causal Transformer pretrained on randomly generated trajectories. More details about consistency,
the PFN architecture, and training process are in Sections E.3.1 and E.3.2.

Validation.

After the policy collects a trajectory of TT guesses and responses hTh_{T}, we measure how much
probability mass the world model places on the true secret code. We construct a synthetic action/
observation pair a~v=c\tilde{a}_{v}=c, s~v=(L,L)\tilde{s}_{v}=(L,L) — the true code paired with a
perfect-score response — append it to the trajectory, and report V=ρ∗​(s~v∣hT,a~v)V=\rho^{*}(\tilde
{s}_{v}\mid h_{T},\tilde{a}_{v}). For the noisy variant, we use ρ\rho instead of ρ∗\rho^{*}.

Results.

As seen in Figure 4, rsurr^{\mathrm{sur}} achieves near-optimal performance in the standard setup
given that the environment has no source of aleatoric noise, matching the performance of training
directly for the task. rsumr^{\mathrm{sum}} and rdlr^{\mathrm{dl}} also achieve comparable
performance. In this setting, all reward functions perform consistently across both the Bayesian
oracle and the trained PFN, further validating that sequence models can serve as effective
predictive models for intrinsic reward computation.

However, the introduction of noisy pegs significantly alters these dynamics. In the noisy variant,
the performance of the policy trained with rsurr^{\mathrm{sur}} falls even below the random
baseline. Conversely, policies trained with rsumr^{\mathrm{sum}} and rdlr^{\mathrm{dl}} remain
robust, retaining high performance despite the corruption mechanism. This confirms that rsurr^{\
mathrm{sur}} becomes a liability in environments with stochastic transitions, while rsumr^{\mathrm
{sum}} and rdlr^{\mathrm{dl}} consistently identify informative actions.

5.3 Alchemy

Alchemy (Wang et al., 2021) is a symbolic meta-learning environment. Each environment defines a
fixed set of transition rules mapping (stone, potion) input pairs to transformed output stones, and
the agent must discover these rules through sequential experimentation.

The environment is parameterized by a discrete ID θ\theta, which determines the transition
dynamics. Stones are four-dimensional discrete vectors whose dimensions have (3,3,3,4)(3,3,3,4)
possible values respectively, and potions take one of 6 values. At each step the policy selects
both the initial stone and the potion (it,pt)(i_{t},p_{t}) and observes final stone ftf_{t} from
the environment. Consecutive observations are conditionally independent given θ\theta (i.e. it+1i_
{t+1} is chosen freely and need not equal ftf_{t}). Therefore, Alchemy is BED with at=(it,pt)a_{t}=
(i_{t},p_{t}), and st+1=fts_{t+1}=f_{t}.

Similar to Mastermind, we consider a noisy variant of the environment in which one randomly chosen
stone type per episode is designated as “evil.” Any transition whose input stone matches the evil
stone has its output replaced with a uniformly random stone.

Predictive model.

Like Mastermind, the oracle ρ∗\rho^{*} performs exact Bayesian inference by enumerating all NN
candidates, and eliminating those that are not consistent (all observed transitions match θ\theta’s
rules). ρ∗\rho^{*} is only tractable when the environment is deterministic. The learned PFN ρ\rho
is a causal Transformer with an autoregressive GRU decoder that factorizes output prediction across
the components of ftf_{t} (see Section E.4.2 for full architectural and training details).

Validation.

After the policy collects a trajectory of TT transitions hTh_{T}, we evaluate how well the world
model can predict held-out transitions from the same environment. From the ground-truth transition
table of the current environment ID, we randomly sample M=10M{=}10 transitions, excluding any whose
initial stone matches the evil stone (so that only clean transitions are used). These are appended
sequentially to the trajectory, and we report the mean predictive log-probability V=1M∑j=1Mlogρ
(s~j∣h1:T,a~1:j−1,s~1:j−1)V=\frac{1}{M}\sum_{j=1}^{M}\log\rho(\tilde{s}_{j}\mid h_{1:T},\,\tilde{a}
_{1:j-1},\tilde{s}_{1:j-1}). For the non-noisy environment we evaluate under both the Bayesian
oracle ρ∗\rho^{*} and the trained PFN ρ\rho; for the noisy (evil stone) variant we evaluate under ρ
\rho only.

Results.

We observe a similar pattern as Mastermind in the Alchemy environment in Figure 4. In the noiseless
task, rsurr^{\mathrm{sur}} and rsumr^{\mathrm{sum}} both exhibit strong performance, followed by
rdlr^{\mathrm{dl}}. Under the noisy variant, rsurr^{\mathrm{sur}} again demonstrates a failure to
generalize, with performance below that of a random policy. In contrast, rsumr^{\mathrm{sum}}
maintains its superiority with both oracle and PFN, achieving the highest validation
log-probability.

The results across all three domains underscore the limitation of surprisal-based curiosity in
realistic noisy settings, and demonstrate the robustness of rsumr^{\mathrm{sum}} and rdlr^{\mathrm
{dl}}. They also demonstrate the effectiveness of trained in-context learners as predictive models
in these settings, as replacements for the intractable BIG.

6 Discussion and Future Work

We demonstrated that in-context learners can efficiently evaluate prediction-based intrinsic
curiosity rewards, under certain conditions. In Bayesian Experimental Design (BED) settings, our
proposed reward rsumr^{\mathrm{sum}} and description length reward from prior work rdlr^{\mathrm
{dl}} asymptotically approximate Bayesian information gain (BIG) and drive exploration effectively,
though rdlr^{\mathrm{dl}} suffers from asymptotic pathologies that rsumr^{\mathrm{sum}} avoids.
Both avoid the pitfalls of standard surprisal. This framework opens several promising directions
for future work.

General BAMDPs.

Extending this approach to general Bayes-Adaptive Markov Decision Processes (BAMDPs) requires
handling environments with temporal structure. Implementing rdlr^{\mathrm{dl}} or BIG requires
access to a model’s parameters, which is challenging using in-context learners that only expose the
model’s predictions. A promising approach is to project the in-context learner’s hidden states into
“task vectors” (Hendel et al., 2023) that represent the environment’s latent dynamics. Modulating
or manipulating these task vectors counterfactually could allow the direct estimation of rdlr^{\
mathrm{dl}} or BIG in latent space. Attempting to approximate rgapr^{\mathrm{gap}} — which
asymptotically converges to BIG — using long context lengths and gaps is also a possibility.

Training the in-context learner.

Matching the true Bayesian posterior predictive over the distribution of policy-sampled
trajectories during training poses significant challenges. Large foundation models trained on broad
data mixtures offer powerful in-context learning, but they are often poorly calibrated to the true
Bayesian posterior predictive (Badola et al., 2025). Furthermore, standard causal Transformers
suffer from length generalization issues and positional encoding leakage, breaking the martingale
properties necessary for Bayesian coherence (Falck et al., 2024). While this is partially mitigated
in BED by using permutation-invariant sequence models trained on synthetic priors, covariate shift
remains a primary failure mode. Specifically, a model trained on trajectories from a random or
fixed policy will degrade when evaluated on informative trajectories collected by an active,
reward-maximizing deployment policy (Yadlowsky et al., 2023). Addressing this distribution shift
may require techniques that jointly fine-tune the predictive model and the policy (Ross et al.,
2011; Ivanova et al., 2024; Azar et al., 2019), though optimizing against non-stationary intrinsic
rewards from a changing ρ\rho might introduce instability.

Online RL setting.

This work focuses on an episodic meta-RL setting, but translating these methods to single-lifetime
online reinforcement learning introduces several difficulties. First, the fixed context window of
sequence models limits the duration over which an agent can accumulate learning progress. Second,
in-context learning empirically exhibits saturation; predictive improvements decrease as the
context length grows (Elmoznino et al., 2025), perhaps requiring periodic weight updates to
consolidate long-term knowledge (Bornschein et al., 2024). Third, the rewards themselves become
computationally intractable in continuous online settings. Currently, the intrinsic reward
computation is naturally episodic and calculated only at the conclusion of a trajectory (where
rsumr^{\mathrm{sum}} evaluates over the remainder of the fully unrolled sequence, and rdlr^{\mathrm
{dl}} requires trajectory reruns with the new parameters). Approximating these episodic metrics for
online scaling will require stochastic sampling or truncated evaluation windows. Finally,
exploration in domains with low-level action spaces is inherently difficult. Leveraging emergent
temporal and action abstractions within autoregressive models could allow intrinsic rewards to
operate over higher-level behaviors, improving the efficiency of information gathering (Kobayashi
et al., 2025).

Comparing rsumr^{\mathrm{sum}} and rdlr^{\mathrm{dl}}.

Empirically, we found that rsumr^{\mathrm{sum}} and rdlr^{\mathrm{dl}} both performed well, but
that the optimal one between the two depended on the environment in which they were deployed,
suggesting that they can be complementary. Theoretical analysis in BED settings established that
rsumr^{\mathrm{sum}} bounds BIG from below, whereas rdlr^{\mathrm{dl}} bounds it from above. The
bounds for each are fundamentally different, rdlr^{\mathrm{dl}} converging as t→∞t\to\infty, and
rsumr^{\mathrm{sum}} as T→∞T\to\infty. rdlr^{\mathrm{dl}} technically recovers BIG but its bias
vanishes together with the signal itself, and rsumr^{\mathrm{sum}} should be more robust and
approach BIG at finite tt. However, our experiments show that this distinction may not manifest in
simple environments, and at finite TT and tt. Future work might be able to characterize the
properties of environments that influence these factors. In addition, because the two bounds
converge asymptotically from opposite sides, future work could explore hybridizing them to yield a
tighter, more stable estimator of true learning progress.

Acknowledgments and Disclosure of Funding

The authors acknowledge Seijin Kobayashi for helpful feedback and discussions. EE and SB are PhD
students at the Université de Montréal & Mila – Quebec AI Institute. GL is an Associate Professor
at the Université de Montréal and a Core Academic Member at Mila – Quebec AI Institute. EE
acknowledges support from Vanier Canada Graduate Scholarship #492702. GL acknowledges support from
NSERC Discovery Grant RGPIN-2018-04821, the Canada Research Chair in Neural Computations and
Interfacing, and a Canada-CIFAR AI Chair.

References

  • Aubret et al. (2023) A. Aubret, L. Matignon, and S. Hassas An information-theoretic perspective
    on intrinsic motivation in reinforcement learning: a survey. Entropy 25 (2), pp. 327. Cited by:
    §3.
  • Azar et al. (2019) M. G. Azar, B. Piot, B. A. Pires, J. Grill, F. Altché, and R. Munos World
    discovery models. arXiv preprint arXiv:1902.07685. Cited by: Appendix B, §B.3, §C.6, §3, §3,
    §4.1, §6.
  • Badola et al. (2025) K. Badola, J. Simon, A. Hosseini, S. M. M. Carthy, T. Munkhdalai, A.
    Goyal, T. Kočiskỳ, S. Upadhyay, B. Fatemi, and M. Kazemi Multi-turn puzzles: evaluating
    interactive reasoning and strategic dialogue in llms. arXiv preprint arXiv:2508.10142. Cited
    by: §6.
  • Barto et al. (2013) A. Barto, M. Mirolli, and G. Baldassarre Novelty or surprise?. Frontiers in
    Psychology 4 (English). External Links: ISSN 1664-1078, Link, Document Cited by: Appendix A.
  • Bellemare et al. (2016) M. Bellemare, S. Srinivasan, G. Ostrovski, T. Schaul, D. Saxton, and R.
    Munos Unifying count-based exploration and intrinsic motivation. Advances in neural information
    processing systems 29. Cited by: Appendix A.
  • Binz et al. (2024) M. Binz, I. Dasgupta, A. K. Jagadish, M. Botvinick, J. X. Wang, and E.
    Schulz Meta-learned models of cognition. Behavioral and Brain Sciences 47, pp. e147. Cited by:
    §2.
  • Bornschein et al. (2024) J. Bornschein, Y. Li, and A. Rannen-Triki Transformers for supervised
    online continual learning. arXiv preprint arXiv:2403.01554. Cited by: §6.
  • Bradley et al. (2024) H. Bradley, A. Dai, H. B. Teufel, J. Zhang, K. Oostermeijer, M.
    Bellagente, J. Clune, K. Stanley, G. Schott, and J. Lehman Quality-diversity through AI
    feedback. In The Twelfth International Conference on Learning Representations, External Links: 
    Link Cited by: Appendix A.
  • Brown et al. (2020) T. Brown, B. Mann, N. Ryder, M. Subbiah, J. D. Kaplan, P. Dhariwal, A.
    Neelakantan, P. Shyam, G. Sastry, A. Askell, et al. Language models are few-shot learners. 
    Advances in neural information processing systems 33, pp. 1877–1901. Cited by: §1.
  • Burda et al. (2019) Y. Burda, H. Edwards, D. Pathak, A. Storkey, T. Darrell, and A. A. Efros 
    Large-scale study of curiosity-driven learning. In International Conference on Learning
    Representations, External Links: Link Cited by: Appendix A, §3.
  • Chentanez et al. (2004) N. Chentanez, A. Barto, and S. Singh Intrinsically motivated
    reinforcement learning. Advances in neural information processing systems 17. Cited by: §3.
  • De et al. (2024) S. De, S. L. Smith, A. Fernando, A. Botev, G. Cristian-Muraru, A. Gu, R.
    Haroun, L. Berrada, Y. Chen, S. Srinivasan, G. Desjardins, A. Doucet, D. Budden, Y. W. Teh, R.
    Pascanu, N. D. Freitas, and C. Gulcehre Griffin: mixing gated linear recurrences with local
    attention for efficient language models. External Links: 2402.19427, Link Cited by: §E.1.4,
    §E.4.3.
  • Ding et al. (2023) L. Ding, J. Zhang, J. Clune, L. Spector, and J. Lehman Quality diversity
    through human feedback. In Second Agent Learning in Open-Endedness Workshop, Cited by: Appendix
    A.
  • Du et al. (2023) Y. Du, E. Kosoy, A. L. Dayan, M. Rufova, A. Gopnik, and P. Abbeel What can AI
    learn from human exploration? intrinsically-motivated humans and agents in open-world
    exploration. In Second Agent Learning in Open-Endedness Workshop, External Links: Link Cited
    by: Appendix A.
  • Duan et al. (2016) Y. Duan, J. Schulman, X. Chen, P. L. Bartlett, I. Sutskever, and P. Abbeel 
    RL2{}^{2}: fast reinforcement learning via slow reinforcement learning. arXiv preprint
    arXiv:1611.02779. Cited by: §2.
  • Elmoznino et al. (2025) E. Elmoznino, T. Marty, T. Kasetty, L. Gagnon, S. Mittal, M. Fathi, D.
    Sridhar, and G. Lajoie In-context learning and occam’s razor. In Forty-second International
    Conference on Machine Learning, External Links: Link Cited by: §6.
  • Falck et al. (2024) F. Falck, Z. Wang, and C. Holmes Is in-context learning in large language
    models bayesian? a martingale perspective. In Proceedings of the 41st International Conference
    on Machine Learning, ICML’24. Cited by: §6.
  • Fedorov (1972) V. Fedorov Theory of optimal experiments. Academic Press, NY. Cited by: §3.
  • Grau-Moya et al. (2024) J. Grau-Moya, T. Genewein, M. Hutter, L. Orseau, G. Deletang, E. Catt,
    A. Ruoss, L. K. Wenliang, C. Mattern, M. Aitchison, and J. Veness Learning universal predictors
    . In Proceedings of the 41st International Conference on Machine Learning, R. Salakhutdinov, Z.
    Kolter, K. Heller, A. Weller, N. Oliver, J. Scarlett, and F. Berkenkamp (Eds.), Proceedings of
    Machine Learning Research, Vol. 235, pp. 16178–16205. External Links: Link Cited by: §2.
  • Hazan et al. (2019) E. Hazan, S. Kakade, K. Singh, and A. Van Soest Provably efficient maximum
    entropy exploration. In International Conference on Machine Learning, pp. 2681–2691. Cited by:
    Appendix A.
  • Hendel et al. (2023) R. Hendel, M. Geva, and A. Globerson In-context learning creates task
    vectors. In Findings of the Association for Computational Linguistics: EMNLP 2023, 
    pp. 9318–9333. Cited by: §6.
  • Hester and Stone (2012) T. Hester and P. Stone Intrinsically motivated model learning for a
    developing curious agent. In 2012 IEEE International Conference on Development and Learning and
    Epigenetic Robotics (ICDL), Vol. , pp. 1–6. External Links: Document Cited by: §3.
  • Hollmann et al. (2022) N. Hollmann, S. Müller, K. Eggensperger, and F. Hutter Tabpfn: a
    transformer that solves small tabular classification problems in a second. arXiv preprint
    arXiv:2207.01848. Cited by: §1.
  • Houlsby et al. (2011) N. Houlsby, F. Huszár, Z. Ghahramani, and M. Lengyel Bayesian active
    learning for classification and preference learning. arXiv preprint arXiv:1112.5745. Cited by:
    §3.
  • Houthooft et al. (2016) R. Houthooft, X. Chen, Y. Duan, J. Schulman, F. De Turck, and P. Abbeel
    Vime: variational information maximizing exploration. Advances in neural information processing
    systems 29. Cited by: §3.
  • Hughes et al. (2024) E. Hughes, M. D. Dennis, J. Parker-Holder, F. Behbahani, A. Mavalankar, Y.
    Shi, T. Schaul, and T. Rocktäschel Position: open-endedness is essential for artificial
    superhuman intelligence. In Proceedings of the 41st International Conference on Machine
    Learning, R. Salakhutdinov, Z. Kolter, K. Heller, A. Weller, N. Oliver, J. Scarlett, and F.
    Berkenkamp (Eds.), Proceedings of Machine Learning Research, Vol. 235, pp. 20597–20616. 
    External Links: Link Cited by: Appendix A.
  • Itti and Baldi (2009) L. Itti and P. Baldi Bayesian surprise attracts human attention. Vision
    research 49 (10), pp. 1295–1306. Cited by: §1, §2, §3.
  • Ivanova et al. (2024) D. R. Ivanova, M. Hedman, C. Guan, and T. Rainforth Step-dad:
    semi-amortized policy-based bayesian experimental design. In ICLR 2024 Workshop on Data-centric
    Machine Learning Research (DMLR), Vol. 2, pp. 21. Cited by: §6.
  • Kim et al. (2019) H. Kim, J. Kim, Y. Jeong, S. Levine, and H. O. Song EMI: exploration with
    mutual information. In Proceedings of the 36th International Conference on Machine Learning, K.
    Chaudhuri and R. Salakhutdinov (Eds.), Proceedings of Machine Learning Research, Vol. 97, 
    pp. 3360–3369. External Links: Link Cited by: Appendix A.
  • Klyubin et al. (2005a) A. S. Klyubin, D. Polani, and C. L. Nehaniv All else being equal be
    empowered. In European Conference on Artificial Life, pp. 744–753. Cited by: Appendix A.
  • Klyubin et al. (2005b) A. S. Klyubin, D. Polani, and C. L. Nehaniv Empowerment: a universal
    agent-centric measure of control. In 2005 ieee congress on evolutionary computation, Vol. 1, 
    pp. 128–135. Cited by: Appendix A.
  • Kobayashi et al. (2025) S. Kobayashi, Y. Schimpf, M. Schlegel, A. Steger, M. Wolczyk, J. von
    Oswald, N. Scherrer, K. Maile, G. Lajoie, B. A. Richards, et al. Emergent temporal abstractions
    in autoregressive models enable hierarchical reinforcement learning. arXiv preprint
    arXiv:2512.20605. Cited by: §6.
  • Levy et al. (2025) G. Levy, C. Colas, P. Oudeyer, T. Carta, and C. Romac WorldLLM: improving
    llms’ world modeling using curiosity-driven theory-making. arXiv preprint arXiv:2506.06725. 
    Cited by: §3.
  • Lindley (1956) D. V. Lindley On a measure of the information provided by an experiment. The
    Annals of Mathematical Statistics 27 (4), pp. 986–1005. Cited by: §1, §1, §2, §3.
  • Little and Sommer (2013) D. Y. Little and F. T. Sommer Learning and exploration in
    action-perception loops. Frontiers in neural circuits 7, pp. 37. Cited by: §3.
  • Lopes et al. (2012) M. Lopes, T. Lang, M. Toussaint, and P. Oudeyer Exploration in model-based
    reinforcement learning by empirically estimating learning progress. Advances in neural
    information processing systems 25. Cited by: §3.
  • MacKay (1992) D. J. MacKay Information-based objective functions for active data selection. 
    Neural Computation 4 (4), pp. 590–604. Cited by: §3.
  • Mikulik et al. (2020) V. Mikulik, G. Delétang, T. McGrath, T. Genewein, M. Martic, S. Legg, and
    P. Ortega Meta-trained agents implement bayes-optimal agents. Advances in neural information
    processing systems 33, pp. 18691–18703. Cited by: §2.
  • Mohamed and Jimenez Rezende (2015) S. Mohamed and D. Jimenez Rezende Variational information
    maximisation for intrinsically motivated reinforcement learning. Advances in neural information
    processing systems 28. Cited by: Appendix A.
  • Müller et al. (2022) S. Müller, N. Hollmann, S. P. Arango, J. Grabocka, and F. Hutter 
    Transformers can do bayesian inference. In International Conference on Learning Representations
    , External Links: Link Cited by: §1, §2, §5.
  • Nagler (2023) T. Nagler Statistical foundations of prior-data fitted networks. In International
    Conference on Machine Learning, pp. 25660–25676. Cited by: §1, §2.
  • Ostrovski et al. (2017) G. Ostrovski, M. G. Bellemare, A. Oord, and R. Munos Count-based
    exploration with neural density models. In International conference on machine learning, 
    pp. 2721–2730. Cited by: Appendix A.
  • Oudeyer et al. (2007) P. Oudeyer, F. Kaplan, and V. V. Hafner Intrinsic motivation systems for
    autonomous mental development. IEEE Transactions on Evolutionary Computation 11 (2), 
    pp. 265–286. External Links: Document Cited by: §3.
  • Oudeyer and Kaplan (2007) P. Oudeyer and F. Kaplan What is intrinsic motivation? a typology of
    computational approaches. Frontiers in neurorobotics 1, pp. 108. Cited by: §3.
  • Oudeyer and Kaplan (2008) P. Oudeyer and F. Kaplan How can we define intrinsic motivation?. In 
    the 8th international conference on epigenetic robotics: Modeling cognitive development in
    robotic systems, Cited by: §3.
  • Pathak et al. (2017) D. Pathak, P. Agrawal, A. A. Efros, and T. Darrell Curiosity-driven
    exploration by self-supervised prediction. In International conference on machine learning, 
    pp. 2778–2787. Cited by: Appendix A.
  • Radford et al. (2019) A. Radford, J. Wu, R. Child, D. Luan, D. Amodei, I. Sutskever, et al. 
    Language models are unsupervised multitask learners. OpenAI blog 1 (8), pp. 9. Cited by: §1.
  • Ross et al. (2011) S. Ross, G. Gordon, and D. Bagnell A reduction of imitation learning and
    structured prediction to no-regret online learning. In Proceedings of the fourteenth
    international conference on artificial intelligence and statistics, pp. 627–635. Cited by: §6.
  • Salge et al. (2014) C. Salge, C. Glackin, and D. Polani Empowerment–an introduction. In Guided
    Self-Organization: Inception, pp. 67–114. External Links: ISBN 978-3-642-53734-9, Document,
    Link Cited by: Appendix A.
  • Schiff (1993) J. L. Schiff Normal families. Universitext, Springer-Verlag, New York. Cited by:
    §C.8.
  • Schmidhuber (1991a) J. Schmidhuber A possibility for implementing curiosity and boredom in
    model-building neural controllers. In From Animals to Animats: Proceedings of the First
    International Conference on Simulation of Adaptive Behavior, External Links: ISBN 9780262256674
    , Document, Link, https://direct.mit.edu/book/chapter-pdf/2311948/9780262256674_caz.pdf Cited
    by: §3.
  • Schmidhuber (1991b) J. Schmidhuber Curious model-building control systems. In Proc.
    international joint conference on neural networks, pp. 1458–1463. Cited by: item 2, §1, §1, §3,
    §3, §4.1.
  • Schmidhuber (2009) J. Schmidhuber Driven by compression progress: a simple principle explains
    essential aspects of subjective beauty, novelty, surprise, interestingness, attention,
    curiosity, creativity, art, science, music, jokes. In Anticipatory Behavior in Adaptive
    Learning Systems, G. Pezzulo, M. V. Butz, O. Sigaud, and G. Baldassarre (Eds.), Berlin,
    Heidelberg, pp. 48–76. External Links: ISBN 978-3-642-02565-5 Cited by: §3, §4.1.
  • Schmidhuber (2010) J. Schmidhuber Formal theory of creativity, fun, and intrinsic motivation
    (1990–2010). IEEE Transactions on Autonomous Mental Development 2 (3), pp. 230–247. External
    Links: Document Cited by: §3, §4.1.
  • Schmüdgen (2017) K. Schmüdgen The moment problem. Graduate Texts in Mathematics, Vol. 277, 
    Springer. Cited by: §C.2.
  • Seitzer et al. (2022) M. Seitzer, A. Tavakoli, D. Antic, and G. Martius On the pitfalls of
    heteroscedastic uncertainty estimation with probabilistic neural networks. In International
    Conference on Learning Representations, External Links: Link Cited by: §E.1.3.
  • Stadie et al. (2015) B. C. Stadie, S. Levine, and P. Abbeel Incentivizing exploration in
    reinforcement learning with deep predictive models. arXiv preprint arXiv:1507.00814. Cited by:
    §3.
  • Stanley and Lehman (2015) K. O. Stanley and J. Lehman Why greatness cannot be planned: the myth
    of the objective. (No Title). Cited by: Appendix A.
  • Storck et al. (1995) J. Storck, S. Hochreiter, J. Schmidhuber, et al. Reinforcement driven
    information acquisition in non-deterministic environments. In Proceedings of the international
    conference on artificial neural networks, Paris, Vol. 2, pp. 159–164. Cited by: §3.
  • Strehl and Littman (2008) A. L. Strehl and M. L. Littman An analysis of model-based interval
    estimation for markov decision processes. Journal of Computer and System Sciences 74 (8), 
    pp. 1309–1331. Cited by: Appendix A.
  • Sun et al. (2011) Y. Sun, F. Gomez, and J. Schmidhuber Planning to be surprised: optimal
    bayesian exploration in dynamic environments. In International conference on artificial general
    intelligence, pp. 41–51. Cited by: §3.
  • Wang et al. (2024) G. Wang, Y. Xie, Y. Jiang, A. Mandlekar, C. Xiao, Y. Zhu, L. Fan, and A.
    Anandkumar Voyager: an open-ended embodied agent with large language models. Transactions on
    Machine Learning Research. External Links: ISSN 2835-8856, Link Cited by: Appendix A.
  • Wang et al. (2021) J. Wang, M. King, N. Porcel, Z. Kurth-Nelson, T. Zhu, C. Deck, P. Choy, M.
    Cassin, M. Reynolds, F. Song, G. Buttimore, D. Reichert, N. Rabinowitz, L. Matthey, D.
    Hassabis, A. Lerchner, and M. Botvinick Alchemy: a structured task distribution for
    meta-reinforcement learning. arXiv preprint arXiv:2102.02926. External Links: Link Cited by:
    §E.4, §5.3.
  • Wang et al. (2019) R. Wang, J. Lehman, J. Clune, and K. O. Stanley Paired open-ended
    trailblazer (poet): endlessly generating increasingly complex and diverse learning environments
    and their solutions. arXiv preprint arXiv:1901.01753. Cited by: Appendix A.
  • Xie et al. (2022) S. M. Xie, A. Raghunathan, P. Liang, and T. Ma An explanation of in-context
    learning as implicit bayesian inference. In International Conference on Learning
    Representations, External Links: Link Cited by: §2.
  • Yadlowsky et al. (2023) S. Yadlowsky, L. Doshi, and N. Tripuraneni Pretraining data mixtures
    enable narrow model selection capabilities in transformer models. arXiv preprint
    arXiv:2311.00871. Cited by: §6.
  • Zhang et al. (2024) J. Zhang, J. Lehman, K. Stanley, and J. Clune OMNI: open-endedness via
    models of human notions of interestingness. In The Twelfth International Conference on Learning
    Representations, External Links: Link Cited by: Appendix A.

Appendix A Additional Prior Work on Intrinsic Curiosity

Surprisal in a Learned Latent Space.

One effective approach for mitigating the problem of aleatoric noise is to measure surprisal in a
learned latent space that only encodes predictable information about the observation (Pathak et
al., 2017; Kim et al., 2019; Burda et al., 2019). However, these methods struggle in partially
observed environments where the state representation depends on a long history, and also remain
sensitive to sources of aleatoric noise that are produced by the policy’s actions (Burda et al.,
2019). Ultimately, the problem of how to train a latent space for surprisal-based exploration that
is robust to aleatoric noise remains open. Since our investigation into the use of in-context
learners for intrinsic curiosity leverages only ρ\rho’s input/output interface for computing
observation-level prediction errors, we do not consider methods that operate in latent space
(although if in-context learners are trained in an appropriate latent space, the approach might be
viable).

Novelty.

An approach with a long history in reinforcement learning is to seek novel states of the
environment, as measured by visitation counts or density models (Hazan et al., 2019; Strehl and
Littman, 2008; Bellemare et al., 2016; Ostrovski et al., 2017). Novelty is deeply related to
surprisal; while the two are not quite equivalent (Barto et al., 2013), they both suffer from the
same fundamental problem: noisy regions of the environment with high observation entropy are a
reliable source of both surprisal and novelty.

Empowerment.

Empowerment is an alternative framework for intrinsic motivation that, instead of gathering
observations that improve a model of the environment, aims to explore regions of the environment
that are controllable by the policy (Klyubin et al., 2005a; Klyubin et al., 2005b; Salge et al.,
2014). Specifically, empowerment is defined as the channel capacity of the policy’s actuation
channel: I⁡(S′;A∣S)I(S^{\prime};A\mid S) where II is mutual information, SS is a random variable
for the history of all past states, AA is a random variable for the agent’s current action, and S′S
^{\prime} is a random variable for the state(s) the policy observes after some future horizon
following the action (S′S^{\prime} can represent a single future state at a fixed horizon, or
potentially a trajectory of such states). The distributions over AA, SS, and S′S^{\prime} depend on
both the environment and the policy, and using empowerment as an intrinsic reward involves
maximizing I⁡(S′;A∣S)I(S^{\prime};A\mid S) with respect to the latter. Intuitively, this yields
policies that learn to exert control over their environment by exploring regions of the state space
where actions provide substantial information about future environment dynamics.

It is unclear how empowerment theoretically relates to the above methods that seek to improve a
model of the environment. While exploring controllable regions of an environment might improve a
model as a side effect (e.g., knowing what is and is not controllable involves understanding the
environment’s dynamics at some level), the two are not equivalent in general. While empowerment
appears to be a viable exploration strategy empirically (Mohamed and Jimenez Rezende, 2015) and can
be highly effective in modeling human exploration behaviour (Du et al., 2023), we instead focus
here on intrinsic rewards that explicitly aim to improve a model of the environment.

Open-endedness.

The aims of intrinsic curiosity closely align with those of research on “open-endedness” (Stanley
and Lehman, 2015), typically characterized as an ongoing process that continually generates new
data, tasks, or behaviours that are interesting, learnable, novel, without being too difficult
relative to an agent’s current capabilities. Most existing approaches operationalize open-endedness
either through diversity/complexity heuristics (Wang et al., 2019; Wang et al., 2024) or through
proxies intended to capture subjective human notions of interestingness (Ding et al., 2023; Bradley
et al., 2024; Zhang et al., 2024).

A recent position paper attempts to mathematically formalize the concept of open-endedness (Hughes
et al., 2024), defining it as a process that continually generates data that is (a) surprising and
(b) learnable, where both are defined in terms of the prediction errors of a world model that has
observed the data up to a particular timepoint. Notably, their learnability component closely
resembles learning progress rewards. We further hypothesize that their explicit novelty term is
redundant: if learning progress is sustained and does not stagnate, then novelty and surprisal are
already implied, because continued progress requires exposure to information not yet captured by
the model.

Appendix B Assumptions for Theoretical Results

The main text states six assumptions in compact form. We unpack each here, flagging the nuances
that matter for the derivations.

(i) Markovian, stationary dynamics given θ\theta.

For each fixed θ\theta, the transition kernel p⁡(st+1∣st,at,θ)p(s_{t+1}\mid s_{t},a_{t},\theta) is
Markov in the state-action chain and time-independent. Stationarity rules out time-indexed families
{Pt}t\{P_{t}\}_{t} in which the conditional law itself depends on chain time. Markov dynamics given
θ\theta are what let us factorize ρ\rho’s joint over future observations (Lemma 4.1); stationarity
ensures that masking-based rewards (rsumr^{\mathrm{sum}} and the NDIGO reward of Azar et al. (2019)
) can be evaluated by anchoring their kernels at the actual chain times.

(ii) ρ\rho is exactly Bayesian over θ\theta.

The sequence model emits the exact posterior-predictive ρ⁡(st′∣ht′)=∫p⁡(st′∣st′−1,at′−1,θ)​p​
(θ∣ht′)​𝑑θ\rho(s_{t^{\prime}}\mid h_{t^{\prime}})=\int p(s_{t^{\prime}}\mid s_{t^{\prime}-1},a_{t^
{\prime}-1},\theta)\,p(\theta\mid h_{t^{\prime}})\,d\theta, with prior p⁡(θ)p(\theta) matching the
distribution of training tasks. In practice ρ\rho is a finite-capacity neural network and exact
Bayesian computation is an idealization; Section B.2 discusses how approximation error propagates
into reward biases.

(iii) Observation-only masking.

The masked history ht′∖th_{t^{\prime}\setminus t} replaces sts_{t} with a mask token while leaving
every other state and every action intact. We assume ρ\rho’s response to ht′∖th_{t^{\prime}\
setminus t} is the posterior-predictive obtained by Bayesian marginalization over the masked sts_
{t}, treating actions as still observed. This is a subtle assumption when ρ\rho is a learned
sequence model: masked queries are out-of-distribution unless ρ\rho has been trained to handle
them. See Section B.3.

(iv) Posterior consistency.

The selection rule generates trajectories under which p⁡(θ∣ht)→δθ∗p(\theta\mid h_{t})\to\delta_{\
theta^{*}} in probability as t→∞t\to\infty. This requires (a) θ\theta identifiability from the
achievable observation distributions under the policy and (b) sufficient coverage of the action
space (each action taken often enough to inform θ\theta). Under standard regularity conditions
(smooth parametric model, positive-definite per-arm Fisher information ℐ⁡(θ)≻0\mathcal{I}(\theta)\
succ 0 uniformly on the parameter support), Bernstein–von-Mises gives the rate Var⁡[p⁡(θ∣ht)]∼ℐ​
(θ∗)−1/t\mathrm{Var}\!\bigl[p(\theta\mid h_{t})\bigr]\sim\mathcal{I}(\theta^{*})^{-1}/t,
controlling both the residual in Theorem 3 and the Jensen gap in Theorem 4 at the 1/t1/t scale.
Strictly, the consistency assumption alone is what the framework requires; the Fisher-information
regularity becomes relevant only when one wants asymptotic rates rather than asymptotic limits.

(v) Policy-generated actions.

Actions are sampled from πϕ\pi_{\phi} conditional on past observations. Because actions are not
generated by the environment, conditioning on a future action does not update the posterior over θ\
theta: p⁡(θ∣ht,at,at+1,…,at′−1)=p⁡(θ∣ht)p(\theta\mid h_{t},a_{t},a_{t+1},\ldots,a_{t^{\prime}-1})=p
(\theta\mid h_{t}). This permits treating future actions as fixed inputs in the derivations, and is
equivalent to the standard interventional treatment of actions in causal Markov chains.

(vi) Mixing given θ\theta.

For each θ\theta in the support of p⁡(θ)p(\theta), I(st;st+k∣θ,ht,at:t+k−1)→0I(s_{t};s_{t+k}\mid\
theta,h_{t},a_{t:t+k-1})\to 0 as k→∞k\to\infty. This rules out absorbing states and other
non-ergodic dynamics under which the Markov chain never forgets sts_{t}. Mixing is what makes the
abductive bias of Theorem 3 (and its rgapr^{\mathrm{gap}}-generalized analogues) decay with the gap
parameter KK; without it, infinite-gap rewards still retain a bias from kernel-mediated coupling
between sts_{t} and the deep future.

B.1 The Prior p⁡(θ)p(\theta) and Meta-Learning

The prior p⁡(θ)p(\theta) throughout this paper is the training distribution ptrain​(θ)p_{\mathrm
{train}}(\theta) over MDPs — ρ\rho is pretrained on trajectories from MDPs sampled from ptrainp_{\
mathrm{train}}, and its posterior-predictive is assumed to be Bayesian under ptrainp_{\mathrm
{train}}. This is a meta-learning setup, and the framework’s predictions extend to deployment
insofar as the deployed environment lies in supp⁡(ptrain)\mathrm{supp}(p_{\mathrm{train}}). We
treat deployment mismatch on θ\theta as a separate source of bias and adopt the train-prior framing
throughout.

A more pointed meta-learning concern is that ρ\rho’s training data — pairs of trajectories and
their corresponding posterior-predictives — depends not only on ptrain​(θ)p_{\mathrm{train}}(\
theta) but on the behavior of the policy that generates the training trajectories. Pretraining ρ\
rho offline, separately from the deployment policy πϕ\pi_{\phi}, therefore requires the pretraining
policy to cover the trajectory distribution adequately. This is mostly tractable in BED settings,
where trajectories factorize across actions given θ\theta. Ensuring enough coverage of action
selection during training should suffice. It becomes increasingly difficult as the domain expands,
and especially in temporally rich BAMDPs where the trajectory distribution depends nonlinearly on
the policy: a deployed reward-maximizing policy can easily probe regions that the pretraining
policy under-sampled, leaving ρ\rho uncalibrated where it matters most. In principle, this can be
alleviated with large enough training samples under enough random policies. Nevertheless, this is a
practical matter to be considered.

This is a potential weakness of any offline-pretraining plus online-policy-training scheme: the
predictive model is fixed during policy optimization, but policy iteration may drift outside ρ\
rho’s effective training support. On the other hand, the scheme opens the door to the use of
massive pretrained but frozen foundation models (maybe even LLMs) to support active ICL in a
variety of environments. Practical investigations of policy training in this regime, including the
robustness of the method to departure from Bayesian exactness of ρ\rho (see next section), and the
question of action selection bias, is exciting future work.

Another avenue for the approach to have practical legs at scale would be that of joint training of
π\pi and ρ\rho. This could take the form of a meta-learning regime in which the predictor and the
data-collection policy improve together so that ρ\rho’s posterior-predictive remains accurate over
the trajectories the policy actually generates. We visit this question in Section 6.

B.2 Inexactitude of ρ\rho’s Bayesian Posterior Predictive

Assumption (ii) demands that ρ\rho be exactly Bayesian. In practice ρ\rho is a finite-capacity
neural network trained by maximum likelihood on a finite dataset, so it differs from the exact
Bayesian posterior-predictive ρ∗\rho^{*} in a controllable but non-zero way. This subsection
discusses the consequences of inexactitude as a property of ρ\rho as a learner — independent of any
choice about how queries are conditioned on the trajectory. Issues specifically arising from masked
queries during training or inference are deferred to Section B.3.

Writing Δ(Y)≔DKL(ρ∗(⋅∣Y)∥ρ(⋅∣Y))\Delta(Y)\coloneqq D_{\mathrm{KL}}(\rho^{*}(\cdot\mid Y)\,\|\,\rho
(\cdot\mid Y)) for the per-context KL between exact and approximate predictives at a conditioning
YY, each reward inherits a bias that is a signed combination of such KLs. For example,

 𝔼ρ∗​[rtsur∣ht]=I⁡(st;θ∣ht)+H⁡(st∣ht,θ)+Δ⁡(ht),\mathbb{E}_{\rho^{*}}[r^{\mathrm{sur}}_{t}\mid h_
 {t}]\;=\;I(s_{t};\theta\mid h_{t})\;+\;\Ent(s_{t}\mid h_{t},\theta)\;+\;\Delta(h_{t}),

showing that surprisal acquires an additional non-negative Δ⁡(ht)\Delta(h_{t}) on top of the
noisy-TV bias. More generally, rewards in ℱ\mathcal{F} formed from log-ratios of predictives at two
conditionings Y+,Y−Y_{+},Y_{-} pick up a differential Δ¯​(Y+)−Δ¯​(Y−)\bar{\Delta}(Y_{+})-\bar{\
Delta}(Y_{-}) rather than an absolute KL, since the two predictive errors partially cancel under
log-subtraction.

Two structural features:

  • •
   
    rsurr^{\mathrm{sur}}’s bias is absolute. A single non-negative KL Δ⁡(ht)\Delta(h_{t}). Contexts
    where ρ\rho predicts poorly are systematically over-rewarded, on top of the aleatoric noisy-TV
    — a model-error-seeking pathology specific to surprisal.

  • •
   
    Log-ratio rewards have differential bias. Rewards of the form logρ(⋅∣Y+)−logρ(⋅∣Y−)\log\rho(\
    cdot\mid Y_{+})-\log\rho(\cdot\mid Y_{-}) inherit only the difference of ρ\rho’s errors at Y+Y_
    {+} and Y−Y_{-}. Uniform approximation error cancels; only the asymmetry between the two
    conditionings survives.

The differential structure of log-ratio rewards is a robustness property: an across-the-board
imperfection in ρ\rho cancels in the log-ratio, leaving only the part of the error that
distinguishes the two conditionings. Whether and how this differential is small in practice is a
separate question — it depends on how ρ\rho is trained and queried. Figure E.1 in Section E.1.3
shows the difference of log-predictive terms between an oracle observer and a trained PFN ρ\rho for
the Gaussian process experiment. Figure 2 in the main text shows very little difference in reward
training outcomes for the same experiment.

B.3 Training ρ\rho With vs. Without Masked Inputs

Computing rsumr^{\mathrm{sum}} (and analogously the NDIGO reward of Azar et al. (2019)) requires
evaluating ρ\rho on histories with one or more past states masked out. A sequence model trained
with standard autoregressive next-token prediction will never have seen a mask token in its
training data, so its behavior on masked queries is out-of-distribution. Two training strategies
are natural:

Standard autoregressive training.

ρ\rho is trained on natural (unmasked) trajectories. At inference, predictives at masked
conditioning sets reflect whatever extrapolation the model performs, with no guarantee of agreement
with the Bayesian marginalization over the masked observation. This is the source of the
differential bias described in Section B.2.

Masked training.

ρ\rho is trained with random masking of past observations during training, in the spirit of masked
language modeling. ρ\rho then learns predictives that are well-calibrated with respect to the
marginalized joint, making mask-baseline rewards potentially more reliable.

We remark that these considerations impact reward evaluation in the full BAMDP setting where parts
of transition tuples (st−1,at−1,st)(s_{t-1},a_{t-1},s_{t}) need to be masked. However in the BED
setting, masking simplifies to simply removing observations from pairs (at−1,st)(a_{t-1},s_{t}) and
is not subject to the subtleties described above. Our experiments take place in the BED setting,
but a more general implementation of ICL intrinsic rewards should consider the points above.

Appendix C Proofs for Theoretical Results

C.1 Interventional Factorization

Several of our derivations rely on factorizing ρ\rho’s joint over future observations into a
product of one-step posterior-predictives. The factorization hinges on two structural features: (a)
Markov dynamics given θ\theta (assumption (i)), and (b) policy-generated actions that do not update
the θ\theta-posterior (assumption (v)). The second is what makes actions interventional in the
causal sense: they enter the joint as fixed inputs, not as random variables to be modeled.

4.1Interventional factorization.

For t1<t2t_{1}<t_{2} and any action sequence at1:t2−1a_{t_{1}:t_{2}-1},

 ρ(st1:t2∣ht1,at1:t2−1)=∏τ=t1t2ρ(sτ∣ht1,st1:τ−1,at1:τ−1),\rho(s_{t_{1}:t_{2}}\mid h_{t_{1}},a_{t_
 {1}:t_{2}-1})\;=\;\prod_{\tau=t_{1}}^{t_{2}}\rho(s_{\tau}\mid h_{t_{1}},s_{t_{1}:\tau-1},a_{t_
 {1}:\tau-1}),

and the same identity holds with masked histories provided masked states lie outside [t1,t2][t_
{1},t_{2}].

Proof.

Under (i), the joint factors as ∏τp⁡(sτ∣sτ−1,aτ−1,θ)\prod_{\tau}p(s_{\tau}\mid s_{\tau-1},a_{\
tau-1},\theta). By assumption (v), conditioning on future actions does not update the posterior
over θ\theta. Marginalizing θ\theta on both sides gives the product of posterior-predictives.
Assumption (iii) extends to masked histories. ∎

This factorization is what allows rsumr^{\mathrm{sum}}’s telescoping representation rtsum=pmi(st;
st+1:T∣ht,at:T−1)r^{\mathrm{sum}}_{t}=\pmi(s_{t};s_{t+1:T}\mid h_{t},a_{t:T-1}) to be used in the
proof of Theorem 3.

C.2 Proof of Theorem 1

See 1

Remark.

The argument is a marginal-indistinguishability counterexample: we construct two BAMDPs in ℳ\
mathcal{M} on which any rt∈ℱr_{t}\in\mathcal{F} has the same unconditional expectation but for
which the expected BIG differs. Since an unbiased per-history estimator of BIG forces matched
unconditional expectations, the contradiction settles the theorem.

Proof.

Step 1: Unconditional reduction.

Suppose rt∈ℱr_{t}\in\mathcal{F} satisfies the per-history identity 𝔼M​[rt∣ht]=IM​(st;θ∣ht)\mathbb
{E}^{M}[r_{t}\mid h_{t}]=I^{M}(s_{t};\theta\mid h_{t}) for every history hth_{t} and every M∈ℳM\in\
mathcal{M}. Taking unconditional expectation,

 𝔼M​[rt]=𝔼M​[IM​(st;θ∣ht)].\mathbb{E}^{M}[r_{t}]\;=\;\mathbb{E}^{M}\bigl[\,I^{M}(s_{t};\theta\mid
 h_{t})\,\bigr].

We will exhibit M1,M2∈ℳM_{1},M_{2}\in\mathcal{M} with 𝔼M1​[rt]=𝔼M2​[rt]\mathbb{E}^{M_{1}}[r_{t}]=\
mathbb{E}^{M_{2}}[r_{t}] but 𝔼M1​[I⁡(st;θ∣ht)]≠𝔼M2​[I⁡(st;θ∣ht)]\mathbb{E}^{M_{1}}\bigl[I(s_{t};\
theta\mid h_{t})\bigr]\neq\mathbb{E}^{M_{2}}\bigl[I(s_{t};\theta\mid h_{t})\bigr], contradicting
the per-history identity for at least one of them. We refer to such a pair (M1,M2)(M_{1},M_{2}) —
or, since the kernel is fixed in our construction, the corresponding prior pair (p1,p2)(p_{1},p_
{2}) — as a witness to the theorem.

Step 2: 𝔼⁡[rt]\mathbb{E}[r_{t}] depends only on the marginal trajectory distribution.

Each rt=f⁡((ρ⁡(Xi∣Yi))i∈I)∈ℱr_{t}=f\bigl((\rho(X_{i}\mid Y_{i}))_{i\in I}\bigr)\in\mathcal{F} has
finite touch horizon

 Q≔max⁡{τ:τ​ is a chain index appearing in some ​Xi​ or ​Yi},Q\;\coloneqq\;\max\bigl\{\tau:\tau\
 text{ is a chain index appearing in some }X_{i}\text{ or }Y_{i}\bigr\},

i.e., the largest chain index referenced by the reward. Under Assumption (ii), ρ⁡(X∣Y)=PmargM​(X∣Y)
\rho(X\mid Y)=P^{M}_{\mathrm{marg}}(X\mid Y) where PmargMP^{M}_{\mathrm{marg}} is the marginal
trajectory law of MM (with θ\theta integrated out). Taking unconditional expectation,

 𝔼M​[rt]=𝔼PmargM​[f⁡((PmargM​(Xi∣Yi))i∈I)],\mathbb{E}^{M}[r_{t}]\;=\;\mathbb{E}_{P^{M}_{\mathrm
 {marg}}}\Bigl[f\bigl((P^{M}_{\mathrm{marg}}(X_{i}\mid Y_{i}))_{i\in I}\bigr)\Bigr],

which depends on MM only through PmargM(s1:Q,a1:Q−1)P^{M}_{\mathrm{marg}}(s_{1:Q},a_{1:Q-1}). In
particular, two BAMDPs with identical marginal trajectory distributions on the first QQ steps yield
identical 𝔼⁡[rt]\mathbb{E}[r_{t}].

Step 3: A non-degenerate kernel family with prior-dependent BAMDPs.

Take the BAMDP family with state space 𝒮={0,1}\mathcal{S}=\{0,1\}, a single (dummy) action, initial
state s1∼Bernoulli⁡(0.5)s_{1}\sim\mathrm{Bernoulli}(0.5), and transition kernel

 p⁡(sτ+1≠sτ∣sτ,θ)=θ,θ∈(0,0.5).p(s_{\tau+1}\neq s_{\tau}\mid s_{\tau},\theta)\;=\;\theta,\qquad\
 theta\in(0,0.5).

The kernel is non-deterministic (θ>0\theta>0) and not independent across time given θ\theta (θ≠0.5\
theta\neq 0.5), so each fixed θ∈(0,0.5)\theta\in(0,0.5) yields a kernel meeting the non-degeneracy
condition of Theorem 1. Different priors p⁡(θ)p(\theta) supported on (0,0.5)(0,0.5) specify
different BAMDPs in ℳ\mathcal{M}.

The marginal trajectory distribution of a sequence s1:Qs_{1:Q} depends only on the number of state
flips k=#⁡{τ:sτ+1≠sτ}k=\#\{\tau:s_{\tau+1}\neq s_{\tau}\}:

 Pmargp(s1:Q)=12∫00.5θk(1−θ)Q−1−kp(θ)dθ.P_{\mathrm{marg}}^{p}(s_{1:Q})\;=\;\tfrac{1}{2}\int_{0}^
 {0.5}\theta^{k}(1-\theta)^{Q-1-k}\,p(\theta)\,d\theta.

The integrand is a polynomial in θ\theta of degree at most Q−1Q-1. Expanding,

 Pmargp(s1:Q)=12∑j=0Q−1βj(k)mj(p),P_{\mathrm{marg}}^{p}(s_{1:Q})\;=\;\tfrac{1}{2}\sum_{j=0}^{Q-1}\
 beta_{j}(k)\,m_{j}(p),

where mj​(p)=∫θj​p​(θ)​𝑑θm_{j}(p)=\int\theta^{j}p(\theta)\,d\theta is the jj-th moment of pp and
the βj​(k)\beta_{j}(k) are combinatorial constants. Hence Pmargp(s1:Q)P_{\mathrm{marg}}^{p}(s_
{1:Q}) depends on pp only through its first Q−1Q-1 moments.

By the truncated Hausdorff moment problem (Schmüdgen, 2017), the set of probability measures on
(0,0.5)(0,0.5) matching any prescribed first Q−1Q-1 moments is convex with multiple distinct
extreme points. Choose two distinct priors p1,p2p_{1},p_{2} on (0,0.5)(0,0.5) that match in their
first Q−1Q-1 moments. Setting M1=(kernel,p1)M_{1}=(\text{kernel},p_{1}) and M2=(kernel,p2)M_{2}=(\
text{kernel},p_{2}), both lie in ℳ\mathcal{M}, and by Step 2,

 𝔼M1​[rt]=𝔼M2​[rt].\mathbb{E}^{M_{1}}[r_{t}]\;=\;\mathbb{E}^{M_{2}}[r_{t}].

Step 4: Expected BIG differs.

Decompose

 𝔼M​[I⁡(st;θ∣ht)]=HmargM​(st∣ht)−𝔼p​[H⁡(st∣st−1,θ)].\mathbb{E}^{M}\bigl[I(s_{t};\theta\mid h_{t})\
 bigr]\;=\;\Ent^{M}_{\mathrm{marg}}(s_{t}\mid h_{t})\;-\;\mathbb{E}_{p}\bigl[\Ent(s_{t}\mid s_
 {t-1},\theta)\bigr].

The marginal entropy HmargM⁡(st∣ht)\Ent^{M}_{\mathrm{marg}}(s_{t}\mid h_{t}) is identical for M1M_
{1} and M2M_{2} (Step 3, marginals match). The aleatoric term is

 𝔼p​[H⁡(st∣st−1,θ)]=𝔼p​[Hbin⁡(θ)],\mathbb{E}_{p}\bigl[\Ent(s_{t}\mid s_{t-1},\theta)\bigr]\;=\;\
 mathbb{E}_{p}\bigl[\Ent_{\mathrm{bin}}(\theta)\bigr],

where Hbin⁡(θ)=−θ​log⁡θ−(1−θ)​log⁡(1−θ)\Ent_{\mathrm{bin}}(\theta)=-\theta\log\theta-(1-\theta)\log
(1-\theta) is the binary entropy function. Since Hbin\Ent_{\mathrm{bin}} is not a polynomial, its
expectation under pp is not determined by any finite number of moments of pp. By the truncated
moment problem, we may choose p1,p2p_{1},p_{2} matching in the first Q−1Q-1 moments but with 𝔼p1​
[Hbin⁡(θ)]≠𝔼p2​[Hbin⁡(θ)]\mathbb{E}_{p_{1}}[\Ent_{\mathrm{bin}}(\theta)]\neq\mathbb{E}_{p_{2}}[\
Ent_{\mathrm{bin}}(\theta)]. Therefore 𝔼M1​[I⁡(st;θ∣ht)]≠𝔼M2​[I⁡(st;θ∣ht)]\mathbb{E}^{M_{1}}\bigl[I
(s_{t};\theta\mid h_{t})\bigr]\neq\mathbb{E}^{M_{2}}\bigl[I(s_{t};\theta\mid h_{t})\bigr].

Conclusion.

We have 𝔼M1​[rt]=𝔼M2​[rt]\mathbb{E}^{M_{1}}[r_{t}]=\mathbb{E}^{M_{2}}[r_{t}] but 𝔼M1​[I⁡(st;θ∣ht)]
≠𝔼M2​[I⁡(st;θ∣ht)]\mathbb{E}^{M_{1}}\bigl[I(s_{t};\theta\mid h_{t})\bigr]\neq\mathbb{E}^{M_{2}}\
bigl[I(s_{t};\theta\mid h_{t})\bigr]. The per-history identity 𝔼M​[rt∣ht]=IM​(st;θ∣ht)\mathbb{E}^
{M}[r_{t}\mid h_{t}]=I^{M}(s_{t};\theta\mid h_{t}) implies matched unconditional expectations, so
it must fail on at least one of M1,M2M_{1},M_{2}. Since the construction works for any rt∈ℱr_{t}\in
\mathcal{F}, the theorem follows. ∎

C.3 Proof of Theorem 2

See 2

Proof.

𝔼⁡[−log⁡ρ⁡(st∣ht)∣ht]=H⁡(st∣ht)\mathbb{E}[-\log\rho(s_{t}\mid h_{t})\mid h_{t}]=\Ent(s_{t}\mid h_
{t}). By the conditioning-information identity, H⁡(st∣ht)=H⁡(st∣ht,θ)+I⁡(st;θ∣ht)\Ent(s_{t}\mid h_
{t})=\Ent(s_{t}\mid h_{t},\theta)+I(s_{t};\theta\mid h_{t}). Under (ii), ρ\rho’s θ\
theta-conditional predictive equals the true kernel, so H⁡(st∣ht,θ)=H⁡(st∣st−1,at−1,θ)\Ent(s_{t}\
mid h_{t},\theta)=\Ent(s_{t}\mid s_{t-1},a_{t-1},\theta). ∎

C.4 Proof of Theorem 3

See 3

Proof.

By Lemma 4.1, ∑t′=t+1Tlogρ(st′∣ht′)=logρ(st+1:T∣ht,st,at:T−1)\sum_{t^{\prime}=t+1}^{T}\log\rho(s_{t
^{\prime}}\mid h_{t^{\prime}})=\log\rho(s_{t+1:T}\mid h_{t},s_{t},a_{t:T-1}) and ∑t′=t+1Tlogρ
(st′∣ht′∖t)=logρ(st+1:T∣ht,at:T−1)\sum_{t^{\prime}=t+1}^{T}\log\rho(s_{t^{\prime}}\mid h_{t^{\
prime}\setminus t})=\log\rho(s_{t+1:T}\mid h_{t},a_{t:T-1}). Subtracting, rtsum=pmi(st;
st+1:T∣ht,at:T−1)r^{\mathrm{sum}}_{t}=\pmi(s_{t};s_{t+1:T}\mid h_{t},a_{t:T-1}). Take expectation:
𝔼[rtsum]=I(st;st+1:T∣ht,at:T−1)\mathbb{E}[r^{\mathrm{sum}}_{t}]=I(s_{t};s_{t+1:T}\mid h_{t},a_
{t:T-1}).

Applying the chain rule against θ\theta:

 I(st;st+1:T∣ht,at:T−1)=\displaystyle I(s_{t};  I(st;θ∣ht,at:T−1)\displaystyle I(s_{t};\theta\mid
 s_{t+1:T}\mid h_{t},a_{t:T-1})=\;              h_{t},a_{t:T-1})
 −\displaystyle-\;                              I(st;θ∣ht,at:T−1,st+1:T)\displaystyle I(s_{t};\
                                                theta\mid h_{t},a_{t:T-1},s_{t+1:T})
 +\displaystyle+\;                              I(st;st+1:T∣θ,ht,at:T−1).\displaystyle I(s_{t};s_
                                                {t+1:T}\mid\theta,h_{t},a_{t:T-1}).

Future actions do not update the θ\theta-posterior, so I(st;θ∣ht,at:T−1)=I(st;θ∣ht)I(s_{t};\theta\
mid h_{t},a_{t:T-1})=I(s_{t};\theta\mid h_{t}). By Markov given θ\theta, st⟂st+2:T∣st+1,θs_{t}\perp
s_{t+2:T}\mid s_{t+1},\theta, and so I(st;st+1:T∣θ,ht,at:T−1)=I(st;st+1∣θ,ht,at)I(s_{t};s_{t+1:T}\
mid\theta,h_{t},a_{t:T-1})=I(s_{t};s_{t+1}\mid\theta,h_{t},a_{t}) after dropping dd-separated
future actions. ∎

Vanishing residual term.

Under assumption (iv), p(θ∣ht,at:T−1,st+1:T)→δθ∗p(\theta\mid h_{t},a_{t:T-1},s_{t+1:T})\to\delta_{\
theta^{*}} in probability as T→∞T\to\infty; further conditioning on sts_{t} cannot move a delta, so
the residual term vanishes. ∎

C.5 The Log-Ratio Subclass and Its Chain-Rule Decomposition

The proof of Theorem 1 settles the impossibility for the entire class ℱ\mathcal{F} via the
unconditional reduction. A complementary structural perspective is available for a specific
sub-class of ℱ\mathcal{F} — log-ratio rewards — whose expected value reduces to an
observation-space mutual information rather than to an entropy. This subsection isolates that
sub-class and gives the chain-rule decomposition that underlies the named confound terms in rsumr^
{\mathrm{sum}}’s decomposition (Theorem 3) and in the rone​(K)r^{\mathrm{one}}(K) decomposition (
Section C.6).

The log-ratio subclass ℱLR\mathcal{F}_{\mathrm{LR}}.

For any linear combination of log likelihoods rt=∑ici​log⁡ρ⁡(Xi∣Yi)∈ℱr_{t}=\sum_{i}c_{i}\log\rho(X_
{i}\mid Y_{i})\in\mathcal{F}, taking expectation under ρ∗\rho^{*} gives

 𝔼[rt]=−∑iciH(Xi∣Yi)=−∑iciH(Xi)+∑iciI(Xi;Yi).\mathbb{E}[r_{t}]\;=\;-\sum_{i}c_{i}\Ent(X_{i}\mid Y_
 {i})\;=\;-\sum_{i}c_{i}\Ent(X_{i})\;+\;\sum_{i}c_{i}\,I(X_{i};Y_{i}).

The marginal-entropy contribution ∑ici​H⁡(Xi)\sum_{i}c_{i}\Ent(X_{i}) vanishes identically iff, for
each distinct random variable XX appearing among the XiX_{i}’s, the coefficients on terms with that
XX sum to zero. Rewards meeting this constraint can be written as a sum of matched log-ratios

 rt=∑kαk​[log⁡ρ⁡(Xk∣Yk+)−log⁡ρ⁡(Xk∣Yk−)].r_{t}\;=\;\sum_{k}\alpha_{k}\bigl[\log\rho(X_{k}\mid Y_
 {k}^{+})-\log\rho(X_{k}\mid Y_{k}^{-})\bigr].

We denote this sub-class ℱLR\mathcal{F}_{\mathrm{LR}}. Membership:

  • •
   
    rtsum∈ℱLRr^{\mathrm{sum}}_{t}\in\mathcal{F}_{\mathrm{LR}}: each summand is a paired log-ratio
    with Xk=st′X_{k}=s_{t^{\prime}} for the same future state.

  • •
   
    rtone​(K)∈ℱLRr^{\mathrm{one}}_{t}(K)\in\mathcal{F}_{\mathrm{LR}}: a single paired log-ratio
    with X=st+KX=s_{t+K}.

  • •
   
    rtsur∉ℱLRr^{\mathrm{sur}}_{t}\notin\mathcal{F}_{\mathrm{LR}}: its single unpaired term −log⁡ρ⁡
    (st∣ht)-\log\rho(s_{t}\mid h_{t}) has expectation equal to the conditional entropy H⁡(st∣ht)\
    Ent(s_{t}\mid h_{t}), with the marginal entropy of sts_{t} surviving.

Chain-rule decomposition.

For rt∈ℱLRr_{t}\in\mathcal{F}_{\mathrm{LR}}, the expected reward reduces to a signed sum of
observation-space conditional MIs:

 𝔼⁡[rt]=∑jαj​I​(Aj;Bj∣Cj),\mathbb{E}[r_{t}]\;=\;\sum_{j}\alpha_{j}\,I(A_{j};B_{j}\mid C_{j}),

with Aj,Bj,CjA_{j},B_{j},C_{j} subsets of the trajectory and θ\theta absent from all arguments
(since ρ\rho marginalizes over it). Each term decomposes via the chain rule against θ\theta:

 I⁡(Aj;Bj∣Cj)=I⁡(Aj;θ∣Cj)⏟signal-like−I(Aj;θ∣Cj,Bj)⏟residual+I(Aj;Bj∣θ,Cj)⏟abductive.I(A_{j};B_{j}
 \mid C_{j})\;=\;\underbrace{I(A_{j};\theta\mid C_{j})}_{\textnormal{signal-like}}\;-\;\underbrace
 {I(A_{j};\theta\mid C_{j},B_{j})}_{\textnormal{residual}}\;+\;\underbrace{I(A_{j};B_{j}\mid\
 theta,C_{j})}_{\textnormal{abductive}}.

Reading the three terms.

  • •
   
    Signal-like terms quantify how the observation subsets AjA_{j} inform θ\theta given the
    conditioning CjC_{j}.

  • •
   
    Residual terms measure the θ\theta-information that AjA_{j} retains after BjB_{j} is also
    observed.

  • •
   
    Abductive terms capture dependencies between observation subsets that operate through the
    kernel’s dynamics rather than through their shared dependence on θ\theta.

This decomposition is exactly what produces the named offsets in Theorem 3 (one-step abductive and
residual for rsumr^{\mathrm{sum}}) and in Theorem 5 (KK-step abductive and residual for rone​(K)r^
{\mathrm{one}}(K)). It does not extend to rewards outside ℱLR\mathcal{F}_{\mathrm{LR}}: rsurr^{\
mathrm{sur}} in particular requires its own analysis via the entropy chain rule H⁡(st∣ht)=H⁡
(st∣ht,θ)+I⁡(st;θ∣ht)\Ent(s_{t}\mid h_{t})=\Ent(s_{t}\mid h_{t},\theta)+I(s_{t};\theta\mid h_{t}),
yielding the BIG-plus-aleatoric form of Theorem 2.

Relation to Theorem 1.

For log-ratio rewards, the chain-rule decomposition gives a useful diagnostic of where the bias
relative to BIG comes from: typically a non-vanishing abductive or a non-vanishing residual. It is
not, however, the engine of Theorem 1. The proof above (Section C.2) operates at the level of
unconditional expectations and works for the entire class ℱ\mathcal{F} uniformly, including
non-log-ratio rewards, without invoking the chain-rule decomposition.

C.6 NDIGO reward Decomposition

The NDIGO reward of Azar et al. (2019) is the single-summand analogue of rsumr^{\mathrm{sum}} at
horizon KK. We thus refer to it as rone​(K)r^{\mathrm{one}}(K), and define in the ρ\rho setting as:

 rtone(K)≔logρ(st+K∣ht,st,at:t+K−1)−logρ(st+K∣ht,at:t+K−1).r^{\mathrm{one}}_{t}(K)\;\coloneqq\;\
 log\rho(s_{t+K}\mid h_{t},s_{t},a_{t:t+K-1})-\log\rho(s_{t+K}\mid h_{t},a_{t:t+K-1}).

5Decomposition of rone​(K)r^{\mathrm{one}}(K).

Under assumptions (i)–(iii) and (v),

 𝔼[rtone(K)∣ht,at:t+K−1]=\displaystyle\mathbb{E}[r^{\ I⁡(st;θ∣ht)\displaystyle I(s_{t};\theta\mid
 mathrm{one}}_{t}(K)\mid h_{t},a_{t:t+K-1}]\;=\;\;    h_{t})
 +\displaystyle+\;                                    I(st;st+K∣θ,ht,at:t+K−1)\displaystyle I(s_
                                                      {t};s_{t+K}\mid\theta,h_{t},a_{t:t+K-1})
 −\displaystyle-\;                                    I(st;θ∣ht,at:t+K−1,st+K).\displaystyle I(s_
                                                      {t};\theta\mid h_{t},a_{t:t+K-1},s_{t+K}).

Proof.

By assumption (v), ρ(st∣ht,at:t+K−1)=ρ(st∣ht)\rho(s_{t}\mid h_{t},a_{t:t+K-1})=\rho(s_{t}\mid h_
{t}), hence rtone(K)=pmi(st;st+K∣ht,at:t+K−1)r^{\mathrm{one}}_{t}(K)=\pmi(s_{t};s_{t+K}\mid h_
{t},a_{t:t+K-1}) (where pmi\pmi is point-wise mutual information). Take expectation and apply the
chain rule against θ\theta as in the proof of Theorem 3. ∎

At finite KK the residual I(st;θ∣ht,at:t+K−1,st+K)I(s_{t};\theta\mid h_{t},a_{t:t+K-1},s_{t+K})
does not vanish: a single KK-step-ahead observation does not identify θ\theta. rone​(K)r^{\mathrm
{one}}(K) is therefore doubly biased relative to I⁡(st;θ∣ht)I(s_{t};\theta\mid h_{t}) at finite KK
(abductive plus persistent residual), and under BED structure reduces to I⁡(st;θ∣ht)I(s_{t};\theta\
mid h_{t}) minus a residual. In the limit K→T−tK\to T-t, rone​(K)r^{\mathrm{one}}(K) approaches a
single-summand analogue of rsumr^{\mathrm{sum}}.

C.7 Beyond Finite Touch Horizon: The Gap Reward

The decomposition theorems above place rsumr^{\mathrm{sum}} and rone​(K)r^{\mathrm{one}}(K) at
opposite structural extremes within the log-ratio subclass:

  • •
   
    rtsumr^{\mathrm{sum}}_{t} queries the entire future block st+1:Ts_{t+1:T} with no gap. Its
    epistemic residual vanishes as T→∞T\to\infty (the block contains infinite data, identifying θ\
    theta), but its one-step abductive I(st;st+1∣θ,ht,at)I(s_{t};s_{t+1}\mid\theta,h_{t},a_{t})
    persists — the block starts immediately at t+1t+1, fully coupled to sts_{t} through the Markov
    kernel.

  • •
   
    rtone​(K)r^{\mathrm{one}}_{t}(K) queries a single state st+Ks_{t+K} at gap KK. For K→∞K\to\
    infty in a mixing chain, the abductive I(st;st+K∣θ,ht,at:t+K−1)I(s_{t};s_{t+K}\mid\theta,h_
    {t},a_{t:t+K-1}) vanishes; but the epistemic residual I(st;θ∣ht,at:t+K−1,st+K)I(s_{t};\theta\
    mid h_{t},a_{t:t+K-1},s_{t+K}) persists — a single state cannot identify θ\theta.

rsumr^{\mathrm{sum}} controls the residual; roner^{\mathrm{one}} controls the abductive. Each
pushes one of two independent structural dials. This invites a unification: query a block that
simultaneously starts at a gap KK and extends to the trajectory horizon TT.

2Gap reward.

For 1≤K≤T−t1\leq K\leq T-t,

 rtgap(K)≔logρ(st+K:T∣ht,st,at:T−1)−logρ(st+K:T∣ht,at:T−1).r^{\mathrm{gap}}_{t}(K)\;\coloneqq\;\
 log\rho\bigl(s_{t+K:T}\mid h_{t},s_{t},a_{t:T-1}\bigr)-\log\rho\bigl(s_{t+K:T}\mid h_{t},a_
 {t:T-1}\bigr).

The two limits of Definition 2 recover the rewards above. At K=1K=1, the conditioning block extends
from t+1t+1 to TT and Lemma 4.1 together with assumption (iii) yields rtgap​(1)=rtsumr^{\mathrm
{gap}}_{t}(1)=r^{\mathrm{sum}}_{t} exactly. Restricting the block of rtgap​(K)r^{\mathrm{gap}}_{t}
(K) to its first state st+Ks_{t+K} recovers rtone​(K)r^{\mathrm{one}}_{t}(K). rgapr^{\mathrm{gap}}
is the two-parameter family that interpolates between them by independently choosing a gap KK and a
block size T−t−K+1T-t-K+1.

Beyond assumptions (i)–(v), the gap-reward analysis crucially uses assumption (vi): mixing of the
chain given θ\theta, ruling out absorbing states and other non-ergodic dynamics under which the
chain never forgets sts_{t}.

6Decomposition of rgapr^{\mathrm{gap}}.

Under (i)–(iii) and (v),

 𝔼[rtgap(K)∣ht,at:T−1]=\displaystyle\
 mathbb{E}[r^{\mathrm{gap}}_{t}(K)\mid  I⁡(st;θ∣ht)\displaystyle I(s_{t};\theta\mid h_{t})
 h_{t},a_{t:T-1}]\;=\;\;
                                        I(st;st+K:T∣θ,ht,at:T−1)⏟abductive at gap ​K\displaystyle\
 +\displaystyle+\;                      underbrace{I(s_{t};s_{t+K:T}\mid\theta,h_{t},a_{t:T-1})}_
                                        {\textnormal{abductive at gap }K}
                                        I(st;θ∣ht,at:T−1,st+K:T)⏟residual at block end ​T.\
 −\displaystyle-\;                      displaystyle\underbrace{I(s_{t};\theta\mid h_{t},a_
                                        {t:T-1},s_{t+K:T})}_{\textnormal{residual at block end }
                                        T}.

Both correction terms are non-negative.

Proof.

By Lemma 4.1, rtgap(K)=pmi(st;st+K:T∣ht,at:T−1)r^{\mathrm{gap}}_{t}(K)=\pmi(s_{t};s_{t+K:T}\mid h_
{t},a_{t:T-1}); by (v), ρ(st∣ht,at:T−1)=ρ(st∣ht)\rho(s_{t}\mid h_{t},a_{t:T-1})=\rho(s_{t}\mid h_
{t}). Take expectation and apply the chain rule against θ\theta as in the proof of Theorem 3:

 I(st;st+K:T∣ht,at:T−1)=\displaystyle I(s_{t};s_ I(st;θ∣ht,at:T−1)\displaystyle\;I(s_{t};\theta\
 {t+K:T}\mid h_{t},a_{t:T-1})\;=\;               mid h_{t},a_{t:T-1})
 −\displaystyle-\;                               I(st;θ∣ht,at:T−1,st+K:T)\displaystyle\;I(s_{t};\
                                                 theta\mid h_{t},a_{t:T-1},s_{t+K:T})
 +\displaystyle+\;                               I(st;st+K:T∣θ,ht,at:T−1).\displaystyle\;I(s_{t};
                                                 s_{t+K:T}\mid\theta,h_{t},a_{t:T-1}).

By (v), I(st;θ∣ht,at:T−1)=I(st;θ∣ht)I(s_{t};\theta\mid h_{t},a_{t:T-1})=I(s_{t};\theta\mid h_{t}).
∎

Theorem 6 subsumes Theorem 3 (K=1K=1) and reveals that the abductive of rgapr^{\mathrm{gap}} is a
KK-shifted analogue of the one-step abductive of rsumr^{\mathrm{sum}}. The decomposition
immediately yields a positive counterpart to the impossibility theorem in the iterated infinite
limit.

6.1rgapr^{\mathrm{gap}} recovers BIG in the double limit.

Under (i)–(vi),

 limK→∞limT→∞𝔼[rtgap(K)∣ht,at:T−1]=I(st;θ∣ht).\lim_{K\to\infty}\;\lim_{T\to\infty}\;\mathbb{E}[r^
 {\mathrm{gap}}_{t}(K)\mid h_{t},a_{t:T-1}]\;=\;I(s_{t};\theta\mid h_{t}).

Proof.

Inner limit (T→∞T\to\infty, KK fixed). As T→∞T\to\infty the conditioning block st+K:Ts_{t+K:T}
grows without bound. Under (iv), p(θ∣ht,at:T−1,st+K:T)→δθ∗p(\theta\mid h_{t},a_{t:T-1},s_{t+K:T})\
to\delta_{\theta^{*}} in probability, so the residual I(st;θ∣ht,at:T−1,st+K:T)→0I(s_{t};\theta\mid
h_{t},a_{t:T-1},s_{t+K:T})\to 0.

Outer limit (K→∞K\to\infty). With the residual at zero, only the abductive remains. By (i), the
dependence of st+K:Ts_{t+K:T} on sts_{t} given θ\theta funnels through st+Ks_{t+K}: I(st;
st+K:T∣θ,ht,at:T−1)=I(st;st+K∣θ,ht,at:t+K−1)I(s_{t};s_{t+K:T}\mid\theta,h_{t},a_{t:T-1})=I(s_{t};s_
{t+K}\mid\theta,h_{t},a_{t:t+K-1}) (data-processing along the post-st+Ks_{t+K} chain). By (vi),
this vanishes as K→∞K\to\infty. ∎

Remarks.

  • •
   
    Outside ℱ\mathcal{F}. The limit reward in Corollary 6.1 cannot be expressed as a function of
    finitely many likelihoods, hence not a member of ℱ\mathcal{F}. This is consistent with Theorem 
    1: at every finite T,KT,K, rtgap​(K)∈ℱLR⊂ℱr^{\mathrm{gap}}_{t}(K)\in\mathcal{F}_{\mathrm{LR}}\
    subset\mathcal{F} and inherits the bias of Theorem 6; only the asymptotic limit escapes ℱ\
    mathcal{F}.

  • •
   
    Marginalization burden. Computing rtgap​(K)r^{\mathrm{gap}}_{t}(K) requires ρ\rho to evaluate
    predictives on histories where sts_{t} is masked and the intermediate states st+1:t+K−1s_
    {t+1:t+K-1} are absent — effectively asking ρ\rho to marginalize over a gap of K−1K-1
    unobserved states. Assumption (iii) — that ρ\rho’s response to such queries is the correctly
    marginalized posterior-predictive — becomes increasingly strained as the gap grows; see Section
     B.3.

  • •
   
    Mixing failure. Assumption (vi) fails for environments with absorbing states or other
    non-ergodic structure given θ\theta. In such cases the abductive does not decay with KK and the
    double limit need not yield BIG.

C.8 Necessity boundlessly growing gaps for Log-Ratio Rewards

Corollary 6.1 shows that an infinite gap together with an infinite block is sufficient to recover
BIG within a log-ratio reward. We now prove the converse: the same double-limit geometry is also
necessary. Concretely, any countable linear combination of matched log-ratios that universally
identifies BIG must place all of its weight at infinite gap, with each non-trivial block of
infinite size.

Let ℱLR,T\mathcal{F}_{\mathrm{LR},T} (cf. Section C.5) be the set of estimators which are linear
combinations of log ratios involving subsets of observations up to a horizon TT, i.e.,

 ℱLR,T={∑Z⊆{t+1,…,T}cZ[logρ(sZ∣ht,st,at:T)−logρ(sZ∣ht,at:T)]:cZ∈ℝ,∀Z⊆{t+1,…,T}}.\displaystyle\
 mathcal{F}_{\mathrm{LR},T}=\left\{\sum_{Z\subseteq\{t+1,\ldots,T\}}c_{Z}\Bigl[\log\rho(s_{Z}\mid
 h_{t},s_{t},a_{t:T})-\log\rho(s_{Z}\mid h_{t},a_{t:T})\Bigr]:c_{Z}\in\mathbb{R}\,,\forall Z\
 subseteq\{t+1,\ldots,T\}\right\}\,.

The following theorem shows that for any sequence of estimators chosen from ℱLR,T\mathcal{F}_{\
mathrm{LR},T} which is asymptotically equal to the Bayesian Information Gain I⁡(st;θ|ht)I(s_{t};\
theta|h_{t}) as T→∞T\to\infty in a universal fashion over all BAMDPs, this sequence must involve an
unboundedly growing gap akin to the one discussed in Corollary 6.1.

7Necessity of a growing gap.

Let ℳ\mathcal{M} be the class of BAMDPs satisfying (i)–(vi) whose transition kernels are
non-deterministic and not independent across time given θ\theta. Suppose that (Rt,T)T(R_{t,T})_{T}
is a sequence of estimators such that

  • •
   
    Rt,T∈ℱLR,TR_{t,T}\in\mathcal{F}_{\mathrm{LR},T} for all TT, i.e.,

     Rt,T=∑Z⊆{t+1,…,T}cZ,T[logρ(sZ∣ht,st,at:T)−logρ(sZ∣ht,at:T)],R_{t,T}=\sum_{Z\subseteq\{t+1,\
     ldots,T\}}c_{Z,T}\Bigl[\log\rho(s_{Z}\mid h_{t},s_{t},a_{t:T})-\log\rho(s_{Z}\mid h_{t},a_
     {t:T})\Bigr]\,,

  • •
   
    limT→∞𝔼M[Rt,T∣ht,at:T]=I(st;θ∣ht)\lim_{T\to\infty}\mathbb{E}^{M}[R_{t,T}\mid h_{t},a_{t:T}]=I
    (s_{t};\theta\mid h_{t}) for every M∈ℳM\in\mathcal{M} and every ht,at:∞h_{t},a_{t:\infty}, and

  • •
   
    there exist constants C>0C>0 and B≥1B\geq 1 such that |Wk,T|≤∑kZ=k|cZ,T|≤C​Bk|W_{k,T}|\leq\sum_
    {k_{Z}=k}|c_{Z,T}|\leq CB^{k} for all kk and TT.^1^1 1 It is worth noting that this is a mild
    assumption. In practice, numerically stable estimators must satisfy this.

If we define for each block of observations Z⊂{t+1,…}Z\subset\{t+1,\ldots\} the gap kZ≔min⁡
{k≥1:t+k∈Z}k_{Z}\coloneqq\min\{k\geq 1:t+k\in Z\} and write

 Wk,T≔∑Z⊆{t+1​…,T}:kZ=kcZ,T,W_{k,T}\coloneqq\sum_{\begin{subarray}{c}Z\subseteq\{t+1\,\ldots,T\}:\
 \ k_{Z}=k\end{subarray}}c_{Z,T}\,,

then

 limT→∞Wk,T=0.\lim_{T\to\infty}W_{k,T}=0\,.

In other words, as T→∞T\to\infty, almost all the weight ∑ZcZ,T\sum_{Z}c_{Z,T} “will become
concentrated on blocks of infinite gap”.

Proof.

Step 1: Chain-Rule Decomposition.
By definition, each matched log-ratio term in ℱLR,T\mathcal{F}_{\mathrm{LR},T} evaluates to a
pointwise mutual information. Taking the expectation of Rt,TR_{t,T} and applying the chain rule
against the environment parameter θ\theta, we obtain:

 𝔼M[Rt,T∣ht,at:T]\displaystyle\   =∑ZcZ,TI(st;sZ∣ht,at:T)\displaystyle=\sum_{Z}c_{Z,T}I(s_{t};s_
 mathbb{E}^{M}[R_{t,T}\mid h_     {Z}\mid h_{t},a_{t:T})
 {t},a_{t:T}]
                                  =(∑ZcZ,T)​I​(st;θ∣ht)+∑ZcZ,T​AZ−∑ZcZ,T​EZ,\displaystyle=\Big(\
                                  sum_{Z}c_{Z,T}\Big)I(s_{t};\theta\mid h_{t})+\sum_{Z}c_{Z,T}A_
                                  {Z}-\sum_{Z}c_{Z,T}E_{Z}\,,

where AZ≔I(st;sZ∣θ,ht,at:T)A_{Z}\coloneqq I(s_{t};s_{Z}\mid\theta,h_{t},a_{t:T}) is the abductive
term and EZ≔I(st;θ∣ht,at:T,sZ)E_{Z}\coloneqq I(s_{t};\theta\mid h_{t},a_{t:T},s_{Z}) is the
epistemic residual.

Step 2: Isolating the Abductive Bias.
The abductive term AZA_{Z} captures dependencies generated purely by the known transition kernel,
relying on the prior p⁡(θ)p(\theta) only through the posterior p⁡(θ∣ht)p(\theta\mid h_{t}). We may
evaluate the limit on an environment M∈ℳM\in\mathcal{M} whose prior is a degenerate point mass at
some θ∗\theta^{*}. For such a prior, there is no uncertainty about the environment, so the true
Bayesian Information Gain I⁡(st;θ∣ht)=0I(s_{t};\theta\mid h_{t})=0 and all epistemic residuals EZ=
0E_{Z}=0 identically. The universal convergence hypothesis then reduces to:

 limT→∞∑ZcZ,T​AZ​(θ∗)=0for every ​θ∗.\lim_{T\to\infty}\sum_{Z}c_{Z,T}A_{Z}(\theta^{*})=0\quad\  (7)
 text{for every }\theta^{*}\,.

Step 3: The Markov Funnel.
Under assumption (i), conditional on the exact parameter θ∗\theta^{*}, the environment is
Markovian. By the data-processing inequality, the dependence of any block sZs_{Z} on the state sts_
{t} must funnel strictly through the chronologically earliest state in that block, st+kZs_{t+k_
{Z}}. Dropping the separated future actions, we have:

 AZ(θ∗)=I(st;st+kZ∣θ∗,ht,at:t+kZ−1)≔IkZ(θ∗).A_{Z}(\theta^{*})=I(s_{t};s_{t+k_{Z}}\mid\theta^{*},h_
 {t},a_{t:t+k_{Z}-1})\coloneqq I_{k_{Z}}(\theta^{*})\,.

Grouping the terms in equation 7 by their gap kZ=kk_{Z}=k, we rewrite the limit in terms of the gap
weights Wk,TW_{k,T}:

 limT→∞∑k=1TWk,T​Ik​(θ∗)=0for every ​θ∗.\lim_{T\to\infty}\sum_{k=1}^{T}W_{k,T}I_{k}(\theta^     (8)
 {*})=0\quad\text{for every }\theta^{*}\,.

Step 4: AR(1) Instantiation and Power Series Extraction.
To evaluate equation 8, we instantiate MM with the Gaussian AR(1) family parameterized by α∈(0,1)\
alpha\in(0,1), for which the mutual information across a gap kk is Ik​(α)=−12​log⁡(1−α2​k)I_{k}(\
alpha)=-\frac{1}{2}\log(1-\alpha^{2k}). Letting x=α2∈(0,1)x=\alpha^{2}\in(0,1), we define the
sequence of functions:

 FT​(x)≔∑k=1TWk,T​(−log⁡(1−xk)).F_{T}(x)\coloneqq\sum_{k=1}^{T}W_{k,T}\big(-\log(1-x^{k})\big)\,.

By hypothesis, limT→∞FT​(x)=0\lim_{T\to\infty}F_{T}(x)=0 for all x∈(0,1)x\in(0,1). We extend FT​(x)
F_{T}(x) to the complex plane as FT​(z)F_{T}(z), which is analytic on the open unit disk |z|<1|z|
<1. Expanding the principal branch of the logarithm in its Maclaurin series −log⁡(1−zk)=∑m=1∞zk​mm-
\log(1-z^{k})=\sum_{m=1}^{\infty}\frac{z^{km}}{m}, we express FT​(z)F_{T}(z) as:

 FT​(z)=∑k=1TWk,T​∑m=1∞zk​mm=∑n=1∞Vn,Tn​zn,F_{T}(z)=\sum_{k=1}^{T}W_{k,T}\sum_{m=1}^{\infty}\frac
 {z^{km}}{m}=\sum_{n=1}^{\infty}\frac{V_{n,T}}{n}z^{n}\,,

where we have grouped terms by n=k​mn=km and defined Vn,T≔∑d|n,d<Td​Wd,TV_{n,T}\coloneqq\sum_{d|n,
\;d<T}dW_{d,T}.

Now recall that the theorem statement assumes that there exist constants C>0C>0 and B≥1B\geq 1 such
that |Wk,T|≤∑kZ=k|cZ,T|≤C​Bk|W_{k,T}|\leq\sum_{k_{Z}=k}|c_{Z,T}|\leq CB^{k} for all kk and TT. For
zz strictly inside the unit disk, we may bound the logarithmic term using its Maclaurin series: |
−log⁡(1−zk)|≤∑m=1∞|z|k​m/m≤|z|k/(1−|z|)|-\log(1-z^{k})|\leq\sum_{m=1}^{\infty}|z|^{km}/m\leq|z|^{k}
/(1-|z|). Applying our exponential bound to the weights yields:

 |FT​(z)|≤∑k=1T|Wk,T|​|−log⁡(1−zk)|≤C1−|z|​∑k=1∞(B​|z|)k.|F_{T}(z)|\leq\sum_{k=1}^{T}|W_{k,T}|\,|-
 \log(1-z^{k})|\leq\frac{C}{1-|z|}\sum_{k=1}^{\infty}(B|z|)^{k}\,.

This geometric series converges absolutely for B​|z|<1B|z|<1. Thus, defining a bounding radius
R≔min⁡(1/2,1/B)R\coloneqq\min(1/2,1/B), the sequence of functions {FT​(z)}\{F_{T}(z)\} is uniformly
bounded by a finite constant on any closed ball B⁡(0,r)B(0,r) with r<Rr<R.

Because FT​(x)→0F_{T}(x)\to 0 pointwise on the real interval (0,R)(0,R), and this interval contains
an accumulation point strictly within B⁡(0,R)B(0,R), the Vitali-Porter Convergence Theorem (Schiff,
1993, Section 2.4) guarantees that FT​(z)F_{T}(z) converges uniformly to 00 on any strictly smaller
closed contour γ\gamma enclosing the origin. By Cauchy’s Integral Formula, the nn-th Maclaurin
coefficient is given by:

 Vn,Tn=12​π​i​∮γFT​(z)zn+1​𝑑z.\frac{V_{n,T}}{n}=\frac{1}{2\pi i}\oint_{\gamma}\frac{F_{T}(z)}{z^
 {n+1}}\mathrm{d}z\,.

Since FT​(z)F_{T}(z) converges uniformly to 00 on γ\gamma, we may pass the limit inside the
integral, yielding:

 limT→∞Vn,Tn=0⟹limT→∞Vn,T=0for all ​n≥1.\lim_{T\to\infty}\frac{V_{n,T}}{n}=0\implies\lim_{T\to\
 infty}V_{n,T}=0\quad\text{for all }n\geq 1\,.

Step 5: Strong Induction.
We prove limT→∞Wk,T=0\lim_{T\to\infty}W_{k,T}=0 by strong induction on kk. For the base case k=1k=
1, we have V1,T=W1,TV_{1,T}=W_{1,T} (since d<Td<T trivially for T>1T>1), and thus limT→∞W1,T=0\lim_
{T\to\infty}W_{1,T}=0. For the inductive step, assume that limT→∞Wd,T=0\lim_{T\to\infty}W_{d,T}=0
for all proper divisors d<nd<n. By definition, for T>nT>n:

 Vn,T=n​Wn,T+∑d|nd<nd​Wd,T.V_{n,T}=nW_{n,T}+\sum_{\begin{subarray}{c}d|n\\ d<n\end{subarray}}dW_
 {d,T}\,.

Taking the limit as T→∞T\to\infty:

 0=n​limT→∞Wn,T+∑d|nd<nd​(limT→∞Wd,T)⏟=0⟹limT→∞Wn,T=0.0=n\lim_{T\to\infty}W_{n,T}+\sum_{\begin
 {subarray}{c}d|n\\ d<n\end{subarray}}d\underbrace{\Big(\lim_{T\to\infty}W_{d,T}\Big)}_{=0}\
 implies\lim_{T\to\infty}W_{n,T}=0\,.

This holds for all integers n≥1n\geq 1, proving that all finite gap weights strictly vanish as T→∞T
\to\infty. Consequently, any estimator universally approaching BIG must concentrate its weight
strictly at an unbounded, infinite gap.

∎

Remark (relation to Theorem 1).

The earlier impossibility theorem covers ℱ\mathcal{F} at every finite touch horizon QQ: at finite
TT, no member of ℱ\mathcal{F} identifies BIG, even without log-ratio structure. Theorem 7 extends
the negative result into the limit: even allowing countable log-ratio combinations and asymptotic
TT, the only escape is a structural double limit in which both the gap and the block grow without
bound. As mentioned in the main text, this imposes severe implementation limitations since
predictive models ρ\rho are well suited for long sequence modeling (large TT) but less so for
marginalization over unobserved sequence blocks (large gaps).

C.9 Proof of Corollary 3.1

See 3.1

Proof.

Under BED structure, given θ\theta and the action sequence, st∼p(⋅∣at−1,θ)s_{t}\sim p(\cdot\mid a_
{t-1},\theta) and st+1∼p(⋅∣at,θ)s_{t+1}\sim p(\cdot\mid a_{t},\theta) are independent. Hence I(st;
st+1∣θ,ht,at)=0I(s_{t};s_{t+1}\mid\theta,h_{t},a_{t})=0, and the abductive term in Theorem 3 drops
out. ∎

C.10 Proof of Theorem 4

See 4

Proof.

Under BED, p⁡(st∣st−1,at−1,θ)=p⁡(st∣at−1,θ)p(s_{t}\mid s_{t-1},a_{t-1},\theta)=p(s_{t}\mid a_{t-1},
\theta), so the integral in Equation 4 simplifies:

 ∫p⁡(st∣at−1,θ)​p​(θ∣ht,st)​𝑑θ=𝔼p⁡(θ∣ht,st)​[p⁡(st∣at−1,θ)]=ρ⁡(st∣ht)​𝔼p⁡(θ∣ht,st)​[L⁡(θ)],\int p
 (s_{t}\mid a_{t-1},\theta)\,p(\theta\mid h_{t},s_{t})\,d\theta\;=\;\mathbb{E}_{p(\theta\mid h_
 {t},s_{t})}[p(s_{t}\mid a_{t-1},\theta)]\;=\;\rho(s_{t}\mid h_{t})\,\mathbb{E}_{p(\theta\mid h_
 {t},s_{t})}[L(\theta)],

where the last equality uses the definition L⁡(θ)=p⁡(st∣at−1,θ)/ρ⁡(st∣ht)L(\theta)=p(s_{t}\mid a_
{t-1},\theta)/\rho(s_{t}\mid h_{t}). Hence

 rtdl=log⁡𝔼p⁡(θ∣ht,st)​[L⁡(θ)].r^{\mathrm{dl}}_{t}\;=\;\log\mathbb{E}_{p(\theta\mid h_{t},s_{t})}
 [L(\theta)].

By Jensen’s inequality,

 rtdl=𝔼p⁡(θ∣ht,st)​[log⁡L⁡(θ)]+𝒥t,r^{\mathrm{dl}}_{t}\;=\;\mathbb{E}_{p(\theta\mid h_{t},s_{t})}[\
 log L(\theta)]+\mathcal{J}_{t},

with 𝒥t≥0\mathcal{J}_{t}\geq 0 the Jensen gap. Bayes’ rule gives L⁡(θ)=p⁡(θ∣ht,st)/p⁡(θ∣ht)L(\
theta)=p(\theta\mid h_{t},s_{t})/p(\theta\mid h_{t}), so 𝔼p⁡(θ∣ht,st)[logL(θ)]=DKL(p(θ∣ht,st)∥p
(θ∣ht))\mathbb{E}_{p(\theta\mid h_{t},s_{t})}[\log L(\theta)]=D_{\mathrm{KL}}\!\bigl(p(\theta\mid
h_{t},s_{t})\,\|\,p(\theta\mid h_{t})\bigr). Taking expectation over sts_{t}, the KL term
integrates to I⁡(st;θ∣ht)I(s_{t};\theta\mid h_{t}) (Bayesian-surprise identity). ∎

Remark.

Theorem 4 shows an important limitation of rdlr^{\mathrm{dl}}. While technically, rdlr^{\mathrm
{dl}} does recover BIG asymptotically, it does so as t→∞t\to\infty which also implies that BIG I⁡
(st;θ∣ht)I(s_{t};\theta\mid h_{t}) itself vanishes. This means that rdlr^{\mathrm{dl}} biases
vanish at the same time as the BIG signal itself does. In contrast, rsumr^{\mathrm{sum}} also
recovers BIG asymptotically, but it does so as T→∞T\to\infty, meaning that for long trajectories,
one can hope to recover minimally biased BIG signal for finite tt. However, the practical effect of
finite-time biases may or may not be consequential for a useful policy, depending on the
environment. Experiments in the main text illustrate this point.

Appendix D Implementing rdlr^{\mathrm{dl}} in General BAMDPs vs. BED

The reward rdlr^{\mathrm{dl}} defined in Equation 4 contains an integral term

 ∫p⁡(st∣st−1,at−1,θ)​p​(θ∣ht,st)​𝑑θ\int p(s_{t}\mid s_{t-1},a_{t-1},\theta)\,p(\theta\mid h_{t},s_
 {t})\,d\theta

that pairs the kernel at the trajectory input (st−1,at−1)(s_{t-1},a_{t-1}) with a posterior
conditioned on the subsequent observation sts_{t}. We explain here why this combination is
generically not implementable through ρ\rho’s standard predictive interface, and how the BED
restriction restores implementability via a counterfactual action commitment.

Why no ρ\rho-implementation in general BAMDPs.

A standard ρ\rho query ρ⁡(s′∣h)\rho(s^{\prime}\mid h) evaluates the kernel at the most recent (s,a)
(s,a) in the conditioning history hh, averaged under the posterior p⁡(θ∣h)p(\theta\mid h) that this
same history induces. The integrand above departs from this template in a structural way: the
kernel input is (st−1,at−1)(s_{t-1},a_{t-1}) from time t−1t-1, while the posterior is p⁡(θ∣ht,st)p
(\theta\mid h_{t},s_{t}) from after sts_{t} has been observed. In a general MDP, where the kernel
genuinely depends on the chain-position state, no rearrangement of conditioning subsets in ρ\rho’s
query interface produces this combination — evaluating ρ\rho at (st−1,at−1)(s_{t-1},a_{t-1}) gives
the posterior only up to time t−1t-1, and evaluating ρ\rho after sts_{t} shifts the kernel input as
well. Implementing rdlr^{\mathrm{dl}} in general BAMDPs therefore requires explicit Bayesian-update
machinery beyond ρ\rho’s forward-pass interface.

BED counterfactual identity.

In BED the transition kernel has no st−1s_{t-1} dependence, p⁡(st∣st−1,at−1,θ)=p⁡(st∣at−1,θ)p(s_{t}
\mid s_{t-1},a_{t-1},\theta)=p(s_{t}\mid a_{t-1},\theta), so the integrand becomes

 ∫p⁡(st∣at−1,θ)​p​(θ∣ht,st)​𝑑θ=ρ⁡(st+1=st|ht,st,at=at−1),\int p(s_{t}\mid a_{t-1},\theta)\,p(\
 theta\mid h_{t},s_{t})\,d\theta\;=\;\rho\bigl(s_{t+1}=s_{t}\,\big|\,h_{t},s_{t},\,a_{t}=a_{t-1}\
 bigr),

recovering Equation 6. The right-hand side is a standard ρ\rho query: condition on the history
(ht,st)(h_{t},s_{t}), commit to the action at=at−1a_{t}=a_{t-1} (a counterfactual on the action
choice, valid because actions are policy-generated and thus interventional), and read off the
predictive probability of the next outcome equaling the observed sts_{t}. This makes rdlr^{\mathrm
{dl}} implementable in BED at the cost of one hypothetical rollout step in ρ\rho’s context.

The key takeaway: BED collapses the chain-time mismatch by removing the st−1s_{t-1} dependence in
the kernel — an obstruction that no rearrangement of ρ\rho’s queries can resolve in a general MDP.
It could be possible to address this issue with a modified sequence model architecture that allows
multiple channels for the same sequence location, together with a gating mechanism to implement a
form of counterfactual “superposition”. This remains a non-trivial design.

Appendix E Experimental Details

E.1 Gaussian Processes

E.1.1 Environment

The Gaussian Process (GP) environment presents the agent with an unknown function f:ℝ2→ℝf:\mathbb
{R}^{2}\to\mathbb{R} that must be explored through sequential point queries. At the start of each
episode, a function ff is sampled from a Gaussian process prior f∼𝒢​𝒫​(0,k)f\sim\mathcal{GP}(0,k),
where kk is the rational quadratic kernel:

 k⁡(𝐱,𝐱′)=σ2​(1+‖𝐱−𝐱′‖22​α​ℓ2)−α,k(\mathbf{x},\mathbf{x}^{\prime})=\sigma^{2}\left(1+\frac{\|\  (9)
 mathbf{x}-\mathbf{x}^{\prime}\|^{2}}{2\alpha\ell^{2}}\right)^{-\alpha},

with α=1\alpha=1, lengthscale ℓ=4.0\ell=4.0, and σ2=1\sigma^{2}=1.

Since a GP sample is an infinite-dimensional function, we represent it in practice via finite-grid
sampling: we place R=10R=10 equally spaced points along each of the dd input dimensions in
[−xmax,,xmax][-x_{\max},,x_{\max}] and take their Cartesian product, yielding a grid 𝐗g​r​i​d\
mathbf{X}_{grid} of N=RdN=R^{d} locations. To draw a sample 𝐲grid\mathbf{y}_{\mathrm{grid}} from
this multivariate Gaussian we use the standard Cholesky method. Function values at arbitrary query
points are then obtained via GP posterior interpolation. We use xmax=10x_{\max}=10.

When the agent queries location 𝐱t\mathbf{x}_{t}, it observes:

 yt=f⁡(𝐱t)+ϵt,ϵt∼𝒩⁡(0,σnoise2​(𝐱t)),y_{t}=f(\mathbf{x}_{t})+\epsilon_{t},\qquad\epsilon_{t}\   (10)
 sim\mathcal{N}\bigl(0,\sigma_{\text{noise}}^{2}(\mathbf{x}_{t})\bigr),

where the noise variance σnoise2​(𝐱)\sigma_{\text{noise}}^{2}(\mathbf{x}) is spatially varying
according to a fixed checkerboard noise map. 𝐗g​r​i​d\mathbf{X}_{grid} is partitioned into an 8×88\
times 8 grid of cells; Tiles alternate between high-noise (σnoise2=5\sigma_{\mathrm{noise}}^{2}=5)
and low-noise (σnoise2≈0\sigma_{\mathrm{noise}}^{2}\approx 0) regions. This creates a
heteroscedastic noise landscape that an effective exploration policy must learn to navigate.

E.1.2 Bayesian Oracle World Model

The Bayesian oracle leverages the closed-form GP posterior. Given a history of observations
{(𝐱i,yi)}i=0t−1\{(\mathbf{x}_{i},y_{i})\}_{i=0}^{t-1}, the posterior predictive distribution for a
new query 𝐱t\mathbf{x}_{t} is Gaussian:

 p⁡(yt∣𝐱t,ht)=𝒩⁡(yt,μt,σt2),p(y_{t}\mid\mathbf{x}_{t},h_{t})=\mathcal{N}\bigl(y_{t};\,\mu_     (11)
 {t},\sigma_{t}^{2}\bigr),

where the posterior mean and variance are given by the standard GP regression formulae:

 μt\displaystyle\ =𝐤∗⊤​(K+Σnoise)−1​𝐲<t,\displaystyle=\mathbf{k}_{*}^{\top}(K+\Sigma_{\text    (12)
 mu_{t}           {noise}})^{-1}\mathbf{y}_{<t},
 σt2\displaystyle =k⁡(𝐱t,𝐱t)−𝐤∗⊤​(K+Σnoise)−1​𝐤∗,\displaystyle=k(\mathbf{x}_{t},\mathbf{x}_    (13)
 \sigma_{t}^{2}   {t})-\mathbf{k}_{*}^{\top}(K+\Sigma_{\text{noise}})^{-1}\mathbf{k}_{*},

with 𝐤∗=[k⁡(𝐱i,𝐱t)]i=0t−1\mathbf{k}_{*}=[k(\mathbf{x}_{i},\mathbf{x}_{t})]_{i=0}^{t-1}, Ki​j=k⁡
(𝐱i,𝐱j)K_{ij}=k(\mathbf{x}_{i},\mathbf{x}_{j}), and Σnoise=diag⁡(σnoise2​(𝐱0),…,σnoise2​(𝐱t−1))\
Sigma_{\text{noise}}=\operatorname{diag}(\sigma_{\text{noise}}^{2}(\mathbf{x}_{0}),\ldots,\sigma_{\
text{noise}}^{2}(\mathbf{x}_{t-1})).

For the predictive distribution used by the surprisal-based rewards, the predictive variance
additionally includes the noise at the query point:

 σ~t2=σt2+σnoise2​(𝐱t).\tilde{\sigma}_{t}^{2}=\sigma_{t}^{2}+\sigma_{\text{noise}}^{2}(\       (14)
 mathbf{x}_{t}).

Per-step Surprisal

The observation surprisal is the negative log-likelihood under the predictive Gaussian:

 −log⁡p⁡(yt∣𝐱t,ht)=12​log⁡(2​π)+12​log⁡σ~t2+(yt−μt)22​σ~t2.-\log p(y_{t}\mid\mathbf{x}_{t},h_
 {t})=\frac{1}{2}\log(2\pi)+\frac{1}{2}\log\tilde{\sigma}_{t}^{2}+\frac{(y_{t}-\mu_{t})^{2}}   (15)
 {2\tilde{\sigma}_{t}^{2}}.

Per-step Information Gain (Bayesian Surprise)

The information gain reward uses the KL divergence between the GP posterior and the predictive
observation distribution. In exact form:

 Gt=12​log⁡(1+σt2σnoise2​(𝐱t))−σt22​(σt2+σnoise2​(𝐱t))+σt2⋅(yt−μt)22​(σt2+σnoise2​(𝐱t))2,\
 text{G}_{t}=\frac{1}{2}\log\!\left(1+\frac{\sigma_{t}^{2}}{\sigma_{\text{noise}}^{2}(\mathbf
 {x}_{t})}\right)-\frac{\sigma_{t}^{2}}{2(\sigma_{t}^{2}+\sigma_{\text{noise}}^{2}(\mathbf{x}  (16)
 _{t}))}+\frac{\sigma_{t}^{2}\cdot(y_{t}-\mu_{t})^{2}}{2(\sigma_{t}^{2}+\sigma_{\text{noise}}
 ^{2}(\mathbf{x}_{t}))^{2}},

where σt2\sigma_{t}^{2} is the latent GP posterior variance (without observation noise).

E.1.3 In-Context PFN World Model

Data-Generating Process

The PFN is trained to predict the next observation yty_{t} in-context from the history of observed
pairs. For each training example, a function ff is sampled from the same GP prior as the
environment (rational quadratic kernel with α=1\alpha=1, ℓ=4.0\ell=4.0, spatially-varying noise),
and Ttrain=100T_{\text{train}}=100 random query locations are drawn uniformly from [−10,10]2
[-10,10]^{2}.

Model Architecture

The PFN uses a causal Transformer decoder. Each past observation (xj,yj)(x_{j},y_{j}) is embedded
by summing a linear projection of xjx_{j} and a linear projection of yjy_{j} into a single “pair”
token. This pair token is interleaved with an isolated embedding of the next query xj+1x_{j+1},
yielding a sequence of length 2​(t−1)2(t{-}1). The causal Transformer decoder has model dimension d
=512d=512, MLP hidden dimension F=1024F=1024, N=12N=12 layers, and H=8H=8 attention heads with head
dimension d/H=64d/H=64. The model uses LayerNorm and GELU activations. The Transformer output at
each query-token position (odd positions) is projected via a learned linear layer to produce a
22-dimensional output (μ^t,σ^t)(\hat{\mu}_{t},\hat{\sigma}_{t}), representing a predicted mean and
log-standard-deviation.

Training

The training objective is the mean negative log-likelihood of the observed yy values under the
predicted Gaussian, with a variance-weighted loss to encourage better calibration (Seitzer et al.,
2022):

 ℒ=1(T−1)​B​∑b=1B∑t=1T−1[sg⁡(σ^t2)]β⋅[12​log⁡(2​π​σ^t2)+(yt−μ^t)22​σ^t2],\mathcal{L}=\frac{1}
 {(T-1)B}\sum_{b=1}^{B}\sum_{t=1}^{T-1}\bigl[\operatorname{sg}(\hat{\sigma}_{t}^{2})\bigr]^{\  (17)
 beta}\cdot\left[\frac{1}{2}\log(2\pi\hat{\sigma}_{t}^{2})+\frac{(y_{t}-\hat{\mu}_{t})^{2}}{2
 \hat{\sigma}_{t}^{2}}\right],

where sg⁡(⋅)\operatorname{sg}(\cdot) denotes stop-gradient, σ^t2=exp⁡(2​σ^t)\hat{\sigma}_{t}^{2}=\
exp(2\hat{\sigma}_{t}) is the predicted variance, and β=1\beta=1. The model is optimized using Adam
with a learning rate of 10−410^{-4}, batch size B=128B=128, for 10610^{6} training steps.

We collect random trajectories from Gaussian processes, and plot the correlation between the
predictions of the Bayesian oracle world model and the trained PFN world model in Figure E.1. The
trained PFN is effective at approximating the exact likelihood and, as evidenced by other results,
is effective even in settings where the oracle cannot be computed.

Refer to caption
Figure E.1: The correlation between the values computed by an exact bayesian oracle vs our learned
PFN for likelihood and reward of randomly generated trajectories from Gaussian processes.

E.1.4 Policy


Figure E.2: Training reward vs Validation log-probability over the course of training a policy for
Gaussian Process. This highlights that in noisy environments, rsurr^{\mathrm{sur}} can behave
oppositely to the intended effect of learning the environment’s dynamics.

The policy is trained using REINFORCE with a learned value baseline. At each training step, B=32B=
32 trajectories of length T=26T=26 are collected by rolling out the current policy in the
environment. A single trajectory-level reward RR is computed from the world model, and the policy
is updated with a single gradient step per batch. The REINFORCE objective combines a policy
gradient loss and a value baseline loss:

 ℒ=ℒπ+λV​ℒV,\mathcal{L}=\mathcal{L}_{\pi}+\lambda_{V}\mathcal{L}_{V},  (18)

where λV=0.5\lambda_{V}=0.5.

Model Architecture

The policy is a sequence model that processes the history of observations recurrently. The backbone
is a Griffin model (De et al., 2024), a recurrent architecture based on linear recurrences (LRU).
The Griffin configuration uses model dimension d=128d=128, MLP dimension 256256, LRU dimension
128128, 44 attention heads, and 22 recurrent layers.

At each step tt, the observation ot=(𝐱t,yt)o_{t}=(\mathbf{x}_{t},y_{t}) is encoded by concatenating
𝐱t∈ℝ2\mathbf{x}_{t}\in\mathbb{R}^{2} and yt∈ℝy_{t}\in\mathbb{R}, yielding a 3-dimensional vector,
which is projected to the model dimension d=128d=128 via a learned linear layer.

The sequence model output at each step is mapped to logits over the 6464 discrete grid locations
via a single linear layer. A separate linear layer maps the sequence model output to a scalar value
estimate V⁡(st)∈ℝV(s_{t})\in\mathbb{R}.

E.2 Decomposition of rewards

In Section 4.3, we analyzed the theoretical behaviour of curiosity rewards in BED environments.
Corollary 3.1 showed that rsumr^{\mathrm{sum}} decomposes into BIG−Residual\text{BIG}-\text
{Residual} terms, while Theorem 4 showed that rdlr^{\mathrm{dl}} decomposes into BIG+JensenGap\text
{BIG}+\text{JensenGap} terms. In our Gaussian process environment using an Bayesian oracle
observer, all terms can be computed analytically. After having trained a policy with either rsumr^
{\mathrm{sum}} or rdlr^{\mathrm{dl}}, we can therefore check whether the reward empirically
decomposes into the expected terms that our theory predicts.


Figure E.3: Empirical decompositions of (i) rsumr^{\mathrm{sum}} into BIG and a subtracted residual
term and (ii) rdlr^{\mathrm{dl}} into an additive Jensen gap, in a Gaussian process environment
with oracle in-context learner.

Figure E.3 plots the resulting reward, BIG, and nuisance term as a function of time, averaged
across 10 training seeds and 1000 unrolled trajectories. We find that both rewards decompose
exactly into their theoretical constituents, thus validating our theoretical analyses. Indeed,
plotting BIG−Residual\text{BIG}-\text{Residual} overlaps exactly with rsumr^{\mathrm{sum}}, and
plotting BIG+JensenGap\text{BIG}+\text{JensenGap} overlaps exactly with rdlr^{\mathrm{dl}}.

E.3 Mastermind

At the start of each episode, the environment samples a secret code 𝐜∗∈{0,1,…,C−1}L\mathbf{c}^{*}\
in\{0,1,\ldots,C-1\}^{L} uniformly at random, where CC is the number of colours and LL is the code
length. In our experiments we set C=6C=6 and L=7L=7, yielding 67=279,9366^{7}=279{,}936 possible
codes. We hold out 10% of the possible codes for evaluation.

At each timestep tt, the agent submits a guess 𝐠t∈{0,1,…,C−1}L\mathbf{g}_{t}\in\{0,1,\ldots,C-1\}^
{L} and receives a response (bt,wt)(b_{t},w_{t}) where:

  • •
   
    Black peg bt∈{0,…,L}b_{t}\in\{0,\ldots,L\} is the number of exact matches (correct colour in
    the correct position), and

  • •
   
    White peg wt∈{0,…,L}w_{t}\in\{0,\ldots,L\} is the number of colour matches (correct colour
    regardless of position), computed as wt=∑c=0C−1min⁡(count​(c,𝐜∗),count​(c,𝐠t))w_{t}=\sum_{c=0}^
    {C-1}\min\!\bigl(\text{count}(c,\mathbf{c}^{*}),\,\text{count}(c,\mathbf{g}_{t})\bigr), where
    count​(c,𝐯)\text{count}(c,\mathbf{v}) is the number of occurrences of colour cc in vector 𝐯\
    mathbf{v}.

Note that wt≥btw_{t}\geq b_{t} always holds, and colours are not double-counted: if the code
contains NN repeats of a colour but the guess has M>NM>N entries of that colour, only NN contribute
to wtw_{t}.

The episode proceeds for a fixed trajectory length of T=8T=8 guesses. A random initial guess is
generated at the start of each episode and forms the first observation.

E.3.1 Bayesian Oracle World Model

The Bayesian oracle world model maintains the exact posterior over secret codes by enumerating over
all CLC^{L} possible codes. Given a history of guesses and responses {(𝐠1,b1,w1),…,
(𝐠t−1,bt−1,wt−1)}\{(\mathbf{g}_{1},b_{1},w_{1}),\ldots,(\mathbf{g}_{t-1},b_{t-1},w_{t-1})\}, the
oracle computes the set of consistent codes—those that would have produced the observed responses
for all past guesses. Formally, a code 𝐜\mathbf{c} is consistent at step tt if for all i<ti<t:
exact​(𝐜,𝐠i)=bi\text{exact}(\mathbf{c},\mathbf{g}_{i})=b_{i} and colour​(𝐜,𝐠i)=wi\text{colour}(\
mathbf{c},\mathbf{g}_{i})=w_{i}.

The uniform prior over codes, combined with consistency filtering, yields the posterior probability
of a newly observed response (bt,wt)(b_{t},w_{t}) given a new guess 𝐠t\mathbf{g}_{t} as:

 p(bt,wt∣ht=[ht−1,st−1,𝐠t])=|{𝐜∈𝒞t:exact​(𝐜,𝐠t)=bt∧colour​(𝐜,𝐠t)=wt}||𝒞t|,p(b_{t},w_{t}\mid
 h_{t}=[h_{t-1},s_{t-1},\mathbf{g}_{t}])=\frac{|\{\mathbf{c}\in\mathcal{C}_{t}:\text{exact}(\  (19)
 mathbf{c},\mathbf{g}_{t})=b_{t}\wedge\text{colour}(\mathbf{c},\mathbf{g}_{t})=w_{t}\}|}{|\
 mathcal{C}_{t}|},

where 𝒞t\mathcal{C}_{t} denotes the set of codes consistent with all observations prior to step tt.

E.3.2 In-Context PFN World Model

Data-Generating Process

The PFN is trained on synthetically generated game trajectories. For each training example, a
secret code is sampled uniformly at random, and then a sequence of Ttrain=50T_{\text{train}}=50
random guesses is generated. Each guess is drawn uniformly from {0,…,C−1}L\{0,\ldots,C-1\}^{L}, and
the corresponding responses (bt,wt)(b_{t},w_{t}) are computed by the environment.

Model Architecture

The in-context world model is a causal Transformer decoder that processes interleaved guess and
response tokens. Specifically, each guess 𝐠t\mathbf{g}_{t} is one-hot encoded over the CC colours
for each of the LL positions, yielding a vector of dimension C×LC\times L, which is then projected
to the model dimension dd via a linear layer. Each response (bt,wt)(b_{t},w_{t}) is one-hot encoded
as a concatenation of two vectors of dimension (L+1)(L+1) each, giving a total dimension of 2​(L+1)
2(L+1), which is projected to dd via a separate linear layer. For a trajectory of TT steps, the
input sequence is constructed by interleaving pairs and isolated guesses. A pair token is formed by
summing the guess and response embeddings for the same timestep: 𝐩t=embedg​(𝐠t)+embedr​(bt,wt)\
mathbf{p}_{t}=\text{embed}_{g}(\mathbf{g}_{t})+\text{embed}_{r}(b_{t},w_{t}). The isolated guess
token for the next step is embedg​(𝐠t+1)\text{embed}_{g}(\mathbf{g}_{t+1}). These are interleaved
as [𝐩1,embedg​(𝐠2),𝐩2,embedg​(𝐠3),…][\mathbf{p}_{1},\text{embed}_{g}(\mathbf{g}_{2}),\mathbf{p}_
{2},\text{embed}_{g}(\mathbf{g}_{3}),\ldots], resulting in a sequence of length 2​(T−1)2(T-1).

The sequence is processed by a causal (left-to-right) Transformer decoder with the model dimension
d=512d=512, MLP hidden dimension = 1024, 12 layers, and 8 attention heads with head dimension = 64.
The Transformer output at every odd position (i.e., the isolated guess positions) is used to
predict the corresponding response. A linear readout maps the Transformer output to (L+1)2(L+1)^{2}
logits, representing the joint distribution over all possible (bt,wt)(b_{t},w_{t}) pairs via a
softmax. The target is the index bt⋅(L+1)+wtb_{t}\cdot(L+1)+w_{t}.

The in-context world model is trained to minimize the cross-entropy loss of predicting the response
at each step. In each training step, a fresh batch of B=128B=128 random trajectories is generated
on-the-fly (no fixed dataset).

E.3.3 Policy

The policy is trained using REINFORCE with a learned value baseline. At each training step, a batch
of B=32B=32 trajectories of length T=8T=8 is collected by rolling out the current policy in the
environment. The total loss for REINFORCE is:

 ℒ=ℒπ+λV​ℒV,\mathcal{L}=\mathcal{L}_{\pi}+\lambda_{V}\mathcal{L}_{V},  (20)

where λV=0.5\lambda_{V}=0.5. When using the in-context world model (PFN observer), an entropy bonus
−λH​ℋ​[πϕ]-\lambda_{H}\mathcal{H}[\pi_{\phi}] with λH=0.01\lambda_{H}=0.01 is added to the loss to
encourage exploration.

Model Architecture

The policy model’s backbone is a Griffin model with the same configuration as the one for GP. The
guess 𝐠t\mathbf{g}_{t} is one-hot encoded over the CC colours for each position, yielding a vector
of dimension C×LC\times L. The exact matches btb_{t} and colour matches wtw_{t} are each one-hot
encoded into vectors of dimension L+1L+1. These are concatenated into a single vector of dimension
C⋅L+2​(L+1)C\cdot L+2(L+1) and projected to the model dimension dd via a linear layer.

The policy produces a value estimate and an action distribution. A linear layer maps the sequence
model output at each step to a scalar value estimate V⁡(st)V(s_{t}). The action (a guess of LL
colour values) is generated autoregressively, one position at a time, using a GRU cell. At each
position k∈{1,…,L}k\in\{1,\ldots,L\}:

 1. 1.
   
    The GRU cell updates its hidden state using the current input (the sequence model output for k=
    1k=1, or the embedding of the previously selected colour for k>1k>1).

 2. 2.
   
    A linear readout produces logits over the CC colours for position kk.

 3. 3.
   
    A colour is sampled from Categorical​(softmax​(logitsk/τ))\text{Categorical}(\text{softmax}(\
    text{logits}_{k}/\tau)), where τ\tau is the temperature (set to 1.01.0 during training).

 4. 4.
   
    The selected colour is embedded and fed as input to the GRU for the next position.

The log-probability of the full action is the sum of the per-position log-probabilities: logπ
(at∣st)=∑k=1Llogπk(at(k)∣st,at(1:k−1))\log\pi(a_{t}\mid s_{t})=\sum_{k=1}^{L}\log\pi_{k}(a_{t}^
{(k)}\mid s_{t},a_{t}^{(1:k-1)}).

E.4 Alchemy

The Alchemy environment is inspired by the latent-chemistry paradigm of Wang et al. (2021). At the
start of each episode, an environment is sampled with a hidden chemistry: a set of deterministic
transition rules governing how potions transform stones. Stones are described by D=4D=4 discrete
dimensions with value counts (3,3,3,4)(3,3,3,4), yielding 3×3×3×4=1083\times 3\times 3\times 4=108
distinct stone types. Potions are described by a single discrete dimension with P=6P=6 values. Each
chemistry defines a fixed mapping from (initial_stone, potion) pairs to resulting stones; if a
given (stone, potion) pair has no matching rule, the stone is returned unchanged and the transition
is marked as invalid. The full set of chemistries and their transition rules are stored as a
precomputed transition table of shape [Nenvs,Nrules,2​D+P][N_{\text{envs}},N_{\text{rules}},2D+P].

At each timestep tt, the agent chooses an action at=(it,pt)a_{t}=(i_{t},p_{t})—an initial stone and
a potion—and the environment returns an observation (vt,ft)(v_{t},f_{t}) where vt∈{0,1}v_{t}\in\
{0,1\} indicates whether the transition was valid (i.e., a matching rule exists in the current
chemistry), and ftf_{t} is the resulting stone (equal to iti_{t} when vt=0v_{t}=0).

The episode proceeds for a fixed trajectory length of T=5T=5 steps. A random initial transition is
sampled from the environment’s transition table at the start of each episode and forms the first
observation.

E.4.1 Bayesian Oracle World Model

Similar to Mastermind, the Bayesian oracle world model maintains the exact posterior over hidden
chemistries by enumerating all NenvsN_{\text{envs}} candidate environments. Given a history of
observations, the oracle computes a consistency mask: a candidate chemistry θ\theta is consistent
at step tt if, for all prior observations (it′,pt′,vt′,ft′)(i_{t^{\prime}},p_{t^{\prime}},v_{t^{\
prime}},f_{t^{\prime}}) with t′<t{t^{\prime}}<t:

 1. 1.
   
    If vt′=1v_{t^{\prime}}=1: chemistry θ\theta contains a rule (it′,pt′)↦ft′(i_{t^{\prime}},p_{t^
    {\prime}})\mapsto f_{t^{\prime}} that produces the observed output stone, and

 2. 2.
   
    If vt′=0v_{t^{\prime}}=0: chemistry θ\theta has no rule matching the input pair (it′,pt′)(i_{t^
    {\prime}},p_{t^{\prime}}).

The posterior probability of a new observation st=(vt,ft)s_{t}=(v_{t},f_{t}) is computed as:

 ρ(st∣ht=[ht−1,st−1,(it,pT)])=|{e∈ℰt:e​ is consistent with ​(it,pt,vt,ft)}||ℰt|,\rho(s_{t}\
 mid h_{t}=[h_{t-1},s_{t-1},(i_{t},p_{T})])=\frac{\bigl|\{e\in\mathcal{E}_{t}:e\text{ is       (21)
 consistent with }(i_{t},p_{t},v_{t},f_{t})\}\bigr|}{|\mathcal{E}_{t}|},

where ℰt\mathcal{E}_{t} denotes the set of chemistries consistent with all observations prior to
step tt.

This world model provides exact Bayesian inference but scales linearly in the number of candidate
environments, which must be enumerated exhaustively.

E.4.2 In-Context PFN World Model

The world model is a 12-layer causal Transformer with model dimension d=512d{=}512, 8 attention
heads, and feed-forward dimension 10241024. Each observation ot=(it,pt,ft)o_{t}=(i_{t},p_{t},f_{t})
is split into an input token (encoding iti_{t} and ptp_{t} via concatenated one-hot vectors,
projected to ℝd\mathbb{R}^{d}) and an output token (encoding ftf_{t} similarly). These are
interleaved into a sequence [in1,out1,in2,out2,…][\text{in}_{1},\text{out}_{1},\text{in}_{2},\text
{out}_{2},\dots] of length 2​T2T and processed with causal attention, so that the hidden state at
each input position int\text{in}_{t} attends to all tokens up to and including outt−1\text{out}_
{t-1}.

The output prediction head is an autoregressive GRU decoder that, starting from the Transformer’s
hidden state at int\text{in}_{t}, sequentially predicts each component of the observation output:
first a validity flag (2 categories), then each of the four stone dimensions (3, 3, 3, and 4
categories respectively). At each step the GRU receives the embedding of the previous ground-truth
component (teacher forcing during training), updates its hidden state, and produces logits over the
current component’s categories. Invalid logit positions (where the category index exceeds the
component’s vocabulary) are masked to −∞-\infty before the softmax.

The model is trained on sequences of Ttrain=50T_{\mathrm{train}}{=}50 transitions sampled with
uniformly random actions from uniformly sampled environment IDs, with batch size B=64B{=}64 and the
Adam optimizer at learning rate 10−410^{-4}.

Data-Generating Process

The PFN is trained on synthetically generated transition trajectories. For each training example, a
chemistry (environment) is sampled uniformly at random, and then a sequence of Ttrain=20T_{\text
{train}}=20 random transitions is generated. Each transition draws a random initial stone and
potion uniformly from their respective value ranges, and the corresponding observation (vt,stout)
(v_{t},\textbf{s}_{t}^{\text{out}}) is computed by the environment.

Model Architecture

The in-context world model is a causal Transformer decoder that processes interleaved input
(action) and output (observation) tokens. Each input token encodes the initial stone and potion via
per-dimension one-hot vectors. The stone dimensions (3,3,3,4)(3,3,3,4) and potion dimension (6)(6)
are each one-hot encoded and concatenated, yielding a vector of dimension of 19, which is projected
to the model dimension dd via a linear layer. Each output token encodes the validity indicator vtv_
{t} and the final stone. The validity is one-hot encoded into a vector of dimension 2, and is
concatenated with the final stone’s one-hot into a 15-dimensional vector, which is projected to dd
via a separate linear layer.

For a trajectory of TT steps, the input sequence is constructed by interleaving input and output
embeddings. For each timestep tt, the input embedding and output embedding are placed at positions
2​t2t and 2​t+12t+1, resulting in a sequence of length 2​T2T. The sequence is processed by a causal
Transformer decoder with model dimension d=512d=512, MLP hidden dimension F=1024F=1024, N=12N=12
layers, H=8H=8 attention heads with head dimension d/H=64d/H=64.

Similar to Mastermind’s policy, the output prediction uses a GRU-based autoregressive decoder that
conditions each output component’s prediction on the previously observed ground-truth components
(teacher forcing during training). The per-step loss is the sum of the per-component negative
log-probabilities: −∑k=15logp(ot(k)∣ot(1:k−1),ht)-\sum_{k=1}^{5}\log p(o_{t}^{(k)}\mid o_{t}^
{(1:k-1)},h_{t}). The in-context world model is trained to minimize the cross-entropy loss of
predicting the output at each step. In each training step, a fresh batch of B=64B=64 random
trajectories is generated on-the-fly (no fixed dataset), and the model is optimized using Adam with
a learning rate of 10−410^{-4}.

E.4.3 Policy

The policy is trained using PPO (Proximal Policy Optimization). At each training step, a batch of B
=32B=32 trajectories of length T=5T=5 is collected by rolling out the current policy in the
environment. The PPO objective uses K=4K=4 update epochs per batch of collected data.

Model Architecture

The policy is the same sequence model as used in Mastermind, with a Griffin backbone model (De et
al., 2024), and a GRU to predict the final output components autoregressively conditioned on the
Griffin’s output. The log-probability of the full action is the sum of the per-component
log-probabilities: logπ(at∣st)=∑k=15logπk(at(k)∣st,at(1:k−1))\log\pi(a_{t}\mid s_{t})=\sum_{k=1}^
{5}\log\pi_{k}(a_{t}^{(k)}\mid s_{t},a_{t}^{(1:k-1)}).

Experimental support, please view the build logs for errors. Generated by L A T E xml [LOGO] .

Instructions for reporting errors

We are continuing to improve HTML versions of papers, and your feedback helps enhance accessibility
and mobile support. To report errors in the HTML that will help us improve conversion and
rendering, choose any of the methods listed below:

  • Click the "Report Issue" ( ) button, located in the page header.

Tip: You can select the relevant text first, to include it in your report.

Our team has already identified the following issues. We appreciate your time reviewing and
reporting rendering errors we may not have found yet. Your efforts will help us improve the HTML
versions for all readers, because disability should not be a barrier to accessing research. Thank
you for your continued support in championing open access for all.

Have a free development cycle? Help support accessibility at arXiv! Our collaborators at LaTeXML
maintain a list of packages that need conversion, and welcome developer contributions.

We gratefully acknowledge support from our major funders, member institutions, , and all
contributors.
About · Help · Contact · Subscribe · Copyright · Privacy · Accessibility · Operational Status
(opens in new tab)
Major funding support from
Simons Foundation Simons Foundation International Schmidt Sciences
 
