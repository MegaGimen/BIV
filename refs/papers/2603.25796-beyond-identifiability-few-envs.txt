source: https://arxiv.org/html/2603.25796
arxiv: https://arxiv.org/abs/2603.25796
note: HTML dump via w3m; not a PDF.
========================================================================

Report GitHub Issue

×
Title: [                    ]

Content selection saved. Describe the issue below:

Description: [                    ]
Submit without GitHub Submit in GitHub
[smileybone] arXiv is now an independent nonprofit! Learn more ×
arXiv logo Back to arXiv
Why HTML? Report Issue Back to Abstract Download PDF   

 1. Abstract
 2. 1 Introduction
     1. Notation
     2. 1.1 Related work
         1. 1.1.1 Causal representation learning
         2. 1.1.2 Factor models
 3. 2 Model
     1. 2.1 Model
     2. 2.2 Identifiability
 4. 3 Estimation
     1. 3.1 Reconstructing intervention targets
     2. 3.2 Recovering the decoder
     3. 3.3 Learning the latent causal graph
 5. 4 Statistical guarantees
     1. 4.1 Estimators
         1. Intervention targets.
         2. Decoder matrix and representations.
         3. Latent causal graph.
         4. Main result.
     2. 4.2 A key tool: Projection based eigen-counting
     3. 4.3 Finite-sample rates
 6. 5 Discussion
 7. References

License: arXiv.org perpetual non-exclusive license
arXiv:2603.25796v1 [stat.ML] 26 Mar 2026

Beyond identifiability: Learning causal representations
with few environments and finite samples

Inbeom Lee    Tongtong Jin    Bryon Aragam

Abstract

We provide explicit, finite-sample guarantees for learning causal representations from data with a
sublinear number of environments. Causal representation learning seeks to provide a rigourous
foundation for the general representation learning problem by bridging causal models with latent
factor models in order to learn interpretable representations with causal semantics. Despite a
blossoming theory of identifiability in causal representation learning, estimation and
finite-sample bounds are less well understood. We show that causal representations can be learned
with only a logarithmic number of unknown, multi-node interventions, and that the intervention
targets need not be carefully designed in advance. Through a careful perturbation analysis, we
provide a new analysis of this problem that guarantees consistent recovery of (a) the latent causal
graph, (b) the mixing matrix and representations, and (c) unknown intervention targets.

1 Introduction

The impressive performance of generative models is due in large part to their ability to learn
internal “representations” from unstructured datasets. Unfortunately, owing to the complexity of
modern generative models, probing these representations is difficult if not impossible. Causal
representation learning (Schölkopf et al., 2021, CRL,) is an emerging discipline that probes the
statistical foundations of representation learning from a causal perspective: Models should learn
abstract representations that come paired with causal semantics and interpretability “baked in”. As
a result, models generalize better out-of-distribution (Fan et al., 2024; Peters et al., 2016),
learn abstract concepts (Taeb et al., 2022; Rajendran et al., 2024), and can be probed (de la
Fuente et al., 2025; Jin et al., 2025). By exploiting causal models and assumptions, it is now
known that identifiability of the latent representations is possible across a variety of settings
and applications. For a statistical perspective on this problem, see Moran and Aragam (2026).

Despite this rich and developing theory of identifiability in CRL, even basic statistical
properties such as consistency and finite-sample estimation are poorly understood. Surprisingly,
this remains the case even after restricting to simple families such as linear models. On the other
hand, this is unsurprising given the well-known difficulties with estimation in latent factor
models (Drton, 2009; Bai and Li, 2012; Auddy and Yuan, 2025) and causal models (Robins et al., 2003
; Uhler et al., 2013). Indeed, either of these problems on its own presents substantial challenges,
so combining these problems only makes things that much more difficult. Moreover, it is not clear
to what extent various assumptions are necessary, or if stronger assumptions are needed to
translate existing identifiability results into estimators with appropriate guarantees.

In this paper, we contribute to closing this gap and provide finite-sample, nonasymptotic
guarantees for CRL in high-dimensional, linear factor models. Achieving finite-sample rates in this
setting involves several novel techniques in order to bridge the dual complications that arise from
combining causality and factor models. Unlike traditional factor models that either assume
uncorrelated factors or that impose sparsity conditions on the factor structure, and unlike
traditional causal models that often assume faithfulness, our results require neither. This takes
us a step beyond traditional factor models by integrating techniques from causality and model
selection, which fundamentally alters the statistical setting that we are in. Although we focus on
the linear setting, our results represent a necessary and important first step towards the general
case of principled representation learning in nonlinear generative models.

As in classical models, without additional assumptions neither the latent factors (a.k.a.
representations) nor their causal relationships are identifiable. A key driver of recent
developments in CRL is the observation that data arising from multiple environments can help break
the identifiability barrier. A common type of environment arises from causal interventions in the
latent space: In vision applications, these may be concept edits or style manipulation arising from
data augmentation. In biological applications, these may be CRISPR knockouts or drug
administration. More generally, any proxy intervention where the observables are indirectly
perturbed. For our results we will assume we have data arising from multiple environments, where
each environment arises from latent, unknown interventions on these factors.

This gives rise to a natural tradeoff: Additional environments give stronger identifiability, but
this comes at the cost of more expensive data collection. The question that arises then is: What is
the fewest number of environments KK necessary? With interventional environments, it is known that
identifying causal models is possible with K=o⁡(d)K=o(d) environments (Eberhardt et al., 2005),
where dd is the number of causal variables (in our setting, this will be the number of latent
factors, also known as the latent dimension). The natural question that emerges is whether or not
this bound is achievable for learning causal representations, and is closely related to important
problems in out-of-distribution generalization (Peters et al., 2016; Rosenfeld et al., 2020; Chen
et al., 2022; Fan et al., 2024; Gu et al., 2025). When restricted to single-target interventions, a
known necessary condition (Squires et al., 2023) is that K=Ω⁡(d)K=\Omega(d). Thus, improving
existing results to require only sublinear environments necessitates consideration of multi-target
interventions, where it is known that identification is possible with K=O⁡(log⁡d)K=O(\log d)
interventions (Eberhardt et al., 2005) when the variables are observed and the targets in each
environment are known. This leaves open the problem of identifying latent causal models (as in CRL)
with unknown intervention targets all with only a logarithmic number of environments. While there
have been recent advances on identifiability using multi-target interventions, translating these
results into practical estimators with finite-sample guarantees has remained elusive. One of the
main contributions of this paper is to propose a new estimator that requires only logarithmic
environments in this more challenging setting with unknown, latent targets and to characterize its
finite-sample behaviour.

We adopt the usual set-up of linear CRL: There is a latent (unobserved) causal model over the
latent causal representations ZZ, and we observe X=B​ZX=BZ, where X∈ℝpX\in\mathbb{R}^{p} is
potentially much higher dimensional than Z∈ℝdZ\in\mathbb{R}^{d}, i.e. p≫dp\gg d. The latent causal
model over ZZ is given by an unknown linear structural equation model (SEM) over ZZ. Taken
together, the joint model between XX and ZZ can be written as:

 X=BZ,B∈ℝp×d,Z=ATZ+ν,A∈ℝd×d,νi⟂⁣⟂νj.\displaystyle\begin{aligned} X&=BZ,\quad B\in\mathbb{R}^{p
 \times d},\\ Z&=A^{T}Z+\nu,\quad A\in\mathbb{R}^{d\times d},\quad\nu_{i}\mathchoice{\mathrel
 {\hbox to0.0pt{$\displaystyle\perp$\hss}\mkern 4.0mu{\displaystyle\perp}}}{\mathrel{\hbox      (1)
 to0.0pt{$\textstyle\perp$\hss}\mkern 4.0mu{\textstyle\perp}}}{\mathrel{\hbox to0.0pt{$\
 scriptstyle\perp$\hss}\mkern 4.0mu{\scriptstyle\perp}}}{\mathrel{\hbox to0.0pt{$\
 scriptscriptstyle\perp$\hss}\mkern 4.0mu{\scriptscriptstyle\perp}}}\nu_{j}.\end{aligned}

The matrix AA defines a linear structural equation model over ZZ with a corresponding directed
acyclic graph (DAG) 𝖦\mathsf{G}—also known as the latent causal graph—that will be our primary
interest, along with the causal representations ZZ. We do not assume 𝖦\mathsf{G} is known, and we
seek to learn it from data. We also estimate the mixing matrix BB that decodes how representations
are transformed into the observations XX. For this reason the map Z↦B​ZZ\mapsto BZ is sometimes
called a “decoder”.

Notation

For an integer K≥1K\geq 1, we use the set notation [K]:={1,…,K}[K]\mathrel{\mathop{\mathchar 58\
relax}}=\{1,\dots,K\}. We denote the cardinality of a set SS by |S||S|. We write col⁡(W)\col(W) for
the column space of a matrix WW, rank⁡(W)\rank(W) for its rank, and PWP_{W} for the orthogonal
projector onto col⁡(W)\col(W). The dimension of a linear space 𝒰\mathcal{U} is denoted by dim(𝒰)\
dim(\mathcal{U}). The ii-th row, jj-th column, and (i,j)(i,j)-th element of a matrix WW are denoted
by W[i,⋅]W_{[i,\cdot]}, W[⋅,j]W_{[\cdot,j]}, and W[i,j]W_{[i,j]}, respectively. ‖W‖2\|W\|_{2} and
‖W‖F\|W\|_{F} denote the spectral and Frobenius norms, respectively, and W†W^{\dagger} denotes the
Moore-Penrose pseudoinverse. We denote the minimum non-zero and maximum singular values as σmin​
(W),σmax​(W)\sigma_{\min}(W),\sigma_{\max}(W), respectively, the minimum non-zero and maximum
eigenvalue as λmin+​(W),λmax​(W)\lambda^{+}_{\min}(W),~\lambda_{\max}(W), respectively, and the
jj-th largest eigenvalue as λj​(W)\lambda_{j}(W). For any two sequences ana_{n} and bnb_{n}, we
write an≲bna_{n}\lesssim b_{n} if there exists some fixed positive constant CC such that an≤C​bna_
{n}\leq Cb_{n}. We also use the following notation to refer to the maximum and minimum: a∨b=max⁡
(a,b)a\vee b=\max(a,b), a∧b=min⁡(a,b)a\wedge b=\min(a,b).

1.1 Related work

Our results relate most closely to parallel lines of work on causal representation learning and
latent factor models. Below we discuss our contributions in the context of this related work.

1.1.1 Causal representation learning

Causal representation learning (Schölkopf et al., 2021) has its origins in the structural equation
modeling and factor analysis literature (Bollen, 1989); the specific linear causal model considered
here was first studied in Silva et al. (2006). There is also a close relationship to the ICA
literature; see Hyvärinen and Morioka (2016); Khemakhem et al. (2020) for important contributions
on the role multi-environment data can play in identifying latent representations. We refer
interested readers to these papers for additional historical references on the broader
identifiability problem. In recent years, the identifiability theory of both linear and nonlinear
CRL models has blossomed. Although we do not intend a detailed review, we discuss some of the most
closely related works below and defer to Moran and Aragam (2026) for a more in-depth discussion.

For causal identifiability, CRL often exploits the additional information afforded by multiple
environments such as latent interventions that are widely available in applications. With
interventions, several key issues arise:

 1. 1.
   
    Are the interventions latent or observed?

 2. 2.
   
    Are the intervention targets known or unknown?

 3. 3.
   
    How many interventions are required?

Most identifiability results assume latent, single-node interventions on every node (Ahuja et al.,
2023; Squires et al., 2023; Buchholz et al., 2023; von Kügelgen et al., 2023; Jin and Syrgkanis,
2023, starting with). More closely related to our setting, several identifiability results use
multi-target interventions (Lippe et al., 2022; Lippe et al., 2023; Bing et al., 2024; Talon
et al., 2024; Ahuja et al., 2024; Li et al., 2024a; Ng et al., 2025). An advantage of multi-node
interventions is that one only needs (in principle) a sublinear number of environments. A key
contribution of our work is to obtain precise, finite-sample error bounds in exactly this setting
with sublinear environments. Moreover, we accomplish this without assuming the interventions
targets are known, as commonly assumed (Lippe et al., 2022; Talon et al., 2024; Li et al., 2024b).

By comparison, the extension of these identifiability results to provably consistent estimators has
been slower, arguably due to the well-known complications in learning both causal models and latent
factor models. To the best of our knowledge, the only other work that provides error bounds is
Acartürk et al. (2024), which establishes an O(n−1/4)O(n^{-1/4}) rate in the same noiseless, linear
mixing setting of X=B​ZX=BZ with unknown single-node interventions. As a result, this result
necessarily requires Ω⁡(d)\Omega(d) environments due to known lower bounds (Squires et al., 2023).
These results also rely on the rank-one fingerprint left by single-node interventions, a strategy
which breaks down with multiple targets since source nodes can become mixed with non-source nodes
and the resulting rank structure becomes more intricate. Other related results include Buchholz and
Schölkopf (2025), which considers approximate identifiability in nonlinear ICA, and Fokkema et al.
(2025), which simplifies the analysis by assuming that the decoder is known a priori. By contrast,
accounting for the ill-conditioning that arises from an unknown decoder that must be learned from
data is a major hurdle in our analysis.

1.1.2 Factor models

Classical works have shown that the factor loading matrix BB (also called the mixing matrix or
decoder) is not identifiable without extra conditions on the structure of BB or ΣZ\Sigma_{Z} (
Anderson et al., 1956; Lawley and Maxwell, 1962). Many assumptions have been proposed, each having
different tradeoffs between assumptions on BB and ΣZ\Sigma_{Z}, with some notable ones being (i)
restricting the latent variables to be uncorrelated, (ii) requiring BB to have a d×dd\times d lower
triangular matrix as a submatrix, and (iii) imposing a low-rank condition and fixing specific
entries to be zero in BB (Jöreskog et al., 1979; Peeters, 2012). Our causal setting precludes type
(i) assumptions.

One of the most common assumptions is the pure child assumption (Donoho and Stodden, 2003; Silva
et al., 2006; Arora et al., 2013; Bing et al., 2020b), which is a strengthening of (ii). Several
recent works invoke this pure child assumption (Bing et al., 2020a; Bunea et al., 2020; Bing
et al., 2022; Kim and Zhou, 2023), including several CRL identification results (Moran et al., 2022
; Huang et al., 2022; Chen et al., 2024) and works involving a relaxation of pure children to a
so-called subset condition (Kivva et al., 2021; Zheng et al., 2022; Jiang and Aragam, 2023).

As our method is applicable to high-dimensional data, it naturally has ties to the high dimensional
factor analysis literature (Fan et al., 2008; Fan et al., 2011; Bai and Li, 2012; Fan et al., 2013;
Bing et al., 2022), which encompasses works that take advantage of the latent factor structure in
high-dimensional tasks such as covariance estimation (Fan et al., 2013), clustering (Bunea et al.,
2020), and topic modeling (Bing et al., 2020a). Other related problems include ICA and low-rank
matrix factorization, both of which avoid the causal framing as discussed previously. See Montanari
and Wu (2024); Auddy and Yuan (2025); Zhou and Chen (2025).

The approach to identifying BB and the latent causal structure through multiple environments
provides an alternative that relaxes the constraints on the latent structure. While the
aforementioned statistical works provided sharp characterizations for finite sample recovery, they
are less concerned with the implications the structural constraints had besides allowing
identifiability. On the other hand, while CRL is interested in investigating the latent structure
itself, less emphasis has been placed on finite sample results. Our work finds itself at the
intersection of these two fields, and we combine the best of both worlds by allowing the latent
structure to be unconstrained while also providing a likelihood-free estimator with finite-sample
guarantees.

2 Model

In this section, we first introduce our model which includes the data generating mechanism and the
intervention mechanism before presenting an identifiability result in Theorem 2.1.

2.1 Model

Recall the basic model (1). The latent SEM parameters A=(au​v)A=(a_{uv}) define a latent causal
graph 𝖦=𝖦⁡(A)=(𝖵,𝖤)\mathsf{G}=\mathsf{G}(A)=(\mathsf{V},\mathsf{E}) in the obvious way: 𝖵=Z\mathsf
{V}=Z and (u,v)∈𝖤(u,v)\in\mathsf{E} if and only if au​v≠0a_{uv}\neq 0. We do not impose specific
distributional assumptions on the noise ν\nu; it is allowed to be non-Gaussian, Gaussian, or mixed.
The mixing matrix BB is assumed to have full column rank, but otherwise no sparsity assumptions are
imposed. Our primary interest is to recover 𝖦\mathsf{G} and the representations ZZ from
observations on XX. Since BB is full rank, ZZ can be easily recovered from BB. Thus we will
emphasize estimation of BB and 𝖦\mathsf{G} in the sequel.

Without additional assumptions, neither 𝖦\mathsf{G} nor ZZ are identifiable. To rescue
identifiability we assume we are given additional observations of XX arising from different
environments in the form of latent interventions on the latent causal model. We intervene on both
the latent factors and the noise, indexed by kk and ℓ\ell, respectively. We consider the general
setting of multiple unknown intervention targets, i.e. we allow any number of nodes to be
intervened on, and we denote the set of nodes that are intervened on in the kk-th environment as I⁡
(k)⊂[d]I(k)\subset[d]. Formally, we denote X(k),ℓX^{(k),\ell}, Z(k),ℓZ^{(k),\ell}, ν(k),ℓ\nu^{(k),\
ell} to be the counterparts of the aforementioned X,Z,νX,Z,\nu where nodes I⁡(k)I(k) are subject to
intervention (for k=0,1,…,Kk=0,1,\ldots,K, with k=0k=0 denoting the observational environment) and
the scale of the noise is shifted between ℓ=1\ell=1 and ℓ=2\ell=2. It follows that our model can be
expressed as:

              =[A(k)]T​Z(k),ℓ+ν(k),ℓ,νi(k),ℓ⟂⁣⟂νj(k),ℓ,\displaystyle=~[A^{(k)}]^{T}Z^{(k),\
 Z(k),ℓ\      ell}+\nu^{(k),\ell},\quad\nu^{(k),\ell}_{i}\mathchoice{\mathrel{\hbox to0.0pt{$\
 displaystyle displaystyle\perp$\hss}\mkern 4.0mu{\displaystyle\perp}}}{\mathrel{\hbox to0.0pt
 Z^{(k),\ell} {$\textstyle\perp$\hss}\mkern 4.0mu{\textstyle\perp}}}{\mathrel{\hbox to0.0pt{$\  (2)
 ~            scriptstyle\perp$\hss}\mkern 4.0mu{\scriptstyle\perp}}}{\mathrel{\hbox to0.0pt{$
              \scriptscriptstyle\perp$\hss}\mkern 4.0mu{\scriptscriptstyle\perp}}}\nu^{(k),\
              ell}_{j},
 X(k),ℓ\
 displaystyle =B​Z(k),ℓ\displaystyle=~BZ^{(k),\ell}                                             (3)
 X^{(k),\ell}
 ~

where A(k)∈ℝd×dA^{(k)}\in\mathbb{R}^{d\times d} represents the latent DAG in the kk-th environment
after intervention. We intervene on the nodes I⁡(k)I(k) by zero-ing out the corresponding columns
in A(k)A^{(k)} and diagonal entries in Σν(k),ℓ:=cov⁡(ν(k),ℓ)\Sigma_{\nu}^{(k),\ell}\mathrel{\mathop
{\mathchar 58\relax}}=\cov(\nu^{(k),\ell}). Without loss of generality we assume that all the
latent factors ZZ are centered, and thus that XX is centered. Aside from subgaussianity of XX in
the finite-sample results, we will not impose any further distributional assumptions on ν\nu, ZZ.
In particular, although linear, we do not assume Gaussianity in the noise, the latents, or the
observations (see Remark 3.1).

We assume a standard diversity and coverage condition on the intervention design that stems from
well-known necessary conditions (Eberhardt et al., 2005; Hyttinen et al., 2013):

 1. (A1)
   
    (a) K≲log⁡dK\lesssim\log d, (b) There is an observational environment k=0k=0, and (c) For every
    pair j1≠j2j_{1}\neq j_{2}, there exists some k1,k2∈{1,…,K}k_{1},k_{2}\in\{1,...,K\} such that
    Zj1Z_{j_{1}} is intervened on in environment k1k_{1} while Zj2Z_{j_{2}} isn’t and Zj2Z_{j_{2}}
    is intervened on in environment k2k_{2} while Zj1Z_{j_{1}} isn’t.

The condition that K≲log⁡dK\lesssim\log d is not necessary; in our technical results the dependence
on KK is made explicit in the rates. We emphasize the dependence between KK and dd here to
emphasize that we are most interested in sublinear environments, which is the minimum the number of
environments possible.

Condition (A1) is the conventional strongly separating systems assumption (Hyttinen et al., 2013)
combined with an observational environment. Condition (A1) is practically relevant as it is
applicable to combinatorial/multiplex CRISPR data, where multiple genes are simultaneously
perturbed in each environment (Shen et al., 2017; Horlbeck et al., 2018).

We also impose a technical condition on the noise interventions:

 1. (A2)
   
    The two noise covariance matrices, Σν(0),1,Σν(0),2\Sigma_{\nu}^{(0),1},\Sigma_{\nu}^{(0),2}
    where Σν(0),1=diag⁡(σ1,1,…,σd,1)\Sigma_{\nu}^{(0),1}=\diag(\sigma_{1,1},~...~,\sigma_{d,1}), Σν
    (0),2=diag⁡(σ1,2,…,σd,2)\Sigma_{\nu}^{(0),2}=\diag(\sigma_{1,2},~...~,\sigma_{d,2}) satisfy

     σj,1σj,2≠σj′,1σj′,2​ for all ​j≠j′\displaystyle\frac{\sigma_{j,1}}{\sigma_{j,2}}~\neq~\frac{\
     sigma_{j^{\prime},1}}{\sigma_{j^{\prime},2}}~\text{ for all }j\neq j^{\prime}

Similar conditions on the noise have been used previously for various identifiability results. For
example, Rothenhäusler et al. (2015) applied a similar noise variance ratio condition to flow
cytometry data (on biomarker levels in single cells) in multiple environments corresponding to
shift interventions. Kivva et al. (2022) apply this condition to identify generative models
comprised of ReLU networks.

2.2 Identifiability

We now present an identifiability result that ensures the estimation problem is well-defined and
that provides the foundation for our finite-sample results:

Theorem 2.1.

(Identifiability​with​unknown​intervention​environments)\mathrm
{(Identifiability~with~unknown~intervention~environments)} Under the model (2-3) and (A1)-(A2), the
following can be identified from KK unknown, multi-node intervention environments:

 1. (a)
   
    the latent causal graph 𝖦⁡(A)\mathsf{G}(A) (up to label permutation),

 2. (b)
   
    the causal representations ZZ (up to scale and label permutation),

 3. (c)
   
    the decoder BB (up to scale and label permutation),

 4. (d)
   
    the intervention targets I⁡(k)I(k) for each kk (up to label permutation).

We also include the following corollary to highlight that sublinear environments is indeed
sufficient for identifiability:

Corollary 2.2.

K≍log⁡dK\asymp\log d environments suffice to identify the latent causal graph, causal
representations, decoder, and intervention targets.

This is information-theoretically optimal in the sense that, even if all the latent factors were
fully observed and all the intervention targets were known, then Ω⁡(log⁡d)\Omega(\log d)
interventions are known to be necessary (Eberhardt et al., 2005). Moreover, since the ambient
dimension pp is typically much larger than the embedding dimension dd, we also have

 K≪d≪p.\displaystyle K\ll d\ll p.

Although not always required in our technical results, this reflects the general setting that we
are most interested in.

3 Estimation

Our main goal is to move beyond identifiability results such as Theorem 2.1 towards a precise
characterization of the typical finite-sample estimation behaviour in this model. It is worth
pausing to consider the available strategies.

For example, if we were impose specific assumptions on the noise ν\nu, such as Gaussianity (
Anderson et al., 1956; Lawley and Maxwell, 1962), then a likelihood-based approach would be
appropriate. However, in our setting we allow unknown, non-Gaussian ν\nu for which there is no
likelihood. Then the question becomes, “without a working distribution, how can one identify and
estimate the model parameters?”. One could either use a quasi-likelihood approach (Bai and Li, 2012
) and maximize some objective function, or one could use a PCA-based method on the observed
covariance matrix Σ^X\widehat{\Sigma}_{X} to construct the factor loading space (Bai, 2003; Fan
et al., 2013). Without additional assumptions on the latent structure, these methods can only
estimate the column space of BB (as opposed to BB itself). In contrast, our goal is to estimate not
only the decoder matrix BB via the mixing model (3), but also the latent structure through the
latent causal model (2). Thus, in order to simultaneously estimate both without a priori
constraints, we require a fundamentally different approach.

By using multiple environments, the CRL literature provides an alternative to go beyond the
aforementioned bottleneck and estimate the latent causal factors without distributional assumptions
or constraints on the latent structure. Many of these results, starting with Squires et al. (2023),
rely on the rank-one fingerprint left by Ω⁡(d)\Omega(d) single-node interventions in order to
identify source nodes. Unfortunately, these techniques break down with multiple intervention
targets: With multiple targets, the source nodes can be mixed with non-source nodes and the rank
structure becomes more intricate.

To overcome this hurdle, instead of identifying source nodes from each environment separately, we
propose a novel technique that exploits combinations of environments, and in doing so allows us to
maximally extract identifying power from these environments. As a result, we need substantially
fewer total environments, only O⁡(log⁡d)O(\log d) compared to Ω⁡(d)\Omega(d). Moreover, this
approach involves only second-order statistics making it appropriate for general noise
distributions. We introduce a pipeline that efficiently analyzes the intersections of column spaces
of ΣX(k),ℓ\Sigma_{X}^{(k),\ell} over the different environments, extracts the dimensions of these
intersections, and constructs the unknown intervention targets to then be used to identify the
decoder BB and the latent causal graph 𝖦\mathsf{G} via a generalized eigenvalue problem.

In this section, we present our basic methodology at the population level to simplify the initial
exposition. In the next section (Section 4) we present our main error bounds and outline the
challenges involved in extending these ideas to finite samples. We will proceed in three steps: (1)
Consistently estimating the unknown multi-node intervention targets I⁡(k)I(k), (2) Consistently
estimating the unknown linear mixing matrix BB, and finally (3) Consistently estimating the latent
graph, 𝖦\mathsf{G}. Note that from an estimate of BB, we can directly estimate the causal
representations ZZ by taking Z^=B^†​X\widehat{Z}=\widehat{B}^{\dagger}X.

3.1 Reconstructing intervention targets

Although not the main target of estimation, we first identify the unknown intervention targets.
While this information is also useful in some applications, in our case it serves as an important
ingredient for estimating the decoder BB.

First, define the following quantity for any T⊆[K]T\subseteq[K]:

 g⁡(T):\displaystyle g(T)~\mathrel  =dim[⋂k∈Tcol⁡(ΣX(k),ℓ)].\displaystyle=~\dim\bigg[\bigcap_   (4)
 {\mathop{\mathchar 58\relax}}      {k\in T}\col\Big(\Sigma_{X}^{(k),\ell}\Big)\bigg].

This is the dimension of the shared column space between the environments indexed by TT. The
following lemma shows that these numbers suffice to identify the targets I⁡(k)I(k):

Lemma 3.1.

The unknown intervention targets I⁡(k)I(k) can be identified from the collection {g(T):T⊆[K]}\{g(T)
\mathrel{\mathop{\mathchar 58\relax}}T\subseteq[K]\}. Moreover, there is a poly⁡(d)\poly(d)-time
algorithm that recovers I⁡(k)I(k) from {ΣX(k),ℓ}k∈[K],ℓ=1,2\big\{\Sigma_{X}^{(k),\ell}\big\}_{k\in
[K],~\ell=1,2}.

It is clear that g⁡(T)g(T) is directly identified from the environment-specific covariances ΣX(k),ℓ
\Sigma_{X}^{(k),\ell}; computing g⁡(T)g(T) in practice amounts to routine SVD gymnastics. The main
thrust of this lemma is that I⁡(k)I(k) can then be reconstructed from the g⁡(T)g(T), of which there
are at most O⁡(d)O(d) to compute.

By exploiting rank information in the covariances in this way, we avoid distributional assumptions
on the noise by focusing on second-order statistics. To see where this hidden rank structure comes
from, we start with the environment-specific covariance matrices ΣX(k),ℓ\Sigma_{X}^{(k),\ell}. The
column spaces of these matrices are directly linked to both the decoder BB and the intervention
targets I⁡(k)I(k) through the following identity:

 col⁡(ΣX(k),ℓ)=col⁡(B[⋅,S⁡(k)]),S⁡(k):=I​(k)c.\displaystyle\col(\Sigma_{X}^{(k),\ell})=\col(B_  (5)
 {[\cdot,S(k)]}),\quad S(k)\mathrel{\mathop{\mathchar 58\relax}}=I(k)^{c}.

This suggests that the decoder and the intervention targets can be recovered through a careful
analysis of the column spaces of the environment-specific covariances.

Of course, we don’t know BB or the targets I⁡(k)I(k), but we can still exploit the hidden rank
structure of ΣX(k),ℓ\Sigma_{X}^{(k),\ell}, which are rank-deficient: With the exception of
potential observational environments, rank⁡(ΣX(k),ℓ)=|S⁡(k)|:=rk<d\rank(\Sigma_{X}^{(k),\ell})=|S
(k)|\mathrel{\mathop{\mathchar 58\relax}}=r_{k}<d. Moreover, each environment induces rank
constraints on BB: In the kkth environment, only the columns in BB corresponding to S⁡(k)S(k) are
effective in the model. The latter fact is exploited next to estimate the decoder BB.

Remark 3.1.

By relying solely on second-order statistics, we also avoid linear non-Gaussian (i.e. LiNGAM,
Shimizu et al., 2006) type assumptions that are commonly used for identifiability in CRL. Our
results allow for arbitrary combinations of Gaussian and non-Gaussian latent variables and/or error
terms.

3.2 Recovering the decoder

The next step is to recover the decoder matrix BB. Using the targets I⁡(k)I(k) recovered in the
first step, define the set of environment indices that support node jj as follows:

 κj:={k:j∈I(k)c}\displaystyle\kappa_{j}\mathrel{\mathop{\mathchar 58\relax}}=\big\{k\mathrel{\  (6)
 mathop{\mathchar 58\relax}}j\in I(k)^{c}\big\}

Then we take intersections of col⁡(ΣX(k),ℓ)\col(\Sigma_{X}^{(k),\ell}) over κj\kappa_{j} to recover
each column B⋅jB_{\cdot j} one by one due to the following relation:

 col⁡(B⋅j)=⋂k∈κjcol⁡(ΣX(k),ℓ).\displaystyle\col\big(B_{\cdot j}\big)=\bigcap_{k\in\kappa_{j}}\  (7)
 col\Big(\Sigma_{X}^{(k),\ell}\Big).

The reason BB can be recovered in this manner is because the column space of ΣX(k),ℓ\Sigma_{X}^
{(k),\ell} spans the same space as the column space of B[⋅,S⁡(k)]B_{[\cdot,S(k)]} as shown in (5).
We can thus recover the entire mixing matrix (up to scale and permutation) and go beyond rotational
invariance without having to assume the conventional (but involved) sparsity restrictions on BB.

3.3 Learning the latent causal graph

Now that we have the decoder matrix BB, it is straightforward to recover the representations ZZ
from XX. The last step is to recover the latent causal graph 𝖦\mathsf{G} over ZZ.

Using the recovered linear mixing BB, we peel off the observed layer from the observational
environment in ΣX(0),ℓ=B​ΣZ(0),ℓ​BT\Sigma_{X}^{(0),\ell}=B\Sigma_{Z}^{(0),\ell}B^{T} using the
pseudoinverse B†B^{\dagger} and solve a generalized eigenvalue problem for the now accessible
latent covariances ΣZ(0),1,ΣZ(0),2\Sigma_{Z}^{(0),1},\Sigma_{Z}^{(0),2}:

 ΣZ(0),1​t\displaystyle\Sigma_{Z}^      =λ⋅ΣZ(0),2​t.\displaystyle=~\lambda\cdot\Sigma_{Z}^     (8)
 {(0),1}t~                              {(0),2}t.

The generalized eigenvalue problem (8) yields dd solutions for the generalized eigenvectors tmt_{m}
(m=1,…,dm=1,\ldots,d), and when combined into an d×dd\times d matrix, TZ=[t1​|⋯|​td]∈ℝd×dT_{Z}=
[\,t_{1}\,|\,\cdots\,|\,t_{d}\,]\in\mathbb{R}^{d\times d}, we gain information on the latent causal
graph 𝖦\mathsf{G} through the following identity:

 TZ=(I−A(0))​D\displaystyle T_{Z}~=~\big(I-A^{(0)}\big)D  (9)

for some diagonal scaling matrix D=(dj​j)D=(d_{jj}) with dj​j≠0d_{jj}\neq 0. Thus, TZT_{Z} encodes
the latent causal graph 𝖦\mathsf{G} through its zero pattern, yielding a direct estimate of 𝖦\
mathsf{G} once we have the solutions to (8).

4 Statistical guarantees

In the previous section, we described a procedure at the population level for learning causal
representations. To extend this to a data-driven procedure that estimates causal representations
from finite samples, a natural approach is to replace the environment-specific covariance matrices
with their sample versions, i.e.

 Σ^X(k),ℓ:=1n∑i=1nk,ℓ[X(k),ℓ][X(k),ℓ]T in place of ΣX(k)​ℓ=𝔼([X(k),ℓ][X(k),ℓ]T).\displaystyle\
 widehat{\Sigma}_{X}^{(k),\ell}\mathrel{\mathop{\mathchar 58\relax}}=\frac{1}{n}\sum_{i=1}^{n_{k,\
 ell}}\big[X^{(k),\ell}\big]\big[X^{(k),\ell}\big]^{T}\quad\text{ in place of }\quad\Sigma_{X}^
 {(k)\ell}=\mathbb{E}\Big(\big[X^{(k),\ell}\big]\big[X^{(k),\ell}\big]^{T}\Big).

In this section we outline the resulting estimators and our main statistical results on the
finite-sample behaviour of these estimators.

4.1 Estimators

In principle, the estimators can be straightforwardly derived by “plugging-in” sample covariances.
However, in practice there is a thresholding step needed to handle finite-sample artifacts. Thus,
before presenting the main statistical results, we formally define the estimators based on Section 
3.

We are given nk,ℓn_{k,\ell} i.i.d. copies of {Xi(k),ℓ}i=1nk,ℓ\{X_{i}^{(k),\ell}\}_{i=1}^{n_{k,\
ell}} from model (2-3) for each k∈[K]k\in[K] and ℓ=1,2\ell=1,2. Denote the smallest sample size
over all environments as nmin:=mink,ℓ⁡nk,ℓn_{\min}\mathrel{\mathop{\mathchar 58\relax}}=\min_{k,\
ell}n_{k,\ell} and denote the maximum support size as r:=maxk⁡rk=maxk⁡|S⁡(k)|r\mathrel{\mathop{\
mathchar 58\relax}}=\max_{k}r_{k}=\max_{k}|S(k)| where S⁡(k):=I​(k)cS(k)\mathrel{\mathop{\mathchar
58\relax}}=I(k)^{c} is the complement of the intervention target set as defined in (5). For the
finite-sample analysis, we will also assume for each k,ℓk,\ell that X(k),ℓX^{(k),\ell} is a
centered sub-Gaussian random vector with ‖X(k),ℓ‖ψ2≤Cψ2\|X^{(k),\ell}\|_{\psi_{2}}\leq C_{\psi_{2}}
for some finite constant Cψ2>0C_{\psi_{2}}>0, with the sub-Gaussian norm defined in Vershynin
(2018). To simplify the theorem statements, we also assume balanced sample sizes nk,ℓ≍n/Kn_{k,\ell}
\asymp n/K although this is not needed in the proofs.

To facilitate stating the main results, we let the non-zero eigenvalues of ΣX(k),ℓ\Sigma_{X}^{(k),\
ell} be λ1(k),ℓ≥…≥λrk(k),ℓ>0\lambda_{1}^{(k),\ell}\geq...\geq\lambda_{r_{k}}^{(k),\ell}>0 and
define the following:

 λ−\           :=mink,ℓλrk(k),ℓ,λ+:=maxk,ℓλ1(k),ℓ.\displaystyle\mathrel{\mathop{\mathchar 58\
 displaystyle\ relax}}=~\min_{k,\ell}~\lambda_{r_{k}}^{(k),\ell},~~~~~\lambda^{+}~\mathrel{\mathop
 lambda^{-}~   {\mathchar 58\relax}}=~\max_{k,\ell}~\lambda_{1}^{(k),\ell}.

Intervention targets.

In the first step of reconstructing I⁡(k)I(k) (cf. Section 2.1), we define a corresponding plug-in
estimator for any T⊆[K]T\subseteq[K] similar to (4) in Section 3. Given a threshold ρ≥0\rho\geq 0
to be chosen later, let

               =dimρ[⋂k∈Tcol(Σ^X(k),ℓ)]:=|{i:λi([∏k∈TPΣ^X(k),ℓ][∏k∈TPΣ^X(k),ℓ]T)≥ρ}|,\
 g^ρ​(T)\      displaystyle=~\dim_{\rho}\bigg[\bigcap_{k\in T}\col\Big(\widehat{\Sigma}_{X}^
 displaystyle\ {(k),\ell}\Big)\bigg]~\mathrel{\mathop{\mathchar 58\relax}}=~\Bigg|\bigg\{i\    (10)
 widehat{g}_{\ mathrel{\mathop{\mathchar 58\relax}}\lambda_{i}\bigg(\Big[\underset{k\in T}{\
 rho}(T)~      prod}P_{\widehat{\Sigma}_{X}^{(k),\ell}}\Big]\Big[\underset{k\in T}{\prod}P_{\
               widehat{\Sigma}_{X}^{(k),\ell}}\Big]^{T}\bigg)\geq\rho\bigg\}\Bigg|,

where λi\lambda_{i} denotes eigenvalues as mentioned in the notation section. Recall that PΣ^X
(k),ℓP_{\widehat{\Sigma}_{X}^{(k),\ell}} is the orthogonal projection onto col⁡(Σ^X(k),ℓ)\col(\
widehat{\Sigma}_{X}^{(k),\ell}). Thus, (10) estimates the dimension of the shared column space
between the environments indexed by TT by thresholding the eigenvalues of a certain matrix defined
with respect to the sample covariance matrices Σ^X(k),ℓ\widehat{\Sigma}_{X}^{(k),\ell}. Of course,
with population level inputs, the natural choice for tuning would have been ρ=1\rho=1, but with
finite samples determining the correct threshold is a more delicate matter.

Decoder matrix and representations.

To estimate the decoder, no thresholding is needed: We simply use a vanilla plug-in version of (7)
(cf. Section 3.2):

 B^[⋅,j]=⋂k∈κ^jcol(Σ^X(k),ℓ)whereκ^j:={k:j∈I^(k)c}.\displaystyle\widehat{B}_{[\cdot,~j]}~=~\
 underset{k\in\widehat{\kappa}_{j}}{\bigcap}\col\big(\widehat{\Sigma}_{X}^{(k),\ell}\big)~~~\  (11)
 text{where}~~~\widehat{\kappa}_{j}~\mathrel{\mathop{\mathchar 58\relax}}=~\big\{k\mathrel{\
 mathop{\mathchar 58\relax}}j\in\widehat{I}(k)^{c}\big\}.

From this, the representations ZZ can be recovered simply by inversion, i.e. Z=B†​XZ=B^{{\dagger}}
X.

Latent causal graph.

Similar to how we constructed the d×dd\times d matrix TZT_{Z} by stacking the dd generalized
eigenvectors obtained from (8) (cf. Section 3.3), we define a corresponding plug-in quantity T^Z\
widehat{T}_{Z} by first solving an empirical generalized eigenvalue problem with Σ^Z(0),ℓ\widehat{\
Sigma}_{Z}^{(0),\ell} instead of ΣZ(0),ℓ\Sigma_{Z}^{(0),\ell}, i.e.

 Σ^Z(0),1​t\displaystyle\widehat{\Sigma} =λ⋅Σ^Z(0),2​t.\displaystyle=~\lambda\cdot\widehat{\   (12)
 _{Z}^{(0),1}t~                          Sigma}_{Z}^{(0),2}t.

As before, this yields dd generalized eigenvectors t^m\widehat{t}_{m}, which we stack into the
matrix T^Z=[t^1​|⋯|​t^d]\widehat{T}_{Z}=[\widehat{t}_{1}\,|\,\cdots\,|\,\widehat{t}_{d}]. We then
define an estimator of 𝖦\mathsf{G} as the graph 𝖦^α\widehat{\mathsf{G}}_{\alpha} obtained by
thresholding T^Z\widehat{T}_{Z} with a tuning parameter α\alpha; i.e. whose edges are determined by

 edge ​j1→j2​ is in ​𝖦^α⟺|[T^Z][j1,j2]|>α.\displaystyle\text{edge }j_{1}\to j_{2}\text{ is in
 }\widehat{\mathsf{G}}_{\alpha}~\Longleftrightarrow~\big|[\widehat{T}_{Z}]_{[j_{1},j_{2}]}\    (13)
 big|>\alpha.

Again, determining a data-driven level for α\alpha that correctly recovers 𝖦\mathsf{G} is key.

Main result.

Before presenting the main theorems, we present an informal summary of the main results in the
following:

Theorem 4.1 (Informal).

Under (A1)-(A3), bounded eigenvalues for ΣZ\Sigma_{Z}, and balanced samples, we have with
probability greater than 1−(1/p​n)1-(1/pn):

 infdiagonal⁡Dd≻0​1d​‖B^−B​Dd‖F\displaystyle\underset{\       ≲r​log⁡(p​n)n.\displaystyle\
 diagonal~D_{d}\succ 0}{\inf}~\frac{1}{\sqrt{d}}\|\widehat{B} lesssim~\sqrt{\frac{r\log(pn)}   (14)
 -BD_{d}\|_{F}~                                               {n}}.

Moreover, with the same probability, 𝖦^α=𝖦\widehat{\mathsf{G}}_{\alpha}=\mathsf{G} for α≍d2​log⁡
(p​n)/n\alpha~\asymp~\sqrt{d^{2}\log(pn)/n} as long as

 amin\displaystyle a_{\min} ≳d2​log⁡(p​n)n.\displaystyle\gtrsim~\sqrt{\frac{d^{2}\log(pn)}     (15)
 ~                          {n}}.

While implementation with (10-13) is straightforward in practice, deriving the finite-sample error
bounds above for such a “plug-in” scheme presents several challenges:

 1. 1.
   
    As in both classical and high-dimensional factor analysis, we only have noisy views of the
    column spaces based on the empirical covariances Σ^X(k),ℓ\widehat{\Sigma}_{X}^{(k),\ell}.
    Although tools like Davis-Kahan and Wedin- or Weyl-type bounds are available, g⁡(T)g(T)
    involves taking intersections over multiple environments, which complicates the application of
    these bounds, considering that col⁡(W^1)∩col⁡(W^2)\col(\widehat{W}_{1})\cap\col(\widehat{W}_
    {2}) is a non-linear transformation of (W^1,W^2)(\widehat{W}_{1},\widehat{W}_{2}).

 2. 2.
   
    Learning the latent causal graph 𝖦^​(A)\widehat{\mathsf{G}}(A) requires a perturbation analysis
    on generalized eigenvectors, which involves the inversion of empirical covariances Σ^Z−1/2\
    widehat{\Sigma}_{Z}^{-1/2} and empirical pseudoinverses B^†\widehat{B}^{\dagger} that are
    ill-conditioned and thus present additional challenges compared to standard eigenvalue
    perturbation bounds.

In the sequel, we describe how these challenges can be overcome.

4.2 A key tool: Projection based eigen-counting

A key step in the analysis is to understand the sensitivity of the discrete object g⁡(T)g(T) to
noisy perturbations of the column spaces. In order to gain control over this, we will relate g⁡(T)g
(T) to the spectral behaviour of certain projection matrices.

Lemma 4.2 below presents a general result that connects a carefully constructed projection matrix
to the intersection of linear spaces that is essential to controlling deviations in g⁡(T)g(T).
Define

 Q⁡(T):=(∏k∈TPΣX(k),ℓ)​(∏k∈TPΣX(k),ℓ)T\displaystyle Q(T)~\mathrel{\mathop{\mathchar 58\
 relax}}=~\bigg(\prod_{k\in T}P_{\Sigma_{X}^{(k),\ell}}\bigg)\bigg(\prod_{k\in T}P_{\Sigma_    (16)
 {X}^{(k),\ell}}\bigg)^{T}

To keep Q⁡(T)Q(T) well-defined, we order these projections by ascending kk. We first relate the
eigenspace of Q⁡(T)Q(T) to the shared column space that defines g⁡(T)g(T) in (4).

Lemma 4.2.

Consider the symmetric square matrix Q⁡(T)Q(T) defined in (16). The eigenspace of Q⁡(T)Q(T) spanned
by the unit eigenvalues, denoted by Eig1⁡(Q⁡(T))\Eig_{1}(Q(T)), exactly equals the shared column
space across TT, i.e.

 Eig1⁡(Q⁡(T))=⋂k∈Tcol⁡(ΣX(k),ℓ).\Eig_{1}(Q(T))~=~\bigcap_{k\in T}\col(\Sigma_{X}^{(k),\ell}).

In particular, we have

 dim(⋂k∈Tcol⁡(ΣX(k),ℓ))=g⁡(T)=rank⁡(Eig1⁡(Q⁡(T))).\displaystyle\dim\Bigg(\bigcap_{k\in T}\col(\
 Sigma_{X}^{(k),\ell})\Bigg)~=~g(T)~=~\rank\big(\Eig_{1}(Q(T))\big).

When applied to the column space of the observed covariance matrices, this result establishes the
second equivalence in (17). Thus, we so far have the following useful equivalences:

 I⁡(k),k∈[K]​⟺Lemma ​3.1​g​(T),T⊆[K]​⟺Lemma ​4.1​rank⁡(Eig1⁡(Q⁡(T))),T⊆[K].\displaystyle I
 (k),~k\in[K]~~~\overset{\text{Lemma }3.1}{\Longleftrightarrow}~~~g(T),~T\subseteq[K]~~~\      (17)
 overset{\text{Lemma }4.1}{\Longleftrightarrow}~~~\rank\big(\Eig_{1}(Q(T))\big),~T\subseteq
 [K].

This suggests that deviations in g⁡(T)g(T) are dictated by the spectral properties of QQ, and
provides a convenient link to control deviations in g⁡(T)g(T) through Q⁡(T)Q(T). For the resulting
perturbation analysis, we need an empirical counterpart of (16), Q^\widehat{Q} using sample
covariances:

 Q^​(T):=(∏k∈TPΣ^X(k),ℓ)​(∏k∈TPΣ^X(k),ℓ)T\displaystyle\widehat{Q}(T)~\mathrel{\mathop{\
 mathchar 58\relax}}=~\bigg(\prod_{k\in T}P_{\widehat{\Sigma}_{X}^{(k),\ell}}\bigg)\bigg(\     (18)
 prod_{k\in T}P_{\widehat{\Sigma}_{X}^{(k),\ell}}\bigg)^{T}

The next lemma is a general perturbation bound on Q⁡(T)Q(T) that makes this link explicit:

Lemma 4.3.

For all non-empty subsets of intervention indices T⊆[K]T\subseteq[K], with Q​(T),Q^​(T)Q(T),~\
widehat{Q}(T) defined in (16) and (18), respectively, we have for all eigenvalues λj​(⋅),1≤j≤p\
lambda_{j}(\cdot),~1\leq j\leq p, the following uniform perturbation bound with probability ≥1−(1/
p​n)\geq 1-(1/pn):

 maxT⊆[K]​max1≤j≤p​|λj​(Q^​(T))−λj​(Q⁡(T))|≲λ+λ−⋅r​log⁡(p​n)n\displaystyle\underset{T\
 subseteq[K]}{\max}~\underset{1\leq j\leq p}{\max}~\bigg|\lambda_{j}\Big(\widehat{Q}(T)\Big)-  (19)
 \lambda_{j}\Big(Q(T)\Big)\bigg|~\lesssim~\frac{\lambda^{+}}{\lambda^{-}}\cdot\sqrt{\frac{r\
 log(pn)}{n}}

Now that we have control over perturbations to Q⁡(T)Q(T), the last step is to connect this back to
g⁡(T)g(T). First, by (10), we have

 g^ρ(T)=|{i:λi(Q^(T))≥ρ}|.\displaystyle\widehat{g}_{\rho}(T)=\Big|\big\{i\mathrel{\mathop{\
 mathchar 58\relax}}\lambda_{i}\big(\widehat{Q}(T)\big)\geq\rho\big\}\Big|.

When the matrix BB is severely ill-conditioned, the observed covariances ΣX(k),ℓ\Sigma_{X}^{(k),\
ell} become ill-conditioned. If this is the case, then Q^​(T)\widehat{Q}(T) is no longer reliable
to recover g⁡(T)g(T). To avoid this kind of degeneracy, we introduce a mild regularity condition on
the eigengap in BB:

 1. (A3)
   
    The decoder B∈ℝp×dB\in\mathbb{R}^{p\times d} satisfies

     λmin​(BT​B)λmax​(BT​B):=1−ρ∗≳{(λ+λ−)2⋅d​r​log⁡(p​n)n}1/3,\displaystyle\frac{\lambda_{\min}(B^
     {T}B)}{\lambda_{\max}(B^{T}B)}\mathrel{\mathop{\mathchar 58\relax}}=1-\rho^{*}~\gtrsim~\Bigg\
     {\bigg(\frac{\lambda^{+}}{\lambda^{-}}\bigg)^{2}\cdot\frac{dr\log(pn)}{n}\Bigg\}^{1/3},

    for ρ∗=ρ∗​(B)\rho^{*}=\rho^{*}(B) defined above.

Thus, while we allow for ill-conditioned BB (i.e. 1−ρ∗≈01-\rho^{*}\approx 0), the severity of this
ill-conditioning is controlled by (A3). In reasonable settings with p≫n≫dp\gg n\gg d, this lower
bound is allowed to vanish as n→∞n\to\infty (see Remark 4.1).

Lemma 4.4.

Under (A3), we have

 ℙ[g^ρ(T)=g(T) for all T⊆{1,2,…,K}]≥1−1p​n\displaystyle\mathbb{P}\Big[\widehat{g}_{\rho}\big(T\
 big)=g\big(T\big)\text{ for all }T\subseteq\{1,2,...,K\}\Big]~\geq~1-\frac{1}{pn}

for the estimand g⁡(T)g(T) defined in (4) and the plug-in estimator g^ρ​(T)\widehat{g}_{\rho}(T)
defined in (10) with tuning parameter ρ\rho satisfying 1−ρ≲λ+λ−​r​log⁡(p​n)/n1-\rho\lesssim\frac{\
lambda^{+}}{\lambda^{-}}\sqrt{r\log(pn)/n}.

Lemma 4.3 is the perturbation result that uniformly controls the deviation of all eigenvalues of Q⁡
(T)Q(T) over all possible environment index sets TT; Lemma 4.4 shows that with a data-driven choice
of ρ\rho, accurate eigen-counting is guaranteed (with high probability) even with noisy versions of
the column spaces. This result connects the equivalence structure in (17) to our finite-sample
estimators.

Remark 4.1.

Condition (A3) is closely related to existing assumptions from the literature, and in many cases
significantly relaxes these assumptions. For example, the commonly assumed pervasiveness assumption
(Fan et al., 2008; Fan et al., 2013) requires 1−ρ∗≍11-\rho^{*}\asymp 1, which obviously implies
(A3). Another example is when the entries of BB are i.i.d. sub-Gaussian: In this case, 1−ρ∗≍11-\rho
^{*}\asymp 1 with high probability. More generally, (A3) holds trivially when the columns of BB are
nearly orthogonal, which of course holds with high probability in the i.i.d. sub-Gaussian setting.
By contrast, we allow 1−ρ∗1-\rho^{*} to vanish asymptotically, allowing for BB to become
ill-conditioned in high-dimensional settings. To emphasize this, we leave the dependence on ρ∗\rho^
{*} explicit in our results, noting that under any of the above settings, this will just be
constant.

4.3 Finite-sample rates

The pipeline for converting noisy column spaces into accurate estimates of g⁡(T)g(T), combined with
Lemma 3.1, ensures high probability recovery of the intervention targets I⁡(k)I(k):

Theorem 4.5.

(Intervention​target​estimation​consistency)\mathrm{(Intervention~target~estimation~consistency)}
Under (A3), with the plug-in estimator in (10) with tuning parameter 1−ρ≲λ+λ−​r​log⁡(p​n)/n1-\rho\
lesssim\frac{\lambda^{+}}{\lambda^{-}}\sqrt{r\log(pn)/n}, the estimated intervention sets I^​(k)
​ for all ​k\widehat{I}(k)\text{ for all }k satisfy

 ℙ[I^(k)=I(k) for all k∈[K]]≥1−1p​n.\displaystyle\mathbb{P}\Big[\widehat{I}(k)=I(k)\text{ for all
 }k\in[K]\Big]~\geq~1-\frac{1}{pn}.

Next we present the second main result, estimation consistency for the mixing matrix, BB.

Theorem 4.6.

(Mixing​matrix​estimation​consistency;Fixed​design)\mathrm{(Mixing~matrix~estimation~consistency;
~Fixed~design)} Under (A1) and (A3), we have with probability greater than 1−(1/p​n)1-(1/pn) that

 infdiagonal⁡Dd≻0​‖B^−B​Dd‖F\displaystyle\   ≲d1−ρ∗⋅λ+λ−​r​log⁡(p​n)n\displaystyle\lesssim~\frac{\
 underset{\diagonal~D_{d}\succ 0}{\inf}~\|\  sqrt{d}}{1-\rho^{*}}\cdot\frac{\lambda^{+}}{\lambda^
 widehat{B}-BD_{d}\|_{F}~                    {-}}\sqrt{\frac{r\log(pn)}{n}}

Since each column in BB is only identifiable up to scale, the result is presented in terms of an
infimum over all possible positive definite diagonal matrices Dd∈ℝd×dD_{d}\in\mathbb{R}^{d\times
d}.

Finally, we present a finite-sample result for estimating the latent causal graph. To simplify the
main result, we state the result here assuming ΣZ(0),1,ΣZ(0),2\Sigma_{Z}^{(0),1},\Sigma_{Z}^{(0),2}
have bounded eigenvalues; in our technical results the eigenvalues of ΣZ\Sigma_{Z} are allowed to
be general. For notational convenience we express the above by simply saying ΣZ\Sigma_{Z} has
bounded eigenvalues. Let amina_{\min}, amaxa_{\max} denote the minimum and maximum non-zero
coefficients in AA, and mdegm_{\deg} denote the maximum in-degree of 𝖦\mathsf{G}.

Theorem 4.7.

(Latent​graph​estimation​consistency;Fixed​design)\mathrm{(Latent~graph~estimation~consistency;
~Fixed~design)} Under (A1)-(A3), and λmin​(ΣZ)≍λmax​(ΣZ)≍1\lambda_{\min}(\Sigma_{Z})\asymp\lambda_
{\max}(\Sigma_{Z})\asymp 1, we have

 ℙ[𝖦^α=𝖦]≥1−1p​nwithα≍11−ρ∗⋅(λ+λ−)3⋅d2​log⁡(p​n)n,\displaystyle\mathbb{P}\big[\widehat{\
 mathsf{G}}_{\alpha}=\mathsf{G}\big]~\geq~1-\frac{1}{pn}\quad\text{with}\quad\alpha~\asymp~\   (20)
 frac{1}{1-\rho^{*}}\cdot\bigg(\frac{\lambda^{+}}{\lambda^{-}}\bigg)^{3}\cdot\sqrt{\frac{d^
 {2}\log(pn)}{n}},

given the following holds:

 amin\         ≳amax​mdeg1−ρ∗⋅(λ+λ−)3⋅d2​log⁡(p​n)n.\displaystyle\gtrsim~\frac{a_{\max}\sqrt
 displaystyle  {m_{\deg}}}{1-\rho^{*}}\cdot\bigg(\frac{\lambda^{+}}{\lambda^{-}}\bigg)^{3}\    (21)
 a_{\min}~     cdot\sqrt{\frac{d^{2}\log(pn)}{n}}.

Recall that 𝖦^α\widehat{\mathsf{G}}_{\alpha} is defined by thresholding the estimated generalized
eigenvectors T^Z(k)\widehat{T}_{Z}^{(k)}. To determine a precise statistical rate for α\alpha, we
undergo a perturbation analysis on the generalized eigenvectors.

Remark 4.2.

Bounded eigenvalues for ΣZ\Sigma_{Z} is a common assumption (Fan et al., 2011; Park, 2020; Bing
et al., 2022), and thus motivates our presentation of the rate in Theorem 4.7. Note that this
boundedness assumption can be made regardless of whether pervasiveness (i.e. σmin2​(B)≍σmax2​(B)≍p\
sigma^{2}_{\min}(B)\asymp\sigma^{2}_{\max}(B)\asymp p) is assumed (Fan et al., 2011; Fan et al.,
2013; Wang and Fan, 2017) or not. Our technical results include the case where the largest
eigenvalue of ΣZ\Sigma_{Z} grows with the latent dimension dd.

5 Discussion

This paper studies causal representation learning in linear latent factor models in high-dimensions
with multiple unknown intervention environments. Our main contribution is to provide finite-sample
guarantees for a novel estimator of causal representations that applies to settings with only a
logarithmic number of environments. This paper helps fill a gap between recent identifiability
theory in CRL and finite sample estimation questions that come up in practice, while also matching
the known lower bound for the number of environments.

The crux of the method is that the latent structure can be extracted from second-order statistics
through careful exploitation of the information in different combinations of environments. The
central technical device, a projection-based eigen-counting procedure, converts noisy estimates of
column space intersections into consistent recovery of the unknown intervention targets, I⁡(k)I(k),
which is then used to isolate each column in the decoder BB, which in turn leads to a generalized
eigenvector analysis on the recovered latent covariances for consistent estimation of the latent
graph, 𝖦\mathsf{G}.

We hope these results provide a useful step toward a statistical understanding of causal
representation learning in which identifiability, estimation, and intervention design are all
analyzed within a common framework. Of course, our results raise more questions than they answer,
and this is just a first step in this endeavour.

References

  • Acartürk et al. (2024) E. Acartürk, B. Varıcı, K. Shanmugam, and A. Tajer. Sample complexity of
    interventional causal representation learning. Advances in Neural Information Processing
    Systems, 37:39350–39385, 2024.
  • Ahuja et al. (2023) K. Ahuja, D. Mahajan, Y. Wang, and Y. Bengio. Interventional causal
    representation learning. In Proceedings of the 40th International Conference on Machine
    Learning, ICML’23. JMLR.org, 2023.
  • Ahuja et al. (2024) K. Ahuja, A. Mansouri, and Y. Wang. Multi-domain causal representation
    learning via weak distributional invariances. In International Conference on Artificial
    Intelligence and Statistics, pages 865–873. PMLR, 2024.
  • Anderson et al. (1956) T. W. Anderson, H. Rubin, et al. Statistical inference in factor
    analysis. In Proceedings of the third Berkeley symposium on mathematical statistics and
    probability, volume 5, pages 111–150, 1956.
  • Arora et al. (2013) S. Arora, R. Ge, Y. Halpern, D. Mimno, A. Moitra, D. Sontag, Y. Wu, and
    M. Zhu. A practical algorithm for topic modeling with provable guarantees. In International
    conference on machine learning, pages 280–288. PMLR, 2013.
  • Auddy and Yuan (2025) A. Auddy and M. Yuan. Large-dimensional independent component analysis:
    Statistical optimality and computational tractability. The Annals of Statistics, 53(2):477–505,
    2025.
  • Bai (2003) J. Bai. Inferential theory for factor models of large dimensions. Econometrica, 71
    (1):135–171, 2003.
  • Bai and Li (2012) J. Bai and K. Li. Statistical analysis of factor models of high dimension. 
    2012.
  • Bing et al. (2024) S. Bing, U. Ninad, J. Wahl, and J. Runge. Identifying linearly-mixed causal
    representations from multi-node interventions. In Causal Learning and Reasoning, pages 843–867.
    PMLR, 2024.
  • Bing et al. (2020a) X. Bing, F. Bunea, Y. Ning, and M. Wegkamp. Adaptive estimation in
    structured factor models with applications to overlapping clustering. 2020a.
  • Bing et al. (2020b) X. Bing, F. Bunea, and M. Wegkamp. Optimal estimation of sparse topic
    models. Journal of machine learning research, 21(177):1–45, 2020b.
  • Bing et al. (2022) X. Bing, F. Bunea, and M. Wegkamp. Inference in latent factor regression
    with clusterable features. Bernoulli, 28(2):997–1020, 2022.
  • Bollen (1989) K. A. Bollen. Structural equations with latent variables, volume 210. John Wiley
    & Sons, 1989.
  • Buchholz and Schölkopf (2025) S. Buchholz and B. Schölkopf. Robustness of nonlinear
    representation learning. arXiv preprint arXiv:2503.15355, 2025.
  • Buchholz et al. (2023) S. Buchholz, G. Rajendran, E. Rosenfeld, B. Aragam, B. Schölkopf, and
    P. Ravikumar. Learning linear causal representations from interventions under general nonlinear
    mixing. Advances in Neural Information Processing Systems, 36:45419–45462, 2023.
  • Bunea et al. (2020) F. Bunea, C. Giraud, X. Luo, M. Royer, and N. Verzelen. Model assisted
    variable clustering: Minimax-optimal recovery and algorithms. Annals of statistics, 48(1):111,
    2020.
  • Chen et al. (2022) Y. Chen, E. Rosenfeld, M. Sellke, T. Ma, and A. Risteski. Iterative feature
    matching: Toward provable domain generalization with logarithmic environments. Advances in
    neural information processing systems, 35:1725–1736, 2022.
  • Chen et al. (2024) Z. Chen, R. Cai, F. Xie, J. Qiao, A. Wu, Z. Li, Z. Hao, and K. Zhang. 
    Learning discrete latent variable structures with tensor rank conditions. Advances in Neural
    Information Processing Systems, 37:17398–17427, 2024.
  • de la Fuente et al. (2025) J. de la Fuente, R. Lehmann, C. Ruiz-Arenas, J. Voges,
    I. Marin-Goni, X. Martinez-de Morentin, D. Gomez-Cabrero, I. Ochoa, J. Tegner, V. Lagani,
    et al. Interpretable causal representation learning for biological data in the pathway space. 
    arXiv preprint arXiv:2506.12439, 2025.
  • Donoho and Stodden (2003) D. Donoho and V. Stodden. When does non-negative matrix factorization
    give a correct decomposition into parts? Advances in neural information processing systems, 16,
    2003.
  • Drton (2009) M. Drton. Likelihood ratio tests and singularities. The Annals of Statistics, 37
    (2):979 – 1012, 2009. doi: 10.1214/07-AOS571.
  • Eberhardt et al. (2005) F. Eberhardt, C. Glymour, and R. Scheines. On the number of experiments
    sufficient and in the worst case necessary to identify all causal relations among N variables. 
    In F. Bacchus and T. Jaakkola, editors, Proceedings of the Twenty-First Conference on
    Uncertainty in Artificial Intelligence (UAI’05), pages 178–184. AUAI Press, 2005. ISBN
    0-9749039-1-4. URL https://dblp.org/rec/conf/uai/EberhardtGS05.
  • Fan et al. (2008) J. Fan, Y. Fan, and J. Lv. High dimensional covariance matrix estimation
    using a factor model. Journal of Econometrics, 147(1):186–197, 2008.
  • Fan et al. (2011) J. Fan, Y. Liao, and M. Mincheva. High dimensional covariance matrix
    estimation in approximate factor models. Annals of statistics, 39(6):3320, 2011.
  • Fan et al. (2013) J. Fan, Y. Liao, and M. Mincheva. Large covariance estimation by thresholding
    principal orthogonal complements. Journal of the Royal Statistical Society Series B:
    Statistical Methodology, 75(4):603–680, 2013.
  • Fan et al. (2024) J. Fan, C. Fang, Y. Gu, and T. Zhang. Environment invariant linear least
    squares. The Annals of Statistics, 52(5):2268–2292, 2024.
  • Fokkema et al. (2025) H. Fokkema, T. van Erven, and S. Magliacane. Sample-efficient learning of
    concepts with theoretical guarantees: from data to concepts without interventions. arXiv
    preprint arXiv:2502.06536, 2025.
  • Gu et al. (2025) Y. Gu, C. Fang, P. Bühlmann, and J. Fan. Causality pursuit from heterogeneous
    environments via neural adversarial invariance learning. The Annals of Statistics, 53
    (5):2230–2257, 2025.
  • Horlbeck et al. (2018) M. A. Horlbeck, A. Xu, M. Wang, N. K. Bennett, C. Y. Park, D. Bogdanoff,
    B. Adamson, E. D. Chow, M. Kampmann, T. R. Peterson, et al. Mapping the genetic landscape of
    human cells. Cell, 174(4):953–967, 2018.
  • Huang et al. (2022) B. Huang, C. J. H. Low, F. Xie, C. Glymour, and K. Zhang. Latent
    hierarchical causal structure discovery with rank constraints. Advances in neural information
    processing systems, 35:5549–5561, 2022.
  • Hyttinen et al. (2013) A. Hyttinen, F. Eberhardt, and P. O. Hoyer. Experiment selection for
    causal discovery. The Journal of Machine Learning Research, 14(1):3041–3071, 2013.
  • Hyvärinen and Morioka (2016) A. Hyvärinen and H. Morioka. Unsupervised feature extraction by
    time-contrastive learning and nonlinear ICA. In D. D. Lee, M. Sugiyama, U. von Luxburg,
    I. Guyon, and R. Garnett, editors, Advances in Neural Information Processing Systems 29: Annual
    Conference on Neural Information Processing Systems 2016, December 5-10, 2016, Barcelona, Spain
    , pages 3765–3773, 2016.
  • Jiang and Aragam (2023) Y. Jiang and B. Aragam. Learning nonparametric latent causal graphs
    with unknown interventions. Advances in Neural Information Processing Systems, 36:60468–60513,
    2023.
  • Jin and Syrgkanis (2023) J. Jin and V. Syrgkanis. Learning causal representations from general
    environments: Identifiability and intrinsic ambiguity. arXiv preprint arXiv:2311.12267, 2023.
  • Jin et al. (2025) J. Jin, V. Syrgkanis, S. Kakade, and H. Zhang. Discovering hierarchical
    latent capabilities of language models via causal representation learning. arXiv preprint
    arXiv:2506.10378, 2025.
  • Jöreskog et al. (1979) K. G. Jöreskog, D. Sörbom, and J. Magidson. Advances in factor analysis
    and structural equation models. (No Title), 1979.
  • Khemakhem et al. (2020) I. Khemakhem, D. Kingma, R. Monti, and A. Hyvarinen. Variational
    autoencoders and nonlinear ica: A unifying framework. In International conference on artificial
    intelligence and statistics, pages 2207–2217. PMLR, 2020.
  • Kim and Zhou (2023) D. Kim and Q. Zhou. Structure learning of latent factors via clique search
    on correlation thresholded graphs. In International Conference on Machine Learning, pages
    16978–16996. PMLR, 2023.
  • Kivva et al. (2021) B. Kivva, G. Rajendran, P. Ravikumar, and B. Aragam. Learning latent causal
    graphs via mixture oracles. Advances in Neural Information Processing Systems, 34:18087–18101,
    2021.
  • Kivva et al. (2022) B. Kivva, G. Rajendran, P. Ravikumar, and B. Aragam. Identifiability of
    deep generative models without auxiliary information. Advances in Neural Information Processing
    Systems, 35:15687–15701, 2022.
  • Lawley and Maxwell (1962) D. N. Lawley and A. E. Maxwell. Factor analysis as a statistical
    method. Journal of the Royal Statistical Society. Series D (The Statistician), 12(3):209–229,
    1962.
  • Li et al. (2024a) A. Li, Y. Pan, and E. Bareinboim. Disentangled representation learning in
    non-markovian causal systems. Advances in Neural Information Processing Systems,
    37:104843–104903, 2024a.
  • Li et al. (2024b) X.-C. Li, K. Zhang, and T. Liu. Causal structure recovery with latent
    variables under milder distributional and graphical assumptions. In The Twelfth International
    Conference on Learning Representations, 2024b.
  • Lippe et al. (2022) P. Lippe, S. Magliacane, S. Löwe, Y. M. Asano, T. Cohen, and E. Gavves. 
    Intervention design for causal representation learning. In UAI 2022 Workshop on Causal
    Representation Learning, 2022.
  • Lippe et al. (2023) P. Lippe, S. Magliacane, S. Löwe, Y. M. Asano, T. Cohen, and E. Gavves. 
    Biscuit: Causal representation learning from binary interactions. In Uncertainty in Artificial
    Intelligence, pages 1263–1273. PMLR, 2023.
  • Montanari and Wu (2024) A. Montanari and Y. Wu. Fundamental limits of low-rank matrix
    estimation with diverging aspect ratios. The Annals of Statistics, 52(4):1460–1484, 2024.
  • Moran and Aragam (2026) G. Moran and B. Aragam. Towards interpretable deep generative models
    via causal representation learning. Journal of the American Statistical Association, (to
    appear):1–32, 2026.
  • Moran et al. (2022) G. E. Moran, D. Sridhar, Y. Wang, and D. Blei. Identifiable deep generative
    models via sparse decoding. Transactions on Machine Learning Research, 2022.
  • Ng et al. (2025) I. Ng, S. Xie, X. Dong, P. Spirtes, and K. Zhang. Causal representation
    learning from general environments under nonparametric mixing. In The 28th International
    Conference on Artificial Intelligence and Statistics, 2025.
  • Park (2020) G. Park. Identifiability of additive noise models using conditional variances. 
    Journal of Machine Learning Research, 21(75):1–34, 2020.
  • Peeters (2012) C. F. Peeters. Rotational uniqueness conditions under oblique factor correlation
    metric. Psychometrika, 77:288–292, 2012.
  • Peters et al. (2016) J. Peters, P. Bühlmann, and N. Meinshausen. Causal inference by using
    invariant prediction: identification and confidence intervals. Journal of the Royal Statistical
    Society. Series B (Statistical Methodology), pages 947–1012, 2016.
  • Rajendran et al. (2024) G. Rajendran, S. Buchholz, B. Aragam, B. Schölkopf, and P. Ravikumar. 
    From causal to concept-based representation learning. Advances in Neural Information Processing
    Systems, 37:101250–101296, 2024.
  • Robins et al. (2003) J. M. Robins, R. Scheines, P. Spirtes, and L. Wasserman. Uniform
    consistency in causal inference. Biometrika, 90(3):491–515, 2003.
  • Rosenfeld et al. (2020) E. Rosenfeld, P. Ravikumar, and A. Risteski. The risks of invariant
    risk minimization. arXiv preprint arXiv:2010.05761, 2020.
  • Rothenhäusler et al. (2015) D. Rothenhäusler, C. Heinze, J. Peters, and N. Meinshausen. 
    Backshift: Learning causal cyclic graphs from unknown shift interventions. Advances in neural
    information processing systems, 28, 2015.
  • Schölkopf et al. (2021) B. Schölkopf, F. Locatello, S. Bauer, N. R. Ke, N. Kalchbrenner,
    A. Goyal, and Y. Bengio. Toward causal representation learning. Proceedings of the IEEE, 109
    (5):612–634, 2021. arXiv:2102.11107.
  • Shen et al. (2017) J. P. Shen, D. Zhao, R. Sasik, J. Luebeck, A. Birmingham,
    A. Bojorquez-Gomez, K. Licon, K. Klepper, D. Pekin, A. N. Beckett, et al. Combinatorial
    crispr–cas9 screens for de novo mapping of genetic interactions. Nature methods, 14(6):573–576,
    2017.
  • Shimizu et al. (2006) S. Shimizu, P. O. Hoyer, A. Hyvärinen, A. Kerminen, and M. Jordan. A
    linear non-gaussian acyclic model for causal discovery. Journal of Machine Learning Research, 7
    (10), 2006.
  • Silva et al. (2006) R. Silva, R. Scheines, C. Glymour, P. Spirtes, and D. M. Chickering. 
    Learning the structure of linear latent variable models. Journal of Machine Learning Research,
    7(2), 2006.
  • Squires et al. (2023) C. Squires, A. Seigal, S. S. Bhate, and C. Uhler. Linear causal
    disentanglement via interventions. In International conference on machine learning, pages
    32540–32560. PMLR, 2023.
  • Taeb et al. (2022) A. Taeb, N. Ruggeri, C. Schnuck, and F. Yang. Provable concept learning for
    interpretable predictions using variational autoencoders. arXiv preprint arXiv:2204.00492,
    2022.
  • Talon et al. (2024) D. Talon, P. Lippe, S. James, A. Del Bue, and S. Magliacane. Towards the
    reusability and compositionality of causal representations. arXiv preprint arXiv:2403.09830,
    2024.
  • Uhler et al. (2013) C. Uhler, G. Raskutti, P. Bühlmann, and B. Yu. Geometry of the faithfulness
    assumption in causal inference. The Annals of Statistics, pages 436–463, 2013.
  • Vershynin (2018) R. Vershynin. High-dimensional probability: An introduction with applications
    in data science, volume 47. Cambridge university press, 2018.
  • von Kügelgen et al. (2023) J. von Kügelgen, M. Besserve, L. Wendong, L. Gresele, A. Kekić,
    E. Bareinboim, D. Blei, and B. Schölkopf. Nonparametric identifiability of causal
    representations from unknown interventions. Advances in Neural Information Processing Systems,
    36:48603–48638, 2023.
  • Wang and Fan (2017) W. Wang and J. Fan. Asymptotics of empirical eigenstructure for high
    dimensional spiked covariance. Annals of statistics, 45(3):1342, 2017.
  • Zheng et al. (2022) Y. Zheng, I. Ng, and K. Zhang. On the identifiability of nonlinear ICA:
    Sparsity and beyond. In Neural Information Processing Systems, 2022.
  • Zhou and Chen (2025) Y. Zhou and Y. Chen. Deflated heteropca: Overcoming the curse of
    ill-conditioning in heteroskedastic pca. The Annals of Statistics, 53(1):91–116, 2025.

Experimental support, please view the build logs for errors. Generated by L A T E xml [LOGO] .

Instructions for reporting errors

We are continuing to improve HTML versions of papers, and your feedback helps enhance accessibility
and mobile support. To report errors in the HTML that will help us improve conversion and
rendering, choose any of the methods listed below:

  • Click the "Report Issue" ( ) button, located in the page header.

Tip: You can select the relevant text first, to include it in your report.

Our team has already identified the following issues. We appreciate your time reviewing and
reporting rendering errors we may not have found yet. Your efforts will help us improve the HTML
versions for all readers, because disability should not be a barrier to accessing research. Thank
you for your continued support in championing open access for all.

Have a free development cycle? Help support accessibility at arXiv! Our collaborators at LaTeXML
maintain a list of packages that need conversion, and welcome developer contributions.

We gratefully acknowledge support from our major funders, member institutions, , and all
contributors.
About · Help · Contact · Subscribe · Copyright · Privacy · Accessibility · Operational Status
(opens in new tab)
Major funding support from
Simons Foundation Simons Foundation International Schmidt Sciences
 
