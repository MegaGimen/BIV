# Muse Glimmer external agent eval (Harbor). Python >= 3.12 for Harbor.
#
# On the GPU train host (recommended):
#   source .venv-muse/bin/activate
#   pip install -r requirements-eval.txt
#   # .venv-muse already has torch/transformers/peft for loading Muse + --ckpt
#
#   uv python install 3.12 && uv venv .venv-eval --python 3.12
#   uv pip install --python .venv-eval/bin/python -r requirements-eval.txt
#
harbor>=0.21.0
pyyaml>=6.0.2
fastapi>=0.115.0
uvicorn>=0.32.0
tensorboardX>=2.6.2
