# Muse agent-eval environment check (this host)

Generated by local recon for Harbor-based external benches. **No GPU model was started.**

## Host

| Item | Value |
|--|--|
| GPU | none (`nvidia-smi` unavailable) |
| Docker | 29.1.3, daemon OK (`hello-world` succeeds) |
| Disk (train) | ~36–37G free on `/` (tight; prefer not re-download full suites often) |
| System Python | 3.11 (Anaconda) — **Harbor needs ≥3.12** |

## Python / Harbor

| Item | Value |
|--|--|
| Venv | `train/.venv-eval` (uv CPython **3.12.11**) |
| Harbor | **0.21.0** (`harbor` / `hb`) |
| Activate | `source train/.venv-eval/bin/activate` |

Install:

```bash
cd train
uv python install 3.12
uv venv .venv-eval --python 3.12
uv pip install --python .venv-eval/bin/python -r requirements-eval.txt
```

## Agents resolved

| CLI name | Class | Role for three-way align |
|--|--|--|
| `terminus-2` | `Terminus2` | **TB 2.1** — matches Meta methodology |
| `mini-swe-agent` | `MiniSweAgent` | **SWE Verified/Pro** — closest public bash-thin scaffold to Meta “bash + file” |
| `oracle` / `nop` | installed | smoke / dry checks |

## Datasets (Harbor Hub) — downloaded OK

| Suite | Hub id | Tasks |
|--|--|--|
| Terminal-Bench 2.1 | `terminal-bench/terminal-bench-2-1` | 89 |
| SWE-Bench Verified | `swe-bench/swe-bench-verified` | 500 |
| SWE-Bench Pro | `scale-ai/swe-bench-pro` | 731 |

Recon copies under `/tmp/harbor_*` (ephemeral). Formal runs should use Harbor cache / `-d org/name` (re-download) or `-p` local export.

## Sandbox note vs Meta

- Meta **TB 2.1**: Terminus 2 + **E2B**.
- **This host**: Docker OK → `test.py` 默认 `--env docker`。推理不在本机，走 AutoDL 公网 vLLM。

## Model serving (AutoDL, not this host)

AutoDL: `bash scripts/serve_muse_vllm.sh`（默认最新 LoRA；`--base` 无 adapter）→ `:6006` → 公网 `:8443`。  
本机 Harbor 只请求 model id（`muse-lora` / `Muse-Glimmer-30B`），**不需要本地 ckpt**。
本机 Harbor 默认 `MUSE_BASE_URL` /
`https://u741253-ltqs-498e6237.westd.seetacloud.com:8443/v1`。

See [README.md](README.md).

## Dry-run

```bash
cd train
.venv-eval/bin/python scripts/test.py --dry-run
```
