# Shuffled-obs control matched to configs/qlora_20gb.yaml (≤20GB QLoRA).
#   python scripts/train_sft.py --config configs/qlora_20gb_shuffled.yaml

model:
  name: Qwen/Qwen3.5-9B
  source: huggingface
  max_seq_length: 8192
  load_in_4bit: true
  load_in_16bit: false
  lora_r: 16
  lora_alpha: 16
  lora_dropout: 0.0
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj

data:
  train_file: data/processed/train_shuffled.jsonl
  eval_file: data/processed/eval_shuffled.jsonl
  packing: false
  ds_cache_dir: outputs/ds_cache

train:
  output_dir: outputs/wm_sft_qlora20_shuffled
  seed: 42
  num_train_epochs: 2
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 1
  gradient_accumulation_steps: 16
  learning_rate: 2.0e-4
  warmup_ratio: 0.03
  weight_decay: 0.01
  logging_steps: 10
  eval_steps: 200
  save_steps: 35
  save_total_limit: 3
  lr_scheduler_type: cosine
  optim: adamw_8bit
  bf16: true
  report_to: tensorboard
  instruction_part: "<|im_start|>user\n"
  response_part: "<|im_start|>assistant\n"

eval:
  max_new_tokens: 1024
  temperature: 0.0
  max_samples: 200
