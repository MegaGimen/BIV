# Fast pilot to test the world-model hypothesis (real I/O SFT).
# Not a replacement for full-scale confirmation.
#
# Knobs vs default.yaml:
#   - ~half train/eval samples (keep max_seq_length=8192; do NOT truncate)
#   - packing true when building from text (ignored if reusing pretokenized ready)
#   - 1 epoch
#
#   python scripts/train_sft.py --config configs/pilot.yaml
# Control twin:
#   python scripts/train_sft.py --config configs/pilot_shuffled.yaml

model:
  name: Qwen/Qwen3.5-9B
  source: huggingface
  max_seq_length: 8192
  load_in_4bit: false
  load_in_16bit: true
  lora_r: 16
  lora_alpha: 16
  lora_dropout: 0.0
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj

data:
  train_file: data/processed/train.jsonl
  eval_file: data/processed/eval.jsonl
  packing: true
  # ~half of full corpus (~643k / ~33k); preserves long-context observation labels
  max_train_samples: 320000
  max_eval_samples: 16000
  ds_cache_dir: outputs/ds_cache

train:
  output_dir: outputs/wm_sft_pilot
  seed: 42
  num_train_epochs: 1
  per_device_train_batch_size: 8
  per_device_eval_batch_size: 2
  gradient_accumulation_steps: 2   # effective batch ~16
  learning_rate: 2.0e-4
  warmup_ratio: 0.03
  weight_decay: 0.01
  logging_steps: 10
  eval_steps: 100
  save_steps: 35
  save_total_limit: 3
  lr_scheduler_type: cosine
  optim: adamw_8bit
  bf16: true
  report_to: tensorboard
  instruction_part: "<|im_start|>user\n"
  response_part: "<|im_start|>assistant\n"

eval:
  max_new_tokens: 1024
  temperature: 0.0
  max_samples: 200
