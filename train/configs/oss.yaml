# Non-secret OSS defaults for Bailian/SFT dataset upload (Tokyo).
# Secrets stay in train/.env (gitignored): OSS_ACCESS_KEY_ID / OSS_ACCESS_KEY_SECRET

endpoint: https://oss-ap-northeast-1.aliyuncs.com
region: ap-northeast-1
# Create a bucket in OSS 东京 (ap-northeast-1), then set the name here (not a secret).
bucket: ""
prefix: biv-wm-sft/
