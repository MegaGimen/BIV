# Non-secret OSS defaults for Bailian/SFT dataset upload (Tokyo).
# Secrets stay in train/.env (gitignored): OSS_ACCESS_KEY_ID / OSS_ACCESS_KEY_SECRET
#
# On Aliyun ECS in the SAME region, use the internal endpoint (faster, no public egress):
endpoint: https://oss-ap-northeast-1-internal.aliyuncs.com
#region public (only if not on Aliyun ECS / cross-network):
#endpoint: https://oss-ap-northeast-1.aliyuncs.com
region: ap-northeast-1
bucket: agenttools
prefix: biv-wm-sft/

