# World-model SFT defaults for a single 40GB GPU (e.g. A100 40GB).
# Hypothesis: fit real tool observations -> transfer to coding-agent skill.

model:
  # Prefer ModelScope in CN; set source: huggingface to use HF/hf-mirror instead.
  name: Qwen/Qwen3.5-9B
  source: modelscope            # modelscope | huggingface | local
  max_seq_length: 8192
  load_in_4bit: false          # bf16 LoRA; set true only if VRAM is tight
  load_in_16bit: true
  lora_r: 16
  lora_alpha: 16
  lora_dropout: 0.0
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj

data:
  # Prepared by scripts/prepare_data.py
  train_file: data/processed/train.jsonl
  eval_file: data/processed/eval.jsonl
  # Optional control arm (shuffled observations) prepared the same way
  control_train_file: data/processed/train_shuffled.jsonl
  packing: false

train:
  output_dir: outputs/wm_sft
  seed: 42
  num_train_epochs: 2
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 1
  gradient_accumulation_steps: 8   # effective batch ~8
  learning_rate: 2.0e-4
  warmup_ratio: 0.03
  weight_decay: 0.01
  logging_steps: 10
  eval_steps: 200
  save_steps: 200
  save_total_limit: 3
  lr_scheduler_type: cosine
  optim: adamw_8bit
  bf16: true
  report_to: tensorboard
  # Response-only loss markers for Qwen ChatML
  instruction_part: "<|im_start|>user\n"
  response_part: "<|im_start|>assistant\n"

eval:
  max_new_tokens: 1024
  temperature: 0.0
  max_samples: 200
