# Muse Glimmer-30B WM SFT stack (TRL + PEFT + Accelerate). Branch: Muse.
#
#   # Prefer reuse: copy .venv-axolotl → .venv-muse, then:
#   source .venv-muse/bin/activate
#   pip install -r requirements-muse.txt
#   # muse_glimmer needs transformers main (PyPI may lag):
#   pip install -e vendor/transformers   # sync src from a machine with GitHub
#
# Seetacloud: ModelScope is default for Muse weights.
# HF twin only if needed: export HF_ENDPOINT=https://hf-mirror.com && --source huggingface

torch>=2.4.0
# Prefer editable vendor/transformers (5.16.dev+) for model_type=muse_glimmer
transformers>=4.51.0
datasets>=3.0.0
accelerate>=1.0.0
trl>=0.15.0
peft>=0.14.0
bitsandbytes>=0.50.0
# Fused CE for long-context / CP (avoids materializing [B,S,V] logits)
liger-kernel>=0.5.0
sentencepiece>=0.2.0
protobuf>=4.25.0

huggingface_hub>=0.26.0
modelscope>=1.20.0
pyyaml>=6.0.2
tqdm>=4.66.0
numpy>=1.26.0

# Eval helpers (eval_wm.py)
scipy>=1.11.0
scikit-learn>=1.4.0
tensorboard>=2.17.0

requests>=2.32.0
python-dotenv>=1.0.0
