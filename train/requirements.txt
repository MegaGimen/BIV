# CUDA training env for BIV world-model SFT (40GB-class GPU recommended).
# Install into a fresh venv on the training machine — do not rely on the BIV runtime venv.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -U pip
#   pip install -r requirements.txt
#
# Unsloth must match your CUDA/torch stack. If the pin fails, follow:
#   https://unsloth.ai/docs/get-started/install

# Core training stack
torch>=2.4.0
transformers>=4.51.0
datasets>=3.0.0
accelerate>=1.0.0
trl>=0.15.0
peft>=0.14.0
# ≥0.50 accepts **kwargs on Params4bit (fixes transformers≥5 `_is_hf_initialized`)
bitsandbytes>=0.50.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# Unsloth (LoRA / QLoRA for Qwen3.5)
unsloth>=2025.12.0
unsloth_zoo>=2025.12.0

# Data + config
huggingface_hub>=0.26.0
modelscope>=1.20.0
pyyaml>=6.0.2
tqdm>=4.66.0
numpy>=1.26.0
pandas>=2.2.0

# Eval helpers
scipy>=1.11.0
scikit-learn>=1.4.0

# Optional logging
tensorboard>=2.17.0

# Bailian Files API upload (scripts/upload_bailian.py) + optional OSS path
requests>=2.32.0
oss2>=2.18.0
python-dotenv>=1.0.0

# ---------------------------------------------------------------------------
# Qwen3.5 Unsloth fast path (Scheme B: fla + causal-conv1d CUDA kernels)
# Do NOT plain-pin causal-conv1d: wheels often miss your torch/CUDA combo.
#
# Rule: nvcc must match torch.version.cuda (cu128 → CUDA 12.8 toolkit;
# nvidia-smi "CUDA 13.0" is only the driver cap — ignore it for builds).
#
#   pip install -U ninja packaging "flash-linear-attention[cuda]"
#   export CUDA_HOME=/usr/local/cuda-12.8    # for torch *+cu128
#   export PATH="$CUDA_HOME/bin:$PATH"
#   nvcc -V                                  # must print 12.8.x
#   CAUSAL_CONV1D_FORCE_BUILD=TRUE pip install -U "causal-conv1d>=1.4.0" --no-build-isolation
#   python -c "import fla, causal_conv1d; print('ok')"
#
# After upgrading torch (e.g. 2.10 → 2.11 to silence vllm cpp-extension skip):
#   export CUDA_HOME=/usr/local/cuda-12.8 && export PATH="$CUDA_HOME/bin:$PATH"
#   CAUSAL_CONV1D_FORCE_BUILD=TRUE pip install --force-reinstall --no-deps \
#       "causal-conv1d>=1.4.0" --no-build-isolation
# Docs: https://github.com/fla-org/flash-linear-attention
#       https://github.com/Dao-AILab/causal-conv1d
# ---------------------------------------------------------------------------
ninja>=1.11.0
packaging>=24.0
# flash-linear-attention / causal-conv1d: follow the block above (not bare pip -r)
