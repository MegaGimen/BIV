# CUDA training env for BIV world-model SFT (40GB-class GPU recommended).
# Install into a fresh venv on the training machine — do not rely on the BIV runtime venv.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -U pip
#   pip install -r requirements.txt
#
# Unsloth must match your CUDA/torch stack. If the pin fails, follow:
#   https://unsloth.ai/docs/get-started/install

# Core training stack
torch>=2.4.0
transformers>=4.51.0
datasets>=3.0.0
accelerate>=1.0.0
trl>=0.15.0
peft>=0.14.0
bitsandbytes>=0.45.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# Unsloth (LoRA / QLoRA for Qwen3.5)
unsloth>=2025.12.0
unsloth_zoo>=2025.12.0

# Data + config
huggingface_hub>=0.26.0
modelscope>=1.20.0
pyyaml>=6.0.2
tqdm>=4.66.0
numpy>=1.26.0
pandas>=2.2.0

# Eval helpers
scipy>=1.11.0
scikit-learn>=1.4.0

# Optional logging
tensorboard>=2.17.0

# Optional: Qwen3.5 Unsloth fast path (otherwise falls back to slow torch ops)
#   pip install -U "flash-linear-attention[cuda,conv1d]"
# If causal-conv1d wheels fail for your torch/CUDA, build from source:
#   pip install causal-conv1d>=1.4.0
# Docs: https://github.com/fla-org/flash-linear-attention#installation
#       https://github.com/Dao-AILab/causal-conv1d
